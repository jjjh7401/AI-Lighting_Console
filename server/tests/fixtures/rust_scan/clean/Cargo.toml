# NEGATIVE CONTROL fixture — the legitimate Stage-2 shell dependency set.
[package]
name = "grandma3-copilot-shell"
version = "0.0.0"
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
tauri = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
