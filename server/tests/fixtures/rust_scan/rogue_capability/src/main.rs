// POSITIVE CONTROL fixture (capability case). Source and manifests are clean;
// the network surface is granted declaratively by a Tauri v2 capability file,
// which no source-level marker would ever reveal.

fn main() {
    println!("shell start");
}
