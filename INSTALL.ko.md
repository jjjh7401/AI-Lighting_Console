# GrandMA3 Copilot 설치 안내

이 앱은 정식 배포(Apple 공증)를 거치지 않은, 소수에게 직접 전달되는 버전입니다.
아래 순서대로만 하면 문제없이 실행됩니다.

## 1. 앱을 어디에 둘지

전달받은 `GrandMA3-Copilot-x.y.z.dmg` 파일을 더블클릭해서 엽니다. 열린 창에서
`GrandMA3 Copilot` 아이콘을 옆에 있는 `Applications` 폴더 아이콘 위로 끌어다
놓으세요. 복사가 끝나면 dmg 창은 닫고(꺼내기) 이제 `/Applications` 폴더의
아이콘으로 실행하면 됩니다.

## 2. 처음 실행할 때 무엇을 보게 되는지 — 전달받은 방법에 따라 다릅니다

- **AirDrop, 메일 첨부, 메신저/브라우저로 받은 파일**인 경우: macOS가 이 파일에
  "인터넷에서 받음" 표시(quarantine)를 자동으로 붙입니다. 이 경우 처음
  실행하면 바로 아래 **3번 차단 해제**가 필요합니다.
- **USB, `scp`/`rsync`/`git`로 직접 받은 파일**인 경우: 이런 표시가 붙지 않아서
  바로 실행됩니다. 이 경우 3번은 건너뛰고 4번으로 가면 됩니다.

어느 쪽인지 확신이 없으면 그냥 실행해 보고, 아래 경고 화면이 뜨면 3번을
따라 하세요.

## 3. "확인되지 않은 개발자" 경고가 뜬 경우 — 차단 해제

macOS 15 이상에서는 아이콘을 마우스 우클릭한 뒤 "열기"를 눌러도 더 이상
통하지 않습니다. 다음 둘 중 하나로 해제하세요.

**방법 A — 터미널 명령어 (가장 확실함)**

`터미널.app`을 열고 아래 명령어를 그대로 붙여넣은 뒤 Enter를 누릅니다.

```bash
xattr -dr com.apple.quarantine "/Applications/GrandMA3 Copilot.app"
```

명령어 실행 후 다시 `/Applications`에서 앱을 더블클릭하면 정상 실행됩니다.

**방법 B — 시스템 설정 화면**

`시스템 설정` → `개인정보 보호 및 보안` 으로 들어가면 화면 아래쪽에 이 앱이
차단되었다는 문구와 함께 **"그래도 열기"** 버튼이 보입니다. 그 버튼을 누르고,
다시 뜨는 확인 창에서 한 번 더 **열기**를 누르면 됩니다.

## 4. 정상적으로 실행됐을 때

앱 창이 뜨고 잠시 후 화면 상단(또는 창 안)에 콘솔(onPC) 연결 상태 표시가
나타납니다. 별도의 터미널 창이나 에러 팝업 없이 채팅 UI가 바로 보이면
정상입니다. 이후 실행부터는 1~3번 과정 없이 바로 켜집니다.

## 문제가 계속되면

- "손상되었기 때문에 열 수 없습니다"처럼 삭제를 권하는 강한 경고가 뜬다면
  앱을 다시 전달받아 보세요(전송 중 파일이 손상되었을 가능성이 있습니다).
- 위 3번을 했는데도 안 열리면, `/Applications/GrandMA3 Copilot.app` 경로가
  정확한지(폴더명이 다르면 명령어의 경로도 맞게 바꿔야 합니다) 확인해 주세요.
