---
id: SPEC-COPILOT-UNREQ-001
artifact: plan
version: "0.1.0"
status: draft
created: 2026-08-25
updated: 2026-08-25
---

# SPEC-COPILOT-UNREQ-001 — 실행 계획

## 0. 이 계획의 형태 — 첫 마일스톤이 게이트다

보통의 M1→M4 순차 진행이 아니다. **M1 은 통과·불통과를 가르는 게이트**이고,
M1 이 불통과면 M2·M3 은 착수하지 않는다(spec.md C.4 · REQ-UNREQ-006).
M4 는 M1 과 독립이므로 병행 가능하다.

```
M1 (측정·게이트) ──통과──> M2 (범위 확정) ──> M3 (구현)
       └────불통과────> 중단, 대안 재검토
M4 (게이트 v3→v4) ── M1 과 독립, 병행 가능
```

## 1. 마일스톤

### M1 — C 의 비용을 A·B 와 같은 단위로 잰다 [게이트]

| 항목 | 내용 |
|---|---|
| 산출물 | `server/tests/` 하위 측정 하네스 1종 + `.moai/reports/` 결과 |
| 요구 | REQ-UNREQ-001 ~ 004 |
| 선행 | 없음 |
| 판정 | spec.md F.1 ~ F.3 |

작업 순서:

1. 코퍼스 21 시나리오의 `instruction` 에서 **명명된 대상**을 뽑는 추출기를 만든다.
   기존 정규식(`_CUE_PRESET_REF` 등)을 먼저 뒤지고, 없는 것만 새로 쓴다.
2. `mock.commands` 의 **쓰기 대상**을 뽑는다. `Store`/`Label`/`Assign`/`Copy` 의 대상 토큰.
3. 대조하여 세 부류로 가른다: **요청됨** / **요청되지 않음** / **판정 불가**(번호 미지정 위임).
4. 날조 대조군을 같은 실행에 넣는다 — 번호가 명시된 시나리오가 「요청되지 않음」에 안 들어가야
   한다. 안 들어감을 출력으로 보인다.
5. 세 수치를 A·B 표와 같은 형식으로 낸다(분모 포함).

주의: 이 단계는 **판정만** 한다. 세션 코드를 고치지 않는다.

### M2 — 범위 확정

| 항목 | 내용 |
|---|---|
| 산출물 | 결정 문서(어느 쓰기 경로에 대조를 붙일지 + 근거 수치 인용) |
| 요구 | REQ-UNREQ-005 · 006 |
| 선행 | **M1 통과** |
| 판정 | spec.md F.4 |

REQ-UNREQ-006 의 중단 조건을 먼저 확인한다 — 「판정 불가」가 「요청되지 않음」을 넘으면
여기서 멈추고 SPEC 을 재작성한다.

### M3 — 구현

| 항목 | 내용 |
|---|---|
| 산출물 | `session.py` 의 요청 대조 + 질문 카드 경로 |
| 요구 | REQ-UNREQ-007 ~ 009 |
| 선행 | **M2 완료** |
| 판정 | spec.md F.5 |

기존 질문 카드 패턴을 재사용한다. 새 UI 금지. 게이트 무변경.

### M4 — 게이트 클로즈드셋 v3→v4 [M1 과 독립]

| 항목 | 내용 |
|---|---|
| 산출물 | `blacklist.yaml` v4 + 13건 테스트 갱신 + `UNCHANGED_SAFE` 정리 |
| 요구 | REQ-UNREQ-010 ~ 013 |
| 선행 | 없음 |
| 판정 | spec.md F.6 |

갱신 대상 13건(t86 실측, 분모 10183):
looks_instantiate 6 · measurement_runner 4 · writegate 2 · safety_ruleset 1.

`UNCHANGED_SAFE` 에서 `Store Preset 4.1` 만 빼고, 남는
`Store Group 3`·`Store Cue 12`·`Store Page 3`·`Store Macro 21`·`Assign …`·`Copy …` 이
**여전히 비준 대상**임을 주석으로 명시한다 — 한 줄 지우면서 나머지 비준까지 흐리면 안 된다.

## 2. 측정 규율 (모든 마일스톤 공통)

- 블랙리스트를 변형해 재는 작업은 **백업 + `shasum` 대조**로 복원한다. `git checkout` 금지
  (미커밋분이 날아간다).
- 기준선을 먼저 잡고 비교한다. 기준선 없는 실패 수는 귀속 불가다.
- 실패 수를 적을 때 **분모를 함께** 적는다(spec.md A.2 — 항목 1개당 테스트 4건이 늘어난다).
- 인용 수치에는 출처 트리·커밋을 붙인다.
- **콘솔 발사 금지.** 판정은 검사 층에서 낸다.

## 3. 이 계획이 안 하는 것

- `Label` 계열 · `Store` 동사 전체 · `Assign`/`Copy` 전량 측정 · 축약 구멍(t91) ·
  MA3 실기 확인.

## 4. 미해결 — plan 단계에서 못 정한 것

1. **M1 하네스를 어디에 둘 것인가.** `server/tests/` 안이면 CI 가 매번 돌리고,
   `.moai/reports/` 스크립트면 안 돈다. 전자가 회귀 방어에 낫지만 코퍼스 변경 때마다
   깨질 수 있다. run 단계 첫 판단.
2. **「명명된 대상」 추출을 어디까지 할 것인가.** 번호만인지, 「그 프리셋」 같은 지시어까지인지.
   M1 의 「판정 불가」 정의가 여기 달렸다.
3. **M4 를 이 SPEC 에 두는 것이 맞는가.** M1 과 독립이라 별도 카드로 떼도 된다.
   묶어 둔 이유는 근거 수치가 같은 실측에서 나왔기 때문 — 떼면 근거가 문서 밖을 가리킨다.

## 5. §4 의 세 건 — 결정됨 (2026-08-25, 리드)

§4 는 plan 단계에서 못 정한 것을 남긴 목록이었다. 세 건 모두 결정됐으므로 §4 는 종결이고,
아래가 유효한 결정이다.

| §4 항목 | 결정 | 근거 |
|---|---|---|
| M1 하네스 위치 | **`server/tests/`** | `.moai/reports/` 아래는 CI 가 절대 안 돈다. 게이트 판정 근거를 CI 밖에 두면 「CI 가 봉인을 덮은 척한다」 계열(t65·t74)의 재현이다 |
| 「명명된 대상」 추출 범위 | **둘 다 재고 따로 보고** — 번호만 / 지시어 포함 | 여기서 하나를 고르면 M1 이 재려던 것을 미리 정해 버린다. 두 수치가 갈리면 그 자체가 M2 의 재료다 |
| M4 의 소속 | **이 SPEC 에 둔다** (M1 과 병행, M1 을 막지 않음) | 갈라 놓으면 REQ-UNREQ-012 의 비준 보존 주석이 고아가 된다 |

### 5.1 M4 를 M1 뒤로 미루는 실행상의 이유 (병행 허용이지만 순서는 직렬)

리드는 M4 를 M1 과 병행 가능하다고 했고 의존은 실제로 없다. 다만 **같은 파일을 만진다** —
M4 는 `blacklist.yaml` 을 바꾸고, M1 의 전량 스위트 측정은 그 파일이 원본일 때의 값이어야 한다.
병행하면 M1 의 분모가 M4 진행 상태에 따라 달라진다. 그래서 M1 완료 후 M4 를 실행한다.
이것은 리드 결정의 변경이 아니라 실행 순서의 선택이다.

### 5.2 중단 임계 — M1 착수 전에 확정함

리드는 임계 근거를 「M2 전에」 문서로 정하라고 했다. 실제로는 **M1 측정 전**에 확정했다
(spec.md D.5). 임계의 반편향 성질은 「측정 전에 정해졌다」는 데서 나오고, M1 이 숫자를 만들기
때문이다 — 숫자를 본 뒤 조이면 G.2 가 막으려던 편향이 그 자리로 돌아온다.
「M1 전」은 「M2 전」의 부분집합이므로 리드 지시를 어긴 것이 아니라 더 이르게 지킨 것이다.
