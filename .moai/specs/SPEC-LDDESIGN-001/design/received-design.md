# 수령 설계 인덱스 — `src/DESIGN.md` 구조화 지도

수령: 2026-09-22. 이 문서는 `src/DESIGN.md`(206줄)의 내용을 요약하지
않는다 — 블록별로 어느 REQ를 채우고, 어느 기존/신규 컴포넌트에
착지하며, NEW/EXTENDED 어느 쪽인지만 인덱싱한다. 상세 문구는 항상
`src/DESIGN.md`의 해당 절 번호를 보라.

## 감독 판정 요약 — 채택 / 변경 채택 / 미채택 (2026-09-22)

| 판정 | 대상 | 비고 |
|---|---|---|
| **채택** (디자인 그대로) | CUE SHEET 14열 재구성(§4.4) · 컨셉 패널 "한눈에" 5단계 카드 + 탭 3개(§4.2) 전체 | 감독이 `src/DESIGN.md`의 해당 절 전체를 그대로 승인 |
| **변경 채택** (디자인 취지는 유지, 세부 조정) | 없음 — 이 SPEC의 5건 결정 중 순수 "취지 유지 + 세부만 조정"은 없다. CUE SHEET·컨셉 패널은 그대로 채택, 폰트는 완전 미채택이지 부분 변경이 아니다 | — |
| **미채택** (디자인과 다르게 결정) | 폰트(`src/DESIGN.md` §2의 IBM Plex Sans KR + IBM Plex Mono) | 시스템 폰트 스택 + `tabular-nums`로 대체(REQ-099). 이 SPEC에서 디자인 문서와 다른 길을 택한 **유일한** 지점 |
| **디자인 범위를 넘는 추가** (생성기가 스스로 더한 것, `src/DESIGN.md`에 없던 요구) | PLAN CUE 수정요청 생성기의 항목별 제거(`✕`, REQ-100) · 자유 입력 한 줄(REQ-101) | `src/DESIGN.md` §4.5는 변경 스택(diff 누적)까지만 규정 — 항목별 제거 수단과 자유 입력 한 줄은 감독이 M7 확정 단계에서 별도로 요구사항화했다 |

**한눈에 답: "받은 디자인대로 만들었는가?"** — CUE SHEET와 컨셉
패널은 그렇다. 폰트는 아니다(의도적 이탈, 기록 보존 목적).
PLAN CUE 수정요청 생성기의 항목별 제거·자유 입력 한 줄은 디자인이
정의한 변경 스택 위에 감독이 추가로 얹은 요구다.

## 0. 범위 정정 (DESIGN.md §0)

| 항목 | 내용 |
|---|---|
| 정정 | M7 = 런북 모드 페이지. 메인 화면(워크시트·프리셋 풀 브라우저·콘솔 연결)은 범위 밖 |
| 재사용 | 프리셋 풀 팝업 → 메인 화면 컴포넌트(`PresetPoolPopup.tsx`/`PoolTile.tsx`) 그대로 마운트 |
| 착지 | `ui/src/components/RunbookMode.tsx`(기존, 263줄) 확장 |
| 분류 | EXTENDED — 재구현 아님 |

## 1. 화면 구조 5블록 + 컨셉 패널 (DESIGN.md §1)

| 항목 | 내용 |
|---|---|
| REQ | REQ-078(순서 고정) |
| 착지 | 5블록 순서·존재는 기존과 동일. 신규 삽입점만 블록 1↔2 사이 |
| 신규 치수 | 전체 폭 1600px 고정 캔버스(신규 확정값 — spec.md 미언급) |
| 분류 | EXTENDED(순서) + NEW(치수 확정) |

## 2. 토큰 (DESIGN.md §2)

`design/tokens-final.md`로 이관 — 측정-기존/신규 병합 완료. 요약만:
근거 등급 4종(도형 포함) · GATE 4상태 · Q### 배지 2상태 · 원샷 점 3종
· 그룹 선택 빨강 2px · 프리셋 풀 배지 3종. **폰트는 감독 결정
(2026-09-22)으로 `src/DESIGN.md`의 IBM Plex 2종을 미채택** —
시스템 폰트 스택 + `tabular-nums`로 대체(REQ-099, `tokens-final.md`
참조).

## 3. 데이터 모델 (DESIGN.md §3)

| 항목 | 내용 |
|---|---|
| REQ | 화면 요구 형태 — M2 `CueV2`(design.md §1)와 필드 이름은 다르나 개념은 대응 |
| 착지 | UI 레이어 전용 뷰 타입. `server/` 쪽 `CueV2`를 그대로 노출하지 않고 화면용으로 얇게 변환하는 어댑터가 필요할 수 있음(구현 시 결정) |
| **트래킹 해석 필수 규칙** | `groups` 필드는 **직전 큐 대비 약어**(`동일`/`+X`) — PLAN CUE 카드는 캐리포워드해 유효 그룹 집합으로 해석 후 사용. 헤드룸(투입/잔여)도 이 집합 크기에서 파생 — **별도 상수 금지**. `design/implementation-notes.md`에 상세 |
| 분류 | NEW(뷰 타입) |

## 4. 블록별 사양 (DESIGN.md §4)

### 4.1 오늘의 곡 (§4.1)
경미한 변경 — 큐 순서 칩 줄 제거(타임라인 Q줄과 중복이므로). REQ-078
범위 내. EXTENDED.

### 4.2 컨셉 패널 (§4.2, REQ-079/080/097/098) — **"한눈에" + 탭 3개 채택(감독 결정 2026-09-22)** — 채택
| 항목 | 내용 |
|---|---|
| 착지 | 신규 `ConceptPanel.tsx` |
| 구조 확대 → 채택 | REQ-079/080은 "인과 불릿 + 6칸표 + 클릭 시 4칸 설명"만 규정. DESIGN.md는 그 위에 **"한눈에" 5단계 카드**(시작→쌓기→강조→예고→정점→마무리) + **탭 3개**(연출/재료/문법) 구조를 추가로 요구했고, 감독이 §4.2 전체를 **그대로 채택**했다(REQ-097 카드, REQ-098 탭) — 정보 구조 확대인지 단순 세분화인지의 판단은 "확대"로 확정, 더 이상 미결이 아니다 |
| 분류 | NEW(컴포넌트) + 확대(정보 구조, 채택됨) — `spec-gap.md` §4 |

### 4.3 타임라인 (§4.3, REQ-081)
기존 `SongTimeline.tsx` 확장. 내부 폭 2200px 가로 스크롤(신규 치수),
현재 구간 강조 규칙, 점 줄 겹침 회피(홀짝 오프셋), 세로 플레이헤드
신규 규정. EXTENDED.

### 4.4 CUE SHEET (§4.4, REQ-082/096) — **14열 확정(감독 결정 2026-09-22)** — 채택
`design.md` §2.3 및 `spec-gap.md` §2에 전체 대조 있음. **감독이
디자인 그대로 채택** — 신규 5열 추가 + 기존 9열 유지 + 기존 5열
(TC Out·Dur·Mood·Trans·Note) 제거 확정. `Trans` 값의 데이터 모델
존속은 REQ-096이 별도로 지킨다. 실제 착지는
`CueSheetTimeline.tsx`(기존, 714줄) — 열 배열(`SHEET_COLUMNS`)
재정의가 필요하다. 색 레일(7px, 좌측) · 구간 전환 상단 구분선 ·
직전 큐 대비 ▲▼ 표기는 DESIGN.md 신규 규정. EXTENDED(대규모 재구성).

### 4.5 PLAN CUE 카드 (§4.5, REQ-087~095) — "PLAN CUE 수정요청 생성기", 편집기 아니다
| 항목 | 내용 |
|---|---|
| REQ 대비 | REQ-083은 "하단 3줄 + Q### 배지"만 규정. 2026-09-22 감독 결정으로 아래 확대분이 REQ-LDDESIGN-087~095(spec.md §3.15)로 요구사항화됐다 — 이름은 **"PLAN CUE 수정요청 생성기"**로 확정: 제안된 큐 카드에서 바꿀 항목을 고르고 누르면 코파일럿에게 보낼 수정요청 문구를 만들어 주는 생성기이지, 큐시트를 로컬에서 직접 변형하는 편집기가 아니다(변형·판정은 여전히 서버 `apply_cue_sheet_edit`가 단일 진실 지점, REQ-092). |
| DESIGN.md 확대 | 좌(조작면: 그룹 다중 선택 칩·스테퍼·4택 Fade/Track·4택 MIB·변경 스택 diff) + 우 452px(콘솔 반영 값 패널 — 풀 1/2/4/21 참조 행 + 프리셋 풀 팝업 연동). 변경 스택의 취소 버튼은 **"선택 취소"**이며 기존 `CueSheetTimeline.tsx:612`의 `↶ 되돌리기`와는 다른 별개 컨트롤이다(REQ-095). |
| 착지 | `ExecutionPreviewCard.tsx` 또는 관련 카드(발신 `screens.md` #4가 추정한 파일) — 실제로는 t281/t291이 이미 `CueSheetTimeline.tsx` 안에 초안 편집(`onSelectCue`/`onUndoDraft`/`onRedoDraft`/`onSaveDraft`/`onApplyDraft`)을 배선해 두었으므로, 이 생성기가 그 배선과 같은 컴포넌트인지 별도 카드인지는 구현 시 확인 필요 |
| 분류 | 확대(범위) — 채택됨(2026-09-22). `spec-gap.md` §3 참조 |
| 파생 경고 6종 | 후렴 상승 곡선 역전 · 팬 폭 정점 도달 · 리저브 위반 · 잔여 그룹 <4 · MIB live→GATE WARN 예고 · 페이저 속도=BPM 동기. 기존 헤드룸 경고와의 중복 여부는 REQ-LDDESIGN-093이 이미 해소했다(REQ-050·027·090·066 재사용, 임계값만 신규) |
| **생성기 추가 — 디자인 범위를 넘는 것(2026-09-22 감독 결정)** | `src/DESIGN.md` §4.5는 변경 스택(diff 누적 + 전체 「선택 취소」)까지만 규정했다. 항목별 제거(`✕`, REQ-100)와 자유 입력 한 줄(REQ-101)은 디자인 문서에 없던 요구다 — 감독이 M7 확정 5건 중 두 건으로 별도 채택했다. `✕`는 diff 줄 하나만 지우고 「선택 취소」와 다른 핸들러다(REQ-095 3상태 모델 불변). 자유 입력 한 줄은 REQ-092 경로(파서 → `apply_cue_sheet_edit`)를 그대로 따른다. |

### 4.6 상태줄 (§4.6, REQ-084)
GATE 요약 배지 4종 + 상세 펼침 3열 표. REQ-084와 개념 일치. EXTENDED.

### 4.7 스크롤 연동 (§4.7, REQ-085)
구현 주의사항(동기 실행, ref 매 호출 재해석, lock + 80ms 트레일링
타임아웃)은 `design/implementation-notes.md`로 이관. REQ-085와 개념
일치하나 실행 세부는 REQ에 없던 신규 지시. EXTENDED + NEW(구현 세부).

## 5. 하지 말 것 (DESIGN.md §5)
발신 `constraints.md`(현 `handoff-out/constraints.md`)와 실질적으로
동일 — 새 화면·라이트 테마·색만 구분·비유 카피·범위 밖 기능·콘솔
실제 방출 컨트롤 금지가 유지됨. 변경 없음.

## 6. 구현 순서 제안 (DESIGN.md §6)
9단계: 토큰 → 트래킹 해석 유틸 → CUE SHEET 14열 → 타임라인 → 스크롤
연동 → PLAN CUE 수정요청 생성기 → 프리셋 풀 팝업 교체 → 컨셉 패널 →
상태줄 GATE. `design/implementation-notes.md`에 그대로 인용.
