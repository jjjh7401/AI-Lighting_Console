# 코드 근거 조사 — SPEC-LDDESIGN-001

[요구사항](spec.md) · [계획](plan.md) · [인수 기준](acceptance.md)

이 문서는 2026-09-21에 나온 11편의 조사·검증 보고서(`reports/*-20260921.md`),
설계 문서(`src/Lighting_Director/AI_Lighting_Copilot_Research_Integration.md`),
정본(`docs/proposals/song-structure-lighting-standard.md`)의 핵심 수치·결정을
spec.md의 각 REQ 그룹에 대응해 압축한다. 원문은 각 보고서 파일을 직접 참조하라
— 이 문서는 원문을 대체하지 않는다.

## 0. 실행 프로토타입의 위치

모든 보고서의 실행값은 `.moai/state/verify/f12e5c95-t429/`의 스크립트가
만들었다. 핵심 스크립트:

- `final_integrated.py` — 오늘 자 모든 결정을 통합한 최종 프로토타입.
  8곡(`pilot_baseline.json`)을 돌려 13게이트(G1~G13)를 계산한다. 이
  SPEC의 §3 요구사항 다수가 이 스크립트의 `apply()`/`build()` 함수를
  근거로 삼는다(spec.md 각 REQ의 "근거" 열).
- `escalation_audit.py` — §4 회차 규칙 v1→v2→v3 반영 검사.
- `tracking_timing_mib.py` — §5.3~5.5 트래킹·타이밍·MIB 검사.
- `vocab_demo.py` — 어휘 9/8/7종 닫힘 검사.
- `candidates_demo.py` — 적용 후보 C1~C9 개별 실행.

측정 트리: `WT-chorus-collision`(`4d4cc94f`, `origin/main@9dd21171` +
t429 수정) — primary checkout(`469e41b2`)보다 19커밋 앞선다(§ plan.md A).

## 1. 문서 분석과 업그레이드 방향 (`lighting-director-upgrade-20260921.md`)

전면 교체(선택지 A)·별도 앱(선택지 B) 둘 다 기각, **점진 통합**(선택지 C)
채택 — 검증된 하류(compile→safety→OSC)를 유지하고 상류만 교체한다.

구조적 문제 3가지:
1. 큐 생성 경로 두 갈래(`design/song_cue_composer.py` vs
   `looks/songcue.py`, `web/session.py:8707`와
   `orchestrator/tools.py:3230`가 각각 호출) — REQ-LDDESIGN-003이 닫는다.
2. 후렴 차별화 지식 3군데 상충(`staging_cases.py`, `songcue.py` 사다리,
   정본 §7.1) — spec.md §2.1이 새 정본(§3.7)으로 통일을 확정.
3. 스키마가 닫혀 있고 절댓값이다 — 큐 모델 v2(§3.4)가 동작형으로 연다.

권고 로드맵 8단계가 plan.md의 M0~M8 순서 그대로 채택됐다.

## 2. 실증 검증 (`lighting-director-verification-20260921.md`)

**개선폭**: 후렴 정체성 유지율 0.302(현 경로, 8곡) → 0.877(문서 규칙
프로토타입, 동일 8곡). 동작×레이어 40조합 중 35 완전 표현.

**🔴 t429 회귀**: 최신 `origin/main`에서 프로덕션 업로드 경로
(`tools.py:3230`)가 후렴 2회 이상 곡 8곡 중 7곡에서 큐 생성 자체에
실패한다(`movement_line_collision`). 바이섹트 범인: `be76c9ef`(t382,
PR #468). 수정은 브랜치 `WT-chorus-collision`(`4d4cc94f`)에 있고
`origin/main` 미병합 — 이 SPEC의 M0(REQ-LDDESIGN-001) 전제.

후렴 4판본 실행 가능 비율: V2 사다리 1.00(그러나 실제 곡에서 값 줄
충돌로 조립 실패) · V4 새 문서 0.80 · V3 정본 0.75 · V1 지식시드 0.50.

## 3. 곡 하나(Rain)의 감독 관점 결과 (`one-song-director-view-20260921.md`)

t429 수정 반영 후 Rain 실측: Sequence 1 · Timecode 7 · 12큐 · 유보 무빙
6/감광 3. 후렴이 흰색↔빨강으로 번갈아(정체성 유지 1/5). 워크시트
스펙 방향 결과(프로토타입): 정체성 유지 4/5(0.8), 피날레 새 축 BLIND,
헤드룸 경고 없음.

앱 흐름 7단계(§3.14 UI가 이 흐름 위에 컨셉 패널·CUE SHEET 신규
5열·PLAN CUE 카드 3줄+배지·상태줄을 추가하고, §3.15가 PLAN CUE
수정요청 생성기[REQ-LDDESIGN-087~095, 9개 REQ]를 신설한다 —
2026-09-22 정정: REQ-082의 "12열+2열" 전제는 실측으로 틀렸음이
드러났고 목표는 기존 9열 유지 + 신규 5열 추가다, spec.md §3.14
참조): 곡 첨부 → 분석 → 인터뷰 5~6문항 → 큐 초안 → 콘솔 반영
승인 → 리허설 수정 → Merge.

## 4. 큐 밀도 — Rain 단일곡 (`cue-density-director-view-20260921.md`)

실험 A(분할 단위 낮추기, 8마디→4마디→2마디): 2마디에서 29큐지만
구간 안에서 팔레트가 회전(연출이 아니라 난수). 실험 B(3층 워크시트):
시퀀스 큐 21 = 구간 12 + 프레이즈 9, 원샷 8. 5개 스펙 요구사항이 §3.6의
근거다.

## 5. 큐 밀도 — 8곡 교차 (`cue-density-8songs-20260921.md`)

- 8곡 중 후렴 2개 이상인 7곡 전부에서 현 경로가 번갈아 결함(정체성
  평균 0.30).
- 분할을 2마디로 낮추면 8곡 전부에서 구간 안 색 변화가 재현된다(밀도를
  분할 단위로 정하지 않는 REQ-036의 직접 근거).
- 3층 규칙 자동 적용: 후렴 3개 이상 6곡에서 정체성 0.80~0.92.
- **규칙 구멍 발견**: Ice cream(후렴 2개)은 "마지막 후렴 흰색" 규칙 때문에
  정체성 0.0 — 후렴 3개 미만이면 흰색 전환을 끄는 조건 필요
  (REQ-LDDESIGN-028의 "후렴 3회 이상" 조건 직접 근거).

## 6. 문서 전수 대조 (`document-gap-audit-20260921.md`)

문서 항목 41개 중 코드에 온전히 있는 것 7, 부분 15, 없음 19. **가장
크게 놓친 것: §3 컬러 스크립트** — "컨셉 계층 없음" 한 줄로 뭉갰으나
실제로는 유보·언더페인팅·브리지가 전부 없고, **`_arc_palette`가
후렴 회차마다 보조색을 반대로 회전시킨다**(정체성 규칙과 정면 충돌,
실측: `(blue, warm white) (blue, magenta) (blue, warm white)...`).

이 발견이 REQ-LDDESIGN-004·031(회전 로직 제거)과 AC-LDDESIGN-016(회귀
방지 시험)의 직접 근거다.

다른 크게 놓친 것: §5.3 트래킹 4모드, §5.4 타이밍 계층, §6 안전
플레이백, §8.3 곡 경계 규칙, §10 근거 등급 표기, §11 "[공개 근거 없음]"
표시 — 전부 §3.9~§3.13이 닫는다.

## 7. 적용 후보 9개 (`apply-candidates-20260921.md`)

C1~C9 각각을 Rain(22큐 전체 표)과 8곡(컬러 검사)에 실제로 얹었다.

| 후보 | REQ 매핑 |
|---|---|
| C1 팔레트 4칸 + 컬러 스트립 | REQ-LDDESIGN-012, 026 |
| C2 후렴 정체성 + 유보 해제 | REQ-LDDESIGN-020, 031, 042 |
| C3 3층 트리거 큐 + 원샷 레인 | REQ-LDDESIGN-036~040 |
| C4 트래킹 정책 열 | REQ-LDDESIGN-053~057 |
| C5 트리거별 페이드 기본값 | REQ-LDDESIGN-058 |
| C6 시작·종료 안전 큐 | REQ-LDDESIGN-068~069 |
| C7 큐 설명 자동 생성 | REQ-LDDESIGN-023, 070 |
| C8 큐마다 근거 등급 | REQ-LDDESIGN-022, 071 |
| C9 헤드룸 4축 | REQ-LDDESIGN-050 |

C1·C2는 `_arc_palette`/`per_chorus`를 **대체**한다. C4·C5는 콘솔 문법
프로브가 필요(§4 B군). 나머지는 프로브 없이 가능.

## 8. 문서 어휘로 닫고 재검증 (`vocab-closed-verification-20260921.md`)

8곡 전부 문서 어휘만으로 큐시트가 나온다. 구간 어휘 사용 5~7/9(Post-
Chorus·Rap/Solo/Dance Break는 판정기가 못 냄). 트리거 4/8(10토큰 기준),
원샷 4/7. 재매핑 곡당 1~2건, 규칙 삽입(Pre-Chorus) 곡당 1~3개 — 전부
"감독 확인" 표시가 붙는다(REQ-LDDESIGN-009).

## 9. §4 후렴 반복 에스컬레이션 검사 (`chorus-escalation-audit-20260921.md`)

v1(어제)은 §4를 절반만 반영 — 상승 축 11개 중 3개만 썼다. 검사기가
자기 규칙의 결함을 둘 찾았다:
1. Pre-Chorus 언더페인팅 색이 후렴 "복원"에 안 실려 그대로 새어 들어감
   (8곡 중 4곡) → **복원은 색을 포함한다**(REQ-LDDESIGN-020).
2. 회차 4에서 모션을 최대치로 써버려 피날레에 남은 단계가 0인 곡 넷
   → 모션은 후렴 수에 맞춰 분배한다(REQ-LDDESIGN-044).

v3에서 정체성 8곡 전부 1.0, 회차당 새 축 평균 1.7~4.0(v1은 1.3~3.0),
피날레 전 남은 단계 1~3.

## 10. 트래킹·타이밍·MIB (`tracking-timing-mib-20260921.md`)

- 트래킹: 프레이즈 큐를 Track으로 두면 8곡 중 6곡에서 누출(다음 구간으로
  값이 이어짐) → Cue Only로 전환(REQ-LDDESIGN-056).
- 함정: 상대 감소가 트래킹된 앞 값에 겹쳐 곱해짐(Too Cool 절 밝기
  50→25→12) → `reduce`는 구간 기준을 참조(REQ-LDDESIGN-021).
- 타이밍: Rain 31큐 — Snap 7·Short 17·Long 7·순차 6.
- MIB: Rain 포지션 변화 8회 중 어두운 이동 4·Mark 삽입 1·**켜진 채
  이동 3**(REQ-LDDESIGN-064~066이 이 3건을 0으로 만드는 규칙).

## 11. 최종 검증 — 통합 규칙 + 8곡 + 게이트 13개 (`final-verification-20260921.md`)

11편의 결정을 `final_integrated.py`로 통합해 8곡에 실행. **게이트 13 ×
8곡 = 104칸 — 통과 98 · 해당 없음 6 · 실패 0.** n/a 6칸의 구조적 이유:
DinoDino(후렴 1개 — G2·G3·G4 n/a), Ice cream(피날레 없음 — G4·G10
n/a), 절 3개 미만 곡(G10 n/a).

이번 통합에서 규칙 결함 4건을 추가로 고쳤다(§3.7·§3.11의 직접 근거):
① 상대 감소 겹침 곱셈 → 구간 기준 참조, ② 절이 무버를 끄면서 동시에
포지션 변경 → 포지션은 무버 소등 뒤 Mark에서만, ③ 연속 후렴·연속
브릿지에서 어두운 창 없는 포지션 변경 → 유지, ④ 후렴 4회 이후 프레이즈
큐 1개로 제한.

게이트 정의 G1~G13 전체(REQ 매핑은 spec.md §3 각 REQ의 `[G#]` 표시):

| # | 게이트 | 검사 | REQ |
|---|---|---|---|
| G1 | 어휘 닫힘 | 구간·트리거·원샷 이름 enum 밖이면 실패 | REQ-005~007, 016 |
| G2 | 후렴 정체성 | Chorus 쌍 색 동일(Final Chorus 제외) | REQ-030, 042 |
| G3 | 회차마다 새 축 | 5회차까지 새 축 0인 쌍 없음 | REQ-043 |
| G4 | 피날레 새 축 + 여유 | 새 축 ≥1, 직전 남은 모션 ≥1 | REQ-044, 048 |
| G5 | 헤드룸 경고 0 | 4조건 | REQ-047, 051 |
| G6 | 컬러 | 유보색 조기 0 · 브리지 위반 0 | REQ-027, 029 |
| G7 | 후렴 주색 동일 | Chorus 색 집합 크기 ≤1 | REQ-030 |
| G8 | 후렴 앞 빌드업 | 필요 자리마다 삽입 | REQ-037 |
| G9 | 트래킹 | Block·Release·Cue Only 미반영 | REQ-054, 057 |
| G10 | 상대 감소 겹침 없음 | 절 2 이후 밝기 동일 | REQ-021 |
| G11 | 타이밍 | 페이드 종류·초, 순차 딜레이 | REQ-058~060 |
| G12 | MIB | 켜진 채 이동 0 | REQ-064~066 |
| G13 | 큐 밀도 | 시퀀스 큐 10~45 | REQ-041 |

## 12. 설계 문서와 정본의 핵심 결정 근거

- `AI_Lighting_Copilot_Research_Integration.md` §10이 도입한 3등급
  (Verified/Practitioner Pattern/Designed Rule) + 프로토타입이 추가한
  4번째 등급(`director` — 판정기 원출력) = REQ-LDDESIGN-022.
- 정본 §6.1 "밝기는 예외다 — 밝기만 누적한다"(감독 결정 2026-09-12)와
  §7.1 아껴두기 사다리 표의 충돌은 SPEC-LDCLIMAX-001/LDACCENT-001이
  이미 정리했다 — 이 SPEC의 §3.7 회차 규칙은 그 결정과 독립된 구간
  전체 레벨(초·큐 단위가 아니라 후렴 전체)의 에스컬레이션이므로 직접
  충돌하지 않는다(REQ-LDDESIGN-077의 경계 진술).
- 정본 §9 큐 밀도 표(DIY·DJ 5, 원오프 실무 약 10, 대형 투어 수백)는
  REQ-LDDESIGN-041의 10~45 범위 근거(구간 수 중앙값 10 부근 + 8곡 실측
  분포).

## 13. 미검증 — §F 잠정값의 원천

plan.md §F의 3개 잠정값은 이 조사 과정에서 직접 발견된 미검증 항목에
채택된 값이다(M8 콘솔 프로브로 실측치 교체 예정):

- MIB 이동 1.5초/정착 0.5초는 **가정값**이다 — "기종별 실측 없음"
  (`tracking-timing-mib-20260921.md` 한계 절).
- Block/Release, 축별 딜레이, Mark 셋 다 "이 저장소에서 실기 관측
  0건"(`server/director/emit.py:60` `AXIS_TIMING_OBSERVED` 주석과
  일치).
- 스트로브 정책은 `server/web/preview.py:131-139`가 이미 `danger`
  분류하고 있으나, 곡별로 감독이 명시 승인해야 하는지는 미정 — 잠정값
  `allow_strobe: false`로 채택.

모든 보고서가 공통으로 명시한 한계: 프로토타입 값이며 콘솔 명령·린트
미연결, 실기 미접속. 구간 이름은 전부 현 판정기 출력(정답 라벨 없음).
워크시트는 자동 규칙 적용 결과이며 감독이 직접 쓰면 달라진다.
