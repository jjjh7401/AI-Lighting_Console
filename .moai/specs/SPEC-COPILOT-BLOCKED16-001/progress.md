# SPEC-COPILOT-BLOCKED16-001 — 진행 기록 (progress)

## 세션 · 기준

| 항목 | 값 |
|---|---|
| 세션 | 칸반 **plan** 열 세션 `plan-tjxy3n` |
| 세션 표기 근거 | `env \| grep MOAI_KANBAN_LABEL` → `MOAI_KANBAN_LABEL=plan-tjxy3n` (2026-08-18 실측) |
| 워크트리 | `/Users/studiox/orca/workspaces/AI-Lighting_Console/blocked16` |
| 브랜치 · HEAD | `jjjh7401/blocked16` · `15590e3` (착수 시 clean) |
| 카드 | t1 · Class C (설계 변경) |
| 지시자 | lead 세션 `lead-tjxy3n` |
| 일자 | 2026-08-18 |

## §A. plan 단계 산출물

| 파일 | 상태 | 판본 |
|---|---|---|
| `spec.md` | 신규 | v0.7.0 |
| `plan.md` | 신규 | v0.6.0 |
| `acceptance.md` | 신규 | v0.7.0 |
| `progress.md` | 이 파일 | — |

> **행 수 열을 두지 않는 이유** — 초판은 행 수를 적었고 편집 한 번에 곧바로 어긋나
> 한 차례 정정해야 했다. 판본과 달리 행 수는 이 표가 **관리하지 않는 값**이므로,
> 적는 순간 중복 출처가 된다(REQ-B16-008 R). 필요하면 `wc -l`로 그때 잰다.

run 단계에서 `probe.md`(M0) · `disposition.md`(M1)가 추가된다.

## §B. plan 단계가 실제로 재본 것 — 카드 전제 대비

카드는 넷(채널폭 · 페이드 · 큐 내용 · 그룹 멤버십)의 성립 여부가 M0에서 갈린다고 지시했다.
plan 단계에서 저장소를 실측한 결과 **넷 중 둘은 이미 다른 SPEC이 측정을 끝냈다.**
카드 자신이 "기존 실측은 다시 재지 말고 승계할 것"이라 지시했으므로 이는 범위 재협상이
아니라 그 지시의 이행이다.

| 값 | plan 단계 판정 | 근거(명령 → 관측) |
|---|---|---|
| 그룹 멤버십 | **측정 완료 · NEGATIVE — 승계** | `grep -n "게이트 A — 멤버십은 읽을 수 없다" .moai/specs/SPEC-COPILOT-GROUPGEN-001/progress.md` → `232:` ; `grep -n "grandMA3 does not expose group membership" server/groupgen/write.py` → `410:` |
| 채널폭 | **부분 승계 · 현행 읽기 표면 한정 미성립** — 미측정 경로 2건 잔존 | `sed -n '644,652p' .moai/specs/SPEC-COPILOT-PRECHK-001/progress.md` → 소유 SPEC이 "전건 부정"을 **P2-5로 철회**하고 미측정 후보 I-14·I-15를 열거 |
| 페이드 | **측정 완료 · 부분 GO — 승계** | `grep -n "CueInFade" .moai/specs/SPEC-COPILOT-CUETIME-001/progress.md` → `16:` `Part CueInFade = 5.0 (Cue의 CueFade prop은 not readable — Part 레벨이 정답)`, M1 완료 2026-08-14 |
| 큐 내용 | **미측정 — M0 대상** | `grep -n "CURRENT_CUE_PROPERTY_CANDIDATES" server/web/cue_monitor.py` → `62:` (시퀀스 핸들 `CurrentCue`는 라이브 확정, 큐 노드 내부는 미측정) |

→ 넷 중 **셋이 이미 측정됐다**(초판은 둘이라 적었다 — 페이드 오분류, 감사 P0-1).

추가로 **팬/틸트 현재값**(3단계 포커스 차트)이 M0 대상에 들어간다 —
`grep -n "ARRANGE_READ_AXES" server/orchestrator/tools.py` → `1319: ("posx","posy","posz")`
로 좌표축 판독은 실재하나 pan/tilt는 후보에 없다.

## §C. 콘솔 접속 상태 — 착수 시점 실측

```bash
ps aux | grep -i "gma3\|grandma\|onpc" | grep -v grep
lsof -nP -iUDP | grep -E ":8000|:9005"
```

관측:

```
(ps: 출력 없음)
python3.1 12689 studiox 4u IPv4 ... UDP 127.0.0.1:9005
```

**onPC는 붙어 있지 않다.** 9005를 점유한 것은 앱 서버다.

→ M0는 `미측정` 상태로 시작할 공산이 크며, plan은 그 경우를 **결론이 아니라 분기**로
설계했다(REQ-B16-002 · plan §B M0-5). `판독 불가`는 프로브 원장의 적법값이 아니며
AC-005가 이를 기계적으로 막는다.

## §D. plan 단계에서 발견한 서술 정정 3건

원본 backlog 원장을 그대로 처분표로 옮기면 안 되는 자리다. 상세는 `spec.md` §A.7.

| # | 원문 | 정정 |
|---|---|---|
| 정정-1 | 2단계 "장비 채널폭을 직접 읽지 못해" | 모드별 폭은 열거된다. 막히는 것은 픽스처↔모드 **조인** |
| 정정-2 | 6단계 "페이드 값 판독 경로가 확인되지 않아" | 속성명 후보 `CueFade`가 특정돼 있고 코드 상수는 읽힌다고 주장한다. 미확인인 것은 그 주장 |
| 정정-3 | 8단계 "콘솔이 되돌아오는 신호를 주지 않아 원리적으로 불가" | 콘솔 **상태**는 확인된다. 불가한 것은 DMX 출력이 실물 장비에 도달했는가 |

## §E. 설계 판단 기록

### §E.1 양성 대조군 도입 — GROUPGEN 설계에 없던 항

날조(음성) 대조군만으로는 **전 후보 `ok:false`**인 배치를 판정으로 쓸 수 없다.
프로브 장치 자체가 죽었을 가능성이 배제되지 않기 때문이다. GROUPGEN 게이트 A에서는
`COUNT`가 우연히 `ok:true`를 답해 장치 생존이 확인됐지만, 그 우연에 기대는 설계는
다음 프로브에서 무너진다.

→ 게이트마다 **기지 GO 속성**(A·B는 시퀀스 `CurrentCue`, C는 `posx`)을 양성 대조로 함께 쏜다.
**음성 대조군은 거짓 GO를, 양성 대조군은 거짓 NEGATIVE를 막는다.**

### §E.2 측정 종류 열 — ②가 두 종류를 뭉개는 것을 막는다

②(측정 선행)에 페이드·큐 내용(**읽기**)과 복원 경로 4-b·7-b(**쓰기**)가 함께 들어간다.
읽기는 M0에서 끝나고 쓰기는 M0 밖이며 별도 안전 설계를 요구한다. 열이 없으면 다음 세션이
4-b를 M0 결과로 착각한다.

### §E.3 조준을 도구 표에 넣지 않는 결정

넣으면 LDGUIDE가 실측으로 확정한 `33` 주장이 34로 어긋나고, 그 SPEC이 교정한 바로 그
유형의 결함(선언한 수와 실제가 어긋남)을 재생산한다. → 비도구 기능 항목으로 별도 표기.

## §F. AC 뮤테이션 검증 — plan 단계 수행분

규약 5에 따라 작성 시점에 수행했다. 원자료는 `acceptance.md` §G.

- **커버리지 9/12.** 미검증 3건(AC-007 · AC-008 · AC-010)은 대상 산출물이 아직 없다.
  "돌려 봤는데 괜찮았다"가 아니라 **대상이 없어 돌리지 못했다**이다.
- **뮤테이션 자체가 두 번 헛돌았다.** AC-004 1차는 존재하지 않는 문자열을 치환해
  아무것도 바꾸지 않았고, AC-006 1차는 행을 지워 검사 대상이 아니라 행수 가드가 잡았다.
  두 경우 모두 재시도해 실제로 잡았고, `diff`로 적용 여부를 먼저 확인하는 절차를 §G에 남겼다.
- **죽은 코드 1행 발견.** AC-006 초안 awk의 `for(r in want) ;`는 `want`가 정의된 적 없어
  0회 순회하는 죽은 행이었다. `split()` 기반으로 교체했다.
- **AC-011이 자기 자신을 잡았다.** 금지형을 명시하는 문장(`즉시(1~2주)`)까지 걸려,
  검사 범위를 산출물 2종으로 좁히고 그 축소가 여는 구멍을 §A AC-011에 명시했다.
  가이드 쪽 시간 예측은 LDGUIDE `AC-LDG-007`/`§D.7`이 소유함을 실측 확인했다
  (`grep -n "시간 예측" .moai/specs/SPEC-COPILOT-LDGUIDE-001/acceptance.md` → `98:` `164:`).

- **lead 지적으로 결함 1건 추가 발견(2026-08-18).** 초판 `acceptance.md` HISTORY가
  `AC-001~010`이라 적었으나 실제 정의는 11건이었다. 숫자만 고치지 않고 **중복 출처를
  제거**했다 — AC 총계의 유일 출처를 `<!-- AC-COUNT: n -->` 리터럴 1곳으로 두고, HISTORY는
  총계 대신 증분만 적는다. 대조 가드로 AC-012(REQ-B16-008)를 신설하고 3방향 뮤테이션했다(§G.9).
  같은 원리로 이 문서 §A의 **행 수 열도 제거**했다 — 관리하지 않는 값을 적으면 중복 출처가 된다.

### §F.1 보고 문면 정정 — "잔여 grep 0건" (lead 지적, P3)

lead에 보낸 1차 회신에 세션 표기 정정 결과를 **"잔여 grep 0건"**이라 적었다. lead가 재보니
**1건**이었다(`progress.md:180` — §J 판본 귀속표의 `run-tjxy3n → plan-tjxy3n` 정정 기록).
그 1건은 남아 있어야 하는 자리이므로 파일 결함은 아니다. 어긋난 것은 **회신 문면**이다.

**원인은 범위가 아니라 시점이다.** 순서를 되짚으면: ① grep 실행 → 출력 0건 → ② §I·§J 작성
(이 과정에서 `run-tjxy3n` 문자열이 새로 들어감) → ③ ①의 값을 그대로 회신에 적음. 측정한
뒤 트리를 바꾸고 옛 측정값을 보고한 것이다. 저장소 규율로는 **읽은 값은 즉시 낡는다**에
해당하며, 커밋·푸시 직전 상태를 다시 읽으라는 조항과 같은 뿌리다.

교정: 수를 적을 때 **측정 시각 또는 측정 후 무변경 여부**를 함께 적는다. 이 건의 정확한
표기는 `잔여 1건(정정 기록 자체, 유지 대상)`이다.

이 항목은 파일 수정을 요구하지 않으며, 기록으로만 남긴다.

## §G. 닫지 않은 것 — 잔여 위험

1. **`exec` 미사용은 사후 기계 검증이 불가하다.** `probe.md`에 `exec`이 없다는 것은
   쓰지 않았다는 증거가 아니라 적지 않았다는 사실이다(`acceptance.md` §C.1).
2. **미측정 분기의 내용은 기계로 세지 않는다.** AC-005는 판정 열의 값 집합만 본다.
   GO·NEGATIVE 두 분기를 실제로 설계했는지는 사람이 본다(§C.2).
3. **프로브 후보 속성의 충분성은 AC가 보지 않는다.** 하한 9행은 역할 누락을 잡고
   속성 후보 누락은 잡지 못한다(§C.3).
4. **그룹 속성 73/101 미관측 잔여.** 승계하되 닫지 않는다. 처분은 이 잔여를 계약 문면에
   명시하는 형태로 고정한다 — "판독 불가"가 아니라 "관측 가능한 표면에서 판독 불가"다.
5. **AC-007·008·010 미검증.** run 착수 시 수행한다.

## §H. 감독 위임 결정 — lead 확정분 (2026-08-18)

Q1·Q2·Q4는 감독 위임을 받은 lead가 결정했다. **처분은 셋 다 ③(이번 SPEC 비범위)로 유지되며,
바뀐 것은 다음 SPEC의 방향이다.** 아래 결정과 근거를 M1 처분표의 `배정 근거` 열에 그대로 박는다.

| # | 질문 | lead 결정 | 근거 | 처분 |
|---|---|---|---|---|
| Q1 | 1단계 어긋남 판정 | **감독 판정 보조표**로 확정. 유형 분류 채택 안 함 | 어긋남의 정답이 설치 오차 / 도면 오류 / 의도적 변경 중 무엇인지에 달려 있는데 앱은 그 맥락을 갖지 못한다. 유형 분류를 넣으면 분류 기준이 곧 판정 기준이 되는데 그 기준을 세울 근거가 지금 없다 | ③ |
| Q2 | 5단계 사용자 정의 룩·장르 | **파일 편집 경로 우선.** 화면 학습 기능은 이번에 열지 않음 | 룩 32개·장르 4개가 이미 파일로 고정돼 있어 같은 형식을 여는 것은 새 표면이 거의 0이고 git으로 되돌릴 수 있다. 화면 저장은 저장 경로·검증·충돌 해소를 전부 새로 만들어야 하는데 무엇을 저장하고 싶은지에 대한 요구가 실사용에서 아직 나오지 않았다 | ③ |
| Q3 | 8단계 다중 콘솔 | (감독 결정 불필요 — 아래 §H.1) | 하드웨어 전제 부재 | ③ |
| Q4 | 6단계 오디오 구간 자동 분할 | **명시적 비범위로 고정.** 외부 오디오 의존 추가 안 함 | 디코딩·업로드·분석 의존은 배포 표면을 크게 넓힌다(맥 전용 v0.5.0 범위 축소 이력). 얻는 것은 사람이 구간 몇 줄 적는 수고의 대체다 | ③ |

**다음 SPEC 후보로 남기는 것** (이 SPEC이 구현하지 않는다):

- Q1 파생 — 실사용에서 어긋남의 반복 패턴이 쌓인 뒤 분류를 얹는다.
- Q2 파생 — 파일 편집 경로가 실제로 쓰이는지 관찰한 뒤 화면 저장 기능을 판단한다.
- Q4 파생 — 오디오 의존 대신 **구간 입력을 쉽게 만드는 쪽**: 붙여넣기 형식 관대화, 곡 길이 기반 등분 제안.

### §H.1 Q3이 이 표에서 결정 대기가 아닌 이유 (번호 건너뜀 해명)

초판 §H는 Q1·Q2·Q4만 실었고 Q3이 빠져 번호가 건너뛰었다. **누락이 아니라 분류 차이였으나,
그 사실을 어디에도 적지 않은 것이 결함이다.** 여기서 닫는다.

Q3(8단계 다중 콘솔)은 `spec.md` §D 표에 **처분 ③ · 결정 불필요**로 이미 닫혀 있다. 닫힌 자리는
`spec.md` §D 4행이며, 사유는 **"하드웨어 자원(콘솔 2대) 전제가 없다"** — 감독의 선호가 아니라
측정 대상 자체가 존재하지 않는다. Q1·Q2·Q4는 근거가 있어도 어느 쪽이든 갈 수 있는 제품 결정이라
감독 위임이 필요했고, Q3은 그렇지 않다.

→ 규약: **§D의 열린 질문 번호는 §H에 전량 나타난다.** 결정 불필요 항목도 그 사실을 값으로 적는다.
번호만 빠진 채로 두면 다음 세션이 누락으로 읽는다.

## §I. plan 종료 상태

| 항목 | 값 |
|---|---|
| plan 종료 | **감사 상한 도달 · 최종 FAIL 0.67 · blocking 12건 교정 완료.** 잔여는 §B.9.1로 run 게이트에 물림. kickoff 판단은 lead |
| status | `draft` |
| 문서 판본 | `spec.md` v0.3.0 · `plan.md` v0.2.0 · `acceptance.md` v0.2.0 |
| 감사 | 1회차 0.62(15건) · 2회차 0.65(11건) · **3회차 0.67(12건, 상한 도달)** — 전 회차 전건 교정 |
| lead 지시 처리 | 1차: 1(Q 결정) ✅ · 2(Q3 해명) ✅ · 3(세션 표기) ✅ · 4(plan-audit) 진행 중<br>2차: 결함1(AC 선언 수) ✅ REQ-008+AC-012 신설 · 정정2(잔여 grep 표현) ✅ 아래 §F.1 |
| 다음 단계 | **lead의 kickoff 판단** → PASS 계열이면 Implementation Kickoff Approval → Implementation Kickoff Approval → **M2 즉시 착수 가능**(측정 무의존) · M0는 콘솔 기동 대기 |

## §J. plan-audit

### 1회차 — **FAIL · 0.62 / 1.00**

| 항목 | 값 |
|---|---|
| 감사자 | `plan-auditor` (독립 spawn, 읽기 전용) |
| 판정 | **FAIL** |
| 점수 | **0.62 / 1.00** |
| 결함 | **P0 3건 · P1 4건 · P2 8건** |
| 감사 대상 판본 | `spec.md` v0.1.0 (§D·§H는 v0.2.0에서 재확인 — 감사 결함 목록에 §D·§H 항목 없음) |
| 6개 감사 우선순위 | **전부 수행**. 미수행 0 |

**절차 사고 1건**: 감사가 산출물을 자기 응답 본문에만 적고 판정 없이 `idle` 신호를 보냈다.
대기 신호는 판정이 아니므로 §J에 기록하지 않고 재요청했고, 2차에 전문이 도착했다.
**없는 점수를 지어내지 않는다**는 원칙이 지켜졌다.

### P0 3건 — 전부 재현했고 전부 교정했다

감사 주장을 그대로 받지 않고 명령을 직접 돌려 재현한 뒤 교정했다.

| # | 결함 | 내 재현 | 교정 |
|---|---|---|---|
| **P0-1** | **페이드는 미측정이 아니다.** CUETIME-001 M1이 2026-08-14 라이브로 Cue 레벨 `CueFade` 반증 + Part 레벨 `CueInFade` = 5.0 판독 확정 | `grep -rn "CueInFade" .moai/specs/SPEC-COPILOT-CUETIME-001/progress.md` → `16:` — **사실** | `spec.md` §A.1 표 · §A.4 전면 재작성 · §A.7 정정-2 · `plan.md` 게이트 A를 **Part 주소화**로 재정의 · 승계 3건화 |
| **P0-2** | **M2가 자기인증.** AC-008이 무변경 HEAD에서 3항목 전부 통과 | 무변경 트리에서 실행 → 조준 3 · 33 주장 2 · 34 오기 0 → **전 항목 통과. 사실** | REQ-B16-005 대상을 `id="appendix-tools"`로 이동 · AC-008 재작성 후 **현행 HEAD에서 FAIL 확인**(§G.10) |
| **P0-3** | **ASSUMPTION-27 승계가 검증되지 않은 유지 주장.** 소유 SPEC이 "전건 부정"을 P2-5로 철회 | `sed -n '644,652p' PRECHK-001/progress.md` → 철회 문면 + 미측정 후보 I-14·I-15 — **사실** | `spec.md` §A.3 전면 재작성. 처분을 **③ → ②**로 변경 |

**P0-1이 가장 아프다.** 이 SPEC은 REQ-B16-002로 "미측정을 부재로 읽지 말라"를 못 박아 놓고,
그 **거울상**(측정된 것을 미측정으로 읽기)을 스스로 저질렀다. 원인은 단순하다 —
CUETIME-001의 `spec.md`(검증 *계획*)를 읽고 `progress.md`(실측 *기록*)를 열지 않았다.
그대로 M0를 돌렸으면 이미 반증된 핸들을 다시 쏘고, 그 NEGATIVE로 **"큐시트 페이드 열은
비운다"를 영구 계약으로 굳혔을 것이다.** 읽히는 값을 두고 비운다고 못 박는 형태다.

교훈을 `plan.md` M0-4에 [HARD]로 박았다: **승계는 소유 SPEC의 `progress.md`를 연다.**

### P1 4건 — 전부 교정

| # | 결함 | 교정 |
|---|---|---|
| P1-1 | AC-006이 **게이트 통째 누락**을 못 잡음(게이트 축만 데이터 유도) | 게이트 집합을 `split("A B C")`로 하드코딩 + 첫 글자 정규화. 재현: A만 9행 원장 → `B/C 6건 없음` 검출(§G.6) |
| P1-2 | AC-007이 행 번호를 검증 안 함 + `spec.md` 행번호 오기 2건 | 좌표 해소 단계 신설(awk 단일 명령, 3방향 검증 §G.11). `footprint.py:131→130` · `cue_monitor.py:47→49` |
| P1-3 | 멤버십 승계가 잔여 한계 2개 중 1개만 옮김 | `plan.md` M0-4에 우회 경로 사망 판정(`응답기 1.7.0 확장은 증거상 사망했다`) 인용 추가 |
| P1-4 | M3 경계 검사에 대응 AC 없음 | **REQ-B16-009 + AC-013 신설**(변경 파일 허용 2경로). `AC-LDG-013`은 허용 경로가 달라 위임 불가 |

### P2 8건 — 8건 교정

| # | 내용 | 처리 |
|---|---|---|
| P2-1 | AC-004 셀 안 파이프 밀림 fail-open | `NF != 10` 가드 신설. 재현 검증됨 |
| P2-2 | AC-011 대상 파일 부재 시 `PASS` | 존재 단언으로 교체 |
| P2-3 | plan에 정정 행 서식 없음 | `plan.md` M1에 `| 정정-` 서식 [HARD] 추가 |
| P2-4 | 식별자 체계 이원화(`6-a` vs `D-n`) | `plan.md` M1에 D-1~D-16 ↔ 산문 대응표 추가 |
| P2-5 | HISTORY AC 선언 수 불일치 | **lead가 먼저 지적해 이미 교정**(REQ-B16-008 + AC-012) |
| P2-6 | `/^[①②③]$/`가 `LC_ALL=C`에서 미매치 | 교대형 `/^(①\|②\|③)$/`로 교체. 재현: 브래킷 `0` / 교대 `1` |
| P2-7 | frontmatter `module`이 PRESERVE와 모순 | `server/** 무접촉(plan §C.1 PRESERVE)`으로 정정 |
| P2-8 | 초판이 승계 참조를 `AC-019`(LDG 없음)로 적었으나 실제 ID는 `AC-LDG-019` | 3곳 정정. 재현: LDGUIDE `acceptance.md`에서 `AC-019` 단어 경계 매치 `0`건 / `AC-LDG-019` `1`건. **교정 중 부분 문자열 치환으로 이 설명 행 자체가 한 번 뭉개졌다**(`grep -c 'AC-019'`가 `AC-LDG-019` 안에 매치되는 규약 6 함정을 정규식 치환에서 재생산). 단어 경계로 재확인해 복원 |

### 감사가 CLEAN으로 확인한 것 (점수의 근거)

감사가 명령을 직접 돌려 재현한 것 중 핵심:

- **§G 뮤테이션 원장 8/11을 전건 재현했고 부풀려지지 않았다** — LDGUIDE의 4/19 선례와 다르다.
- 도구 수 33 · GROUPGEN 인용 2건 행번호 정확 · `props COUNT` 4/4=0 전사 충실 ·
  footprint 상단 인용 정확 · `ARRANGE_READ_AXES:1319` 정확 · `responder_roundtrip` 플래그 4종 실재.
- **`--skip-exec`가 정말 읽기 전용인가** — 본문 정독으로 `if not skip_exec:`가 유일한 `exec`
  스텝을 감싼다 확인. **§C.3 프로브 안전 경계 CLEAN.**
- AC-009가 LDGUIDE `AC-LDG-019`의 충실한 승계이며 `tn >= 30` 가드를 **추가**해 원본보다 강함.
- 게이트 D(승계 확인)는 **자기인증이 아님** — 앵커가 사라지면 실제로 NEGATIVE로 떨어진다.
- 미측정 분기는 **값 층위에서 진짜 분기** — AC-005가 `판독 불가`를 기계적으로 거부.

### 감사가 확인하지 못한 것 (감사의 천장)

1. **라이브 프로브 전량.** 지시대로 콘솔 미접촉. 게이트 A·B·C 실제 판정은 M0 몫.
   특히 P0-1 교정형(임의 큐에서 Part 경로 주소화)은 라이브로만 답한다 — CUETIME이 확인한
   것은 자기가 만든 Seq 101 Cue 1 한 건이다.
2. `progress.md` §C의 콘솔 미접속 실측(시점 스냅샷이라 재현 보장 없음).
3. §G.6의 "죽은 코드 1행" — 초안이 남아 있지 않아 **검증 불능**(SUSPECTED 아님).
4. AC-005 파이프 밀림의 최악 조합 — 구성 가능하다고 판단만 하고 만들지 않음.
   P2-1의 `NF` 가드가 두 경우를 함께 닫는다.
5. PRECHK I-15의 성립 여부 — 콘솔 미재확인. **P0-3의 논거는 "그 경로가 성립한다"가 아니라
   "소유 SPEC이 미측정으로 남겼다"**이므로 이 한계에 영향받지 않는다.
6. v0.2.0의 §D/§H 개정본 — lead 통보 범위를 신뢰해 재감사하지 않음(결함 목록에 해당 항목 없음).

### 2회차 — **FAIL · 0.65 / 1.00** (회귀 없음, 3회차 가용)

산출물: `.moai/specs/SPEC-COPILOT-BLOCKED16-001/plan-audit-report-r2.md` (626행).
**파일 채널이 통했다** — 1회차는 응답 본문에만 적혀 유실됐고, 2회차는 파일을 지정해
내가 직접 읽었다. 감사가 다시 판정 없이 `idle`을 보냈으나 이번에는 기다릴 필요가 없었다.

| 항목 | 값 |
|---|---|
| 판정 | **FAIL** · **0.65** (1회차 0.62 → 회귀 아님, STOP 신호 없음) |
| 결함 | **P0 1건 · P1 6건 · P2 4건** (blocking 7) |
| run 진입 | 차단 |
| 필수 통과 기준 | **실패 0건** — FAIL은 루브릭 점수와 blocking 결함에서 나온다 |
| 루브릭 | Clarity 0.75 · Completeness 0.75 · Testability 0.60 · **Traceability 0.50** |

#### 왜 점수가 거의 안 올랐는가 — 기계 층은 닫혔고 내용 층이 열렸다

감사가 1회차 교정의 **기계 층을 전건 재현**했고 전부 판별력을 보였다(§6 CLEAN 목록).
점수가 0.62 → 0.65에 그친 이유는 **Traceability 0.50**이다.

**1회차 P0-1의 실패 경로 — "소유 SPEC의 계획서를 읽고 실측 기록을 열지 않았다" — 가
한 번만 밟힌 것이 아니었다. 세 번이었고, 갈수록 교묘해졌다.**

| # | 형태 | 적발 | 무엇이 통과했는가 |
|---|---|---|---|
| 1 | 계획서만 읽고 **실측 기록을 안 열었다** | 1회차 P0-1 | — |
| 2 | 기록을 열었으나 **뒷부분을 안 읽었다** | 2회차 N-1 | 인용 좌표는 정확히 해소된다 |
| 3 | 기록을 끝까지 읽었으나 **기록이 가리킨 곳을 안 열었다** | 2회차 P0-1r | 좌표 해소 + 게이트 D + AC-007 **전부 통과** |

세 번째가 가장 교묘하다 — 기록을 열었고, 인용도 정확하고, 내가 P0-1 대응으로 만든
게이트 D도 통과한다. **P0-1에 대응해 만든 게이트가 P0-1의 결함 계열을 구조적으로 못 잡았다.**

#### P0-1r — 게이트 A가 실측되지 않은 주소 형식을 썼다 (재현함)

```bash
sed -n '21p' .moai/specs/SPEC-COPILOT-CUETIME-001/progress.md
#  → - 신규 실측 함정 2건 스킬 §3b 기록: 큐 이름 점(.) 제거, CueInFade
sed -n '166,170p' .claude/skills/ma3-spatial-pointing/SKILL.md
#  → prop:DataPool/Sequences/<n>/<cueName>/<partName>|CueInFade (part 이름은 큐 이름과 동일)
```

나는 `progress.md:16-17`은 읽고 **21행이 가리킨 스킬 문서를 열지 않았다.** 실측 형식은
**이름 기반**인데 내 게이트 A는 `Cue <C>` / `Part <P>` **번호 형식**을 썼고, 그 형식은
저장소 어디에도 실측 기록이 없다.

**왜 P0인가**: ③은 *양성 대조군*이다. 미실측 형식으로 쏘면 기지 GO 기준점이 `ok:false`를
답하고, plan §B M0-3의 자기 규정("`ok:false` 일색을 판정으로 쓸 수 없다")이 발동해
**게이트 A 배치 전체가 무효**가 된다. 6-a는 프로브를 돌리고도 `미측정`으로 남는다.
**틀린 판정보다 나쁘다 — 판정이 없는데 있었다고 믿게 된다.**

교정: 축을 `<n>` → `<cueName>` → `<partName>` 이름 기반으로 재정의. 양성 대조군을
`DataPool/Sequences/101/Pos 228/Pos 228` + `CueInFade`(기대 `5.0`)로 고정.
**점 제거 함정**(`Pos 2.28` → `Pos 228`)을 [HARD]로 병기.

#### N-1 — 인용 창이 문단이 반전하는 바로 그 줄에서 닫혔다 (재현함)

I-15를 "미측정, 다음 후보"로 적었으나 **이미 출하됐다**:

```bash
sed -n '914p' .moai/specs/SPEC-COPILOT-PRECHK-001/progress.md
#  → | 1 | ~~구간 겹침 재개 (후보 I-15)~~ — **출하 완료, SPEC-COPILOT-OVERLAP-001**
grep -n '^status:' .moai/specs/SPEC-COPILOT-OVERLAP-001/spec.md   # 5:status: completed
grep -rn "bound_proves_clear" server/prechk/patch.py               # 72: BOUND_PROVES_CLEAR
```

기전: §A.3 발췌가 `footprint.py` 3~6행에서 닫혔는데 **8행부터 반전한다.** 6행
`shipped disabled`는 OVERLAP 이전의 역사 서술이고 8행부터가 현재 동작이다.
**4행 창이 정확히 그 경계에서 닫혔다.**

이 문면이 처분표로 옮겨졌으면 다음 세션은 **이미 출하된 SPEC을 다시 열도록** 지시받는다.
처분값 ②는 유지된다(I-14가 진짜 미측정). 무너진 것은 배정 근거다.

#### 나머지 blocking 5건

| # | 결함 | 교정 |
|---|---|---|
| N-2 | REQ-B16-004가 **승계 2건**에 멈춰 있어 REQ의 S와 그 REQ의 AC가 서로 다른 수를 요구 | E에 페이드 호명, S를 3건 + 내용 대조 + 후속 반전으로 확장. `plan.md`·AC 제목 전파 |
| N-3 | **REQ-B16-008을 정의한 문서가 자기 수를 두 번째로 틀렸다** — 기대 문자열·산문 `12`, 그리고 `13/13`과 "미검증 3건"이 **같은 문단에서 서로를 부정** | 산문에서 총계 제거(수를 안 쓰는 문면으로), "미검증 0건"으로 정정, §G.9 관측 행에 판본 명기 |
| N-4 | AC-008 **자기인증이 하위검사로 이동** — 종료 앵커가 없어 푸터가 잡히고, 조준·발화조건 검사를 기존 도구 표 행과 푸터가 충족. 게다가 `^<tr data-tool=`이 **승인되지 않은 33행 재작성**을 요구 | 종료 앵커 추가, 전용 리터럴 2종(`non-tool`·`fire-condition`), 행 수는 **현행 마크업**으로 계수(변경 0) |
| N-5 | 게이트 C NEGATIVE→③이 **이 SPEC이 인용해 온 규칙과 모순** ("어떤 프로퍼티 프로브 집합도 부재 증명이 될 수 없다") | ③→**②**, 문면을 "현행 읽기 표면 + 발화한 이름 4개 한정 미성립"으로 범위화 |
| N-6 | **게이트 D가 "승계 확인"이 아니라 "인용 해소 확인"** — 빈 줄을 가리키는 인용도 통과(감사가 재현) | 술어 2개 추가: 인용 행의 **내용 대조** + 소유 SPEC **후속 반전 탐색**. plan M0-4b 신설 |

#### P2 4건

N-7 미추적 오염(AC-013이 못 봄) → AC-013에 `git status --porcelain` 축 + `plan.md` §C.2
명시 경로 스테이징 [HARD]. **감사가 지목한 `.moai/specs/.moai/state/config-cache.json`은
내 세션이 잘못된 상대 경로에 떨어뜨린 파생 캐시였고 제거했다.**
N-8 `cited`가 매치가 아니라 행을 셈 → `grep -oE | wc -l`. N-9 외부 URL 축 → AC-013에 추가.
N-10 게이트 C에 프리셋 표면 미열거 → 잔여에 명기.

#### 게이트 C 판정 — 감사가 내 자진 신고를 **반증했다**

나는 게이트 C의 단일 핸들 축을 P0-1의 거울상 후보로 올렸다. 감사 판정은 **아니다**:

1. **피벗할 하위 층이 없다** — 픽스처 노드는 `childCount 0`(PRECHK `progress.md:296`).
   Cue↔Part와 같은 형상이 아니다.
2. **핸들은 살아 있다** — 같은 슬롯에서 `posx/posy/posz`가 읽힌다. 양성 대조군 설계는 옳다.
3. **Pan/Tilt는 저장소 전역에서 커맨드 표면뿐** — `읽기 경로 0건`.

→ **두 번째 축이 실재한다는 증거가 없으므로 "단일 축"은 여기서 결함이 아니다.**
미리 고치지 않고 넘긴 판단이 옳았다 — 내가 고쳤으면 근거 없는 축을 추가했을 것이다.
게이트 C의 실제 결함은 다른 곳(N-5 · N-10)에 있었다.

#### 감사가 확인하지 못한 것

1. 라이브 프로브 전량(콘솔 미접촉). **P0-1r도 라이브 없이 판정** — 근거는 관측이 아니라
   "SPEC이 쓴 형식 ≠ 저장소에 실측 기록된 형식"이라는 문서 대조다.
2. **번호형이 실제로 실패하는지는 관측하지 않았다.** 번호형도 통하면 P0-1r의 피해는
   줄지만 결함은 남는다 — **기지 GO 기준점은 기지형으로 쏘아야 한다.**
3. "미검증 3건"이 어느 판본의 잔재인지(작성자만 안다 → 내가 정정: 0건이 참).
4. stray 캐시의 생성 주체 — SUSPECTED로 남김.
5. GEARS 형식(1회차 판정 승계, 델타 밖).
6. 프리셋 표면이 실제로 pan/tilt를 답하는가(라이브 필요).

### 2회차 이후 — lead 신규 P0 (감사 밖에서 나옴)

**AC-013의 stray 축이 항상 통과하는 검사였다.** lead가 교정분을 대조하다 잡았다. 재현:

```bash
find . -type d -name '.moai' -not -path './.git/*'
#  ./.moai  ./.moai/specs/.moai  ./.moai/specs/SPEC-COPILOT-BLOCKED16-001/.moai  ./docs/curriculum/.moai
git status --porcelain -uall | grep -c 'BLOCKED16-001/.moai'      # 0
git check-ignore -v .moai/specs/SPEC-COPILOT-BLOCKED16-001/.moai/state/config-cache.json
#  → .gitignore:248:.moai/specs/*/.moai/
```

stray는 하나가 아니라 **셋**이었고, 셋째가 결정적이다 — **SPEC 디렉터리 안**이며 그 자리가
SPEC 세션의 cwd가 가장 자주 놓이는 곳인데, `.gitignore:248`이 명시적으로 무시한다.
`-uall`로도 안 뜬다. **내 검사의 통과는 부재의 증거가 아니라 관측 표면 밖이라는 뜻이었다.**

`.gitignore:248`(`.moai/specs/*/.moai/`)·`:250`(`server/**/.moai/`)은 **누군가 전에 겪고
gitignore로 "고친" 흔적**이다. 저장소 교훈(검사를 고치면 항상 통과하는 새 검사가 생긴다 —
3라운드 중 두 번째 기전이 gitignore였다)의 **세 번째 재발**이다.

교정: stray 축을 `find` 기반 **중첩 `.moai` 검사**로 교체(gitignore를 읽지 않는 표면).
248행을 지우는 방향은 택하지 않았다 — 다른 SPEC의 기대를 깨고, "안 보이게 만든 규칙을
지워서 보이게 한다"는 교정은 범위 밖이다. **보는 쪽을 바꿨다.**
뮤테이션: `docs/.moai` 주입 → 검출, 정리 후 0건.

#### 생산자 — 통제 실험으로 특정함

lead가 재현을 시도했으나 파일이 갱신되지 않아 생산자를 특정하지 못했고, 추측하지 말라고
지시했다. stray 3건을 전부 제거해 기준선을 만든 뒤 쟀다:

| 실험 | 명령 | 결과 |
|---|---|---|
| 1 | 평범한 Bash 호출(훅 발화), cwd=루트 | 재생성 **0** |
| 2 | `(cd .moai/specs && moai todo list)` | `.moai/specs/.moai/` **생성** |
| 3 | `(cd .moai/specs/SPEC-COPILOT-BLOCKED16-001 && moai session current)` | 해당 위치에 **생성** |
| 4 | 실험 2 재실행(파일 이미 존재) | mtime **불변** |

**생산자 = 프로젝트 루트가 아닌 cwd에서 호출된 `moai` CLI.** 상태 캐시를 cwd 상대 경로에 쓴다.
실험 4가 lead의 관측과 정합한다 — **생성은 되지만 이미 있으면 갱신하지 않아** 기존 파일만
보면 안 보인다. 두 관측이 어긋난 게 아니라 서로 다른 조건을 본 것이다.

→ `plan.md` §C.2에 **`moai`는 프로젝트 루트를 cwd로 호출한다 [HARD]** 추가. stray 3건 제거 완료.

### run 착수 게이트 — 감사 상한 이후의 잔여 처리 (lead 확정)

내가 제안하고 lead가 채택했다. `plan.md` §B.9에 [HARD]로 박았다:

> **M0·M1은 인용에서 유래한 전제로 처분을 쓰지 않는다.** 처분표의 각 행이 인용된 전제에
> 기대는 경우, 그 전제를 M0에서 3술어(①좌표 해소 ②내용 일치 ③후속 반전 부재)로 재확인한
> 뒤에 쓴다. 재확인 없이 쓴 처분은 ③이든 ①이든 **무효**다.

감사 3회차가 상한이지만 잔여 위험은 남는다. 잘못된 ③은 아무도 반증하지 않으므로,
감사에서 못 캔 몫을 run 게이트로 넘긴다.

### 3회차 — **FAIL · 0.67 / 1.00** (상한 도달. 4회차 없음)

산출물: `plan-audit-report-r3.md`. 점수 추이 0.62 → 0.65 → **0.67**, 회귀 없음.

| 항목 | 값 |
|---|---|
| 신규 결함 | P0 2 · P1 5 · P2 3 · P3 2 (blocking 7) |
| 2회차 교정 판정 | 닫힘 6 · **이전(relocated) 2** · 미해결 0 |
| **TASK A 커버리지** | **68 / 68** (미확인 0) — 술어 실패 3건 |
| **TASK B 커버리지** | **13 / 13 AC** — 구조적 맹점 6건 |
| 필수 통과 기준 | 실패 0 |

**방식을 바꾼 것이 값을 냈다.** lead 지시대로 표본 늘리기를 버리고 열거로 갔더니
**분모가 생겼고**(68), 술어 실패 3건이 그 안에서 나왔고, 관측 표면 감사가 뮤테이션이
볼 수 없던 맹점 6건을 냈다. 같은 방식으로 4회차를 돌리면 68을 다시 셀 뿐이다.

#### P0-1 (R3-1) — 예고된 변종 (4). 고치는 행위가 같은 계열을 새로 만들었다

```bash
sed -n '160,162p' .claude/skills/ma3-spatial-pointing/SKILL.md | grep -cE '삼킨|Pos 228'   # 0
grep -n '점(\.)은 MA3가 삼킨다' .claude/skills/ma3-spatial-pointing/SKILL.md              # 163
```

내 인용은 `:160-162`, 실제는 **163-165**. 160-162는 3D 프레임 diff에 관한 **다른 주제**다.

세 가지가 겹쳐 P0다. **주장 자체는 참이라** 내용을 읽는 사람은 이상을 못 느끼고 **좌표를
따라가는 사람만 빈손**이 된다. 그리고 이 인용은 **2회차 P0-1r 교정이 새로 단 것**이다 —
"기록이 가리킨 곳을 안 열었다"를 고치면서 SKILL.md 인용 2건을 신설했고 **하나가 오기였다.**
게다가 점 제거 규칙은 게이트 A 양성 대조군의 전부라, 틀리면 기지 GO 기준점이 `ok:false`를
답해 배치 전체가 무효가 된다 — P0-1r이 막으려던 결말 그대로다.

**같은 실패 경로가 이제 네 번째다**: 계획서만 읽기 → 뒷부분 안 읽기 → 가리킨 곳 안 열기 →
**가리킨 곳을 열었으나 좌표를 3행 어긋나게 적기.**

#### P0-2 (R3-2) — 문서가 아니라 지금 트리의 실제 위반. 내가 만들었다

2회차 뒤 stray 청소가 **PRESERVE 경로의 추적 파일을 지웠다**:

```bash
git status --porcelain | grep '^ ?D'
#  → D docs/curriculum/.moai/state/context-usage.json
git ls-files docs/curriculum | grep moai
#  → docs/curriculum/.moai/state/context-usage.json      (추적본이었다)
```

stray 3건을 한 번에 `rm -rf` 했는데 **그중 하나는 2026-08-12에 커밋된 추적 파일**이었고,
`docs/curriculum/**`는 `plan.md` §C.1 PRESERVE 목록에 있다. 나머지 둘의 내용은 확인했으면서
이 하나는 같은 부류로 간주하고 추적 여부를 보지 않았다. **지우기 전에 대상을 본다**는
규율을 두 개는 지키고 하나는 어긴 것이다.

`git restore`로 복원했다. 그리고 이 사고가 **AC-013의 오탐 하나를 드러냈다** — `find` 기반
중첩 `.moai` 검사가 이 추적본을 stray로 잡는다. 술어를 다듬었다: **추적 파일이 0건인
중첩 `.moai`만 stray다.** 양방향 확인(추적본 통과 · 진짜 stray 검출).

#### 산문에 있던 규율을 검사로 옮겼다 — 이번 회차 최대 소득

감사가 §7.2로 정리한 G-1~G-5는 전부 "**산문으로는 정확한데 검사가 없는**" 항목이었다.
세 회차 연속 blocking이 나온 계열이 정확히 거기다.

| # | 규율 | v0.6.0에서 착지한 검사 | 검증 |
|---|---|---|---|
| G-1 | 게이트 D 술어 ②(내용 대조) | AC-007에 `check_token` 3건 | 정정 후 3/3 PASS · 옛 좌표(160) 주입 시 FAIL |
| G-2 | 승계 3건 개별 귀속 | AC-007에 세 패턴 개별 `>=1`(합계 3은 한 소스 3회로 충족됨) | — |
| G-3 | status 판독 불가 처리 | 게이트 D 술어 ③을 **3값화**(`반전 없음` / `반전 있음` / `판독 불가`) | — |
| G-4 | 양성 대조군이 기지 기준점인가 | AC-006에 `Pos 228` 요구 + `Pos 2.28` 금지 | 올바른 주소 PASS · 점 포함 FAIL · 번호형 FAIL |
| G-5 | 점(.) 제거 규칙 | 위 두 번째 줄이 기계로 잡는다 | 동일 |

추가로 R3-6(AC-004 `미정` 리터럴) · R3-7(AC-001 집합 대조) · R3-5(AC-011에 가이드 포함)도
착지시켰다. R3-5는 **위임 전제가 술어 ③에서 무너진** 사례다 — LDGUIDE-001은 `completed`인데
이 SPEC의 M2가 그 가이드를 새로 고친다. 완결된 SPEC의 AC는 이후 변경을 지키지 않는다.

#### 교정 중 자체 발견 — BSD awk 다바이트 등호 버그 (규약 7 신설)

R3-6 교정(`v=="미정"`)을 뮤테이션하다 **위양성**이 나왔다. `③`과 `무관`까지 미정으로 잡혔다.

```bash
printf '③\n' | awk '{print ($0=="미정")}'            # → 1   (거짓이어야 한다)
printf '③\n' | LC_ALL=C awk '{print ($0=="미정")}'   # → 0   (정상)
awk version 20200816
```

**이 플랫폼의 BSD awk는 기본 로케일에서 다바이트 문자열 등호를 전부 참으로 답한다.**
정규식 매치(`~`)는 정상이라 기존 AC-002·003·005는 무사했으나, 등호를 쓰는 순간 무너진다.
**반응은 하는데 판별력이 0인 형태** — AC-007 1차형과 같은 계열이며, 뮤테이션 없이는
"모든 행이 걸리네, 검사가 엄격하구나"로 읽고 넘어갔을 자리다.

→ 규약 7 신설: **`awk`는 반드시 `LC_ALL=C`로 호출한다 [HARD].** 전 호출 12건에 강제했고
양방향 재검증했다(정상 표 PASS · 미정 주입 FAIL, 위양성 0).

#### 상한 이후 잔여 — `plan.md` §B.9.1에 명시 승계

감사가 §7에 정리한 L-1~L-9(라이브 필요) · G-6~G-11(기계 검사 없이 남는 규율)을
`plan.md` §B.9.1에 표로 옮겼다. **분모 밖에 남은 것이지 확인하지 못한 몫이 아니다.**

남기는 이유: 이름 붙여 두지 않으면 다음 세션이 "감사 3회 돌았으니 깨끗하다"로 읽는다.
G-11(AC-012 3출처 동시 오기)은 **닫지 않는 것이 옳다** — 네 번째 출처를 두면
REQ-B16-008 자신을 위반한다.

#### 감사의 천장 (확인하지 못한 것)

라이브 프로브 전량 · R3-1의 실질 피해 크기(잘못된 주소가 정말 `ok:false`를 답하는지) ·
삭제된 파일의 내용 · stray 나머지 둘의 추적 여부(추정이며 증명 아님) · `.gitignore:248`의
생성 주체(SUSPECTED) · 산출물 부재로 AC-001~006·010을 실물에 못 돌린 것 ·
AC-005 열 밀림 최악 조합.

### 3회차 착수 기록 (보존)

lead 지시: **같은 방식으로 한 번 더 훑지 말 것.** 두 회차 연속 blocking이 전부 승계·인용의
내용 층에서 나왔으므로, 3회차도 같은 방식이면 새 변종 하나를 더 찾고 상한에 닿는다.
표본을 늘리는 걸로는 안 끝난다 — **열거로 바꾼다.**

| # | 3회차에 건 것 | 왜 |
|---|---|---|
| 1 | **인용 전수 열거** — SPEC 3종의 모든 `파일:행` 인용과 모든 승계 주장을 목록화하고 각 항목에 3술어. 커버리지를 **N/N으로 진술** | "훑었다"가 아니라 "N건 중 N건"이어야 상한 뒤에 잔여를 말할 수 있다 |
| 2 | **AC 13건 전건의 관측 표면 검사**(신규·필수) — "이 검사가 잡겠다는 결함을 이 검사의 관측 표면이 실제로 볼 수 있는가" | AC-013이 이미 여기서 떨어졌다. 뮤테이션은 "검사가 반응하는가"를 보고 이건 "검사가 그 자리를 볼 수 있는가"를 본다 — **다른 질문이다** |
| 3 | 델타 한정 + 파일 채널 | 2회차대로 |

### 3회차 필요 여부 (2회차 시점 판단 — 기록 보존)

**필요하다.** blocking 7건이 게이트 A 설계 · 게이트 C/D 술어 · AC-008 하위검사를 바꿨고,
`spec.md` §A.3/§A.4가 재작성됐다. 2회차 판정 0.65는 이 판본에 적용되지 않는다.
**3회차가 상한이다** — 그 뒤로는 감사 회차를 늘려도 얻는 것이 없다(LDGUIDE 선례:
같은 깨진 명령을 보는 감사는 회차를 늘려도 못 잡는다).

**교정 후 자체 확인분** (재감사를 대체하지 않는다):

| 검사 | 관측 |
|---|---|
| AC-012 자기검사 | `PASS 선언=13 정의=13 분모=13` |
| 새 AC-008 · 무변경 HEAD | 부록 구간 49행(푸터 제외 확인: `대상 앱` 0건) · `non-tool` 0 · `fire-condition` 0 → **FAIL**. 도구 표 33행 계수 성공(마크업 변경 0) |
| AC-013 미추적 축 | 오염 주입 → `PASS 검출: .moai/specs/STRAY-TEST.md` / 정리 후 `PASS 미추적 오염 0` |
| AC-013 외부 URL | `PASS 외부 URL 0` |
| stray 캐시 | 제거 확인 |

### 2회차 착수 기록 (보존)

lead가 2회차 GO를 냈다. 형식 이유(P0가 §A·게이트 설계를 바꿔 0.62가 이 판본에 적용 안 됨)
외에 lead가 든 실질 이유를 기록한다:

> 1회차 P0 3건이 **전부 "처분을 뒤집는" 결함**이었다(미측정→측정완료 · 자기인증 제거 · ③→②).
> 이 SPEC의 1차 산출물이 처분표이므로 **결함이 산출물의 심장부에 3건** 있었다는 뜻이고,
> 그 기저율이 교정본에서 갑자기 0이 될 이유가 없다.
>
> 그리고 이 결함군은 **비대칭**이다. 잘못된 ①·②는 다음 세션이 재보다 걸린다.
> 잘못된 ③은 "읽을 수 없다"가 영구 계약으로 굳고 **아무도 반증하지 않는다.**
> P0-1이 정확히 그 직전까지 갔다.

**전달 채널 고정 [HARD]**: 1회차는 감사가 산출물을 자기 응답 본문에만 적어 전달되지 않았다.
**"응답으로 보냈다"는 전달이 아니다.** 2회차는 파일 경로를 지정해 거기 쓰게 하고, 내가 그
파일을 읽어 §J에 옮긴다.

- 지정 경로: `.moai/specs/SPEC-COPILOT-BLOCKED16-001/plan-audit-report-r2.md`

**2회차 감사에 건 조건 4건** (lead 지시):

| # | 조건 | 이유 |
|---|---|---|
| 1 | 델타 한정 + 3회차 상한 | 무변경 구간은 1회차 판정 승계 |
| 2 | **승계 3건 전량을 소유 SPEC의 `progress.md`로 재대조** | P0-1의 실패 경로("계획서만 읽고 실측 기록을 안 열었다")가 한 건에서만 밟힌다는 보장이 없다. 통과도 **값으로** 적어야 미대조와 구분된다 |
| 3 | 산출물 전달 채널을 파일로 고정 | 위 |
| 4 | **P0-1의 거울상을 게이트 C·D에도 쳤는지 확인** | 교훈을 게이트 B에만 적용한 것이 P0-1의 뿌리다. 같은 누락이 C·D에 남아 있을 수 있다 |

**조건 4에 대한 자진 신고**: 게이트 C(팬/틸트)는 현재도 **단일 핸들 축**
(`Patch/Stages/1/Fixtures/<slot>`)이다. 미리 고치지 않고 감사에 넘긴다 — 두 가지 이유다.
(a) 내가 먼저 고치면 감사는 내 수정을 승인할 뿐 독립 판정이 아니게 된다.
(b) 페이드와 달리 **대안 핸들이 자명하지 않다.** `server/spatial/pointing.py:11`은
`Attribute 'Pan'/'Tilt' At <n>`을 **커맨드 표면**으로 다루며, 이것이 오브젝트 트리의
픽스처 노드 속성으로도 읽히는지는 미확인이다. 구조적으로 Cue↔Part와 같은 형태일 수 있다.

### 2회차 재감사 필요 여부 (1회차 시점 판단 — 기록 보존)

**필요하다.** P0 3건이 전부 `spec.md` §A와 `plan.md` 게이트 설계를 바꿨고, AC 6건이 교정되고
2건이 신설됐다. 1회차 판정은 이 판본에 적용되지 않는다. 감사자가 "2회차는 결함 델타로
한정"을 예고했다(3회차 상한).

**교정 후 자체 확인분**(재감사를 대체하지 않는다):

| 검사 | 관측 |
|---|---|
| AC-012 자기검사 | `PASS 선언=13 정의=13 분모=13` |
| REQ↔AC 1:1 | 미대응 0 (REQ 9건 ↔ AC 13건) |
| 새 AC-008 · 무변경 HEAD | `FAIL` — 자기인증 제거 확인 |
| 뮤테이션 커버리지 | **13/13** (신규 §G.10~13 + lead 관측 M4) |

---

## §K. run 단계 (run-tjxy3n, 2026-08-18)

기준: 워크트리 `/Users/studiox/orca/workspaces/AI-Lighting_Console/blocked16`,
브랜치 `jjjh7401/blocked16`, base `15590e3`. `git rev-list --count --left-right origin/main...HEAD`
→ `0 0`(착수 시점 동기). 중첩 `.moai` stray 0건(`docs/curriculum/.moai`는 **추적본**이므로
오염이 아니다).

**착수 상태의 정직한 표기**: plan-audit 3회차 최종 판정은 **FAIL 0.67**이며, 교정 후 재판정은
**존재하지 않는다**(상한 도달). run은 그 위에서 시작했고 `plan.md` §B.9 착수 게이트를 이행했다.

### §K.1 빌드 순서와 결과

| 마일스톤 | 상태 | 산출 |
|---|---|---|
| M2 조준 노출 | 완료 | `docs/user-guide.html` 참조 부록에 비도구 기능 절 신설 |
| M0 비파괴 프로브 | 완료(게이트 A·B·C `미측정`, 게이트 D `GO`) | `probe.md` |
| M1 처분표 | 완료 | `disposition.md` |
| M3 회귀·경계 | 완료 | 본 절 §K.4 |

### §K.2 M2 — 조준 노출 (측정 무의존, 먼저 착수)

착수 전 현행 HEAD에서 AC-008을 그대로 돌려 **자기인증이 아님을 확인**했다:

```bash
apx=$(LC_ALL=C awk '/id="appendix-tools"/{f=1} f&&/<footer/{f=0} f' docs/user-guide.html)
printf '%s' "$apx" | grep -cE 'class="non-tool"'        # → 0
printf '%s' "$apx" | grep -cE 'class="fire-condition"'  # → 0
```

교정 후:

```
PASS 비도구 절 1 · 발화 조건 1 · 도구 표 33행
PASS 유령 0 (도구 명단 33건)          # AC-009
PASS 시간 예측 0                       # AC-011
urls=0                                 # 외부 URL
```

**뮤테이션 — 양방향**(정상 입력 PASS + 이상 입력 FAIL 둘 다 확인):

| # | 뮤테이션 | 관측 | 판정 |
|---|---|---|---|
| 0 | 정상 입력 | `PASS rows=33` | 통과해야 함 → 통과 |
| 1 | 비도구 절 삭제 | `FAIL non-tool 0건` | 잡힘 |
| 2 | 발화 조건 표제만 삭제 | `PASS` | **잡히지 않음 — 그러나 결함 아님.** 조건 내용이 ② 행에 그대로 남아 있어 검사가 옳게 통과했다. 표제만 지우는 것은 의미 보존 편집이지 뮤테이션이 아니다 |
| 2b | 발화 조건 **내용 전체** 삭제 | `FAIL 대상 없음` · `FAIL 목표 없음` | 잡힘 |
| 3 | 도구 표에 34행째 추가 | `FAIL rows=34` | 잡힘 |

뮤테이션 2가 통과한 것을 "검사 결함"으로 적지 않는다. 레버를 잘못 당긴 것이며, 2b가
진짜 레버였다(저장소 교훈 `mutation-test-every-relaxed-assertion` 승계).

### §K.3 M0 — 프로브

**게이트 A·B·C = `미측정`.** onPC 미접속이 실측으로 확인됐고(`ps` 출력 없음), 앱 서버의
TCP 8000도 열려 있지 않아 프로브 매체 자체가 발화 불가였다. UDP 9005를 잡은 것은 콘솔이
아니라 로컬 파이썬 프로세스다. **`판독 불가`로 적지 않았다** — AC-005가 기계로 막는 값이며,
plan §A.4가 미측정 분기를 유효한 출력으로 설계해 두었다.

**게이트 D = `GO`.** 승계 3건 전부 3술어 통과. 대조군 2종을 함께 쏴 검사 장치의 판별력
(날조 앵커 3건 → `0 0 0`)과 생존(실재 앵커 3건 → `1 1 2`)을 각각 확인했다.

술어 ③에서 **CUETIME-001만 `판독 불가`**다 — `spec.md`에 frontmatter가 없어 `status:`를
기계로 읽을 수 없다. 값 그대로 원장에 적고 대체 근거(`progress.md:3` M1 완료 선언 +
반전 마커 계수 `0`)를 명령·출력으로 남겼다.

#### §K.3.1 [HARD] run이 잡은 좌표 결함 2계열 — 같은 계열의 4·5번째 재발

**(1) 물려받은 인용의 창이 근거 앞에서 닫혀 있었다.**
`spec.md` REQ-B16-005가 인용한 `server/web/session.py:3546-3555`는 "대상이 없으면
`return None`"을 근거로 삼는데, 그 `return None`은 **3557행**이고 창은 3555에서 닫혔다.
창 안의 3548행 `return None`은 **동사 부재** 경로여서 주장의 근거가 아니다. 좌표는
해소되고 주장도 참인데 **그 좌표가 근거를 담고 있지 않았다** — 술어 ②가 잡는 정확히
그 형태이며, 감사 3회차 R3-1과 같은 계열의 **4번째** 사례다.
→ 정정 좌표 `server/web/session.py:3547-3557`. `probe.md` §3.1에 명령·출력으로 기록.

**(2) run이 새로 단 인용 7건이 어긋났다 — 내가 5번째가 됐다.**
`disposition.md` 초고에서 Q1~Q4를 `spec.md:415~418`로, 게이트 C 프로브를 `probe.md:39`로,
파생 관계를 `plan.md:216`으로 적었다. 전부 좌표는 해소되지만(①통과) **내용이 다른 행**
이었다 — Q 행은 419~422, 게이트 C는 43, 파생 관계는 243이다.

lead가 지시서에서 경고한 그대로다: *결함 계열을 고치는 행위가 같은 계열의 새 사례를
만든다.* 술어 ②를 **자기 산출물의 전 좌표에 기계로 돌려** 잡았다:

```bash
grep -oE '[A-Za-z0-9_./-]+\.(md|py|html):[0-9]+' disposition.md | sort -u | while read -r pair; do
  f=${pair%:*}; n=${pair##*:}; printf '%-70s ' "$pair"; sed -n "${n}p" "$f" | cut -c1-90
done
```

교정 후 20건 전량 재확인 완료. **인용을 다는 행위 자체가 이 계열의 발생원이므로, 다음
세션도 자기가 새로 단 인용에 이 루프를 돌려야 한다.**

### §K.4 M3 — 회귀 · 경계

| # | 검사 | 관측 |
|---|---|---|
| 1 | AC-001 처분표 16행·집합·중복 | `PASS rows=16 · id 집합 1..16 · 중복 0` |
| 2 | AC-002 처분 열 값 집합 | `PASS 처분 열 전량 적법` |
| 3 | AC-003 측정 종류 열 | `PASS 측정 종류 전량 적법` |
| 4 | AC-004 7필드 공란·`미정` | `PASS 공란 0` |
| 5 | AC-005 프로브 판정 열 | `PASS 판정 열 전량 적법` (12행) |
| 6 | AC-006 대조군 쌍 + 게이트 A 기준점 | `PASS 대조군 쌍 전량 존재` · `PASS 게이트A 양성대조 = 기지 기준점, 점 제거됨` |
| 7 | AC-007 승계 인용 3건 + 술어② + 좌표 해소 | `PASS 인용 좌표 전량 해소` |
| 8 | AC-008 조준 노출 | `PASS 비도구 절 1 · 발화 조건 1 · 도구 표 33행` |
| 9 | AC-009 유령 식별자 | `PASS 유령 0` |
| 10 | AC-010 정정 3건 | `PASS 정정 3건` |
| 11 | AC-011 시간 예측 | `PASS 시간 예측 0` |
| 12 | AC-012 AC 수 3중 일치 | 아래 |
| 13 | AC-013 변경 범위·stray·외부 URL | 아래 |

**AC-005/006 뮤테이션 — 양방향**:

| # | 뮤테이션 | 관측 | 판정 |
|---|---|---|---|
| 0 | 정상 입력 | AC-005 `PASS` · AC-006 `PASS` · 기준점 `PASS` | 통과해야 함 → 통과 |
| 1 | 판정을 `판독 불가`로 치환 | AC-005 `FAIL` 9행 적발 | 잡힘 |
| 2 | 게이트 C 3행 삭제 | AC-006 `C: 본프로브/음성대조/양성대조 없음` | 잡힘 |
| 3 | 양성 대조 이름에 점 복원(`Pos 2.28`) | `FAIL 점 포함` | 잡힘 |

### §K.5 §B.9.1 잔여 승계 — run 종료 시점의 값

**분모 밖에 남은 것이지 확인된 몫이 아니다.** 각 항목이 어떻게 됐는지 값으로 적는다.

| # | 잔여 | run 종료 시 값 |
|---|---|---|
| L-1 | 게이트 A 이름 주소의 타 SPEC 큐 일반화 | **여전히 미측정** — 콘솔 미접속. D-10 양분기로 설계 이관 |
| L-2 | 다중 Part 큐의 part 이름 규칙 | **여전히 미측정** — 동상 |
| L-3 | 번호 형식이 정말 실패하는가 | **여전히 미측정.** 게이트 A는 이름 형식으로만 설계했고 번호 형식은 쏘지 않는다 — 기지 기준점은 기지형으로 쏜다는 결정이 유지됐다 |
| L-4 | 게이트 B 큐 노드 내부 값 | **여전히 미측정** |
| L-5 | 게이트 C pan/tilt가 픽스처 노드 속성인가 | **여전히 미측정** |
| L-6 | 게이트 C 포지션 프리셋 저장값 표면 | **여전히 미측정.** D-5 NEGATIVE 분기 문면에 잔여로 명시됨 |
| L-7 | I-14 반환 타입 판독 | **범위 밖** — `exec`/`deploy` 금지. D-2 근거 열과 §2 잔여 문면에 그대로 실림 |
| L-8 | Group 속성 73필드 미관측 | **범위 밖** — `introspect` 페이지네이션 비목표. D-6 계약 문면에 실림 |
| L-9 | `exec` 미사용의 사후 검증 | **원리적으로 산출물에서 증명 불가**(신고된 한계). 발화 0건이므로 이번 run에서는 진위가 자명하나, 그것을 산출물이 증명하지는 못한다 |
| G-6 | `moai` cwd 루트 호출 | **해소(이번 run 한정)** — `moai` CLI를 호출하지 않았고, `find` 축으로 중첩 `.moai` stray 0건을 확인했다 |
| G-7 | 주 체크아웃 무접촉 | **해소(이번 run 한정)** — 모든 쓰기가 워크트리 내부. `git -C` 형태만 사용, `cd` 0건 |
| G-8 | `미측정` 행 양분기 설계 | **해소** — `disposition.md` §3이 4행의 GO/NEGATIVE 분기를 값으로 담는다. **여전히 기계 검사는 없다**(AC-005는 값 집합만 본다) |
| G-9 | 정정 3건의 내용이 §A.7과 같은가 | **사람 리뷰 몫으로 남음.** 내용은 §A.7에서 옮겼으나 AC-010은 행 수만 센다 |
| G-10 | 프로브 후보 속성 목록의 충분성 | **남음** — 미측정이므로 충분성 자체를 재볼 기회가 없었다 |
| G-11 | AC-012 3출처 동시 오기 | **닫지 않는다 — 그것이 옳다.** 네 번째 출처를 두면 REQ-B16-008 자신을 위반한다 |

### §K.6 run이 넘기는 것

1. **콘솔이 켜지면 M0 재발화.** `probe.md` §2의 12행이 그대로 실행 계획이다. 게이트
   A·B·C 9행의 판정만 갈아 끼우면 되고, D 3행은 재실행 불필요(콘솔 무관).
2. **D-10 · D-12 · D-5 · D-16의 처분은 재발화 결과에 따라 갱신된다.** 분기 문면은
   `disposition.md` §3에 이미 있다.
3. **좌표 결함 계열은 닫히지 않았다.** 이번 run에서 2계열 8건이 나왔고 전건 교정했으나,
   기전은 "인용을 새로 다는 행위"이므로 다음 세션도 같은 루프를 돌려야 한다(§K.3.1).

### §K.7 커밋 게이트 — 환경 실패 1건 (근거 있는 "무관")

첫 커밋 시도에서 pre-commit 게이트가 실패했다.

```
sh: vitest: command not found
[pre-commit] FAILED: moai gate reported errors above.
```

**무관하다고 적기 전에 근거를 세웠다**(저장소 교훈 `feedback_unrelated_failure_needs_evidence`
승계 — 실패 메시지가 내가 건드린 파일을 지목하면 내 탓이다):

```bash
ls -d ui/node_modules                                        # 없음 — 이 워크트리는 npm install 이력이 없다
git diff --cached --name-only | grep -cE '\.(js|jsx|ts|tsx|vue|svelte|json)$'   # → 0
ls -d /Users/studiox/Documents/Claude/Code/AI-Lighting_Console/ui/node_modules  # 주 체크아웃에는 있음
```

스테이징된 9건에 JS·TS가 0건이므로 vitest 실패는 이 변경이 만든 것이 아니다.
**그러나 실패를 우회하지 않았다** — `SKIP_MOAI_PRECOMMIT=1`을 쓰면 게이트가 도는데
판별력이 0인 형태가 되므로, 워크트리에 의존성을 설치해 게이트를 **실제로 통과시켰다**.

```bash
npm --prefix ui ci
git check-ignore -v ui/node_modules
#  → .gitignore:223:node_modules/	ui/node_modules      (오염 아님 — AC-013 축에 잡히지 않는다)
```

재커밋 `75af120` 게이트 통과.

### §K.8 AC-012 · AC-013 (커밋 이후 실행)

```
PASS 선언=13 정의=13 분모=13            # AC-012
BASE=15590e3630ffced112eb7b17e8d2c353fcd1fc97
변경 파일 9건 → PASS 허용 2경로뿐        # AC-013
PASS git 가시 오염 0
PASS 중첩 .moai stray 0
PASS 외부 URL 0
```

**AC-001~013 전건 PASS.** 단, 이것은 산출물이 AC를 만족한다는 뜻이지 처분이 옳다는 뜻이
아니다 — 게이트 A·B·C가 `미측정`인 채로 통과했고, 그것이 이 SPEC의 설계다.
