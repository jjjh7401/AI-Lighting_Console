---
id: SPEC-COPILOT-PARITY-001
type: sync-audit
version: "3.0.0"
created: 2026-08-22
updated: 2026-08-22
author: sync-auditor (칸반 카드 t11 · 감사 레인)
verdict: PASS
score: 0.92
completed_gate: BLOCKED
blocker: X-1 (spec.md 정정이 구현 브랜치에 없음 - 조율 결함, 처분 기계적)
verdict_2nd: PASS-WITH-DEBT
score_2nd: 0.90
verdict_1st: PASS-WITH-DEBT
score_1st: 0.86
audited: ff4ac74 + f816973
audited_2nd: 94f9675
audited_1st: fc37abc
threshold: 0.80
tier: M
---

# SPEC-COPILOT-PARITY-001 — 독립 sync 감사

대상: `origin/WT-handle-to-name` 커밋 `fc37abc`. 트리 `.claude/worktrees/t11-sync-audit` · 브랜치 `WT-parity-sync-audit`.
범위: 콘솔 무접촉 · 제품 코드 수정 0건 · 판정만.
같은 감사자가 이 SPEC 의 plan-audit 1차(FAIL 0.72) · 2차(PASS 0.88)를 냈다. **이번은 다른 질문이다 — 구현이 그 SPEC 을 충족하는가.**

---

## §1 Claim — 판정

**PASS-WITH-DEBT — 0.86 / Tier M 기준 0.80.**

**AC 12건 전부 통과**를 재현했고 `completed` 전이는 **정당하다.** 다만 어디에도 공개되지 않은 **주요 결함 1건(D-1)** 과 경미 2건(D-2 · D-3)이 있다. D-1 은 AC 를 깨지 않으나 **이 카드가 스스로 세운 코드 계약을 이 카드의 배선이 어긴다.** 이 카드가 처음부터 끝까지 지켜 온 것이 「공개」이므로, 닫기 전에 **고치거나 공개하거나** 둘 중 하나를 권한다 — 어느 쪽이든 닫힌다.

이 구현의 증거 규율은 이 저장소에서 본 것 중 상위다. 스스로 불리한 항목 둘을 올렸고(D4 초과 · 미검사 갈래), 「8곳 중 1곳만 배선」이라는 자기 설계의 한계를 **구문적 사실로** 못 박았으며, 어긋남 3건을 **열린 채로 사용자 문서에까지** 실었다.

---

## §2 Evidence

### 재현한 것 — 리드 값과 대조

| # | 검증 | 결과 |
|---|---|---|
| R1 | 커밋 사슬 6단 | 배차와 일치 (`5c988bd` → `aabcdde` → `581a1fb` → `d6b1c49` → `4152bd4` → `fc37abc`), `fc37abc` 는 `5c988bd` 의 자손 |
| R2 | 봉쇄 구역 `5c988bd..fc37abc`(`vwx` · `paperwork`) | **빈 출력** — 일치 |
| R3 | `4152bd4..fc37abc` 변경 파일 | `test_prechk_handle_types.py` · `progress.md` 둘뿐 — 일치 |
| R4 | `status:` | `implemented` — 전이 안 됨, 일치 |
| R5 | **전체 스위트 직접 실행** | `9705 passed, 8 skipped, 1 warning in 155.33s` · **exit 0** — 리드 기준선 `9705` 와 일치 |
| R6 | 신규 테스트 14건 | `test_prechk_handle_types.py` **12 passed** · `test_lxseq_tool.py -k handle` **2 passed** |
| R7 | 복구 체크섬 | `shasum -a 256 server/prechk/mode_read.py` → `8fac1096147abe2fab267e93eb7994ee84cc52fac49a0286fcf49957c2787871` — progress.md 가 인용한 `8fac1096…7871` 과 일치 |
| R8 | 신설 코드가 `aabcdde` 이후 무변경 | `aabcdde..fc37abc` 의 `mode_read.py` · `inventory.py` · `tools.py` → **빈 출력** |

**R5 가 이 감사의 중심 증거다.** 1·2차 plan-audit 에서 내가 여덟 항목의 Gaps 중 첫 번째로 적었던 「테스트를 한 건도 돌리지 않았다」가 이번에 닫혔다. 기준선을 남에게 듣지 않고 직접 쟀다.

### 자진 신고 2건 — 둘 다 결함이 아니다

**① D4 문면 초과 → 문서 지체.** `plan.md` D4 는 「A-1 이면 `inventory.py` + 테스트」라 적었고 실제는 `mode_read.py` · `tools.py` 2파일 초과다. 그러나 두 파일 모두 **계획이 지시한 것**이다 — `plan.md` M1-3 이 「(슬롯→이름) 전체 표를 돌려주는 판독을 **신설**」하라 했고(그 문장은 이 감사자의 1차 지적으로 들어갔다), 판독 공유 결정이 호출자의 표 전달을 요구한다. `acceptance.md` 를 임의로 고치지 않은 것은 소유 경계 준수다(manager-spec 소유). **결함 아님 — 문서 지체.**

**② 미검사 갈래 2건 → 닫혔고, 예외도 정당했다.** `mode_read.py:127` · `:130`. 리드 예외(테스트 파일만)가 기계적으로 지켜졌음을 R8 · R7 이 확인한다. 두 테스트는 결과가 같은 두 갈래를 `attempted` 로 가르고, 뮤테이션이 **각각 자기 테스트 한 건만** 죽였다 — 판별력의 증거다(이 저장소가 「뮤테이션 6회 중 2회 통과」로 물린 자리라 이 형태가 중요하다). **예외 승인 정당.**

---

### D-1 (주요) — 트리 판독 실패가 「슬롯 부재」로 보고된다. 이 코드의 자기 주석이 금지한 바로 그 혼동이다

> **`completed` 로 가기 전에 고치거나 공개해야 하는 이유**: 이 카드가 세운 계약을 이 카드의 배선이 어기고 있고, 그 사실이 progress.md · CHANGELOG · README 어디에도 없다. 이 카드가 지켜 온 유일한 방어선이 「공개」다.

**측정했다 — 코드를 읽은 추론이 아니다.** 트리 판독을 `ok:false` 로 거절하는 리더를 만들어 실행했다.

```
attempted: False | pairs: () | by_slot(): {} | is None: False
translate(failed-tree table): ('FixtureType 4', 'slot_absent')
translate(no table at all)  : ('FixtureType 4', 'no_type_table')
```

`by_slot()` 은 판독이 실패해도 `None` 이 아니라 **빈 dict** 를 돌려준다. 유일한 배선(`tools.py:4518`)은 `attempted` 를 보지 않고 `.by_slot()` 을 그대로 넘긴다. 그래서 **트리가 답하지 않았을 때 모든 핸들이 `slot_absent` 로 표시된다.**

이 코드 자신이 그러면 안 된다고 적어 두었다.

- `TypeNameRead` 주석: *"``attempted`` separates 'the tree did not answer' from 'the tree answered and declared nothing'. Both leave ``pairs`` empty, and **a caller that conflates them would report a read failure as 'slot absent'**."*
- `FixtureRecord.fixture_type_untranslated` 주석: 두 사유를 가르는 이유는 **「순서 결함(고칠 수 있다)」과 「리그 사실(고칠 수 없다)」을 혼동하지 않기 위해서**다.

즉 주석이 예고한 실패가 그대로 일어난다. 그리고 이것은 **D2 자신과 같은 결함 부류**다 — 라벨이 틀려 사람이 엉뚱한 손을 움직인다. `slot_absent` 를 읽은 감독은 「내 콘솔에 그 타입이 없다」로 이해해 리그를 손보러 가고, 실제로 필요한 것은 **재시도**다.

**등급을 주요로 두고 차단으로 올리지 않은 이유** — 셋을 함께 적는다.

1. **AC 를 깨지 않는다.** AC-PARITY-009 가 요구하는 것은 「원값 보존 + 미번역 표식」까지이고 둘 다 성립한다. 사유의 정확성을 요구하는 AC 는 없다.
2. **쓰기는 0이다.** D2 와 같이 라벨 결함이지 안전 결함이 아니다.
3. **노출이 부분적으로 가려질 수 있다 — 다만 이것은 내가 재지 않았다.** 배선된 경로에서 `read_type_mode_widths`(`:4510`)가 **같은 트리를 먼저** 읽으므로, 트리가 죽으면 모드 미해석으로 먼저 걸러질 가능성이 있다. **코드 순서를 읽은 추론이며 실행해 확인하지 않았다** — 그래서 이 완화를 근거로 등급을 더 내리지도 않았다.

**고치는 법(제안)**: 호출 지점에서 `attempted` 를 보고 실패 시 `None` 을 넘기거나, `by_slot()` 이 `attempted=False` 일 때 `None` 을 돌려주게 한다. 어느 쪽이든 몇 줄이고, 그 갈래를 고정하는 테스트가 함께 필요하다. **고치지 않기로 한다면 progress.md · CHANGELOG 에 「트리 판독 실패는 현재 `slot_absent` 로 보고된다」를 적으면 이 항목은 닫힌다.**

### D-2 (경미) — CHANGELOG 의 검증 수치가 출하 HEAD 보다 낡았다

CHANGELOG 는 「`9703 passed` · 손대기 전 기준선 `9691` 대비 **+12 = 신규 테스트 수와 정확히 일치**(단위 10 + 통합 2)」로 적었다. 그러나 출하 HEAD `fc37abc` 는 **`9705`** 이고 신규 테스트는 **14건**(단위 12 + 통합 2)이다. `grep -c "9705" CHANGELOG.md` → **0**.

`fc37abc` 후속 커밋이 `progress.md` 는 갱신하면서(§E.4 정정에 `9705` · +2 가 있다) **사용자 문서는 갱신하지 않았다.** 숫자 자체가 틀린 것은 아니고 `d6b1c49` 시점에는 맞았으나, 출하되는 트리와 어긋난다. 이 저장소가 반복해 물린 **「측정한 것과 옮긴 것이 다르다」** 의 시간축 변형이다.

**고치는 법**: CHANGELOG 검증 줄을 `9705` · +14(단위 12 + 통합 2)로 갱신하고 후속 커밋에서 미검사 갈래 2건을 닫았다는 한 줄을 붙인다.

### D-3 (경미) — M2 집계 줄의 산술이 어긋난다

progress.md 분류표 아래 집계: 「어긋남 3(C3 · C4 · C5) · 닫힘 2(C1 · C9) · **무관 5**(C2 · C8 · C10판정 · C11 · C12 · C13 중 판정축)」 — 괄호에 이름이 **여섯** 있는데 수는 **5** 로 적혔다. 검산하면 3 + 2 + 6 = 11 = 「측정됨 11」과 맞으므로 **6이 옳다.** 바로 다음 절의 A-1 표도 「닫을 필요가 없다 | C2 · C8 · C10 · C11 · C12 · C13」로 여섯을 든다.

---

## §3 관찰

- **O-1. 리드가 준 측정 하나가 재현되지 않는다.** 배차문은 `581a1fb..fc37abc -- server/ ui/` → **빈 출력(제품 코드 0)** 이라 적었으나, 그대로 돌리면 `server/tests/test_prechk_handle_types.py | 29 +++++` 가 나온다. **주장은 참이다** — 테스트를 제외한 형태(`':!server/tests/'`)로 다시 재면 빈 출력이고, 제품 코드는 실제로 0건이다. 어긋난 것은 **인용한 명령과 그 출력의 짝**이다. 이 저장소가 이 카드에서만 세 번 물린 모양(8→13 · 20→22 · 9703↔9705)의 같은 계열이며, 이번엔 리드 쪽이다. 명령을 고치거나 「테스트 제외」를 명시하면 닫힌다.
- **O-2. AC-011 은 통과하나, 미측정 2건은 acceptance 가 허용한 종류가 아니다.** `acceptance.md` 는 「**오프라인으로 결론이 안 나는 자리**는 미측정으로 남기는 것이 옳은 결과」라고 **조건을 달았다.** C6(`tools.py:3734`) · C7(`prechk/patch.py:236`)은 오프라인으로 측정 가능한 자리이고, progress.md 자신이 그렇게 적었다 — 「핸들러를 실행해 페이로드를 관측하지 않았다」 · 「`to_dict()` 를 실행하지 않았다」. 즉 **「옳은 미측정」이 아니라 「안 잰 것」이다.** 그럼에도 **AC-011 문면은 통과한다**: 요구는 각 행에 (a) 판정 (b) 측정됨/미측정 (c) 근거가 있을 것이고, 두 행 모두 셋을 갖췄다. 표가 스스로 정직하므로 결함이 아니라 관찰로 둔다 — 다만 「옳은 결과」라는 수식을 이 두 행에 붙이면 그건 과장이다.
- **O-3. A-1 의 표제 이득은 배송되지 않았다 — 그러나 은폐도 없다.** `spec.md §A.4` 는 A-1 을 고른 이유를 「C1~C13 **동시 해결**」로 적었다. 실제로 닫힌 것은 **C1 · C9 둘**이고, 이는 같은 표가 A-2 에 배정한 범위(「C1만 해결」)와 사실상 같다. A-1 의 구조적 이점(자리를 판독 경계에 잡아 두어 다음 호출자가 표만 넘기면 번역이 따라온다)은 남지만, **오늘의 커버리지는 A-1 을 고른 근거와 다르다.** progress.md 가 이를 「8곳 중 1곳」으로 못 박고 CHANGELOG · README 가 열린 3건을 사용자에게까지 알리므로 **은폐는 없다.** `spec.md` 문면이 갱신되지 않은 것은 소유 경계(manager-spec) 때문이며 문서 지체다. 다음 카드가 그 줄을 고칠 자리다.
- **O-4. C3 거짓 경보가 실제로 사용자 문서까지 갔다.** 리드가 [HARD] 로 지킨 세 문장을 열어 확인했다 — CHANGELOG 에 「**모든 타입이 `console_count=0` 으로 보고된다. 실기에 86대가 있어도 「콘솔에 아무것도 없다」로 읽힌다**」 · 「AC 전건 통과가 실기 D2 해소의 증거는 아니다」 · 「이 저장소에는 테스트 CI 가 없다」 셋 다 있다. README 도 「closed **offline**」 · 「**This has not been re-verified on a console**」 · 미번역 소비자 2건을 담는다. **오프라인 보증이 실기 보증으로 승격되지 않았다.**
- **O-5. 내 2차 지적 O-8 이 반영됐다.** 「라이브 관측 단 1건」이 CHANGELOG 에서 「한 번의 판독에서 86행 · 8개 슬롯이 모두 같은 형식」으로 바뀌었다. 근거를 부풀리지 않고 사실에 맞춘 정정이다.

---

## §4 AC 12건 판정

| AC | 판정 | 재현한 증거 |
|---|---|---|
| 001 | PASS | `FakeConsole(handle_types=...)` 생성자 인자 1개. 기본 False → 이름 |
| 002 | PASS | 기본 경로 불변. 스위트 `9691`(M1-0) → `9705`(HEAD), 증가 14 = 신규 테스트 14건. 내가 잰 것은 HEAD 쪽 `9705` 이며 `9691` 은 §E.2 인용이다 |
| 003 | PASS | `test_the_handle_branch_is_off_by_default_and_leaves_the_tree_naming` — 한 테스트 안에서 트리는 이름 · 프로퍼티는 핸들을 함께 단언 |
| 004 | PASS(기록) | 수정 전 RED 는 §E.2 M1-2 의 기록이며 **재현 대상이 아니다**(번역 코드가 이미 있다). 그 목적은 D3 뮤테이션이 보증한다 |
| 005 | PASS | `test_a_handle_answering_console_still_reports_already_patched` — 통과 확인. 호출이 아니라 **결과**(`already_patched`)를 단언한다 |
| 006 | PASS(인용) | 뮤테이션 4종 KILLED 는 §E.2 기록. **내가 재현하지 않았다**(제품 코드 수정 0건 범위) — §5 Gaps 1번 |
| 007 | PASS | `test_a_handle_becomes_the_name_its_slot_declares` — `("FixtureType 10", {10:...})` → 이름, 표식 None |
| 008 | PASS | `test_a_name_passes_through_unchanged_even_when_absent_from_the_library` — **라이브러리에 있는 이름과 없는 이름 양쪽**을 단언(acceptance 요구 그대로) |
| 009 | PASS | `test_an_unresolvable_handle_keeps_the_raw_value_and_says_why` — **원값과 표식을 둘 다** 단언. 「표식만 검사하면 공허하다」는 acceptance 경고가 테스트 주석에 그대로 있다. **단 (a) 갈래의 사유가 틀린다 — D-1** |
| 010 | PASS | `test_the_same_name_on_two_slots_is_not_ambiguous_forward` — 번호가 아니라 「같은 이름 두 슬롯」 형상만 재현 |
| 011 | PASS | C1~C13 열세 행 · 각 행에 판정 · 측정여부 · 근거. 측정 11 · 미측정 2. 집계 산술은 D-3, 미측정의 성격은 O-2 |
| 012 | PASS | **직접 실행**: `9705 passed, 8 skipped, 1 warning in 155.33s` · exit 0 |

**12/12 PASS.** 004 · 006 은 성질상 기록 인용이며 그 사실을 위에 명시했다.

---

## §5 `completed` 전이 판정 — 이 감사가 그 게이트다

**전이해도 된다.** 근거 넷:

1. **AC 12/12 통과**, 그중 핵심 넷(002 · 005 · 009 · 012)은 내가 직접 실행해 재현했다.
2. **범위가 지켜졌다** — 봉쇄 구역 0-diff, 제품 코드는 `581a1fb` 이후 0건, `fc37abc` 예외는 조건대로 테스트 파일만.
3. **열린 것이 열린 채로 공개됐다** — 어긋남 3건이 progress.md · CHANGELOG · README 세 층 모두에 있고, 「오프라인 보증 ≠ 실기 보증」이 사용자 문서에 그대로 있다.
4. **자진 신고 2건은 결함이 아니다**(문서 지체 · 닫힘).

**다만 D-1 은 닫고 가라.** 차단으로 올리지 않는 이유는 §2 에 셋으로 적었으나, 이 카드가 처음부터 지켜 온 방어선이 「공개」이고 **D-1 만이 실재하면서 공개되지 않은 유일한 항목**이다. 고치는 것(몇 줄 + 테스트)과 적는 것(한 줄) 둘 다 싸다. 어느 쪽이든 닫힌다.

D-2 · D-3 은 문서 한 줄씩이며 `completed` 를 막지 않는다.

---

## §6 Baseline-attribution

| 항목 | 값 |
|---|---|
| 대상 | `fc37abc` (`origin/WT-handle-to-name`) |
| 트리 | `.claude/worktrees/t11-sync-audit` · 브랜치 `WT-parity-sync-audit` |
| 스위트 | `uv run pytest server/tests -q` → `9705 passed, 8 skipped, 1 warning in 155.33s` · exit 0 **(이 트리, 이 실행)** |
| 표적 실행 | `test_prechk_handle_types.py` 12 passed · `test_lxseq_tool.py -k handle` 2 passed |
| 프로브 | 거절 리더로 `read_fixture_type_names` → `by_slot()` → `translate_fixture_type` 실행 (D-1 의 근거) |
| 체크섬 | `mode_read.py` = `8fac1096…7871` |
| 읽은 원문 | `mode_read.py`(신설 `TypeNameRead` · `read_fixture_type_names`) · `inventory.py`(`HANDLE_TEXT` · `translate_fixture_type` · `_name_handle_types` · `read_inventory` 시그니처) · `tools.py`(배선 9행) · `progress.md` 384행 · `CHANGELOG.md` PARITY 항목 · `README.md:286-301` · 신규 테스트 14건 |
| 콘솔 | 접속 0건 |

기준선은 **`9705`**(`fc37abc`)를 썼다. `9703`(M1-6) · `9691`(M1-0)은 §E 인용으로만 쓰고, 다른 카드의 `9681` · `9682` 는 인용하지 않았다.

---

## §7 Gaps — 내가 재현하지 않은 것

1. **뮤테이션 4종(D3)을 재현하지 않았다.** 배차 범위가 「제품 코드 수정 0건」이라 번역 지점을 항등으로 바꿔 보지 않았다. AC-006 은 §E.2 의 기록을 **인용**한 것이며 내 관측이 아니다. 대신 복구 체크섬(R7)을 대조해 「본문이 되돌아왔다」까지는 독립 확인했다.
2. **D-1 의 완화가 실제로 성립하는지 재지 않았다.** 「모드 판독이 같은 트리를 먼저 읽어 가려질 것」은 **코드 순서를 읽은 추론**이다. 배선 경로 전체를 태워 확인하지 않았다.
3. **C6 · C7 을 내가 대신 재지 않았다.** M2 의 몫이고 감사가 대신하면 카드를 먹는다.
4. **어긋남 3건(C3 · C4 · C5)의 원인 동일성을 재지 않았다.** progress.md 도 「원인이 하나로 보이나 측정 안 했다」로 남겼고 나도 재지 않았다.
5. **콘솔 무접촉.** 실기 핸들 형식, 슬롯 12, 86대 재조회 — 전부 하지 않았다. 이 카드의 최대 잔여 가정이 그대로다.
6. **UI 스위트를 돌리지 않았다.** CHANGELOG 의 「496 passed 불변」은 인용이다.
7. **`read_inventory` 나머지 7곳의 질의 순증을 재지 않았다.** M1·sync 가 남긴 항목 그대로.
8. **`/code-review` 는 이번에도 내가 돌리지 않았다.** 감독이 병행 중이라고 들었으나 **내 판정은 그것을 기다리지도 참고하지도 않았다** — 두 검사가 독립이어야 대조에 의미가 있다는 배차 지시대로다.

---

## §8 Residual-risk

- **D-1 을 고치면 새 갈래가 생긴다.** `attempted=False` 에 `None` 을 넘기면 그 경로의 표식이 `no_type_table` 로 바뀐다 — 「이 경로가 트리를 안 읽었다」는 뜻인데 실제로는 「읽었으나 답이 없었다」이다. 두 사유로 부족할 수 있으며 세 번째 사유(`table_read_failed`)가 필요할 수 있다. **설계 판단이므로 여기서 정하지 않는다.**
- **A-1 의 구조적 이점은 아직 가설이다.** 「다음 호출자가 표만 넘기면 번역이 따라온다」는 실제로 두 번째 호출자가 배선될 때 증명된다. 오늘은 한 곳뿐이라 A-2 와 커버리지가 같다(O-3).
- **이 감사도 한 형태다.** 원문 대조 + 표적 실행 + 전체 스위트 + 프로브 하나. `/code-review` 가 무엇을 낼지는 모르며, 이 저장소는 감사 2회가 놓친 것을 코드 리뷰가 잡은 전례를 갖는다(t9). **내 목록에 없는 축**: 성능 · 동시성 · 신설 판독의 예외 안전성 · UI · 실기.
- **가짜↔가짜 괴리 발견(t12 재료)이 시사하는 것.** 이번 카드가 찾은 첫 실례는 「가짜 ↔ 실물」이 아니라 **「가짜 ↔ 가짜」** 였다. 오프라인만으로 가능한 축이 남아 있다는 뜻이며, t12 를 「실기 필요」로만 틀 지으면 그 축을 놓친다. 이 지적은 progress.md 가 이미 적었고 내가 동의한다.

---

**결론: PASS-WITH-DEBT 0.86 · `completed` 전이 정당.** D-1 은 고치거나 적어라. D-2 · D-3 은 문서 한 줄씩.

감사자: sync-auditor (t11 감사 레인) · 대상 `fc37abc` · 트리 `.claude/worktrees/t11-sync-audit`


---
---

# 재판정 (2차) — 정정본 `94f9675`

> **1차 판정(PASS-WITH-DEBT 0.86)은 위에 그대로 둔다.** 이 절은 덧붙인 것이며 frontmatter 는 두 판정을 함께 싣는다.

대조 기준: `fc37abc`(1차 대상) → `94f9675`. **「고쳤다」가 아니라 diff 를 읽었다.**
범위: **D-1 · D-2 · D-3 · AC-011 수식** 네 자리 + **정정이 새로 연 것**. `spec.md §A.4` 는 `plan` 레인 미완이라 **이번 범위 밖**이다.

## 재판정 Claim

**PASS-WITH-DEBT — 0.90 / 기준 0.80.** (1차 0.86)

**네 자리 전부 닫혔다.** D-1 은 내가 권한 것보다 **강한 형태**로 닫혔고, run 이 자체 점검에서 **다섯째(절단)** 를 스스로 찾아 함께 닫았다. 새 결함은 **주요 1건(N-1) · 경미 1건(N-2)**.

**`completed` 전이는 이 부채와 무관하게 정당하다.** 게이트 질문은 1차에서 답했고(AC 12/12), 그때 게이트를 물고 있던 부채(D-1)가 닫혔다. N-1 은 **행동이 1차 시점과 동일한** 자리이므로 그때 막지 않은 것을 지금 막을 수 없다.

## 닫힘 판정 — 다섯 갈래를 전부 실측했다

배선(`tools.py`)까지 따라가 **`read_inventory` 를 실제로 통과시켜** 사유를 읽었다. 판독기가 무엇을 아는지가 아니라 **소비자에게 무엇이 도착하는지**를 쟀다 — 이 결함의 자리가 정확히 그 사이였기 때문이다.

```
1 표를 안 넘김(None)      : no_type_table
2 트리가 예외로 죽음      : type_tree_unreadable
3 트리가 ok:false 로 거절 : type_tree_unreadable
4 자식 형태 불량          : slot_absent                     <- N-1
5 전수 목록, 슬롯 없음    : slot_absent
6 절단 목록, 슬롯 안 보임 : slot_unseen_truncated_listing
7 빈 라이브러리(전수)     : slot_absent
```

### D-1 — **닫힘. 그리고 내가 권한 것보다 강한 형태다**

내가 보낸 경고는 「`None` 을 넘기면 사유가 `no_type_table` 로 **옮겨갈 뿐 여전히 틀린다**」였다. 받은 수정은 그 함정을 피하는 데서 그치지 않고 **표현 자체를 없앴다** — 판독 경계가 `Mapping` 이 아니라 **판독 결과 `TypeNameRead`** 를 받는다.

```python
# tools.py:4521
-  type_names = read_fixture_type_names(...).by_slot()
+  type_names = read_fixture_type_names(...)          # 판독 결과를 통째로
```

`_name_handle_types` 주석이 그 차이를 정확히 적는다 — *"Accepting `TypeNameRead` makes that loss **impossible to express**."* **주의하게 만든 것이 아니라 표현할 수 없게 만든 것**이며, 이 둘은 다른 등급의 수정이다. 가드는 잊히고 타입은 안 잊힌다.

측정 2·3행이 `type_tree_unreadable`, 1행이 `no_type_table` 로 갈린다 — 1차에서 내가 `slot_absent` 를 관측했던 그 자리다.

### 넷째(절단) — **닫힘. 독립으로 확인했다**

run 이 **자기 판정**으로 올린 것이라 내가 따로 쟀다. 6행(절단 목록) vs 5행·7행(전수 목록)이 갈린다. 「절단이 무효화하는 것은 **부정 결론**뿐이다」는 이 저장소 규약이 `slot_absent` 라는 부정 결론에 정확히 적용됐고, **이 카드의 조상 결함 D1 이 절단 건이었다**는 계보도 맞다.

`TypeNameRead.truncated` 를 노출해 판독기가 그 사실을 **가지고 나오게** 한 것도 D-1 과 같은 형태의 수정이다 — 소비자가 재계산하지 않는다.

### D-2 — **닫힘. 이월이 아니라 재측정이다**

`9703 / +12` → **`9707 / +16`**(M1 12 + 오류 갈래 2 + D-1 회귀 1 + 절단 회귀 1), 커버리지 `1120 39 97%` → `1130 36 97%`. **내가 직접 돌려 재현했다** — `9707 passed, 8 skipped, 1 warning in 143.75s` · exit 0. 명령과 출력이 같은 실행의 짝이다.

### D-3 — **닫힘.** 「무관 5」→ **6**, 그리고 **검산식**(3+2+6=11=측정됨)을 본문에 실었다. 지적한 것보다 한 걸음 더 갔다 — 다음 사람이 표를 다 읽지 않고도 어긋남을 잡는다.

### AC-011 수식 — **닫힘. 자리도 맞다**

「옳은 미측정」이 「**안 잰 것**」으로 바뀌었고, 고친 자리가 `progress.md` 다. `acceptance.md` 는 manager-spec 소유이므로 **소유 경계를 지키면서 실질을 고친** 처리다.

### 복구 증거 — 독립 대조

`shasum -a 256 server/prechk/inventory.py` → `4209be1cf598…ab23731d`. progress.md 가 MUT-D 복구 증거로 인용한 `4209be1c…731d` 와 일치한다. **복구를 초록이 아니라 체크섬으로 증명하는 방식이 이 카드에서 세 번째로 값을 했다.**

## 새 결함

### N-1 (주요) — 형태 불량 트리가 여전히 「리그 사실」로 보고된다. 그리고 **이번에 쓴 계약이 그 갈래를 더 이상 설명하지 못한다**

**측정값 4행이다.** 트리가 답은 했으나 자식이 형태 불량(`{"i": 1, "name": 123}`)일 때:

```
자식 형태 불량      → slot_absent
전수 목록 슬롯 없음 → slot_absent      ← 바이트 동일
빈 라이브러리(전수) → slot_absent      ← 바이트 동일
```

세 사건이 같은 표식으로 도착한다. 그런데 이번 커밋이 새로 쓴 그 상수의 계약은:

> `slot absent : the tree answered **IN FULL** and does not declare that slot. A rig fact — retrying changes nothing.`

**형태 불량은 「전수 답했다」가 성립하지 않는다.** 페이로드가 파싱을 거부당한 것이고, 그때 지시되는 행동(「리그를 손봐라」)은 틀렸다 — 고칠 곳은 리그가 아니라 판독 쪽이다. **D-1 이 정확히 이 모양이었다.**

**정직하게 갈라 둔다 — 이건 정정이 연 자리가 아니다.**

| | 상태 |
|---|---|
| **행동** | `fc37abc` 와 **동일**. 정정이 바꾸지 않았다 |
| **계약** | **이번에 강해졌다.** `fc37abc` 의 문면은 「the tree was read and simply does not declare that slot」이라 형태 불량도 그럭저럭 들어맞았으나, 「**IN FULL**」은 들어맞지 않는다 |
| **내 1차 감사** | **못 봤다.** 거절 리더만 프로브했고 형태 불량 갈래는 안 돌렸다. **정정이 만든 것이 아니라 내가 놓친 것이다** |
| **공개 여부** | **없다.** `progress.md` 는 이 갈래를 판독기 수준(`:130` · `attempted=True`)에서만 다루고, 그 하류 사유는 어디에도 없다(`grep` 확인) |

**이 카드에서 같은 계열의 세 번째다** — D-1(판독 실패 → 리그 사실) · 넷째(절단 → 리그 사실) · N-1(형태 불량 → 리그 사실). 셋 다 **판독 쪽 사정을 리그 사실로 보고**한다.

**차단 아닌 이유**: 행동이 1차 시점과 같으므로 그때 막지 않은 것을 지금 막을 수 없다. 쓰기 0. 도달하려면 콘솔이 구조적으로 불량한 자식 목록을 돌려줘야 한다.

**처분(설계 판단이므로 정하지 않는다)**: 다섯째 사유(`type_tree_malformed` 류)를 세우거나, 형태 불량을 `attempted=False` 로 분류한다. **다만 후자는 공짜가 아니다** — 이 카드가 세운 doctrine(「응답이 없다」는 재시도할 자리, 「응답이 이상하다」는 아니다)과 충돌하고, `fc37abc` 의 테스트 `test_a_malformed_child_is_attempted_true_and_still_names_nothing` 이 그 분류를 고정하고 있다. **고치지 않기로 한다면 「형태 불량은 현재 `slot_absent` 로 보고된다」한 줄이면 닫힌다.**

### N-2 (경미) — CHANGELOG 새 항목의 제목이 본문과 어긋난다

```
제목 : 번역 실패 사유를 **세 갈래**로 가른다(독립 감사 D-1 수정)
본문 : 이제 **넷**으로 가른다: no_type_table · type_tree_unreadable ·
       slot_absent · slot_unseen_truncated_listing
```

자체 점검이 넷째를 찾은 뒤 본문은 갱신됐으나 **제목이 3 단계에 남았다.** 제목만 읽는 사람은 수를 틀리게 안다.

**적어 둘 값이 있는 이유**: 이 커밋의 임무 하나가 **낡은 수치(D-2) 정정**이었는데, 그 작업을 서술하는 바로 그 문단이 **새 낡은 수치**를 남겼다. 같은 함정이 문서 안에서 한 층 옮겨간 것이다.

---

## 관찰

- **O-1. 완화 가설은 양쪽 미측정으로 기록됐다.** `:4510` 선행 판독이 D-1 을 실기에서 가릴 가능성 — `progress.md §E.4` 「이 정정이 재지 않은 것」에 「감사도 재지 않았고 나도 재지 않았다. 등급 근거로 쓰이지 않았다」로 적혔다. **나도 이번에 재지 않았고, 등급을 움직이지 않았다.**
- **O-2. 파급이 봉쇄됐다.** `type_names=` 를 넘기는 곳은 `tools.py:4527` **하나**, `translate_fixture_type` 호출자는 `inventory.py` 내부 **하나**, `by_slot()` 호출자도 **하나**. 낡은 `Mapping` 시그니처로 부르는 자리가 남아 있지 않다. `inventory.py` 가 `mode_read.TypeNameRead` 를 새로 import 하지만 **순환 없음**(스위트 초록으로 확인).
- **O-3. 수정의 형태가 이 재감사의 배울 점이다.** D-1 과 넷째 둘 다 「값을 잘 다루자」가 아니라 **「잘못 다루는 것을 표현할 수 없게」** 고쳤다(판독 결과를 통째로 넘김 · `truncated` 를 판독기가 들고 나옴). N-1 이 눈에 띄는 이유가 바로 이것이다 — **같은 기법이 한 층 아래에는 적용되지 않았다.**
- **O-4. 8곳 중 1곳 배선은 그대로다.** C3·C4·C5 는 여전히 열려 있고 세 문서에 공개돼 있다. 이번 델타 범위 밖이며 변화 없음.
- **O-5. 내 1차 지적 넷이 전부 반영됐고, 하나는 지적보다 멀리 갔다**(D-3 의 검산식). 그리고 내가 보낸 「`None` 은 답이 아니다」 경고가 실제로 수정 형태를 바꿨다 — **재감사가 아니라 수정 전 전달이 값을 한 사례**로 남길 만하다.

---

## 재점수 — 결함 단위 귀속

**0.86 (1차) → 0.90 (재판정).**

차원별 소급은 이번에도 하지 않는다. 1차가 단일 총점이었으므로 없는 baseline 에 델타를 적을 수 없고, 델타 범위 감사에서는 **결함 단위 귀속**이 실제로 측정된 것과 맞는다.

| 상승분의 출처 | 성격 |
|---|---|
| **D-1 닫힘** | 최대 기여. 게이트를 물고 있던 유일한 부채였고, **권고보다 강한 형태**(표현 불가능화)로 닫혔다 |
| **넷째(절단) 자체 발견** | 지적받지 않은 것을 스스로 찾아 닫았다. 자기 판정이라 내가 독립 확인했고 성립한다 |
| **D-2 재측정** | 이월이 아니라 출하 HEAD 실측. 내가 같은 수를 독립으로 얻었다 |
| **D-3 · AC-011 수식** | 둘 다 닫힘. D-3 은 검산식까지 |
| **−) N-1** | 주요. 다만 **행동은 1차와 동일**하고 계약만 어긋났으며, 내가 1차에서 못 본 자리다 |
| **−) N-2** | 경미. 제목 한 줄 |

**PASS-WITH-DEBT 로 두는 이유**: N-1 이 실재하면서 공개되지 않은 유일한 항목이다. 1차에서 D-1 에 걸었던 조건과 **같은 조건**을 건다 — **고치거나 적어라.** 어느 쪽이든 닫힌다.

**`completed` 는 이 부채가 막지 않는다.** 게이트 질문은 1차에서 AC 12/12 로 답했고, 그때의 게이트 부채(D-1)가 닫혔다. N-1 은 1차 시점에도 같은 행동이었으므로 그때 막지 않은 것을 지금 막을 근거가 없다.

## 재판정 Baseline-attribution

| 항목 | 값 |
|---|---|
| 대상 | `94f9675` (`origin/WT-handle-to-name`) — `fc37abc` 의 자손, 단일 커밋 |
| 트리 | `.claude/worktrees/t11-sync-audit` · `WT-parity-sync-audit` 에 `94f9675` 를 머지 |
| 변경 파일 | `progress.md` · `CHANGELOG.md` · `tools.py` · `inventory.py` · `mode_read.py` · `test_prechk_handle_types.py` — 리드 값과 일치 |
| 봉쇄 구역 `5c988bd..94f9675` | **빈 출력** |
| 스위트 | `9707 passed, 8 skipped, 1 warning in 143.75s` · exit 0 **(이 트리, 이 실행)** |
| 프로브 | `read_inventory` 를 통과시켜 사유 7갈래 실측(위 표) |
| 체크섬 | `inventory.py` = `4209be1c…731d` — 인용과 일치 |
| 콘솔 | 접속 0건 |

## 재판정 Gaps — 이번에도 재현하지 않은 것

1. **뮤테이션 MUT-C · MUT-D 를 재현하지 않았다.** 배차 범위가 「제품 코드 수정 0건」이라 그대로다. 리드가 MUT-D 를 직접 걸어 「자기 테스트 한 건만 죽었다」를 관측했고 **나는 그 관측을 인용한다 — 내 것이 아니다.** 대신 복구 체크섬을 독립 대조했다.
2. **완화 가설 미측정**(O-1). 1차와 같다.
3. **N-1 의 실기 도달 가능성을 재지 않았다.** 실제 콘솔이 형태 불량 자식 목록을 돌려주는지는 콘솔 무접촉이라 모른다. 그래서 **도달성을 근거로 등급을 올리지도 내리지도 않았다.**
4. **콘솔 무접촉 · UI 스위트 미실행 · C6·C7 대신 안 잼 · 어긋남 3건 원인 동일성 미측정** — 1차 그대로.
5. **`spec.md §A.4` 를 보지 않았다.** 배차가 범위 밖으로 지정했다.
6. **`/code-review` 를 기다리지도 참고하지도 않았다.** 이번에도 독립이다.

**내 검사 목록**(대조용): ① diff 재현 ② 배선까지 따라간 사유 실측 ③ 전체 스위트 직접 실행 ④ 체크섬 대조 ⑤ 인터페이스 파급 grep ⑥ 문서 정정 대조.
**목록에 없는 축**: 성능 · 동시성 · 예외 안전성(신설 판독의 `except Exception` 폭) · UI · 실기 · 보안. **N-1 은 ②를 한 갈래 더 돌려서 나왔다** — 코드 리뷰가 같은 자리를 독립으로 짚으면 교차 확인이고, 다른 자리를 짚으면 내 ②가 아직 얕다는 뜻이다.

## 재판정 Residual-risk

- **N-1 을 다섯째 사유로 고치면 사유가 다섯이 된다.** 사유가 늘수록 **소비자가 뭉갤 확률**이 오른다 — 오늘 이 값을 읽는 소비자는 아직 없고(필드만 존재), 첫 소비자가 생길 때 넷/다섯을 실제로 갈라 쓰는지가 그때의 감사 대상이다. **이름을 늘리는 것과 행동을 가르는 것은 다른 일이다.**
- **같은 계열이 네 번째로 나올 자리.** 셋 다 「판독 쪽 사정 → 리그 사실」이었다. 남은 후보는 **`HANDLE_TEXT` 가 못 알아보는 제3의 형식**이며, 이건 표식조차 없이 **이름으로 통과**한다(코드 주석이 스스로 적었다). t12 재료로 이미 넘어가 있다.
- **이 재감사도 한 형태다.** 프로브 하나를 더 돌려 N-1 이 나왔다는 사실 자체가, **갈래를 더 돌리면 더 나온다**는 신호다. 내가 돌린 것은 7갈래이고 그것이 전부라는 보장은 없다.

---

**재판정 결론: PASS-WITH-DEBT 0.90.** D-1 포함 네 자리 전부 닫힘. `completed` 전이는 정당하며 이 부채가 막지 않는다. N-1 은 고치거나 적어라, N-2 는 제목 한 줄.

재감사자: sync-auditor (t11 감사 레인) · 대상 `94f9675` · 기준 `fc37abc`


---
---

# 재판정 (3차) — 정정본 `ff4ac74` + `spec.md` `f816973`

> **앞 두 판정(0.86 · 0.90)은 그대로 둔다.**

대조 기준: `94f9675` → `ff4ac74`(구현 브랜치) · `9e38ae7`~`f816973`(plan 브랜치 `spec.md`).
범위: **N-1 · N-2 · `spec.md` §A.4** + **정정이 새로 연 것**.

## 3차 Claim

**감사 대상 자체는 PASS — 0.92.** N-1 · N-2 · `spec.md` §A.4 **셋 다 닫혔고**, 뮤테이션 방향도 양방향으로 완전하다.

**그러나 `completed` 스탬프는 지금 찍으면 안 된다 — 차단 1건(X-1).** 코드 결함이 아니라 **조율 결함**이다: `spec.md` 정정이 **게이트가 작동할 산출물에 닿지 않았다.** 처분은 기계적이고 싸다.

## 닫힘 판정

### N-1 — **닫힘. 페이로드 7형태를 돌려 확인했다**

지난 두 번 모두 프로브를 한 갈래 더 돌려 결함이 나왔으므로, 이번엔 **먼저** 넓혔다. `read_inventory` 를 통과시켜 실측:

```
A 진짜 빈 라이브러리    att=T trunc=F shape=F → slot_absent
B 자식 형태 불량        att=T trunc=F shape=T → listing_shape_invalid
C node 자체가 없음      att=T trunc=T shape=F → slot_unseen_truncated_listing
D childCount 만 있고 children 없음  att=T trunc=T shape=F → slot_unseen_truncated_listing
E childCount 가 문자열  att=T trunc=T shape=F → slot_unseen_truncated_listing
F children 이 문자열    att=T trunc=F shape=T → listing_shape_invalid
G 절단                  att=T trunc=T shape=F → slot_unseen_truncated_listing
```

**`slot_absent` 은 A 하나뿐이다.** 목록을 믿을 수 없는 여섯 형태는 전부 리그 사실이 아닌 사유로 빠진다. 그 상수의 계약(「answered **IN FULL** and does not declare that slot — a rig fact」)이 이제 **정확히 그 조건을 만족하는 경우에만** 붙는다.

**판독기 분류는 안 바뀌었다** — diff 확인: `mode_read.py` 변경은 `shape_invalid` 필드 추가와 `pairs is None` 반환에 그 플래그를 얹은 것뿐이고 `attempted` 는 그대로다. 카드가 세운 doctrine(「응답 없음」 / 「응답이 이상함」)과 `fc37abc` 테스트가 **살아 있다.** 내가 「후자는 공짜가 아니다」로 경고했던 자리를 건드리지 않고 다섯째로 풀었다.

**배선 무변경** — `tools.py` 는 이번 diff에 없다. 수정이 `prechk` 안에서 끝났다.

### 뮤테이션 방향 완전성 — **양방향이다. 그리고 대조군은 장식이 아니다**

리드가 「뮤테이션 방향의 완전성 자체가 판정 대상」이라 했다. 기록을 보면 두 방향이 다 걸렸다:

| 방향 | 뮤테이션 | 죽인 것 |
|---|---|---|
| **과소 적용**(가드 제거) | MUT-E | `test_a_malformed_listing_is_not_reported_as_a_rig_fact` **1건만** |
| **과잉 적용**(모든 경우에 적용) | MUT-F | **3건** — 그중 `test_an_empty_library_is_still_a_rig_fact` |

**대조군의 비공허성을 내가 독립으로 확인했다.** 위 실측 A와 B는 **`shape_invalid` 하나만 다른 입력**인데 출력이 `slot_absent` / `listing_shape_invalid` 로 갈린다. 즉 과잉 적용하면 A가 B의 답을 내고 그 테스트는 반드시 실패한다 — **제품 코드를 건드리지 않고 판별력을 보였다.**

**가드를 지우는 뮤테이션만으로는 다섯째가 넷째를 잡아먹는 것을 못 잡는다**는 판단이 옳다. 이 방향을 run 이 **요구받지 않고 스스로 걸었다.**

### 정지 조건 — **지켜졌고, 문면도 그 뜻이다**

`progress.md`: 「현재 열거는 **감사가 배선 7갈래를 실측한 것**에 근거한다. 「있을 것 같다」로 늘리지 않는다. 새로 **측정된** 갈래가 나오면 그때 새 카드다.」
코드 주석: 「This enumeration comes from a **measured sweep** of the wiring; a sixth reason needs **a new measurement, not a hunch**.」

**여섯째는 없다** — `grep -c "^UNTRANSLATED_"` → **5**.

### N-2 — **닫힘. 한 번에 맞았다**

제목 「번역 실패 사유를 **다섯** 갈래로 가른다」 · 본문 「이제 **다섯**으로 가른다」 · 상수 **5**. 이 카드가 낡은 수치로 다섯 번 물린 끝에(8→13 · 20→22 · 9703↔9705 · 세 갈래↔넷 · A-1 번짐 칸) **처음으로 한 번에 맞았다.**

### `spec.md` §A.4 (v0.1.4) — **문면만 고쳤다. 결정은 그대로다**

- A-1 번짐 칸: 「C1~C13 동시 해결」 → 「표를 받는 경로가 즉시 닫히고, 나머지는 표를 넘기는 것만으로 따라온다 — 실제 배송분은 아래 [정정 v0.1.4]」
- **A-2 · A-3 행 무변경**, 「권고: A-1」 무변경 → **결정을 되돌리지 않았다**
- [정정 v0.1.4] 블록이 내 O-3 를 그대로 갈랐다 — 「A-1이 산 것은 **오늘의 커버리지가 아니라 나중의 단가**이며, 옛 문면이 그 둘을 구분하지 않았다」. 이 문장이 정확하다
- 8곳 전수(`:2680`·`:2863`·`:3045`·`:3678`·`:4029`·`:4185`·`paperwork/data.py:100` 대 `:4524`)를 구현 커밋에서 확인해 실었다

**복구 체크섬** `inventory.py` = `ee028bee91ced309…` — 인용 `ee028bee…b522` 와 일치.
**전체 스위트** — 내가 직접 돌려 **`9709 passed, 8 skipped, 1 warning in 143.88s` · exit 0**.

## X-1 (차단) — 정정이 **게이트가 작동할 산출물**에 닿지 않았다

> **`completed` 를 지금 찍으면 안 되는 이유**: 스탬프는 구현 브랜치 `spec.md` frontmatter 에 찍히는데, 그 파일은 아직 **내 O-3 지적 문면을 그대로 안고 있다.** 지금 찍으면 카드가 그 상태로 닫힌다.

**측정값이다.**

```
공통 조상                     5c988bd
분기 (구현 ... plan)          7  2
plan 브랜치 단독 커밋
  9e38ae7  v0.1.3  재감사 후속 정정 (M-3 · O-8 · m-4)
  f816973  v0.1.4  sync 감사 O-3 — A-1 번짐 칸
```

구현 브랜치 `ff4ac74` 의 `spec.md` 실측:

```
version: "0.1.2"   status: implemented
A-1 번짐 칸  →  「C1~C13 **동시 해결**」        ← 정정 전 문면 그대로
grep -c "정정 v0.1.4"  →  0
```

`plan.md`(+16) · `acceptance.md`(4행)도 같은 이유로 뒤처져 있다.

**즉 세 갈래가 갈렸다.** 코드와 `progress.md` · `CHANGELOG` 는 구현 브랜치에, `spec.md` · `plan.md` · `acceptance.md` 의 감사 정정 셋(**m-4 HISTORY 정렬 · O-8 「86행 8슬롯」 · v0.1.4 §A.4**)과 M-3 정정은 plan 브랜치에 있다.

**결과 둘, 어느 쪽도 좋지 않다.**

1. 구현 브랜치에서 `completed` 를 찍으면 — 카드가 **「A-1이 C1~C13을 동시 해결한다」를 안은 채** 닫힌다. 내 1차 sync-audit O-3 가 지적하고 plan 레인이 v0.1.4 로 고친 바로 그 문장이며, **고쳤는데 닫는 파일에는 없다.**
2. 나중에 두 브랜치를 합치면 — frontmatter 가 **충돌한다**(`0.1.2`/`implemented` ↔ `0.1.4`/`draft`). 그 충돌을 푸는 사람이 어느 판본이 살지, `status` 를 무엇으로 둘지를 **그 자리에서** 정하게 된다.

**이것은 코드 결함도, `run` 레인의 실수도 아니다.** plan 산출물을 plan 브랜치에서 고치고 구현이 다른 브랜치에 있는 **구조가 연 자리**다. 이 카드에서 「정정이 새 자리를 연」 세 번째이며, 앞의 둘(M-3 · 절단)과 달리 **코드가 아니라 조율에서 열렸다.**

**처분(기계적)**: `completed` 를 찍기 전에 plan 브랜치의 두 커밋(`9e38ae7` · `f816973`)을 구현 브랜치로 가져온다(merge 또는 cherry-pick). frontmatter 는 `version: "0.1.4"` + `status: implemented` 로 정리한 뒤 `completed` 로 넘긴다. **정정 내용에 대한 판단은 필요 없다 — 이미 판정된 것들이다.**

---

## 관찰

- **O-1. `slot_unseen_truncated_listing` 의 이름이 실제 집합보다 좁다.** 실측 C · D · E(node 없음 · `childCount` 만 있고 children 없음 · `childCount` 가 문자열)도 이 표식으로 온다. 이들은 「절단됐다」기보다 **「전수임을 확인할 수 없다」**이다. **그러나 행동은 전부 옳다** — 어느 경우도 리그 편집으로 보내지 않는다. D-1 · N-1 계열과 **반대 방향의 안전한 부정확**이며, 근거인 `_listing_is_whole`(`footprint.py:167`)은 「판단 못 하면 False」가 **문서화된 계약**인 공유 술어다. 이름을 바꾸면 그 술어의 이용자 전부를 건드린다. **결함으로 올리지 않는다.**
- **O-2. `shape_invalid` 와 `truncated` 는 동시에 참일 수 없다.** 판독기가 `pairs is None` 에서 조기 반환하므로 `truncated` 계산에 닿지 않는다. 따라서 `_name_handle_types` 의 우선순위 분기(`shape_invalid` 먼저)는 **판독기 경로에서 도달 불가**이고 테스트도 없다. `TypeNameRead` 를 직접 구성하면 도달한다. **결함 아님** — 우선순위 선택도 옳다(둘 다면 아무것도 못 믿는다).
- **O-3. 이번엔 이 계열의 네 번째가 안 나왔다 — 다만 그것이 「없다」의 증거는 아니다.** 내가 돌린 것은 **7형태**이고, 그 안에서 `slot_absent` 는 하나뿐이었다. 지난 두 번 모두 「한 갈래 더 돌려서」 나왔으므로, 이번에 안 나온 것은 **내가 돌린 범위가 7이라는 뜻**이지 그 이상이 없다는 뜻이 아니다.
- **O-4. 이 카드가 낡은 수치로 물린 것이 다섯 번, 이번이 처음으로 한 번에 맞았다.** 8→13 · 20→22 · 9703↔9705 · 「세 갈래」↔넷 · A-1 번짐 칸. 다섯 번 다 **지우지 않고 적었고**, 그것이 매번 다음 판정을 쉽게 만들었다. X-1 이 걸린 이유도 같은 규율의 연장이다 — 정정을 지우지 않았기 때문에 **어디에 있는지**를 물을 수 있었다.

---

## 3차 재점수 — 결함 단위 귀속

**0.86 → 0.90 → 0.92.** 차원별 소급은 이번에도 하지 않는다(1차가 단일 총점이었다).

| 상승분의 출처 | 성격 |
|---|---|
| **N-1 닫힘** | 다섯째 사유. 내가 「후자는 공짜가 아니다」로 경고한 자리(`attempted` 재분류)를 **건드리지 않고** 풀었다 |
| **뮤테이션 양방향** | 과잉 적용 방향을 **요구받지 않고** 걸었고, 대조군이 실제로 판별한다(내가 독립 확인) |
| **정지 조건** | 코드 주석 + 문서 양쪽, 여섯째 없음. **이름을 늘리는 손을 스스로 묶었다** |
| **N-2 · `spec.md` §A.4** | 둘 다 닫힘. 수치가 처음으로 한 번에 맞았고, §A.4 는 결정을 되돌리지 않고 문면만 고쳤다 |
| **−) X-1** | 차단. 다만 **코드가 아니라 조율**이고 처분이 기계적이다 |

**점수와 게이트를 갈라 적는다.** 감사 대상(구현 + 문서 정정)의 품질은 올랐고 0.92 가 그것이다. **`completed` 게이트는 별개 질문이며 지금은 「아니오」다** — X-1 이 닫히면 「예」다. 점수를 내려 게이트를 표현하지 않는다. 그 둘을 뭉치면 다음 사람이 「0.92 인데 왜 못 닫나」를 되묻게 된다.

## 3차 Baseline-attribution

| 항목 | 값 |
|---|---|
| 대상 | `ff4ac74`(구현, `94f9675` 의 자손) + `f816973`(plan `spec.md`, v0.1.4) |
| 트리 | `.claude/worktrees/t11-sync-audit` · `WT-parity-sync-audit` 에 `ff4ac74` 머지 |
| 변경 파일(`94f9675..ff4ac74`) | `progress.md` · `CHANGELOG.md` · `inventory.py` · `mode_read.py` · `test_prechk_handle_types.py` — `tools.py` 없음 |
| 봉쇄 구역 | `5c988bd..94f9675` 확인분 유지, 이번 diff 에 없음 |
| 스위트 | `9709 passed, 8 skipped, 1 warning in 143.88s` · exit 0 **(이 트리, 이 실행)** |
| 프로브 | `read_inventory` 통과 **7형태** 실측(위 표) |
| 체크섬 | `inventory.py` = `ee028bee91ced309…` — 인용과 일치 |
| 분기 측정 | `ff4ac74...f816973` → **7 2**, 공통 조상 `5c988bd` |
| 콘솔 | 접속 0건 |

## 3차 Gaps — 재현하지 않은 것

1. **뮤테이션 MUT-E · MUT-F 를 재현하지 않았다.** 제품 코드 수정 0건. 리드가 MUT-F 를 직접 걸어 관측한 것을 **인용한다 — 내 것이 아니다.** 대조군 판별력은 제품 코드를 안 건드리고 프로브 A/B 로 독립 확인했으나, **「MUT-F 가 정확히 3건을 죽였다」는 인용이다.**
2. **N-1 실기 도달성 미측정.** 실기가 형태 불량 응답을 실제로 내는지 **아무도 안 쟀다.** 오프라인 도달성만 측정됐다 — 등급에 쓰지 않았다.
3. **완화 가설**(`:4510` 선행 판독) — 여전히 아무도 안 쟀다.
4. **AC 전건 통과 ≠ 실기 D2 해소** — 이 카드의 최대 잔여 가정. 콘솔 무접촉이라 그대로다.
5. **X-1 의 병합 결과를 예측만 했다.** frontmatter 가 충돌할 것이라 적었으나 **실제로 병합해 보지 않았다**(감사가 브랜치를 합칠 자리가 아니다). 자동 병합될 가능성도 있고, 그 경우에도 「어느 판본이 사는가」는 여전히 결정 사항이다.
6. **plan 브랜치의 `plan.md` · `acceptance.md` 정정 내용을 읽지 않았다.** 뒤처져 있다는 **사실만** 쟀다(+16 · 4행).
7. **UI 스위트 · C6 · C7 · 어긋남 3건 원인 동일성** — 앞선 판정 그대로.
8. **`/code-review` 를 기다리지도 참고하지도 않았다.**

**내 검사 목록**: ① diff 재현 ② `read_inventory` 통과 사유 실측(7형태) ③ 전체 스위트 직접 실행 ④ 체크섬 대조 ⑤ 브랜치 분기 측정 ⑥ 문서 수치 정합 ⑦ 뮤테이션 방향 완전성 판정.
**목록 밖**: 성능 · 동시성 · 예외 안전성 · UI · 실기 · 보안 · **병합 실행**.

**대조 단서**: 이번 새 결함(X-1)은 **② 가 아니라 ⑤ 에서 나왔다.** 지난 두 번은 ②를 넓혀서 나왔다. `/code-review` 는 브랜치 위상을 보지 않으므로 **X-1 과 같은 자리를 짚을 수 없다** — 코드 리뷰가 무엇을 짚든 그것은 X-1 의 교차 확인이 아니다. 그쪽이 짚는 것은 ②·목록 밖 축에 대한 정보다.

## 3차 Residual-risk

- **X-1 을 처리해도 같은 구조는 남는다.** plan 산출물과 구현이 서로 다른 브랜치에 사는 한, **다음 감사 정정도 같은 자리에 떨어진다.** 이번엔 기계적으로 풀리지만 구조는 그대로다 — 카드 운영의 문제이지 이 SPEC 의 문제가 아니다.
- **사유가 다섯이고 소비자는 아직 없다.** `fixture_type_untranslated` 를 읽는 코드가 없다(필드만 존재). **첫 소비자가 생길 때 다섯을 실제로 갈라 쓰는지가 그때의 감사 대상**이며, 이름을 늘리는 것과 행동을 가르는 것은 다른 일이다.
- **남은 후보는 표식조차 없는 자리다** — `HANDLE_TEXT` 가 못 알아보는 제3의 형식은 **이름으로 통과**한다(코드 주석이 스스로 적었다). 이 계열의 다음 건이 나온다면 그 자리일 가능성이 높고, 이미 t12 재료다.
- **이 감사도 7형태다.** 그 이상을 안 돌렸다.

---

**3차 결론: 감사 대상 PASS 0.92 · `completed` 게이트는 X-1 이 닫힐 때까지 「아니오」.** 처분은 커밋 둘을 가져오는 것이며, 내용 판단은 필요 없다.

재감사자: sync-auditor (t11 감사 레인) · 대상 `ff4ac74` + `f816973` · 기준 `94f9675`
