# SPEC-COPILOT-PRESETGUARD-001 — 진행 기록 (progress)

> **인용 규율.** 본 SPEC의 정본(`spec.md` · `acceptance.md`)은 줄번호로 인용하지 않고 안정 토큰만 쓴다. `파일:줄`은 **코드 · 룰북 · 타 SPEC 아티팩트**에만 쓰고, **각 마일스톤 착수 직전 재실측**한다. 근거 등급 `[코드]` · `[측정]` · `[실측]` · `[인수]` — **`[실측]`은 라이브 콘솔 직접 관측만**이며, 본 SPEC에는 해당 항목이 **없다**(라이브 불필요).

## §0 인수인계 — 여기서 시작한다 (2026-08-16 작성, plan-phase)

### 한 문단

**무엇**: Position 프리셋(풀 2)의 **비가역 덮어쓰기**를 세 곳에서 막는다 — ① 시작 번호가 확정된 시점에 점유를 검사하고 충돌하면 **어느 슬롯이 사라지는지 번호로** 물어본다(오늘은 번호를 명시하면 검사가 아예 없다) ② 저장 후 풀을 **한 번 되읽어** 결과를 형용사가 아닌 **산술**로 보고한다(오늘은 되읽지 않는다) ③ *"지금 배치로 다시 잡아줘"* 로 **기존 구간을 제자리 갱신**하는 입구를 만든다(오늘은 없다 — 큐가 참조를 들고 있으므로 프리셋만 다시 만들면 모든 큐가 재조준된다). 핵심 설계는 하나다: **확인 카드는 하나, 호출자는 둘** — ①과 ③이 같은 판정 로직을 공유해야 한쪽만 fail-closed가 되는 드리프트가 안 생긴다.

**상태**: **run-phase 진행 중 (in-progress v0.1.0).** 구현 커밋 **1건** (`237c4db` — 점유 가드 · 저장 되읽기 · 재생성 3경로, 신규 테스트 19건) · 라이브 0. plan 산출물 커밋 = `d27a5fc`(문서가 f5cfe15에 미커밋 상태였음 — 뒤늦게 반영). 직접 관측: 전체 스위트 **8880 passed · 8 skipped** (기준선 8861 + 신규 19, 회귀 0) · `ruff check server/` 선재 2건 외 신규 0 · `server/spatial/**`·`tools.py`·`server/safety/**` byte-diff 0 · 뮤테이션 필수 8건 포함 10건 전건 RED(생존 0, 로그 `.moai/state/verify/presetguard/mutations.log`). **미완**: 독립 리뷰(review-tjueyo) 결과를 lead-tjueyo 경유로 **NOT-A-PASS — 결함 9건(차단 3건)** 으로 전달받았고 F1~F9 수정이 승인됐다(리뷰 원문은 이 세션이 직접 관측하지 않음). §E.2 Run-phase Evidence 미작성. base `jjjh7401/MAcopilotpos` HEAD = `f5cfe15`. Tier **M** · REQ **20** · AC **14**(뮤테이션 필수 **8**) · Out of Scope **6항** · ASSUMPTION **81~85** · 열린 결정 **3건**(전부 에이전트 판단 가능) · **라이브 콘솔 세션 0회**.

**이 SPEC의 한 줄**: *가장 확신을 갖고 행동한 운영자가 가장 보호받지 못한다* — 번호를 직접 지목한 경로에만 점유 검사가 없다는 사실(`server/web/session.py:2820`)이 이 SPEC을 요구했다.

### 읽는 순서

1. **`spec.md` §A.2(가드가 뒤집힌 형상) · §A.5(카드 하나·호출자 둘)** — 이 둘이 설계 전체를 규정한다. §A.5를 건너뛰면 확인 카드를 둘로 구현하게 되고, 그 순간 AC-012가 RED가 된다.
2. `spec.md` §B.1 **REQ-001의 "출처 무관" 조항** — 브리핑은 결함을 *"명시 번호 경로"* 로 좁게 기술했으나, 질문 카드의 **자유 입력**도 같은 정도로 무방비다. 이 조항이 가드의 **위치**(if/else 갈래 밖)를 결정한다.
3. `spec.md` §B.2 **REQ-009(`proposal` 분류)** — 이 조항이 없으면 승인 게이트가 켜진 정상 환경에서 되읽기가 **상시 거짓 경보**를 낸다.
4. `plan.md` §C(M0 결정 3건) → §B(M1~M5) → §E(위험 5건 — 특히 4번 "스텁 함정")
5. `acceptance.md` 머리말의 **스텁 함정** → §B(AC 14건) → §C(뮤테이션 8건) → §E(DoD 5번 협상 불가 목록)

### 함정 (다음 소유자가 알아야 할 것)

1. **스텁이 가드를 무력화한다.** 기존 테스트 레지스트리는 `query_state`에 `"{}"` 를 돌려주므로 풀 판독이 `None`(미상)이 된다 → 새 가드가 `unverified` 갈래로 빠져 **가드가 없어도 통과한다.** 점유 가드를 검증하는 모든 테스트는 **풀 판독이 성공하는 리그를 명시 구성**해야 한다. 이것이 이 SPEC 최대의 위양성 함정이다.
2. **두 트리거는 이미 겹친다 — 측정됐다.** `_BASIC_POSITIONS_REQUEST`(`session.py:1579`)의 동사 대안에 `잡아`가 **이미 있어** *"기본 포지션 다시 잡아줘"* 를 매치한다(2026-08-16 직접 실행, 후보 4문장 전건 `True`). ASSUMPTION-83은 **반증됐다.** 따라서 *"교집합을 공집합으로 만든다"* 는 해법은 **불가능**하고(기존 트리거를 좁히면 파괴적 변경), 해법은 **디스패치 우선순위**다(REQ-015).
3. **`_ask_one` → `None`은 두 가지 뜻이다** — UI 미연결 **또는** 무응답. 둘 다 승낙이 아니다. 그리고 이 카드에서 `None`의 대가는 *질문 한 번*이 아니라 *운영자의 프리셋*이다. 기존 시작 번호 카드가 `None`에서 거부하는 선례가 이미 있으므로(`session.py:2862~`), 본 SPEC은 새 규율을 만드는 게 아니라 **선례를 확장**한다.
4. **미상 ≠ 빈칸.** `_position_preset_pool_slots()`의 `None`을 `set()`으로 접으면 판독 실패가 *"검사했고 비어 있었다"* 로 위장된다. 이는 이 SPEC이 닫으려는 결함과 **같은 형상**이다. 되읽기의 `None`도 마찬가지 — 확인됨으로 보고하면 안 된다.
5. **`proposal`은 실패가 아니다.** 안전 게이트가 저장을 보류하면 그 슬롯은 **당연히** 풀에 없다. 이를 미확인 결함으로 보고하면 상시 경보가 나고, 무시되는 경보는 **진짜 미착지도 함께 가린다.**
6. **재생성의 `None` 처리는 신규 저장과 반대다.** 신규 저장은 풀 미상에서 **진행**하고(오늘 동작 보존 + 고지), 재생성은 풀 미상에서 **저장하지 않는다**(표적 구간의 존재가 전제이므로). 이 비대칭은 **의도**이며 AC-013 ③이 고정한다.
7. **`tools.py`를 건드리면 무관한 테스트가 깨진다.** `server/tests/test_songcue_bundle.py`가 그 파일의 diff hunk 위치를 **고정**한다(트립와이어). 본 SPEC은 해당 파일 무변경이며, 변경이 필요해 보이면 그 자체를 블로커로 보고할 것.
8. **`server/spatial/**`는 순수 기하다.** 콘솔을 읽지 않고 풀 점유를 모른다. 점유 판정·확인 카드는 전부 `server/web/session.py`에 산다. `pointing.py` 변경이 불가피하다는 판단이 서면 **먼저 근거를 제시**할 것(REQ-016).
9. **프리셋은 백업할 수 없다.** 좌표 쓰기(`arrange_fixtures`)는 원값을 백업하고 재기록으로 되돌릴 수 있으나, 프리셋 내용을 되읽어 재기록하는 경로는 **없다.** 그래서 이 SPEC의 해답은 *"되돌릴 수 있게 한다"* 가 아니라 *"덮어쓰기 전에 반드시 묻는다"* 다.
10. **성공 기준에 "덮어쓰기가 실제로 막혔다"는 라이브 항목이 없다.** 새 MA3 문법이 0건이므로 실기 세션이 필요 없다(REQ-020). **하드웨어 일정을 잡지 말 것.**
11. **저장 루프를 복제하지 말 것.** 신규 저장과 재생성이 각자 루프를 가지면 룩별 번들·`ClearAll`·라벨 규율이 한쪽만 퇴행한다. M3에서 **공유 헬퍼로 추출**하고 두 경로에 같은 회귀를 건다.

### 다음 소유자의 착수 키트

- **다음 단계**: plan-audit → Implementation Kickoff Approval → **M0**(결정 확인 + 기준선 재실측, 코드 변경 0) → M1.
- **run 진입 전 해소 필요**: **없음.** 라이브 콘솔 불필요 · 사용자 제공 데이터 대기 없음 · 열린 결정 3건(D-1 재생성 어휘 폭 · D-2 단일 후보 카드 생략 여부 · D-3 기존 트리거 축소 여부)은 전부 **에이전트 판단 가능**이며 킥오프에서 사용자가 뒤집을 수 있다.
- **기준선 (plan-phase 측정 — run 킥오프 시 재측정 의무)**:
  - `uv run pytest server/tests -q` → **8861 passed · 8 skipped** (141.26s) [측정]
  - `uv run ruff check server/` → **2 errors** (E501 · `server/safety/console.py` — **선재. 손대지 않는다**) [측정]
- **확장 대상 테스트 클래스**: `TestBasicPositionPresets`(`server/tests/test_web_session.py:1460`) · `TestPositionCueStoreSession`(`:1558`) · 신설 `TestPositionPresetRegeneration`.
- **기준선 재측정 의무**: run-phase 킥오프 시점에 pytest/ruff 기준선을 **다시 측정한다.** plan-phase 수치 재사용 금지.

---

## §E.1 Plan-phase Audit-Ready Signal

- **산출물**: `spec.md` · `plan.md` · `acceptance.md` · `progress.md` (Tier M = 3 정본 + progress)
- **Tier 판정**: **M** — 세션 계층 단일 파일 개정 + 테스트, 예상 **2파일 · 400~700 LOC**(Tier M 범위 300~1000 LOC · 5~15 파일의 하단). Tier L이 아닌 근거: 신규 패키지 0 · 신규 툴 0 · 신규 콘솔 문법 0 · 라이브 세션 0 · 판독 채널 탐색 0 → `design.md`/`research.md`가 다룰 미지가 없다. Tier S가 아닌 근거: 300 LOC 초과 · 요구 20건 · 뮤테이션 필수 AC 8건 · 두 호출자가 공유하는 안전 판정 설계 결정 보유.
- **base**: `jjjh7401/MAcopilotpos` HEAD = `f5cfe15`. 인용 파일의 줄번호는 이 base 기준.
- **SPEC ID 자기검사**: `decomposition: SPEC ✓ | COPILOT ✓ | PRESETGUARD ✓ | 001 ✓ → PASS` (정규식 `^SPEC(-[A-Z][A-Z0-9]*)+-[0-9]{3}$`)
- **구현 범위**: 코드 변경 **0건** · 커밋 **0건** · 라이브 접근 **0건** (plan-phase 계약대로 문서만)
- **열린 결정**: **3건** (spec.md §F D-1 · D-2 · D-3 — 전부 에이전트 판단 가능, 킥오프에서 사용자 재검토 가능)
- **미해소 ASSUMPTION**: 81 · 82 (2건 — 81은 부분 검증이며 NEGATIVE여도 안전 방향 오류, 82는 기존 출하 코드의 전제 승계). **83은 plan-phase에서 반증 판정 완료**, 84는 코드 확인 완료, 85는 결정 완료.
- **plan-phase에서 수행한 실측 2건**: ① 테스트/린트 기준선 직접 측정 ② 트리거 정규식 충돌 직접 실행 검증(ASSUMPTION-83 반증). 브리핑에서 받은 사실은 전건 코드로 재확인했다.
