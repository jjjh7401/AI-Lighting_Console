# SPEC-COPILOT-SHEETPIPE-001 — 구현 계획 (plan)

문서 상태: draft (v0.1.0, 2026-08-23) · **Tier M · 통과 임계 0.80** · 칸반 카드 t10 · **분할 B(전달경로)**

> **참조 규약.** 정본(`spec.md` · `acceptance.md`)은 줄번호로 인용하지 않고 `REQ-SHEETPIPE-001` · `AC-SHEETPIPE-001` · `ASSUMPTION-78` 같은 안정 토큰만 쓴다. 코드와 타 SPEC 아티팩트는 `파일:줄` 좌표를 쓴다.
>
> **기준선 수치를 여기에 적지 않는 이유.** `baseline_measured`는 **의도적으로 비어 있다**(`progress.md` §E.1). A가 이 트리에서 겪은 그대로다 — 다른 트리·다른 세대에서 잰 값을 옮겨 적는 것은 기준선이 아니라 **이월**이다. M0가 A 병합 직후의 이 워크트리에서 직접 재서 채운다.

---

## §A. 맥락과 우선순위 — 바뀔 가능성이 큰 결정부터

### §A.1 가장 먼저 읽을 것 — 이 SPEC에서 결정이 뒤집힐 수 있는 지점

아래 넷은 **데이터 모델과 운영자 눈에 보이는 흐름**을 정하므로 뒤집히면 아래 것들이 전부 따라 움직인다. 기계적인 등재 작업(§B M2)은 맨 뒤에 둔 것이 그래서다.

1. **프레임을 새로 만들지 않는다 (결정 A).** 비이미지 첨부는 계속 `vectorworks_export_upload` 프레임으로 가고, 종류 판정은 **서버**가 한다. UI가 세 번째 프레임을 고를 근거가 없기 때문이다 — 판별기가 서버에 있고 확장자·MIME 분기는 A가 금지했다. **뒤집히면**: 프로토콜 버전 · 빌더 둘 · `messages.py` 검증 · `app.py` 분기 · 기존 테스트가 전부 따라 움직인다.
2. **`vectorworks` 판정은 슬롯에 들어가지 않는다 (결정 C).** 판정이 `vectorworks`면 **그 자리에서** 기존 경로로 흘러가고 슬롯은 건드리지 않는다. **뒤집히면**: 래퍼가 "대상이 툴이 아니라 세션 경로인 행"을 부를 수 있어야 하고, 그 순간 §A.4 ①의 열린 결정이 차단 결함이 된다.
3. **슬롯은 불변 + 통째 교체 + 읽기 통과 뷰다 (결정 F).** 가변·제자리 교체를 고르면 뷰가 필요 없지만, **고른 뒤 짝을 어기면 툴이 영원히 빈 슬롯을 본다**(`spec.md` §F 속성 A). **뒤집히면**: `build_toolset` 배선 두 곳과 `AC-SHEETPIPE-003`의 뮤테이션 대상이 함께 바뀐다.
4. **행 수는 이름 붙은 수다 (결정 E).** `ParseResult`가 세 통으로 나뉘므로 맨 숫자는 어느 통인지 말하지 않는다. **뒤집히면**: 표시 문구와 `AC-SHEETPIPE-006`이 함께 바뀐다.

### §A.2 빌드 순서 vs 무엇이 무엇을 막는가

**빌드 순서**는 M0 → M1 → M2다. **막는 관계**는 그것과 다르다:

- **A가 B 전체를 막는다.** B는 판별하지 않으므로 A의 판별기 없이는 M1의 이음매를 쓸 수 없다(`ASSUMPTION-78`).
- **M1이 M2를 막는다.** 래퍼는 슬롯을 읽으므로 슬롯이 먼저다. 역방향 의존은 없다 — 슬롯은 래퍼 없이도 채워지고 표시된다.
- **M2 안에서는 등재 7지점이 서로를 막지 않는다.** 다만 6·7번은 **전체 스위트를 돌려야** 드러나므로 마지막에 한 번 도는 것으로는 부족하고, M2 안에서 스위트를 두 번 돈다(등재 직후 · 트립와이어 재생성 후).

### §A.3 결정 현황 — 해소 8건 (A~H)

| | 결정 | 내용 | 근거 |
|---|---|---|---|
| **A** | 프레임 재사용 | 비이미지 가지는 `vectorworks_export_upload` 유지, 서버가 라우팅. 이름은 **"비이미지 첨부"를 뜻하는 유산 이름**으로 다시 읽는다 | UI가 분류할 근거 없음 · 이름 변경 비용이 이름의 값보다 크다 (`REQ-SHEETPIPE-008`) |
| **B** | 이음매는 세션 메서드 안 | `session.upload_vectorworks_export` 본문에서 판별 → 분기. `app.py:494` 분기와 `messages.py` 검증은 **한 줄도 안 바꾼다** | 분기가 판정을 미리 알 수 없으므로 `create_task` 모양을 유지해야 한다. 시트 저장은 그 태스크 안에서 즉시 끝난다 |
| **C** | `vectorworks`는 슬롯 밖 | 그 판정은 즉시 기존 경로로. 슬롯은 시트 종류만 받는다 | 래퍼가 "세션 경로"를 형제 툴처럼 부를 방법이 없다. 이 결정이 §A.4 ①을 **오늘의 차단에서 빼 준다** |
| **D** | 넷 표시는 서버 `notice_event` | `upload_layout_image`와 같은 형태. UI 렌더링 코드 0건 | 선례가 있고, UI를 열지 않는 만큼 vitest 델타가 작아진다 |
| **E** | 행 수는 이름 붙은 수 | 어느 통(`records`/`rejected`/`excluded`)을 센 수인지 함께 밝힌다 | 맨 숫자는 거부된 행을 감춰 운영자를 잘못 안심시킨다 |
| **F** | 슬롯은 불변 + 통째 교체 + 읽기 통과 뷰 | `LayoutImageUpload` 쪽 선례를 고른다 | 넷은 함께 정해지고 부분 변경이 없다. 뷰 패턴이 이미 있고 그 docstring이 함정을 적어 두었다 |
| **G** | 래퍼 툴 이름은 `import_uploaded_sheet` | 종류를 이름에 박지 않는다 — 레지스트리가 행을 더해도 툴 이름이 거짓이 되지 않는다 | 종류별 래퍼를 늘리지 않는다는 제외 범위와 짝이다 |
| **H** | 전사 문구를 종류 중립으로 | `useCopilotSocket.ts:336`의 `Vectorworks 파일 업로드: …` → 종류를 단정하지 않는 문구 | 그 문구는 **판정 전에** 나가므로 오늘도 절반은 거짓이다 |

### §A.4 열린 결정 — **1건**

**① [NEEDS CLARIFICATION: A 레지스트리 `target` 열의 두 종 — `AC-FILEARG-023`의 무자격 술어가 `vectorworks` 행을 후보에서 배제하는가]**

**무엇이 걸렸나.** A의 레지스트리(`spec.md` §G)에서 `target` 열은 **두 종(種)**을 담는다 — `patch` 행의 대상은 **등록된 툴 이름**(`import_lxseq_patch`)이고, `vectorworks` 행의 대상은 **세션 경로**(`upload_vectorworks_export`)다. 후자는 `TOOL_NAMES`에 없다(본 세션 확인 — `server/orchestrator/tools.py`의 튜플에 그 이름은 없고, 그것은 `server/web/session.py:9655`의 메서드다).

**충돌.** A의 `AC-FILEARG-023`은 술어를 **한정 없이** 적었다 — "행이 지목한 대상이 **등록 툴 집합에 없으면** 그 행은 판별 후보에서 제외되고 오류로 보고된다". 문면 그대로 읽으면 `vectorworks` 행이 **매번 후보에서 빠지고 매번 오류가 난다**. 그러면 A의 결정 I(Vectorworks를 레지스트리 행으로 등록해 0건 거절을 살린다)가 자기 인수 조건에 의해 무효가 된다.

**두 읽기 모두 A의 문면이 지지한다.** (가) `target`은 두 종이고 `AC-FILEARG-023`은 **툴 종에만** 적용된다 — `REQ-FILEARG-007`이 `vectorworks` 행의 대상을 "기존 세션 업로드 경로"라 명시했으므로 이쪽이 의도로 보인다. (나) 술어는 문면 그대로 무자격이다 — 그렇다면 `vectorworks` 행은 등록될 수 없고 결정 I가 다른 형태를 찾아야 한다.

**왜 B가 혼자 못 정하나.** 이것은 A의 레지스트리 계약이고, B가 한쪽으로 구현하면 **A의 인수 조건 하나가 반드시 빨개진다**. 어느 쪽이 빨개질지를 B가 고르는 것은 A의 결정을 대신하는 일이다.

**오늘 차단은 아니다.** 결정 C(`vectorworks` 판정은 슬롯에 들어가지 않고 즉시 기존 경로로 간다) 덕분에 **B의 래퍼는 이 행을 볼 일이 없다** — 래퍼는 슬롯을 읽고, 슬롯에는 시트 종류만 들어온다. 그러므로 B는 이 마커가 열린 채로도 M1·M2를 구현할 수 있다. **다만 Implementation Kickoff Approval 전에 답이 필요하다**: (나)로 판명되면 A의 `vectorworks` 행이 사라지고, 그러면 B의 이음매에서 "판정이 `vectorworks`" 가지가 **도달 불가**가 되어 `AC-SHEETPIPE-005`의 전제가 무너진다.

**해소 경로.** 오케스트레이터가 A의 소유자에게 (가)/(나) 중 하나를 확정받는다. (가)면 A의 `AC-FILEARG-023` 문면에 "툴 종 대상에 한한다"는 한정을 더하는 **문서 정정 1건**으로 끝나고 B는 그대로 간다.

### §A.5 PRESERVE 재확인

`REQ-SHEETPIPE-009`가 소유한다. **A의 경계와 B의 경계는 모양이 반대다** — A는 `server/sheets/`를 **만들고** 그 밖을 안 건드리며, B는 `server/sheets/`를 **읽기 전용으로 두고** UI·세션·오케스트레이터를 연다. 그래서 A의 요구도 A의 게이트도 빌려 쓸 수 없다.

**[HARD] 게이트는 커밋 뒤에 돌린다.** `server/tests/test_overlap_preserve.py`가 고정 base와 **`HEAD`**를 diff하므로(본 세션 실측 — `:427` · `:456` · `:464`; A가 인용한 `:446`은 그 파일에 `base..HEAD`가 없다) 작업 트리의 미커밋 변경은 그 진단의 **시야 밖**이고, 커밋 전 초록은 운이 아니라 **구조적으로 보장된 거짓 신호**다. 사유와 실행 순서는 `AC-SHEETPIPE-011`이 소유한다.

---

## §B. 마일스톤 M0~M2

**순서는 되돌리기 어려움 순이다.** 데이터 모델과 운영자에게 보이는 흐름(M1)이 기계적 등재(M2)보다 앞선다 — 뒤집힐 가능성이 큰 결정을 먼저 드러내야 검토가 값을 한다.

### M0 — 계약 재확인 · 기준선 · Kickoff 답 기록 (cycle_type=none — 코드 변경 0)

1. **A가 이 워크트리에 있는지 확인한다**(`ASSUMPTION-78`). `server/sheets/`의 판별 진입점과 레지스트리가 실재하고, A의 테스트가 초록인지 본다. 없으면 **여기서 멈춘다** — 순서 위반이지 결함이 아니다.
2. **좌표 11건을 토큰 앵커로 재확인한다**(`spec.md` §H). 줄번호가 아니라 **`grep`으로** 재고, 어긋난 것은 그 자리에서 고쳐 `progress.md`에 적는다. **본 세션의 교훈**: `sed` 출력에서 눈으로 센 좌표 둘이 한 줄씩 어긋났고 `grep`으로 재서야 잡혔다.
3. **기준선을 이 트리에서 직접 잰다** — `uv run pytest server/tests -q`와 `npm --prefix ui run test` 둘 다. 착수 시점의 기존 실패가 있으면 **원인별로 나눠** 적는다(B의 델타가 아닌 것을 B의 델타로 세지 않기 위해).
4. **등재 7지점을 §G의 측정 명령으로 다시 센다.** 특히 6·7번(`test_tools.py`의 리터럴 · 트립와이어)은 첫 명령에 **걸리지 않으므로** 두 번째·세 번째 명령을 반드시 돌린다.
5. **§A.4 ①의 답을 기록한다.** Kickoff에서 (가)/(나) 중 무엇으로 확정됐는지와, (나)라면 `AC-SHEETPIPE-005`를 어떻게 처분할지를 적는다.

**산출**: `progress.md` §E.1의 `baseline_measured` 채움 · 좌표 정정 목록 · 등재 지점 재측정값 · Kickoff 답.

### M1 — 세션 슬롯 · 이음매 · 넷 표시 · 전사 문구 (cycle_type=tdd)

가장 뒤집히기 쉬운 것들이 여기 모여 있다 — 데이터 모델(슬롯) · 사용자에게 보이는 흐름(표시 · 전사 문구) · 오늘 동작의 보존(Vectorworks 가지).

1. **슬롯 타입을 만든다** — 불변 · 통째 교체(결정 F). 넷 + `file_name`을 든다(`REQ-SHEETPIPE-001`).
2. **읽기 통과 뷰를 만들고 `build_toolset` 배선 두 곳을 연다**(`tools.py:1609` 시그니처 · `session.py:3508` 호출부). **짝을 어기면 조용히 무너진다**(`REQ-SHEETPIPE-002` · `AC-SHEETPIPE-003`).
3. **이음매를 연다** — `session.upload_vectorworks_export` 본문에서 A의 판별기를 부르고 판정으로 분기한다. `vectorworks`면 **오늘 두 줄 그대로**, 시트면 슬롯에 담고 표시만, 거절이면 담지 않고 사유를 보인다. `app.py`·`messages.py`는 **열지 않는다**(결정 B).
4. **넷을 표시한다** — 서버 `notice_event`, 행 수는 **이름 붙은 수**(결정 D·E).
5. **UI 두 곳** — 전사 문구를 종류 중립으로(결정 H) · 프레임이 더 이상 한 종류만 나르지 않는다는 주석을 라우터 옆에 남긴다(`REQ-SHEETPIPE-008` ①).
6. 스위트 2종을 돌리고 기준선과 대조한다. **vitest 델타가 0이 아닌 것이 정상이다**(B는 `ui/`를 연다).

**AC**: `002` · `003` · `004` · `005` · `006`.

### M2 — 래퍼 툴 등재 · 거절 3종 (cycle_type=tdd)

기계적이지만 **빠뜨리기로 유명한** 자리다.

1. **래퍼 핸들러를 쓴다** — 슬롯에서 바이트를 꺼내 레지스트리가 지목한 대상 툴의 `file_content_base64`에 넣어 내부 `ToolCall`로 부른다(`REQ-SHEETPIPE-005`).
2. **스키마를 닫는다** — `file_content_base64` 없음 · 경로 인자 없음 · `additionalProperties: False`(`REQ-SHEETPIPE-006`).
3. **거절 3종을 이름으로 낸다**(`REQ-SHEETPIPE-007`).
4. **등재 7지점을 전부 친다**(`spec.md` §G). 6번은 `34 → 35`. 7번은 트립와이어 시작점 목록을 **통째 재생성**하고 **보호 구간 침범 0건**을 함께 확인해 커밋 메시지에 적는다.
5. **스위트를 두 번 돈다** — 등재 직후 한 번(6·7번 누락이 여기서 드러난다), 트립와이어 재생성 후 한 번.
6. **커밋 → 경계 게이트 → 보고**(`AC-SHEETPIPE-011`). 순서를 바꾸지 않는다.

**AC**: `007` · `008` · `009` · `010` · `011`.

---

## §C. 파일 단위 대상 (전체 목록)

**[HARD] 이 목록을 6~7파일로 잡는다.** "4지점"이라 적힌 계획이 반복해서 두 자리를 빠뜨렸고, 그 둘은 마일스톤이 끝난 줄 알았을 때 전체 스위트에서 빨갛게 드러난다.

| 파일 | 무엇을 | 마일스톤 |
|---|---|---|
| `server/web/session.py` | 슬롯 타입 · 읽기 통과 뷰 · 이음매 분기 · 넷 표시 · `build_toolset` 호출부(`:3508`) | M1 |
| `ui/src/useCopilotSocket.ts` | 전사 문구 종류 중립화(`:336`) | M1 |
| `ui/src/App.tsx` | 라우터 옆 주석(프레임이 한 종류만 나르지 않음) — 분기 자체는 **안 바꾼다** | M1 |
| `server/orchestrator/tools.py` | 등재 1·2·3·4 + `build_toolset` 시그니처(`:1609`) | M2 |
| `server/orchestrator/runner.py` | 등재 5 — `_TOOL_TASKS` 한국어 이름 | M2 |
| `server/tests/test_tools.py` | 등재 6 — 리터럴 `34` → `35` | M2 |
| `server/tests/test_songcue_bundle.py` | 등재 7 — 트립와이어 시작점 목록 재생성 | M2 |
| `server/tests/test_sheet_pipe.py` (신규) | B의 인수 테스트 전부 | M1 · M2 |

**열지 않는 것**(열렸다면 `AC-SHEETPIPE-011`이 잡는다): `server/sheets/**`(A) · `server/web/app.py` · `server/web/messages.py` · `ui/src/protocol.ts` · `server/lxseq/**` · `server/vwx/**` · `console/lua/**` · `server/safety/**` · `server/prechk/**` · `server/paperwork/**` · `server/rulebook/assets/**` · `server/web/preview.py`.

## §D. 위험

| 위험 | 표면화 | 완화 |
|---|---|---|
| 슬롯 노출 짝 어김 → 툴이 영원히 빈 슬롯을 본다 | 운영자가 실제로 시켜 볼 때까지 **아무 신호 없음** | `REQ-SHEETPIPE-002` + `AC-SHEETPIPE-003` 뮤테이션 |
| 등재 6·7 누락 | `tools.py`를 읽어서는 안 보이고 전체 스위트에서만 | §G 측정 명령 3종 · M2에서 스위트 2회 |
| 실행 금지를 프레임 전체로 넓혀 읽음 | 오늘 살아 있는 Vectorworks 동작이 사라진다 | `spec.md` §F 속성 B · `AC-SHEETPIPE-005` |
| 확장자 라우팅 "복원" | 나중에 읽는 사람이 유산 이름을 보고 되돌린다 | `REQ-SHEETPIPE-008` ①의 코드 주석 |
| §A.4 ①이 (나)로 판명 | `AC-SHEETPIPE-005`의 전제가 무너진다 | Kickoff 전 해소(M0 5) |

### §D.1 잔여 위험 — 저장 전용 결정과 판별 도입이 남기는 것

1. **`ASSUMPTION-79` — 오늘 되던 업로드가 거절될 수 있다.** A의 판별기가 들어오면 `unknown_sheet_kind`로 떨어지는 파일은 **슬롯에도 들어가지 않고 Vectorworks 경로로도 가지 않는다**. A는 그 대상(헤더 없는 경로 A 내보내기)이 오늘도 패치를 만들지 못한다고 판단했고 근거는 `server/tests/fixtures/vwx/README.md:84-95`이지만, **운영자의 실제 업로드 이력으로는 재지 않았다**. **이것은 B의 이음매에서 표면화하지만 A의 결정이다** — B에서 되돌리지 말고 A로 올린다. 완화는 A의 조건부 재수출 안내(`REQ-FILEARG-023`)이며, 그 문구가 살아 있는지는 A의 `AC-FILEARG-029`가 지킨다.
2. **모델은 슬롯이 채워진 것을 안내로 알지 못한다.** `notice_event`는 `_record_history`를 거치지 않으므로(`spec.md` 사전 확정 사실 7) 모델의 문맥에 들어가지 않는다. 운영자가 말을 꺼내기 전까지 모델은 시트가 올라온 것을 모르고, 잘못 부르면 `no_uploaded_sheet` 거절로 알게 된다. **이것은 결함이 아니라 `analyse_layout_image`와 같은 설계**이며, 그래서 거절 문구가 "다음에 무엇을 할지"를 담아야 한다(`REQ-SHEETPIPE-007`).
3. **유산 프레임 이름은 남는다.** `vectorworks_export_upload`가 시트도 나르게 되지만 이름은 그대로다. 주석으로 막았을 뿐 **기계 검사는 없다** — 이름을 바꾸는 것이 값보다 비싸다는 판단(결정 A)의 대가다.

## §E. 후속 SPEC 예약

LXSEQ-002/003/004가 레지스트리에 행을 더할 때 **B는 열리지 않는다** — 래퍼는 행이 지목한 대상을 부르므로 새 종류는 A의 표에 행 하나로 들어온다. B가 다시 열려야 한다면 그것은 **레지스트리 계약이 실패했다는 신호**이고, 그때 고칠 곳은 A다.

## §F. Phase 4 모드 사전 권고

**solo-sequential.** M1이 M2를 막고 파일이 겹치므로(둘 다 `tools.py`) 병렬 팬아웃의 이득이 없다. 조사 팬아웃도 권하지 않는다 — 좌표는 이미 전수 실측됐고 §H가 소유한다.

## §G. 안티패턴 (이 카드에서 특히)

- **"증명된 패턴이니 그대로 베낀다".** 선례가 **둘**이고 형태가 반대다. 어느 쪽을 베꼈는지 말하지 않은 채 베끼면 §F 속성 A의 함정에 그대로 들어간다.
- **"게이트가 초록이니 안 건드렸다".** 커밋 전 초록은 `base..HEAD` 스코핑 때문에 **구조적으로 보장된 거짓 신호**다. 그리고 초록인 것과 그 가드가 이 트리를 **보는 것**은 다른 문장이다.
- **"부정 grep이 비었으니 없앴다".** 인라인 강조가 문자열을 쪼갠다. 그리고 무엇을 옮겼으면 **긍정으로 전수 훑어** 살아남은 포인터를 찾는다 — 본 문서를 쓰는 동안에도 그 훑기가 자기 오류 3건을 잡았다.
- **"숫자를 적어 두면 다음 사람이 안다".** 숫자만 적고 **그 숫자를 만든 명령과 배제 기준**을 적지 않으면 다음 사람이 다른 명령을 부른다(§G 등재 지점 표가 그래서 그 꼴이다).
- **"툴 등재는 4지점".** 7지점이고, 그중 둘은 툴 이름을 문자열로 담지 않아 첫 grep에 **영원히 걸리지 않는다**.

## §H. 상호참조

- `spec.md` §B(요구) · §F(세 성질) · §G(등재 7지점 정본) · §H(좌표)
- `acceptance.md` §C(인수 기준) · §D(Definition of Done · 계수 규약)
- `SPEC-COPILOT-FILEARG-001` — `spec.md` §G(레지스트리 정본) · `design.md` §1~§4(축 진단 · 위임 계약) · `AC-FILEARG-009`(순서 단언; **표시 단언은 B의 `AC-SHEETPIPE-006`**) · `AC-FILEARG-023`(판별 시점 방어)
- `SPEC-COPILOT-LXSEQ-001` — `REQ-LXSEQ-010`(대상 툴 인자 집합; **"4지점" 문면은 낡았다**) · `REQ-LXSEQ-016`(붙여넣기 금지 — `REQ-SHEETPIPE-006`의 역추적 대상)
