# 기술 스택 (Tech)

> 출처: `/DESIGN.md` §3(시스템 아키텍처), §4(Claude 연계 상세 설계), §5(안전 설계), §9(기술 스택 제안). 이 프로젝트는 그린필드 상태이며, 아래는 계획된 기술 스택이다.

## 1. 서버 (Backend)

| 항목 | 선택 | 비고 |
|---|---|---|
| 언어/런타임 | Python 3.11+ | |
| AI SDK | `anthropic` SDK — tool runner (`client.beta.messages.tool_runner()`) | 수동 도구 실행 루프 불필요 |
| OSC 통신 | `python-osc` | grandMA3로 `/cmd` 전송(UDP), 피드백 수신 |
| 웹 서버 | FastAPI + WebSocket | UI ↔ 오케스트레이터 실시간 통신 |
| 파서 | showfile XML(표준 라이브러리), MVR/GDTF(zip+XML) | 리그 컨텍스트 인식용 (Phase 2) |

## 2. 콘솔측 (Console-side)

- **grandMA3 Lua 5.4 responder 플러그인**: socket 기반 상태 회수. 콘솔 오브젝트 트리 조회, 실행 결과 확인, 플러그인 풀 관리를 담당하는 콘솔 상주 컴포넌트.

## 3. UI

- **한국어 채팅 웹 UI**: React 또는 Svelte (미확정 — DESIGN.md는 두 옵션을 병기, 선택 기준 미정)
- 콘솔 옆 태블릿/세컨드 모니터 사용을 상정
- 음성 입력 옵션 (DESIGN.md §3 다이어그램에 언급, 상세 미정)

## 4. Phase 3 전용 — 음악 분석

- **librosa** 또는 **essentia**: 로컬 처리, API 불필요. 구간/BPM/에너지 분석 → 타임코드 큐리스트 초안 생성에 사용.

## 5. LLM 연계 상세 설계

> [2026-07-16 amendment] SPEC-COPILOT-MVP-001 v0.2.1(REQ-MVP-038~041, 사용자 결정 2026-07-15)에 따라 **프로바이더 교체 가능 추상화 계층**으로 개정. 아래 5.1은 더 이상 Anthropic 단일 프로바이더를 전제하지 않는다.

### 5.1 프로바이더 추상화 및 모델 선택

- **추상화 계층 (REQ-MVP-038)**: LLM 클라이언트는 프로바이더 교체 가능한 추상화 계층 뒤에 위치한다 — 도구 호출·시스템 프롬프트 구성·응답 파싱이 프로바이더 중립 인터페이스로 분리되며, 상위 계층(오케스트레이터·안전 게이트·UI)은 활성 프로바이더를 알지 못한 채 동작한다.
- **듀얼 프로바이더 지원 (REQ-MVP-039)**: Anthropic Claude와 Google Gemini 2개 프로바이더를 지원하며, 프로바이더 선택은 설정 파일로 이루어진다(코드 변경 없이 전환 가능). 동시 다중 모델 라우팅은 Out of Scope 유지.
- **단일 활성 프로바이더 구성 (REQ-MVP-006)**: AI 오케스트레이터는 단일 활성 프로바이더·단일 모델 구성으로 동작한다 — Anthropic 선택 시 `claude-opus-4-8`, Google Gemini 선택 시 설정 파일에 핀된 Gemini 모델.
- **기본 프로바이더 선정 (REQ-MVP-040)**: (i) AC-MVP-002(오류율)와 AC-MVP-003(왕복 시간) 양쪽을 통과하는 프로바이더 중 비용이 낮은 쪽을 기본값으로 선정 — (ii) 운영 중 비용-우선 선정 프로바이더가 AC-MVP-003을 지속 미충족 시 다른 적격 프로바이더로 폴백(설정 전환 + 감사 로그 기록).
- **프로바이더별 캐싱 (REQ-MVP-041)**: 활성 프로바이더가 프롬프트 캐싱을 지원하는 경우(Anthropic prompt caching / Gemini context caching) 해당 메커니즘으로 룰북 고정 프리픽스 캐싱을 적용한다. 캐싱 미지원/최소 토큰 임계 미달 구성에서는 성능·비용 저하를 명시적으로 수용한다.

| 역할 | 프로바이더 활성화 시 모델 | 근거 |
|---|---|---|
| 메인 (명령 생성·플러그인 작성·연출 기획) — Anthropic 선택 시 | `claude-opus-4-8` | 긴 자율 작업·Lua 코드 생성·정확한 문법 준수에 최적. $5/$25 per MTok |
| 메인 — Google Gemini 선택 시 | 설정 파일 핀 모델 | REQ-MVP-006 — 코드 변경 없이 설정 전환 |
| 의도 라우팅·간단 분류 (선택적 최적화, Anthropic 한정) | `claude-haiku-4-5` | "단순 실행 vs 기획" 분류만. $1/$5 per MTok |
| 대규모 쇼 통짜 기획 (옵션, Anthropic 한정) | `claude-fable-5` | 최고 성능 필요 시에만. $10/$50 per MTok. 별도 refusal 처리 필요 |

프로덕션 단순화를 위해 **초기에는 단일 활성 프로바이더(REQ-MVP-040 기본 선정 술어에 따라 결정)**로 시작하고, 비용·오류율 데이터가 쌓이면 폴백/재선정 로직을 정교화한다.

> 배경: SPEC-COPILOT-EVAL-001(boardop 실사용 평가, v0.3.1)이 boardop의 Claude/Gemini 듀얼 지원을 확인했고, 이를 참고해 MVP도 동일 전략을 채택했다(격차 분석 G-13 참조 — MVP는 단일 활성 프로바이더 설계라 boardop의 프로바이더 전환 시 토큰 급증 문제가 발생하지 않음).

### 5.2 Tool Use 설계

Anthropic SDK tool runner 기반, `@beta_tool` 데코레이터로 다음 5개 도구를 정의:

```python
run_commands(commands: list[str], risk_note: str = "") -> str
# MA3 커맨드라인 명령 시퀀스를 검증→승인→OSC 실행하고 결과 반환.
# 파괴적 명령(Delete, Off Everything 등)은 승인 게이트에서 자동 정지.

query_state(path: str) -> str
# Lua responder를 통해 콘솔 오브젝트 트리 조회.
# 예: 'DataPool/Sequences', 'ShowData/Patch'

deploy_plugin(name: str, lua_source: str) -> str
# Lua 플러그인을 pcall 하네스로 컴파일 검증 후 리뷰 게이트 대기 → 배포.

get_rig_context(scope: str) -> str
# showfile XML / MVR에서 패치·그룹·프리셋 어휘·3D 배치 요약 반환.

propose_plan(...)
# 시스템 아키텍처 다이어그램(§3)에 언급된 5번째 도구. 상세 시그니처는 DESIGN.md §4.2 코드 블록에
# 명시되지 않음 — 미정(TBD).
```

- 승인 게이트는 **도구 함수 내부**에서 구현: 위험 명령이면 사용자 확인 전까지 실행하지 않고 "승인 대기/거부" 결과를 반환.
- 기본 설정: `thinking={"type": "adaptive"}` + `output_config={"effort": "high"}`.

### 5.3 프롬프트 캐싱 전략 (비용 최적화의 핵심)

시스템 프롬프트가 크다 (문법 룰북 + 오브젝트 모델 요약 ≈ 1~2만 토큰). 프리픽스 캐싱 구조:

```
[tools 정의]              ─┐
[룰북 + 오브젝트 모델]      ─┴─ 고정 프리픽스 → cache_control (ttl: "1h")
[쇼 스냅샷 · 대화 이력]     ── 매 턴 변하는 부분 → 캐시 뒤에 배치
```

- 캐시 읽기 = 기본 입력가의 ~0.1배 → 룰북 15K 토큰 기준 턴당 $0.075 → **$0.0075**로 감소.
- **주의**: 룰북에 타임스탬프·세션 ID 등 가변 값을 절대 삽입 금지 (프리픽스 1바이트만 바뀌어도 캐시 전체 무효화).
- 쇼 스냅샷은 messages의 마지막 user 턴에 주입 (캐시 프리픽스 보존).

### 5.4 예상 API 비용 (opus-4-8, 캐싱 적용 기준 — 추정치)

| 항목 | 추정치 |
|---|---|
| 1턴 (캐시 읽기 15K + 신규 입력 3K + 출력 1.5K) | ≈ $0.06 |
| 집중 프로그래밍 세션 (100턴) | ≈ $6 (약 8천 원) |
| 월간 헤비 유저 (세션 20회) | ≈ $120 |

자가 수정 루프가 폭주하지 않도록 재시도 상한이 필수다.

## 6. 안전 아키텍처 (Safety Architecture)

4단계 안전 게이트 (데이터 흐름: 명령 생성 → **①문법 검증** → **②위험도 분류**(위험 시 승인 대기) → OSC 실행 → **③인간 승인 UI**를 거쳐야 하는 명령은 대기 → **④라이브 잠금**):

| 계층 | 내용 |
|---|---|
| ① 문법 밸리데이터 | 생성된 명령을 MA3 문법 파서로 실행 전 검사. 파싱 불가 명령은 실행 자체를 차단 |
| ② 위험 명령 분류 | 블랙리스트: `Delete`, `Remove`, `Off Everything`, `Store /overwrite`(라이브 시퀀스), `Shutdown`, `Format` 등 → 무조건 인간 승인. 번들 명령은 all-or-nothing |
| ③ 인간 승인 UI | 위험 명령에 대한 사용자 확인 게이트 |
| ④ 라이브 잠금 모드 | "라이브 모드" 토글 시 AI는 read-only + 제안 카드만 생성, 실행 버튼은 사람 |

추가 안전 요소:
- **파라미터 가드레일**: 스트로브 주파수·디머 급변 등 광과민성 발작 위험 패턴 경고
- **감사 로그**: 모든 실행 명령·승인·거부를 타임스탬프와 함께 기록 (쇼파일 백업 자동화 포함)
- **plugin 배포**: pcall 하네스로 컴파일 검증 후 리뷰 게이트를 거쳐 배포

## 7. 환각(문법 오류) 억제 — 3중 방어

1. **지식 레이어(Knowledge Layer)**: 검증된 MA3 문법 룰북을 시스템 프롬프트에 상주시킨다 (boardop 방식 벤치마킹).
2. **밸리데이터(Pre-execution Validator)**: 생성된 명령을 MA3 문법 파서로 실행 전에 검사한다 (키워드·오브젝트 참조 형식 확인).
3. **실행 피드백 루프(Self-correction Loop)**: 콘솔 에러 응답을 Claude에게 되돌려 자가 수정하게 한다. **재시도 상한 ≤3회**.

## 8. 통신 프로토콜

- **OSC (Open Sound Control)**: boardop에서 검증된 경로를 그대로 채택.
  - out: `/gma3/cmd` (또는 시스템 다이어그램 표기 `/cmd`, UDP)
  - in: `/python/feedback` (피드백 수신)

## 9. 미정(TBD) 항목

- `propose_plan` 도구의 정확한 함수 시그니처 및 반환 형식
- 문법 파서(밸리데이터)의 구체적 구현 방식 (자체 파서 vs 외부 라이브러리)
- 감사 로그의 저장 형식/위치 (파일, DB 등)
- 음성 입력 옵션의 구체적 기술 스택 (STT 엔진 등)
- CI/CD, 테스트 프레임워크, 배포 파이프라인
- 지원 MA3 버전 범위의 정확한 값 ("v2.x 고정"이라고만 언급, 세부 버전 미상세)
