# t70 — SPEC-COPILOT-LXSEQ-004 작성: 전제 셋을 반증하고 썼다

브랜치 `WT-songcue-import` · base `d92cc80` · **콘솔 접촉 0 · 코드 변경 0**
산출: `.moai/specs/SPEC-COPILOT-LXSEQ-004/{spec,plan,acceptance}.md`

## 0. 한 줄

착수 전에 카드·배차서 전제 넷을 재서 **셋이 어긋났다.** 가장 큰 것은 **되읽기 수단이
이미 저장소에 있다**는 것 — 카드는 「미측정」이라 적었고 t4 의 부정적 선례를 인용했는데,
그 결론은 **틀린 객체를 읽은 탓**이라고 저장소가 이미 기록해 두고 있었다.

## 1. 전제 검산 — 넷 중 셋이 어긋났다

| 문면 | 실측 | 계기 | 판정 |
|---|---|---|---|
| Sugar 큐 89건 `*.cue-ex.csv` | 파일 **1개**, 데이터 행 **89개** | `find … -name *.cue-ex.csv \| wc -l` = 1, `wc -l` = 90 | 복수 파일 암시가 틀림 |
| **t69 가 선행이다** | `moai todo` 에 **t69 없음** | `grep -cE "^t69"` = 0, 대조군 `grep -oE "^t7[01]"` = t70 t71 | **선행 카드가 없다** |
| `tools.py:7865` | 그 문구는 **8456행** | `grep -n "Do not pass rig numbers"` | 줄번호 밀림, 문구는 실재 |
| **큐 확인 가능성 미측정** | **존재한다** (§2) | `server/web/cue_monitor.py` 직독 | **반증** |

## 2. 🔴 되읽기 수단 — 저장소가 이미 갖고 있었다

`server/web/cue_monitor.py`:
- `SEQUENCE_PATH_TEMPLATE = "DataPool/Sequences/{sequence_no}"` (:49)
- `_cue_items`(:145) 가 큐 자식마다 `{"no": 슬롯, "name": 이름, "cue_no": 응답기 번호}` 를 낸다
- 모듈 문서가 **2026-08-02 LIVE-VERIFIED** 로 기록하고, 이전의 「어느 채널도 노출 안 한다」
  결론이 **Executor 핸들이라는 틀린 객체를 읽은 탓**이었다고 명시한다

**다만 열리는 것은 번호와 이름까지다.** 큐 내용은 안 온다 — t87·t105 와 같은 형태.
그래서 이 SPEC 의 수용 기준은 「89개가 번호·이름으로 되읽힌다」까지 쓸 수 있고,
「Dim 이 55다」는 못 쓴다. 그 한계를 적는 것이 AC-013 이다.

📏 이건 규약의 「저장소가 이미 갖고 있다 — 짓기 전에 찾아라」가 실제로 값을 낸 자리다.
안 찾았으면 이 SPEC 은 「확인 수단 없음」을 전제로 쓰였을 것이고, 그 전제가 부채가 됐을 것이다.

## 3. 🔴 계기 오류 하나 — 대조군이 잡았다

`grep -rn "Cue" server/rig/*.py` 가 **0건**을 냈고 「큐 판독 경로가 없다」로 갈 뻔했다.
검출력을 확인하니 같은 우주에서 `Preset` 0건 · `Sequence` 0건이었다 —
**`server/rig/` 은 3파일뿐이고 콘솔 객체 어휘를 아예 안 담는다.** 우주가 틀렸다.
전역으로 다시 재니 `cue_monitor.py` 가 나왔다.

🔴 **이 세션에서 같은 형태(술어는 맞고 우주가 틀림)의 네 번째다.** 앞의 셋:
primary 절대경로로 규약 파일 조회 · `which ruff` · `python3`(3.9.6 대 프로젝트 3.11.15).
**넷 다 부재 주장으로 갈 뻔했고 넷 다 대조군이 잡았다.**

## 4. `prepare_songcue` — 확장 아님

`tools.py:8447` 직독: 입력이 `song_title`·`genre`·섹션이고, 「reads the current groups and
sequences **itself**」이며 **"Do not pass rig numbers or copied rig sections"**(:8456).
**generator 이지 importer 가 아니다.** 등재 도구 37종에 `import_lxseq_cues` 가 없다 —
`import_lxseq_{groups,patch,presets}` 셋만 있다. 신규 도구가 맞다.

## 5. 「안 된다」 3분류 적용 (리드 지시)

| 항목 | 관측 | 분류 | 처분 |
|---|---|:--:|---|
| `POS` 값 열 부재 | 시트에 값 열 자체가 없다 | **(C)** | 감독 확인 대기 — **우회 로직 안 짬** |
| `LED-W` 그룹 6행 부재 | patch/group.csv 에 없다 | **(C)** | 감독 확인 대기 |
| `BM` 참조 10건 | 산문이다 | **(C)** | 감독 확인 대기 |
| 큐 내용 되읽기 불가 | 채널이 이름까지만 연다 | **(A)** | 우리 코드 한계 — **근거: `_cue_items` 가 값 필드를 안 싣는다**(직독) |
| t69 선행 카드 부재 | 큐에 없다 | **(C)** | 리드가 이미 정정함 — 선행은 3단계로 충족 |

(A) 하나에만 근거를 댔다. 나머지는 기본값 (C) 다.

## 6. 안 잰 것

- **실기 0회.** 콘솔 접촉 없이 썼다. 쇼 지문·응답기 버전·빈 슬롯은 run 단계 M0 가 다시 잰다.
- **89행 중 몇 행이 미확정 입력에 걸리는지 안 셌다.** M1 의 첫 산출이다.
- **`FX` 4열이 전부 비어 있는지 안 셌다.** 범위에서 뺐지만 「비어 있다」는 미측정이다.
- **감독 답이 안 왔다.** `POS`·`LED-W`·`BM` 셋 다 대기 중이고, 답에 따라 AC-004 가 바뀐다.
- **`moai spec lint` 만 돌렸다.** 0 error · 경고 1건은 `Out of Scope` 헤딩을 더해 해소.
  plan-audit 은 안 돌렸다 — 리드 판정 사항.

## 7. 후속 후보 (카드로 만들지 않는다)

SPEC §9 에 넷 적었다. 여기 하나 더: **`cue_monitor.py` 는 SPEC 없이 산 모듈이다**
(자기 문서가 "Ad-hoc contract, no SPEC on file" 이라 적는다). t201 의 (b) 분류와 같은 계열.
