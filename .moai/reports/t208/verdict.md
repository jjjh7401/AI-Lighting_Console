# t208 — 이번 회차 실측 다섯을 규약에 박았다

- 카드 t208 (Class A, 문서 한 파일) · 워크트리 `.claude/worktrees/t208`
- 브랜치 `WT-protocol-five-traps` · base **`bc4de43`**(착수 시점 `origin/main` 재확인)
- 대상: `.moai/docs/lane-protocol.md` 한 곳. **+62 / −2**

## 1. 주장

다섯을 규약에 실었다. 전부 카드 장부에만 있어서 다음 레인이 다시 밟을 것들이었다.
**절 번호는 하나도 안 건드렸다** — 번호가 인용의 주소이기 때문이다.

| # | 항목 | 자리 |
|---|---|---|
| 1 | `moai session list` 금지 | §1 착수 |
| 2 | 스쿼시 꼬리 — `origin/main` 에서 새로 떼라 | §1 착수 |
| 3 | 린트가 `.moai/reports/**/*.py` 를 잡는다 | §2 |
| 4 | venv — 착수 즉시 `uv sync --group dev` | §1 착수 |
| 5 | 큐 정본 `backlog.json` · 필드 `state` | §1 착수 (기존 항목 교체) |

## 2. 증거 — 문서 카드에도 실행 확인을 붙인다 (규약 §5)

### 2.1 절 번호가 안 밀렸다

    grep -n "^## \\|^### " .moai/docs/lane-protocol.md

헤더 **19개**, HEAD 도 19개. 번호 체계(1 · 2 · 3 · 3.1-3.6 · 4 · 4.0 · 4.1 · 5-9) 동일.

왜 이걸 재는가 — **대조군 있는 측정**으로 답한다:

| 술어 | 결과 |
|---|---|
| `lane-protocol` 을 **읽는** 검사 | **0개** |
| `규약 §` 를 **인용하는** 검사 | **4개** (`test_section_refusal_reason_survives` · `test_lxseq_group_section_request` · `test_lxseq_tool` · `test_groupgen_write`) |

0 을 그냥 「없다」로 쓰지 않으려고 양성 대조군을 같이 쟀다. 결론: **문면 편집은
검사를 못 깨뜨리지만, 번호를 밀면 4개 파일의 인용이 조용히 썩는다.**
그래서 §3 의 「번호는 안 건드린다」를 그대로 지켰다.

### 2.2 (2) 스쿼시 꼬리 기전 — 직접 쟀다

    git merge-base --is-ancestor 16c161b origin/main   → exit 1  (조상 아님)
    git merge-base --is-ancestor bc4de43 origin/main   → exit 0  (조상)

`16c161b` 은 방금 머지된 t186 브랜치의 끝점이고 `bc4de43` 은 그 스쿼시 결과다.
**브랜치 커밋은 `origin/main` 의 조상이 아니다** — 그 위에 꼬리를 얹으면 PR 이
이미 머지된 것을 다시 싣는다는 카드의 주장이 기전으로 확인된다.

### 2.3 (5) 규약에 적은 명령이 실제로 도는가

    jq -r ".items[].state" <primary>/.moai/state/kanban/backlog.json | sort | uniq -c
      4 dropped / 60 picked / 89 queued

    ls .moai/state/kanban/backlog.json   → No such file or directory  (워크트리 안)

둘 다 규약에 적은 그대로다. 🔴 **그리고 여기서 내가 쓴 숫자가 이미 낡았다** —
문서에 적은 152장이 몇 시간 뒤 153장이었다(t208 카드가 늘었다). 그래서
「하한으로만 읽어라 · 하중을 지는 것은 절대값이 아니라 덤프와 정본의 격차」를
같은 항목에 덧붙였다. **규약에 숫자를 박는 그 회차에 그 숫자가 노화했다.**

### 2.4 (3) 린트 제외 부재 — 릴레이 안 하고 직접

    grep -c "moai/reports" pyproject.toml   → 0

`[tool.ruff.format]` 의 `exclude` 는 파일 단위 나열뿐이다(바이트 동일성 핀 파일들).
디렉터리 항목이 없다. 카드에 「리드 확인」으로 와 있었지만 **내가 다시 쟀고**,
규약 문면의 귀속도 내 검산 명령으로 바꿨다.

### 2.5 (1) 금지 항목 — 쏘지 않고 확인했다

🔴 **`moai session list` 는 돌리지 않았다.** 확인과 사고가 같은 행위이기 때문이다
(안전장치를 발사해서 확인하지 마라). 대신 **대안이 실제로 되는지**를 쟀다:

    ListAgents  → 세션 22개

같은 회차에 금지 대상은 `[]` 를 답했었다. 그래서 규약에 대조를 적었다 —
**「위험하지만 유용한 도구」가 아니라 「위험한데 쓸모도 없는 도구」다.**
이 대조가 없으면 다음 사람이 「그래도 필요할 때가 있겠지」로 읽는다.

### 2.6 회귀

규약 §N 을 인용하는 4파일 + 린트 게이트 + 트리 가드: **203 passed**
(워크트리 자체 venv. 착수 직후 `uv sync --group dev` 를 돌렸고, 이번엔 실제로
패키지가 설치됐다 — 즉 `.venv` 가 **정말로 없었다.**)

## 3. 기준 귀속

- 트리 `.claude/worktrees/t208` @ base `bc4de43`
- 자체 venv (`uv sync --group dev`) · 모든 실행 `PYTHONDONTWRITEBYTECODE=1`
- 편집은 전부 `assert src.count(old) == 1` 로 적용을 기계화했다(규약 §3.3)

## 4. 미검증 (gap)

- **(1) 의 파괴 동작을 재현하지 않았다.** 재현이 곧 사고다. 근거는 t186 회차의
  실측 기록과 리드의 독립 확인이며, 이 항목만 유일하게 **내 재측정이 없다.**
- **(2) 의 139줄 → 3줄** 은 plan 레인 실측을 그대로 실었다. 내가 확인한 것은
  그 아래 **기전**(조상 아님)이지 그 두 숫자가 아니다.
- 린트가 `.moai/reports` 를 잡는 것은 **잡힌 사례**로 확인했지, 새 파일을 만들어
  쏴 보지는 않았다(증거 디렉터리를 오염시키지 않으려고).
- 규약 문면이 **읽혀서 행동이 바뀌는지**는 잴 수 없다. 이 카드가 닫아 주는 것은
  「기록이 카드 장부에만 있어 사라진다」까지다.

## 5. 잔여 위험

- §1 이 길어졌다. 착수 절이 길수록 **끝까지 안 읽힐** 위험이 커지고, 그러면
  0번(내 메모리) 항목이 다시 밀려난다 — 이번 회차에 내가 밟은 그 자리다.
  분할은 절 번호를 밀어 인용 4곳을 흔드니 지금은 안 한다. 다음 사람이 판단하도록
  이 위험만 적어 둔다.
- 주 체크아웃은 감독 판정으로 `research/ma3-effects-phaser` 에 그대로 둔다.
  즉 **(4) 의 함정은 계속 열려 있고**, 규약 문면이 유일한 방어다.
