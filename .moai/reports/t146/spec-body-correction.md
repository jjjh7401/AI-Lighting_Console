# t146 — SPEC-LXSEQ-003 본문 두 자리를 t135 실측에 맞췄다

- 카드: `t146` · 브랜치 `WT-lxseq3-spec-body` · 워크트리 `.claude/worktrees/t146`
- base: `origin/main` **493d5a4** · **코드 로직 변경 0**
- 콘솔 접촉 0 (실기 불필요 카드)

## 1. 주장 (Claim)

`SPEC-COPILOT-LXSEQ-003` 본문 두 자리가 t135 실측에 뒤처져 있었다. `Prism` 과 `Frost` 를
**한 사유로 묶고** 있었는데 t135 가 실기로 두 축임을 갈랐다 — `Prism` 은 진짜 부재,
`Frost`·`Focus` 는 **철자**다. 본문 소유권 문제로 t140·t142 가 손대지 못한 자리를
문면만 정정했다.

## 2. 무엇을 고쳤고 무엇을 안 고쳤나 (t140 기준 적용)

t140 이 세운 기준: **날짜·베이스가 박힌 측정 기록은 숫자를 고치지 말고 만료 고지를 붙이고,
다음 사람이 읽고 착수하는 처방·현재상태 서술만 고친다.**

| 자리 | 분류 | 처분 |
|---|---|---|
| `spec.md:167` bm 표 행 (t76 run 레인 M1 실측) | **측정 기록** | 숫자(5/0/3·3) **그대로**, 경고 마커만 추가 |
| `spec.md` A.4-2 본문 (거절 근거 서술) | **측정 기록**(절 제목에 t76 박힘) | **그대로**, 아래 정정 절이 만료를 고지 |
| `spec.md` A.4-2b (신설) | 현재상태 서술 | **신설** — 두 축을 가르고 각 근거의 유효/무효를 명시 |
| `preset-unify-design.md:185` 빔 행 | 현재상태 서술 | 「근거가 흔들렸다」→ 「사유가 두 축이다」로 확정 |
| `preset-unify-design.md:192` | **처방** (「수정은 t142」) | t142 머지 완료 → **만료 고지** + 실제 수행자(t146) 기록 |
| `preset-unify-design.md:246` | 인용 각주 | ⚠️ 범위 확대 — §5 참조 |

**§5.0b 의 실측 블록(196-199행)은 손대지 않았다** — 날짜가 박힌 기록이다.
**§5.0b 절 제목의 「흔들렸다」도 두었다** — 2026-08-30 시점 표현이고, 본문에서 확정을 밝혔다.

## 3. 갈라 놓은 두 축 (신설 A.4-2b 의 본체)

t135 실측 근거: PR #191 → main `fa24d1b`, `.moai/reports/t135/beam-attrs.md` §21·§23;
소스 축은 PR #193. Fixture 501 Robin MegaPointe **단독** 선택, `Off Fixture` 해제, Store 없음.

| 어휘 | 실제 사유 | 이 근거의 상태 |
|---|---|---|
| `Prism` | **진짜 부재** — Robin MegaPointe·MMX Spot 둘 다 Prism 채널 없음 (`Prism1` → `Failed`) | 유효 |
| `Frost` · `Focus` | **철자** — 콘솔 채널명이 `Main Module_Frost1` · `Main Module_Focus1` | **무효** |

그 M0 거절은 2026-07-26 · 응답기 v1.4.1 · Group 13 **전체** 선택에서 틀린 철자로 쏜 결과였다.

🔴 **`Shutter` 는 반증되지 않았다 — 그러나 재확인된 것도 아니다.** t135 측정의 대상이
아니었을 뿐이다. 두 문서 모두에 그 구분을 명시했다: 「반증되지 않았다는 것과 재확인됐다는
것은 다르다.」 이것을 안 적으면 다음 사람이 `Shutter` 도 같이 무효화된 것으로 읽는다.

🔴 **보류 자체는 안 풀린다.** 같은 t135 실측: `Frost` 를 튜플에서 빼면 `BM.04` 가 열리지만
`LXSEQ_PRESET_APPLY_ATTRIBUTE` 에 bm 항목이 없어 적용 줄이 `None` 이고 호출지가
`apply_untranslatable` 로 한 줄도 안 보낸다 — 열면 bm 임포트가 통째로 0건이 된다.
**보류를 푸는 것과 내보낼 경로가 있는 것은 다른 축이다.** 두 문서에 그대로 실었다.

## 4. 안 건드린 것 (카드 금지 사항)

`server/lxseq/preset_parser.py` 의 `_PROBE_REJECTED` 튜플 — **손대지 않았다.**
그 튜플은 콘솔 문자열이 아니라 **감독 시트의 토큰**을 매칭하는 자리이고(t135 별도 실측),
콘솔 채널명에 맞추면 회귀한다. 그 사실을 신설 A.4-2b 에 명시해 다음 사람이 「문면이
정정됐으니 코드도 맞추자」로 가지 않게 했다. 그리고 t141 sync 레인이 지금 그 파일에
앉아 있어 충돌 위험도 있다.

    git diff --name-only  ->  .moai/specs/SPEC-COPILOT-LXSEQ-003/ 두 파일뿐
    코드 파일 변경 0

## 5. 범위 확대 1건 (리드 보고)

`preset-unify-design.md:246` 은 카드가 지목한 두 자리 밖이다. 그 줄은 **`Shutter`** 를
근거로 FX.06 을 판정하는데, 인용한 코드 설명 목록에 이미 무효화된 `Focus`·`Frost` 가
들어 있다. 판정 자체는 유효하므로 **본문을 바꾸지 않고 각주만** 달았다.

근거는 하나다 — **두면 알려진 결함이 남는다.** 같은 파일 안, 같은 무효 근거를 든 자리를
알면서 지나치면 다음 사람이 그 목록을 현재 상태로 읽는다.

## 6. 검증 (Evidence)

    git diff --name-only                     -> spec.md · preset-unify-design.md (2)
    git diff --name-only | grep -v specs/    -> 빈 출력 (코드 0)
    uv run pytest -q                         -> §6.1
    grep -c '_PROBE_REJECTED' 변경분          -> 0

**t151 에서 배운 순서를 지켰다: 커밋한 다음 스위트를 돌린다.** 헝크 트립와이어와
PRESERVE 게이트는 `BASE..HEAD` 를 보므로 커밋 전에 돌리면 거짓 초록이 나온다.

## 7. 안 잰 것 (Gaps)

- **이 문면이 맞다는 것을 실기로 재확인하지 않았다.** t135 리포트와 PR #191·#193 의
  기록을 옮긴 것이고, 콘솔을 다시 쏘지 않았다(카드가 실기 불필요로 지정).
- `Shutter` 의 현재 상태는 **미측정 그대로**다. 이 카드는 그것을 재지 않았고, 두 문서에
  미측정임을 명시만 했다.
- `preset-unify-design.md` §5.0b 절 제목의 「흔들렸다」는 그대로 뒀다 — 날짜 박힌 절이라
  제목까지 고치면 그 시점 기록을 고치는 것이 된다. 본문에서 확정을 밝히는 쪽을 택했다.
- 빔 행 오른쪽 열의 `family_out_of_scope 3` 과 왼쪽 서술 「2건은 범위 밖」의 개수 불일치는
  **이 카드가 건드리지 않았다** — 카드 범위 밖이고, 어느 쪽이 맞는지 재지 않았다.
