# t87 뮤테이션 검증 — 새로 넣은 갈래가 실제로 지켜지는가

판정: **4/4 KILLED**. 복원 체크섬 일치.

초록은 그 자체로 증거가 아니다. 검사가 아무것도 안 지키면서 통과할 수 있으므로,
**방금 추가한 갈래를** 하나씩 죽여 빨개지는지 쟀다. 통째 되돌리기 하나로는 약해서
(경계·불린·키 교체가 따로 있어야 한다) 넷을 쐈다.

기준 커밋: `c18f779`. 대상: `server/lxseq/preset_mapper.py`.
복원은 백업 파일에서 했고 `git checkout` 은 쓰지 않았다 —
미커밋분을 날리는 사고를 이 저장소가 이미 한 번 겪었다.

| # | 뮤테이션 | 판정 | 결과 |
|---|---|---|---|
| M1 | 이름 대조 갈래를 통째로 제거 (`to_plan = storable`) | **KILLED** | 1 failed, 18 passed |
| M3 | `taken.add(record.name)` 제거 — CSV 내 중복만 무력화 | **KILLED** | 1 failed, 36 passed |
| M4 | `if names_incomplete and False` — 미판독 한계 표기 제거 | **KILLED** | 1 failed, 32 passed |
| M5 | 비교 키를 `record.name` → `record.preset_id` 로 교체 | **KILLED** | 1 failed, 18 passed |

## 왜 이 넷인가

- **M1** 은 결함 자체의 복원이다. 이게 살아남으면 이 카드는 아무것도 안 고쳤다.
- **M3** 은 M1 이 가리는 갈래를 따로 잰다. M1 만 쏘면 CSV 내 중복 방지가
  검사되는지 알 수 없다 — 36건이 여전히 통과하고 **한 건만** 빨개진다는 것이
  그 갈래가 따로 지켜진다는 증거다.
- **M4** 는 「거절 대신 한계 표기」라는 판단을 잰다. 표기를 지워도 계획은 똑같이
  나오므로, 이 검사가 없으면 한계가 조용히 사라진다.
- **M5** 는 리드가 카드 본문에서 경고한 **바로 그 실수**다. 콘솔에 가는 것은
  `record.name` 이지 `preset_id` 가 아니어서, 키를 바꾸면 6건 전부 안 맞고
  가드가 통째로 무력해진다 — 그런데 코드는 여전히 그럴듯해 보인다.

## 남는 한계

- 뮤테이션은 **내가 상상한 결함 형태**만 잰다. 4/4 는 「이 넷은 잡힌다」이지
  「어떤 결함도 잡힌다」가 아니다.
- 툴 층(`tools.py` 페이로드)은 여기서 안 쐈다 — 매퍼 갈래만 잰 값이다.

## 재현

복원 체크섬 `8a0c8418ac2aa72ccdf5c2fb465fdd345ee735dee04c3d44fa836a2ca16b09f3`.
뮤테이션 후 매번 백업에서 되돌리고 체크섬을 대조했다.
