t87	picked	매퍼가 이름 중복을 못 본다 — 같은 CSV 를 두 번 돌리면 6건이 통째로 복제된다 (M4 ⑤ 준비 중 run 레인 발견, 리드 코드 실측 확인) || 🔴리드 실측 근거: server/lxseq/preset_mapper.py:166-202 는 candidate=1 부터 올라가며 occupied 에 없는 슬롯만 모으고 zip(storable, empty) 로 짝짓는다. **이름을 한 번도 안 본다.** 점유 슬롯을 피하는 것은 슬롯 보증이지 동일성 보증이 아니다 || 🔴그래서 import_lxseq_presets 는 **멱등이 아니다**. 지금 콘솔에 사고로 생긴 DIM.FULL(슬롯 1)이 있고, 정본 dim 시트 1행이 DIM.FULL 이라 --limit 1 을 쏘면 슬롯 2 에 같은 이름이 하나 더 생긴다. 되돌릴 수단이 없다(Delete 계열이 블랙리스트인지 미측정) || 실측 재료: parse_preset_csv 전수 — dim 6행 전부 storable, col 0/8, bm 0/5 (TOTAL 19 · STORABLE 6). 즉 저장 가능한 6건은 dim 시트 6행 그 자체다 || [HARD] 처방 전에 정할 것: 이름 충돌을 (a) 거절로 볼지 (b) 덮어쓰기 후보로 볼지 (c) 호출자에게 되돌려줄지. 이 저장소에 되돌리기가 없으므로 기본은 (a) 거절이 맞을 가능성이 높지만 정하고 시작할 것 || 대조군: 이름이 이미 있는 상태를 만들고 매퍼를 돌려 planned 가 비는지. 콘솔 불필요 — 매퍼는 순수하다 || 인접: t86(게이트가 Store Preset 을 위험으로 안 본다)와 같은 뿌리의 다른 층이다. t86 은 게이트, 이건 매퍼 || ✅착수게이트 통과 2026-08-25 (run 레인, 리드 재검증): 이름 왕복 6/6 byte_equal · 날조 대조군 Patch/FixtureTypesZZZNotAThing/9999 거절 확인 · truncated=false. 증거 origin/WT-preset-idempotent a93f51c:.moai/reports/t87/name-roundtrip.json. → 축 결정 (a) per-record hold 확정, 재개 안 함 || 🔴본문 정정 (리드가 코드로 확인): 위에서 「사고로 생긴 DIM.FULL(슬롯 1)」이라 적었는데 콘솔 슬롯 1 의 실제 이름은 **'풀'** 이다. 콘솔에 나가는 것은 preset_id 가 아니라 label 텍스트다 — server/spatial/pointing.py:346 이 Label Preset <pool>.<n> '<text>' 를 만들고, server/web/session.py:5344 가 룩 label 을 넘긴다(preset_store_commands 의 3번째 인자). 실측 풀 6건은 풀·쇼 하이·미드·로우·잔광·아웃 이고 DIM 을 담은 이름은 하나도 없다. **이름 비교를 preset_id 로 구현하면 6건 전부 안 맞는다** || 📎배차서 이탈 승인: --probe-only 는 프리셋 풀을 안 읽는다(lxseq_presets_e2e.py:186-187 — 기준선만 읽고 멈춤). 레인이 --action preview 로 대체했고 preview 는 명령 생성 전에 반환하므로 (tools.py:4863) 콘솔 쓰기 0. 내 배차서 전제가 틀렸던 것이고 대체가 맞다
