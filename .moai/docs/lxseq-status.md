# LX-SEQ 연계 — 지금 어디까지 왔고 다음은 어디서 시작하나

> **한 자리에 모은 이유.** 4단계까지 오면서 「무엇이 막혔나」를 조사한 카드가 수십 장
> 쌓였는데, **어디를 치면 조명 행이 열리는지**를 한 장으로 답한 것은 t251 하나뿐이다.
> 다음 사람이 그 지도를 못 찾으면 다시 조사부터 시작한다.
>
> 기준: `origin/main 96828ed` · 2026-09-02 · 갱신 회차 `t262`
> 근거 판정서: `.moai/reports/t251` (지도) · `t255`·`t256` (4단계 판정) · `t261` (COL 실기)
>
> 🔴 **콘솔 수치는 2026-09-07 실측이 정본이다 — §0 을 먼저 읽어라.** 본문의 나머지
> (지도·기여도 표·우선순위)는 여전히 2026-09-02 기준이며 갱신되지 않았다. 두 날짜가
> 한 문서에 있으므로, 어떤 수치를 인용하든 **어느 절에서 가져왔는지**를 같이 적어라.

---

## 0. 콘솔 실측 — 2026-09-07 (이 문서에서 가장 새로운 수치)

읽기 전용 회차다. **콘솔 쓰기 0건.** 채널: `osc.udp://127.0.0.1:8000` → 수신 `9005`,
응답기 `CopilotResponder 1.6.5`(기대값과 일치, 재임포트 불필요).

| 항목 | 경로 | 2026-09-02 기록 | **2026-09-07 실측** |
|---|---|---|---|
| 픽스처 | `Patch/Stages/1/Fixtures` | 86 | **86** — 같음 |
| 그룹 | `DataPool/Groups` | 18 | **18** — 같음(`ALL`~`EVEN`) |
| Dimmer 프리셋 | `DataPool/PresetPools/1` | 6 | **7** — 🔴 중복 한 쌍 |
| Position 프리셋 | `DataPool/PresetPools/2` | 0 | **6** — `POS01`~`POS06` |
| Gobo 프리셋 | `DataPool/PresetPools/3` | — | 0 (⚠️ 아래 한계) |
| Color 프리셋 | `DataPool/PresetPools/4` | 8 | **9** — 🔴 이름 없는 것 하나 |
| Beam 프리셋 | `DataPool/PresetPools/5` | 0 | 0 (⚠️ 아래 한계) |
| Focus 프리셋 | `DataPool/PresetPools/6` | — | 0 (⚠️ 아래 한계) |
| 시퀀스 | `DataPool/Sequences` | (`Sugar r3` 큐 18) | **8개 풀** — 🔴 정체불명 하나 |

### 0.1 🔴 이 회차가 새로 찾은 것 넷

**① Position 6 은 우리 도구가 넣었다 — 미스터리가 아니다.**
이름이 `POS01 보컬 센터 페이스 · 합성좌표` 꼴이고, 그 형식은
`server/lxseq/position_derive.py:135` 가 문서화한 바로 그것이다. 9/2 이후 어느 세션이
넣었고 이 문서만 갱신되지 않았다. 🔴 **`t18`(LXSEQ-002 M4, 「재료 대기」)의 전제가
바뀌었을 수 있다 — 착수 전에 그 카드를 다시 읽어라.**

**② Dimmer 에 `쇼 하이` 와 `쇼 하이 OLD` 가 함께 있다** (카드 `t329`)
6 → 7 의 정체다. 어느 쪽이 사는 값인지는 **이 채널로 원리적으로 못 읽는다**(§4-1).

**③ Color 의 `Preset 32`, 시퀀스의 `Sequence 2000`** (카드 `t330`)
둘 다 grandMA3 가 이름 없이 저장될 때 붙이는 기본 이름이다. COL 축은 파이프라인이
이름으로 대조하는 자리라 그냥 지저분함이 아니다.

**④ 시퀀스 `eset1Fade`** (카드 `t328`)
`Preset1Fade` 에서 앞 두 글자가 사라진 모양이다. 그 문자열은 시퀀스 이름이 아니라
**큐 페이드 프로퍼티 이름**이며(`make_ma3.py:145`), 프로퍼티 세팅 명령이 뭉개져
시퀀스가 만들어진 것으로 읽힌다 — **의도하지 않은 콘솔 쓰기**다.
2026-09-06 회차도 이것을 되읽기 목록에 실었으나 짚지 않았다
(`reports/musicsync-live-write-20260906.md:32`). 원인 미확정. 🔴 처분 금지.

### 0.2 ⚠️ 이 표를 읽을 때의 한계 셋

1. **`0` 을 부재의 증거로 쓰지 마라.** 내용이 있는 Group 도 이 채널이 `COUNT 0` 을
   답한 사례가 있다(`t95`, 111건 실측). Gobo·Beam·Focus 의 `0` 은 「비었다」가 아니라
   **「이 채널로는 못 봤다」**이다.
2. **값은 어느 항목에서도 안 읽었다.** 이 채널은 슬롯 점유와 이름까지만 답한다
   (`t105`, 138건 전수). 이름이 맞다고 값이 맞는 것이 아니다.
3. **쇼는 자주 바뀐다.** 과거에 하루 안에 6 → 20 → 7 로 움직인 기록이 있다.
   이 표를 전제로 삼는 배차서는 **착수 시점에 다시 재라.**

### 0.3 재현 방법

```bash
uv run python -m server.tools.responder_roundtrip \
  --host 127.0.0.1 --port 8000 --listen-port 9005 --skip-exec --path "DataPool/Groups"
```

🔴 **`--listen-port` 는 `9005` 다.** `README.md:40` 과 `console/lua/README.md` 는
`9000` 을 안내하는데 그 값으로는 **살아 있는 콘솔이 무응답으로 보인다**(카드 `t327`).
정본은 `~/MALightingTechnology/gma3_library/inout/osc/copilot_osc_row2_send.xml` 의
`Port="9005"` — `server/deploy/provisioning.py` 가 스테이징하는 임포트 파일이다.

무응답을 만났을 때 **포트를 갈아보기 전에** 계기부터 확인하라: 수신 포트로 자기 자신에게
UDP 를 한 발 쏴서 청취기가 그것을 받는지 본다. 대조군 없는 `0 패킷` 은 부재의 증거가
아니다.

---

## 1. 단계별 현재 상태 (2026-09-02 기준 — 콘솔 수치는 §0 이 정본)

| 단계 | SPEC | 콘솔에 올라간 것 | status |
|---|---|---|---|
| 1 패치 | LXSEQ-001 | 픽스처 **86대** | `implemented` |
| 2 그룹 | LXSEQ-002 | 그룹 **18개** | `in-progress` — M4 가 재료(t22) 대기 |
| 3 프리셋 | LXSEQ-003 | **COL 8/8** (슬롯 1~8) · dim 6 · bm 0 · pos 0 | `in-progress` — 🔴 **AC 전수 판정이 없다** |
| 4 곡 큐 | LXSEQ-004 | 시퀀스 `Sugar r3` 아래 **큐 18개** (Q010~Q180) | `in-progress` — **살아 있는 AC 13개 전부 PASS** |

## 2. 🔴 다음 사람이 먼저 알아야 할 셋

### ① COL 8/8 은 이미 콘솔에 있다 — 남은 것은 저장이 아니라 **검산**

`t261` 이 켈빈 2행을 넣어 8칸이 다 찼다. 그런데 **저장된 색이 시트 값과 같은지는
이 채널로 못 읽는다** — 슬롯 점유와 이름까지만 답한다.

🔴 **그리고 급한 것은 새로 넣은 2개가 아니라 기존 6개다.** COL.01·04·05·06·07·08 은
t261 착수 전에 이미 있었고, **언제 어느 버전으로 저장됐는지 아무도 모른다.** 그 사이
컬러 스케일 변환이 고쳐졌으므로(시트 0–255 → 콘솔 0–100) 수정 전 저장분이면 색이
어긋나 있다. **이름이 같아서 파이프라인이 영원히 건너뛴다.**

→ 확인 요청 문안: `.moai/reports/t261/verdict.md` §3

### ② `BM`·`FX` 열을 비우면 `COL`+`POS` 만으로 큐 **83행 전부**가 열린다

`t251` 이 계열별 기여를 순서 artifact 없이 두 방향으로 쟀다(단독 추가 / 하나만 빼기):

| 계열 | 단독으로 있을 때 | **이것만 없을 때** | 시트가 쓰는 행 |
|---|---|---|---|
| COL | 12 (+11) | **5 (−78)** | 48 |
| FX | 5 (+4) | **25 (−58)** | 30 |
| BM | 1 (+0) | **40 (−43)** | 10 |
| POS | 1 (+0) | **43 (−40)** | 12 |
| DIM | 1 (**+0**) | 83 (**+0**) | **0** |

| 시나리오 | COL 만 | COL+POS |
|---|---|---|
| 시트 원본 | 12행 / 3큐 | 18행 / 5큐 |
| BM 열 비움 | 12행 / 3큐 | 25행 / 6큐 |
| **BM·FX 열 비움** | **43행 / 10큐** | 🟢 **83행 / 18큐 (전부)** |

큐는 **행이 아니라 큐 단위로 통째로** 빠지므로 한 계열만 없어도 크게 무너진다.
cue-ex 89행 중 6행은 영상팀 호출(LED-W)이라 조명 행은 **83**이다.

### ③ 🔴 `DIM` 6행은 큐를 **0행** 연다

cue-ex 의 `Dim` 열은 프리셋 참조가 아니라 **숫자**다(`dim='55'`). 저장은 되지만
큐가 안 쓴다. **「안전한 첫걸음」으로 보이는 자리가 실제로는 0행이다.**

## 3. 가장 적은 일로 가장 많은 행 — 우선순위

| 순위 | 할 일 | 열리는 행 | 성격 |
|---|---|---|---|
| **1** | **COL 8개 색을 감독이 육안 확인** (특히 기존 6개) | 이미 열린 8행의 **신뢰** | 감독 눈 — 유일한 수단 |
| **2** | **BM·FX 열을 이 곡 큐에서 비운다** | COL+POS 와 합쳐 큐 **83행 전부** | 감독 시트 결정, 코드 0줄 |
| **3** | **POS 6을 감독이 콘솔에서 기록** | 큐 **+40행** | 감독 무대 작업 — 정본 §10 이 `POS` 를 「현장 레코드」로 규정 |
| 4 | FX importer 를 만든다 | 큐 **+58행** (열을 살릴 때) | 코드 — 레지스트리 행부터 |
| 5 | LXSEQ-003 AC 전수 판정 | 0행 (장부) | 문서 — `implemented` 의 선행 |
| 6 | BM 범위 결정 | +43 중 일부만 | 🔴 BM.03·04 는 **기종 능력 부재 확정**(t238) — 열어도 안 열린다 |
| **7** | DIM 6행 저장 | 큐 **0행** | 프리셋 6행뿐 |

## 4. 넘기지 말아야 할 한계 넷

1. **값 일치는 어느 축에서도 못 읽는다** — 프리셋 색도, 큐 내용도. 산출물이 스스로
   `unverified: ["value_match"]` 를 싣는다. 「검증된 N건」으로 보고하면 틀린 값이 조용히 영속한다
2. **큐 내용은 클래스에 필드가 없다** — 「아직 못 찾았다」가 아니다.
   `docs/runbooks/console-channel-facts.md` §4
3. **`Clear` 1회 뒤 계기가 0 을 답해도 값은 남을 수 있다** — 같은 런북 §3
4. **자식 목록은 잘린다** — `childCount` 대조 + 페이징 없이 세면 18을 13으로 센다

## 5. 이 문서가 답하지 않는 것

- **2단계 M4** 가 무엇을 기다리는지 (`t22` — Part 와 사용자 큐가 있는 쇼파일)
- **003 의 AC 판정** — 아무도 안 했다(§3 순위 5)
- **큐 12행이 실제로 콘솔에서 열렸는지** — t251 의 숫자는 매퍼 시뮬레이션이고
  실제 큐 재투입은 아직 안 했다
