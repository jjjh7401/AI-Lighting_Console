# 연구 3건 → 코파일럿 → MA onPC: 무엇이 바뀌었고 음악은 어떻게 조명이 되는가

기준: `main@afa7cb1` · 실기 콘솔 grandMA3 onPC, 응답기 1.6.5, 회신 9005 · 2026-09-12~13

## 1. 연구 3건

| 출처 | 문서 | 성격 | 명시된 한계 |
|---|---|---|---|
| Fei Hu (Facebook) | 17 | MA2 효과 용어 → MA3 Phaser/Cue 번역 사전 | "영상 내부 구현을 실측했다가 아니라 게시자가 명명한 사례를 분류한 것" |
| AddOnDesk Positions | 18 | 포지션 프리셋 대량 생성 워크플로우 모델 | 유료 플러그인 내부 계산식 미확인. 공개 매뉴얼 기반 |
| MBLightArts (YouTube) | 19 (6편) · 20 (심층 1편) | 템플릿 showfile 적응 프로세스와 역할 분담 | 프레임 단위 실측 아님. 적용 전 콘솔 조회와 운영자 확인 필요 |

## 2. 세 문서의 공통 결론

세 문서가 서로 다른 출처인데 같은 선을 긋는다: **판단은 감독, 연결과 가드는 코파일럿.**

- 19 §4: "조명감독의 감각적 판단이 필요한 부분은 Copilot이 대신하지 않고, console object와 검증 항목으로 명확히 안내한다"
- 18 §9: "사용자에게는 '몇 개 Pan/Tilt를 만들까요?'보다 먼저 '같은 방향으로 설치된 장비끼리 묶었나요?'를 물어야 한다"
- 17 §7: "빔 속성은 픽스처 프로파일 확인 전 자동 확정하지 않는다"

## 3. 실제로 코드가 된 것 (측정)

| 문서 근거 | 구현 | 위치 |
|---|---|---|
| 18 §9-2, 20 §11 "포지션 자동으로 만들어줘" / "3팬이 이상해" | 체인 순서 대조 + 나눗셈 검사 | `server/spatial/preflight.py` `check_chain_order` · `check_split` (카드 t365, `13595be`) |
| 18 §4 Overwrite/Merge 가드 | Layout 좌표 쓰기를 폐집합에 추가 → 승인 필요 | `server/safety/blacklist.yaml` v9 (카드 t364, `9f54763`) |

측정 방법 주의: 문서 §11 의 intent 이름 11개를 코드에서 grep 하면 0건이다. 이름이 아니라 **능력**으로 봐야 한다 — t365 가 두 행을 다른 이름으로 구현했다. 나머지 9행은 이번에 항목별로 재지 않았다(미측정, 부재 아님).

## 4. 음악 → 콘솔 경로 (실측)

```
오디오 파일
  → analyze()            librosa. BPM · 구간 경계 · onset · RMS. 콘솔 접촉 0
  → D등급 1~5            구간 RMS ÷ 최대 구간 RMS 의 상대 띠
  → 확인 카드            사람이 끄거나 고친다. 건너뛰는 분기 없음
  → prepare_songcue      구간 라벨 + D등급
      → section_vocab    팝 축 / EDM 축을 갈라 라벨 판정 (최장 일치)
      → 룩 배정          장르별 · 회차 기억 · 곡 사이 재사용 회피
      → 번들 조립        사다리 · 드롭 앞 감광 · 무빙 1큐 · CueFade
  → run_commands 재진입  안전 게이트 screen()
  → 콘솔                 OSC 8000 송신 / 9005 회신
```

근거: `server/audio/analyze.py:121`, `server/web/session.py:11518`,
`server/orchestrator/tools.py:2946`, `server/looks/songcue.py:544`

## 4b. 무대 전체를 세우는 12단계 (문서 20 §2)

자막 1,136 segment 기반. 각 단계에 감독 판단과 코파일럿 수행이 갈려 있다.

1 무대 분석 · 2 Patch · 3 하이브리드 그룹 · 4 픽셀 튜브 그룹 · 5 틸트 픽셀 바 ·
**6 포지션 프리셋** · 7 고보/포커스 · 8 빔 프리셋 · 9 컬러 프리셋 · 10 regen all ·
11 레이아웃 정리 · 12 오버라이드

구현 확인: 6번의 전제 검사만(`server/spatial/preflight.py`, t365). 나머지 11단계는 미측정.
기억에 있던 "8/12 구현됨"은 확인하지 않은 값이라 채택하지 않는다.

## 4c. 문서 19 §5.1 안전 게이트 7개

| 게이트 | 조건 | 상태 |
|---|---|---|
| `patch_not_verified` | mode/address/orientation 미확인 | 구현(t365 체인 순서) |
| `profile_dependent_attribute` | 빔 속성 이름 불명 | 능력 판독 경로 존재 |
| `destructive_store` | 프리셋/시퀀스 덮어쓰기 | 구현(폐집합 + 승인) |
| `audience_blind_or_strobe` | 관객 방향/풀 플래시 | 미측정 |
| `grid_not_verified` | 그리드 미확인 | 미측정 |
| `recipe_needs_cook` | 변경 후 레시피 사용 | 미측정 |
| `visual_only_success` | 페이저 되읽기 불가 | 이 회차가 그대로 따름 |

문서 20 §10: "관객 안전을 위해 Copilot은 full-speed strobe를 자동 저장하지 않는다."

## 5. 오늘 실기에서 확인한 것

콘솔에 실제로 쓰고 되읽었다. 종료 코드가 아니라 되읽기가 증거다.

| 시퀀스 | 내용 | 결과 |
|---|---|---|
| 7 `VerifyA` | 8구간 EDM, 후렴 2회 | 큐 8개 저장 |
| 10 `VerifyC4x` | 9구간, 후렴 4회 | 큐 9개 저장 |

| 확인 대상 | 결과 | 증거 |
|---|---|---|
| 반복 후렴이 안 사라지는가 | 통과 | 후렴 1~4회차 모두 같은 룩, 콘솔에 4개 다 존재 |
| 곡이 바뀌면 룩이 바뀌는가 | 통과 | 곡 A `{ambient-hold, drop-crimson, intro-bed}` → 곡 B `{haze-shafts, breakdown-deep, drop-acid}`, 겹침 0 |
| 드롭 앞이 어두워지는가 | 통과 | 드롭 앞 큐 Dimmer 45 → 25, 못 뺀 경우는 사유 7종으로 보고 |
| 무빙이 도는가 | 통과 | 드롭 큐 하나에 Pan/Tilt 대각 페이저. 곡당 1큐는 설계 |

곡 구성 20조합(4장르 × 5구성) 전수: 큐 소실 0건, 후렴 밝기 순서 전부 단조증가.

## 6. 이번 회차에서 고친 결함 4건

| 카드 | 결함 | 머지 |
|---|---|---|
| t366 | 드롭 큐가 값 충돌로 통째 사라짐 | `7197b2e` |
| t367 | 무빙이 드롭이 아니라 첫 코러스에서 돎 | `befac6b` |
| t368 | 후렴 3회차부터 큐가 사라짐 | `c9cba52` |
| t369 | 후렴 3회차가 2회차보다 어두워짐 | `afa7cb1` |

넷 다 뿌리가 하나다: 천장(Dimmer 100)에 닿은 룩은 사다리로 못 올린다.

## 7. 아직 못 하는 것

| 항목 | 상태 | 근거 |
|---|---|---|
| 음악에 맞춘 자동 재생 무장 | 앱이 발화하지 않는다. 운영자가 손으로 실행 | `server/orchestrator/songcue_timecode.py` — 판정 어휘에 `verified` 없음 |
| 저장된 페이저 되읽기 | 콘솔 제약으로 불가. 성공 판정은 명령 verdict + 사람 관측 | 문서 00 §1 함정 ③ |
| 큐 값 되읽기 | 존재·구조는 읽히나 Dimmer 값은 안 나옴 | 실측 2026-09-13 |
| 빔 축 확장 | 이 리그는 Iris 3/15종, Zoom 9/15종. 넓게 쓸 축은 Zoom 하나 | `reports/onsite-round-20260912/rig-beam-axes.txt` |
| CI | 과금 차단. 3~10초 만에 steps 빈 배열로 실패 | runs 34699158477 · 34700114052 · 34701896387 · 34703644076 |

## 8. 다음

- 문서 20 §11 나머지 9행을 항목별로 재서 구현/미구현을 확정
- 17 §6 의 FX 후보 12종 중 안전한 것부터 (`horizontal-chase`, `bounce-run-x`, `vshape-pan-swing`)
- 실기 곡으로 A~D 재확인 (지금은 합성 픽스처)
