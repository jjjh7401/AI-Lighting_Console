# grandMA3 AI 코파일럿 — 설계 계획서

작성일: 2026-07-15 · 상태: 초안 v1

## 1. 제품 정의

**"grandMA3 onPC 위에 얹는 Claude 기반 AI 코파일럿."**
콘솔을 대체하지 않는다. 사용자의 자연어(한국어) 의도를 MA3 커맨드/매크로/Lua 플러그인으로 변환해 실행하고, 콘솔 상태를 읽어 피드백 루프를 도는 **프로그래밍 보조 + 연출 초안 생성 도구**다.

- 실시간 재생(페이드, 이펙트, 타임코드 추종)은 MA3 엔진이 담당
- AI는 "프로그래머" 역할: 큐·프리셋·그룹·매크로·이펙트를 만들고 수정
- 라이브 쇼 중에는 제안 전용(read-only) 모드

## 2. 벤치마크: boardop 분석

가장 가까운 선행 사례. 우리 설계의 출발점이자 검증된 패턴의 원천.

| 요소 | boardop 구현 | 우리 설계에의 시사점 |
|---|---|---|
| 통신 | OSC — 명령 `/gma3/cmd`, 피드백 `/python/feedback` | 그대로 채택 (검증된 경로) |
| 콘솔측 상주물 | Lua responder (상태 조회·검증) + 플러그인 풀 | 자동 배포 방식 채택 |
| AI 도구 | `run_commands`, `deploy_plugin`, `query_state` | 도구 세트 확장 (리그 컨텍스트, 계획 제안 추가) |
| 지식 레이어 | 3계층: ①검증된 MA3 문법 룰 ②콘솔에서 수확한 오브젝트 모델(클래스 565·enum 617·Lua 함수 156) ③사용자 커스텀 룰(@update) | 핵심 아이디어. 문법 환각 억제의 실전 해법 |
| 쇼 컨텍스트 | 매 턴 라이브 스냅샷 주입 (그룹/프리셋/시퀀스/실행기) | 채택 + 프롬프트 캐싱으로 비용 최적화 |
| 안전장치 | blast-radius 필터(파괴적 키워드 → 수동 확인), 플러그인 리뷰 게이트, all-or-nothing, pcall 컴파일 하네스 | 채택 + 라이브 모드 잠금 추가 |
| 라이선스 | FSL-1.1-Apache-2.0 | ⚠️ 내부 사용·평가는 자유. 경쟁 상용 제품에 코드 재사용은 각 릴리스가 Apache로 전환되는 2년 전까지 제약 → **아키텍처는 벤치마킹, 코드는 자체 구현** |
| 명시적 한계 | "라이브 쇼 운영용 아님" | 우리도 동일 원칙 유지 |

## 3. 시스템 아키텍처

```
┌────────────────────────────────────────────────────────┐
│  UI (한국어 채팅 · 웹/데스크톱 · 음성 입력 옵션)          │
└──────────────┬─────────────────────────────────────────┘
               │ WebSocket
┌──────────────▼─────────────────────────────────────────┐
│  AI 오케스트레이터 (Python + Anthropic SDK tool runner) │
│  · 시스템 프롬프트 = 문법 룰북 + 오브젝트 모델 (캐시)     │
│  · 도구: run_commands / query_state / deploy_plugin /   │
│          get_rig_context / propose_plan                 │
│  · 컨텍스트 파이프라인: showfile XML·MVR·GDTF 파서       │
└────┬──────────────────────────────┬────────────────────┘
     │                              │
┌────▼─────────────┐   ┌────────────▼────────────────────┐
│  안전 게이트       │   │  MA3 브리지                     │
│  ① 문법 밸리데이터 │   │  · OSC out: /cmd (UDP)          │
│  ② 위험 명령 분류  │   │  · OSC in: 피드백 수신           │
│  ③ 인간 승인 UI    │   │  · Lua responder: 상태 스냅샷,   │
│  ④ 라이브 잠금     │   │    실행 결과 회수, 플러그인 풀    │
└──────────────────┘   └────────────┬────────────────────┘
                                    │
                       ┌────────────▼────────────────────┐
                       │  grandMA3 onPC (+ MA 하드웨어)   │
                       └─────────────────────────────────┘
```

**데이터 흐름 (1턴):**
사용자 지시 → Claude가 계획 수립(`propose_plan`) → 필요 시 콘솔 상태 조회(`query_state`) → 명령 시퀀스 생성 → 문법 검증 → 위험도 분류(위험 시 승인 대기) → OSC 실행 → Lua responder가 결과 확인 → 실패 시 Claude가 자가 수정 재시도(최대 N회) → 사용자에게 결과 보고.

## 4. Claude 연계 상세 설계

### 4.1 모델 선택

| 역할 | 모델 | 근거 |
|---|---|---|
| **메인 (명령 생성·플러그인 작성·연출 기획)** | `claude-opus-4-8` | 기본값. 긴 자율 작업·코드(Lua) 생성·정확한 문법 준수에 최적. $5/$25 per MTok |
| 의도 라우팅·간단 분류 (선택적 최적화) | `claude-haiku-4-5` | "이건 단순 실행인가, 기획인가" 분류만. $1/$5 per MTok |
| 대규모 쇼 통짜 기획 (옵션) | `claude-fable-5` | 최고 성능이 필요할 때만. $10/$50 per MTok. 별도 refusal 처리 필요 |

프로덕션 단순화를 위해 **초기에는 opus-4-8 단일 모델**로 시작하고, 비용 데이터가 쌓이면 라우팅을 도입한다.

### 4.2 Tool Use 설계 (Anthropic SDK tool runner)

```python
@beta_tool
def run_commands(commands: list[str], risk_note: str = "") -> str:
    """MA3 커맨드라인 명령 시퀀스를 검증→승인→OSC 실행하고 결과를 반환.
    파괴적 명령(Delete, Off Everything 등)은 승인 게이트에서 자동 정지."""

@beta_tool
def query_state(path: str) -> str:
    """Lua responder를 통해 콘솔 오브젝트 트리 조회.
    예: 'DataPool/Sequences', 'ShowData/Patch'"""

@beta_tool
def deploy_plugin(name: str, lua_source: str) -> str:
    """Lua 플러그인을 pcall 하네스로 컴파일 검증 후 리뷰 게이트 대기 → 배포."""

@beta_tool
def get_rig_context(scope: str) -> str:
    """showfile XML / MVR에서 패치·그룹·프리셋 어휘·3D 배치 요약 반환."""
```

- 도구 실행 루프는 `client.beta.messages.tool_runner()` 사용 (수동 루프 불필요)
- **승인 게이트는 도구 함수 내부에서 구현** — 위험 명령이면 사용자 확인 전까지 실행하지 않고 "사용자 승인 대기/거부" 결과를 반환
- `thinking={"type": "adaptive"}` + `output_config={"effort": "high"}` 기본

### 4.3 프롬프트 캐싱 전략 (비용의 핵심)

시스템 프롬프트가 크다(문법 룰북 + 오브젝트 모델 요약 ≈ 1~2만 토큰). 프리픽스 캐싱 설계:

```
[tools 정의]  ─┐
[룰북+오브젝트 모델]  ─┴─ 고정 프리픽스 → cache_control (ttl: "1h")
[쇼 스냅샷·대화 이력]  ── 매 턴 변하는 부분 → 캐시 뒤에 배치
```

- 캐시 읽기 = 기본 입력가의 ~0.1배 → 룰북 15K 토큰이 턴당 $0.075 → **$0.0075**로 감소
- 주의: 룰북에 타임스탬프·세션 ID 등 가변 값 절대 삽입 금지 (프리픽스 1바이트만 바뀌어도 전체 무효)
- 쇼 스냅샷은 messages 마지막 user 턴에 주입 (캐시 프리픽스 보존)

### 4.4 예상 API 비용 (opus-4-8, 캐싱 적용 기준)

| 항목 | 추정치 |
|---|---|
| 1턴 (캐시 읽기 15K + 신규 입력 3K + 출력 1.5K) | ≈ $0.06 |
| 집중 프로그래밍 세션 (100턴) | ≈ $6 (약 8천 원) |
| 월간 헤비 유저 (세션 20회) | ≈ $120 |

인건비 대비 무시 가능한 수준. 단, 자가 수정 루프가 폭주하지 않도록 재시도 상한 필수.

### 4.5 환각(문법 오류) 억제 3중 방어

1. **지식 레이어**: 검증된 문법 룰북을 시스템 프롬프트에 상주 (boardop 방식)
2. **밸리데이터**: 생성된 명령을 MA3 문법 파서로 실행 전 검사 (키워드·오브젝트 참조 형식)
3. **실행 피드백 루프**: 콘솔 에러 응답을 Claude에게 되돌려 자가 수정 (재시도 ≤3회)

## 5. 안전 설계

| 계층 | 내용 |
|---|---|
| 문법 검증 | 파싱 불가 명령은 실행 자체를 차단 |
| 위험 분류 | 블랙리스트: `Delete`, `Remove`, `Off Everything`, `Store /overwrite`(라이브 시퀀스), `Shutdown`, `Format` 등 → 무조건 인간 승인. 번들 명령은 all-or-nothing |
| 파라미터 가드레일 | 스트로브 주파수·디머 급변 등 광과민성 발작 위험 패턴 경고 |
| 라이브 잠금 | "라이브 모드" 토글 시 AI는 read-only + 제안 카드만 생성, 실행 버튼은 사람 |
| 감사 로그 | 모든 실행 명령·승인·거부를 타임스탬프와 함께 기록 (쇼파일 백업 자동화 포함) |

## 6. 개발 로드맵

### Phase 0 — 검증 (1~2주)
- grandMA3 onPC 설치 + boardop 실사용 평가 (Claude API 키만 필요, 하드웨어 불필요)
- 산출물: 격차 목록(한국어 UX, 부족한 도구, 안전성), 자체 구현 범위 확정
- FSL 라이선스 검토 결론 문서화

### Phase 1 — MVP 코파일럿 (4~6주)
- OSC 브리지 + Lua responder 자체 구현 (Python, python-osc)
- Claude tool-use 서버 + 한국어 채팅 UI (FastAPI + WebSocket)
- 안전 게이트 v1 (문법 검증 + 블랙리스트 + 승인 UI)
- **성공 기준**: 대표 프로그래밍 작업 10종(그룹 생성, 프리셋 저장, 큐 스토어, 페이지 셋업, 매크로 작성, 이펙트 적용 등)을 자연어로 수행 · 문법 오류율 <5% · 지시→실행 왕복 <10초

### Phase 2 — 컨텍스트 인식 (6~8주)
- showfile XML / MVR / GDTF 파서 → 리그 이해 ("보컬 쪽 무빙" 해석 가능)
- 프리셋 어휘 온보딩 마법사 (색/포지션/이펙트에 자연어 이름 매핑)
- onPC 3D 스크린샷 → Claude 비전으로 결과 자가 검증 루프
- **성공 기준**: "코러스에서 금색 톤으로 웅장하게" 수준의 추상 지시를 리그에 맞게 실행

### Phase 3 — 연출 자동화 (8~12주)
- 음악 분석(구간/BPM/에너지, 로컬 라이브러리) → 타임코드 큐리스트 초안 생성
- 장르별 룩 템플릿 (발라드/EDM/록/예배 등)
- **성공 기준**: 3분 곡 1개 → 검토 가능한 큐리스트 초안을 10분 내 생성

### Phase 4 — 라이브 보조 (이후)
- 라이브 잠금 모드에서 다음 큐 제안, 버스킹 팔레트 추천, 이상 감지 알림

## 7. 기대 효과

**정량 (추정, Phase 1~2 기준):**

| 작업 | 기존 (숙련자 수동) | AI 코파일럿 | 효과 |
|---|---|---|---|
| 그룹/프리셋 대량 셋업 (신규 쇼) | 2~4시간 | 지시 몇 문장 + 검토 20~40분 | 셋업 시간 대폭 단축 |
| 큐 시퀀스 빌드 (곡 1개) | 30~60분 | 초안 수 분 + 다듬기 | 초안 생성 자동화 |
| 커스텀 Lua 플러그인 1개 | 개발자 없으면 불가능~수일 | 자연어 요청 → 수 분 | **비개발자에게 새 능력 부여** |
| MA3 신규 인력 온보딩 | 수개월 (문법 학습) | 문법 몰라도 즉시 작업 가능 | 러닝커브 완화 |

**정성:**
- 오퍼레이터가 타이핑이 아닌 **연출 판단**에 시간을 쓰게 됨
- 작업 로그 자동 문서화 → 쇼 인수인계·복구 용이
- 한국어 자연어 인터페이스 = 국내 시장 뚜렷한 차별화 (boardop은 영어 전용)
- 소규모 공연장·교회·학교 등 전문 오퍼레이터 없는 현장의 진입장벽 하락

**기대하지 말아야 할 것 (한계 재확인):**
- 라이브 중 실시간 자율 운영 — 안전상 설계에서 배제
- 미적 최종 판단 — AI는 초안, 확정은 사람
- MA 하드웨어 없는 실출력 — onPC 정책상 불가 (개발·프리비즈는 무료로 가능)

## 8. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| MA3 버전 업데이트로 Lua API/OSC 변경 | 지원 버전 명시(v2.x 고정), 버전별 룰북 분리, 업데이트 시 회귀 테스트 스위트 |
| 공연장 인터넷 불안정 (Claude API 접속 불가) | 사전 생성 매크로 캐시, 오프라인 시 실행 전용 모드로 강등 |
| MA EULA (재배포·번들 금지) | "grandMA3용 컴패니언 소프트웨어" 포지션 유지, MA 소프트웨어 미포함 배포. 상용화 전 법률 검토 |
| boardop FSL 라이선스 | 코드 미사용·아키텍처만 벤치마킹으로 리스크 제거 |
| API 비용 폭주 (자가 수정 루프) | 재시도 상한, 턴당 토큰 예산, 사용량 대시보드 |
| 잘못된 명령으로 쇼파일 손상 | 실행 전 자동 쇼파일 백업, 위험 명령 승인 게이트 |

## 9. 기술 스택 (제안)

- **서버**: Python 3.11+, `anthropic` SDK (tool runner), `python-osc`, FastAPI + WebSocket
- **콘솔측**: grandMA3 Lua 5.4 responder 플러그인 (socket 기반 상태 회수)
- **파서**: showfile XML(표준 라이브러리), MVR/GDTF(zip+XML)
- **UI**: 웹 (React or Svelte) — 콘솔 옆 태블릿/세컨드 모니터 상정
- **음악 분석** (Phase 3): librosa 또는 essentia (로컬 처리, API 불필요)
