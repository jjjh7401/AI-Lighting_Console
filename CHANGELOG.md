# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- **SPEC-COPILOT-SCENE-001** — 씬 컴파일러: 룩(정적 값) + 이펙트(스텝 열) + 타이밍을 **하나의 큐**로 합성 (`status: completed`, M0~M8 전 마일스톤 + **머지 전 독립 리뷰 4인 병렬**). LOOKLIB(정지 화면 어휘)·FXLIB(시간축 어휘)이 세운 "의도→메모리 파이프라인"의 **2단계**이며, FXLIB이 `spec.md:42`·`:70`·`:140` 세 곳에서 명시적으로 예약해 둔 좌석을 이행한다. 신규 패키지 `server/scene/`(schema · loader · matching · compile · report · library) + 툴 2종(`find_scene` · `compile_scene`)을 기존 `run_commands` → `gate.screen()` 단일 관문으로만 배선했다. AC-SCENE-001~024 **24건 전량 PASS** — 소프트웨어 22건은 기계 검증(pytest), LIVE AC 2건(**AC-SCENE-019** M0 프로브 정리 · **AC-SCENE-021** M8 종단)은 실제 grandMA3 onPC 2.4.2로 검증. **테스트 3432 → 3911 passed · 5 skipped**(정본 baseline `3c701b1` 기준 신규 **479** — run-phase 362 + 머지 전 리뷰의 테스트 강화 117, 신규 실패 0) + vitest **223 passed / 13 files**(기준선 동일 — sync에서 직접 재실측) · **뮤테이션 47/47 killed(survived 0)** · **라이브 세션 2/2** · PRESERVE diff 빈 출력 · 닫힌 툴 집합 11 → **13**.
  - **M0 — 라이브 프로브가 사용자 확정 정책을 무너뜨렸고, 폴백하지 않은 것이 정답이었다**: 트래킹 정책 D1(전 큐 `/CueOnly`)의 전제인 `ASSUMPTION-41`(플래그 접수)이 **기계 채널 소진**으로 `CONDITION_NOT_MET`이 됐다 — 날조 대조군 **`/CueOnlyy`(고의 오타)가 `ok`를 받고 저장까지 됐고**, 그 큐가 기대한 이름·`cueNo`를 그대로 가져 **재조회 채널마저 비변별**이었다. M0 플랜이 적어 둔 대비책(*"`ok`가 무력하면 재조회에 의존한다"*)이 **함께 무너진 것**이며 이는 플랜이 예상하지 못한 상태다. 에이전트가 무플래그로 조용히 진행했다면 사용자는 정책이 바뀐 사실조차 몰랐을 것이므로 **중단 + 블로커 보고**했고(`plan.md §A.3` 예외 — 사용자 확정 정책의 전제가 무너지면 축소가 아니라 중단이다), 이 규율은 v0.2.0에서 **`REQ-SCENE-021` (b)로 요구화**됐다. 판정 5건은 접두 행 6행으로 기계 판독 가능하게 기록했다(`^` 앵커 grep — `SKIP:` 1 · `DESCOPE:` 1 · `GO:` 3 · `REOPEN:` 1).
  - **M0의 부수 발견 3건 — 실패한 프로브가 더 중요한 사실을 냈다**: **(1)** 저장 순서를 역전시키려는 후속 프로브 D″가 실패하면서 **큐 생성이 실질 append-only**임이 드러났다 — 이미 존재하는 큐보다 낮은 번호는 **플래그 무관** 거부된다(`Not allowed`). 그러면 `/CueOnly`의 보정 대상("다음 큐")이 저장 시점에 **영원히 존재하지 않으므로** `ASSUMPTION-42`의 A=B(대조군이 갈리지 않음)가 설명된다. ⚠️ 룰북 `:56`의 **소수 번호 삽입** 서술과 표면상 충돌하며 소수는 **미측정**이다 — *"삽입 일반 불가"*로 확대 해석하지 않는다. **(2)** 거부 메시지가 원인마다 다르다(점유 큐 재저장 `User Canceled Command` · 역순 저장 `Not allowed`) — **메시지 리터럴을 단정 근거로 쓰면 안 된다.** **(3)** `DataPool/Sequences` 재조회가 childCount **24**에 반환 **18** + `truncated: True` — `REQ-SCENE-013` (d)의 자동배정 거부 가드가 **가상의 방어가 아니라 실재 조건에 대한 방어**임이 확인됐다.
  - **사용자 재결정 — 미발화 커맨드 대신 구조적 회피**: `/CueOnly`를 폐기하고 **속성 집합 균일화 + 미주장 속성 전수 열거**(옵션 D)를 채택했다. 신규 미발화 커맨드 **0개**(FXLIB이 *"신규 시퀀스 Cue 1만"*으로 택한 회피와 같은 계열)이고 *"모든 씬의 값 라인 속성 집합이 동일하다"*는 **정적으로 단정 가능**하다. 근거 조사(룩 32개 전수 파싱)가 코어 4 = **32/32** · 유니온 6 · Zoom 16/32 · Iris 8/32를 냈고 **자산 편집 0건 · PRESERVE 무교차**를 확정했다. `Zoom`/`Iris`는 방향 미측정이라 균일 보장 밖이며 **채움값을 발명하지 않는다.**
  - **개정이 닫지 못한 축을 §D에 명시했다 — Pan/Tilt**: 룩은 Pan/Tilt를 **구조적으로** 가질 수 없고(`server/looks/loader.py:105-110`) fx 12개 중 **8개가 Pan/Tilt 전용**이다. movement 씬 뒤에는 위치가 트래킹되며 룩 값 라인이 덮을 수단이 없다 — **적지 않으면 *"구조적 회피"* 라는 서술 자체가 과잉 주장**이 되므로 제외 항목으로 명시하고 미주장 열거(`REQ-SCENE-014` (d))가 **매 컴파일마다** 노출한다.
  - **관측 천장은 개정으로 바뀌지 않았다 — 이것이 이 SPEC의 중심 AC다**: 트래킹 전파는 **관측 주체가 없다**(`ui/src/components/ExecutionPreviewCard.tsx:61`). *"균일 집합을 발화했다"* 는 정적으로 확인되지만 *"그래서 트래킹이 무해해졌다"* 는 **관측 불가**다. `AC-SCENE-015`가 주장 5종을 각각 **자기 상수**로 분리 강제한다 — (a) 산출물 · (a′) 균일성 · (b) 효과 · (c) 트래킹 무해화 · (d) 미주장 열거. (b)는 **전 경로 무조건** 실리고 (c)는 침묵하지 않고 **추론을 명시적으로 거절**한다(*"위 균일성 확인은 무엇을 발화했는지에 대한 사실이며 트래킹 무해화의 증거가 아닙니다"*) — 독자가 기본값으로 그 추론을 하기 때문이다. 배치도 분리해 두 주장 사이에 **반드시 표제가 하나 들어간다.**
  - **M5 뮤테이션 ④가 부분 일치 검사의 공허함을 실증했다**: payload가 상수 대신 **"직접" 한 단어만 빠진 거의 같은 인라인 산문**을 싣게 하자 `"사람" in text` 부분 일치로는 **통과한다**(실제로 확인). 동일성 검사만 잡는다 — 그 간극을 고정한 테스트를 스위트에 남겼다(`test_a_substring_check_would_miss_a_truncated_notice`). ⑥에서는 **위양성 하나를 잡아 고쳤다**: `"이월됐" not in ...`은 문면 자신의 부정문(*"이월됐다는 뜻이 아닙니다"*)까지 금지하므로 단정형 금지 + 부정문 존재로 바꿨다.
  - **이음매에서만 보이는 결함이 두 번 나왔다 — 슬라이스 테스트로는 둘 다 잡히지 않는다**: **(1)** M2/M3/M4 병렬 웨이브에서 M3가 상류 공개 상수 `server.fx.matching.PATTERN_ALIASES`의 **오늘 27/27 완전히 동일한 사본**을 들고 있었다(대칭차 `[]`) — **같으니까 증상이 0이고** 상류가 별칭 하나만 더하면 씬 매칭만 조용히 몸란다. 사본을 지우고 읽기 import로 교체했다(정정도 **슬라이스 소유자에게 되돌려 디스패치** — 코디네이터가 남의 파일을 고치지 않는다). 반대로 `_PARTICLES`/`_ENDINGS` 중복은 **위반이 아니라고 판정**했다 — looks·fx가 각자 자기 본을 가진 확립된 선례이며 **사본이라고 다 같은 사본이 아니다**. **(2)** M6에서 `compile.py`가 부르는 fx `select_sequence_number`가 **`FxInstantiationError`를 던져** 툴이 구조화 에러 대신 예외로 죽었다. 고친 위치는 툴이 아니라 **컴파일 계층**이다 — 툴에 `except FxInstantiationError`를 더하면 씬을 쓰는 모든 호출자가 fx 예외를 알아야 정확해진다. **사유 코드는 그대로 전달하고 예외 타입만 씬의 것으로 바꾼다.**
  - **값 라인 판별식을 이음매 테스트가 정정했다**: *"`;` 체인이면 룩 값 라인"* 은 **틀렸다** — `_speed_line`도 `;` 체인이다(`Attribute 'Pan' At Speed 112 ; …`). 판별식은 **모든 세그먼트가 절대값(`At <수>`) 형태인 `;` 체인**이며, 이 오판은 실제로 발생했고 fx 단독 씬이 잡아냈다. 리포트의 균일성 주장도 같은 판별식을 쓴다 — **컴파일러를 믿지 않고 산출 문자열을 다시 읽는다**(주장의 내용이 *"빌더가 정렬을 의도했다"* 가 아니라 *"번들이 실제로 그 순서를 담았다"* 이기 때문).
  - **M7 — 커밋 전에는 보이지 않는 테스트가 있다**: `test_songcue_bundle.py`의 `_TOOLS_EXPECTED_HUNK_OLD_STARTS`는 `git diff <BASE>..HEAD`를 읽는 **의도적 트립와이어**다. M6 커밋 **직전** 전체 회귀는 3766 passed로 그린이었고 **직후** 실패했다 — 변경이 아직 `HEAD`에 없었기 때문이다. **git 상태에 의존하는 테스트는 커밋 후 한 번 더 돌린다.** 스냅샷에 old-start **15**를 의도적으로 추가하되 이 가드의 진짜 불변식인 **보호구역 무침범 단정**(`(234,238)` · `(524,569)`)은 손대지 않았다.
  - **강제 코드를 빼도 오늘은 아무 일도 일어나지 않는다 — 그래서 픽스처를 주입한다**: 룩 자산 **32/32가 이미 코어 4를 이 순서로** 담고 있어 균일 정렬 도입은 **바이트 무변화**다. M4 뮤테이션 ⑰이 이를 **실측으로 입증**했다 — 정렬 로직을 제거하자 32개 전수 스윕은 **전부 통과한 채** 반전 픽스처 테스트만 죽었다. 같은 이유로 상류 `_values_line`의 **인자 순서 보존**을 따로 고정했다(상류가 내부 정렬을 도입하면 씬의 균일 강제가 **무음 no-op**이 된다). 충돌 열거 비공허성도 재료 규율이 있다 — **movement fx로 세우면 정답도 ∅이라 통과해 버리므로** dimmer/color fx로만 세운다.
  - **M8 — 종단 라이브(게이트 경유) · 감사 채널의 변별력을 먼저 세웠다**: `ok`를 어느 축의 증거로 쓰기 전에 날조 대조군 **`ZzzNotACommand 999`** 가 `failed`·`Illegal object`·`ok=false`로 갈리는지 확인했다 — M0의 store-플래그 축(`ok`도 재조회도 비변별)과 정반대이며 그래서 이 세션의 기계 주장은 **등급이 높다**. `truncated` 가드가 실물 조건에서 **발화 0건으로 거부**(`reason "sequence_truncated"` · 감사 델타 `state_query` 2건, `command` **0건**)했고, 사용자 GUI 정리 후(191~197 삭제 · childCount 24→17 · `truncated` True→False) 종단 2회를 발화했다. **두 씬이 서로의 대조군**이다 — `SCN GOLD PULSE`(dimmer fx)는 충돌 열거 `[Dimmer]`·미주장 `[Iris, Pan, Tilt]`, `SCN MOON RISE`(movement fx)는 충돌 **`[]`**·미주장 `[Iris, Pan, Zoom]`. **두 열거가 씬마다 실제로 달라졌다.**
  - **효과 관측은 `ok`가 아니라 출력값의 시간 변화로 잰다**: 정지 화면 1장으로는 모션을 주장할 수 없으므로 **시계열 표본 8장 + 픽셀 차분**으로 쟀다. 1회차는 Dimmer가 프레임마다 변하고(4.10·3.67·4.42·…) RGB는 **0.00 ×7 고정**, 2회차는 PanTilt가 변하고(1.46·1.38·…) Dimmer·RGB가 **0.00 ×7 고정** — **fx가 구동하는 축만 움직이고 룩의 정지 값은 그대로다.** 한 순간에 픽스처마다 값이 다르므로(`3·14·26·37·48·59·70·81·92`) 균일 점멸이 아니라 **위상 분산(웨이브)** 이고, 룩이 낸 `Dimmer 88`이 6~92를 순회하므로 **충돌 축에서 이펙트가 이겼다**. **한계를 함께 적는다**: 관측 채널은 **GUI 시트 판독**이지 무대 실물이 아니고, 표본은 **이산**(~1.5s 간격)이라 파형의 정확한 주기·형태는 주장하지 않는다. **이것은 M0 판정의 재측정이 아니다** — 같은 형상이 다른 맥락에서 어긋나지 않았다는 기록이다.
  - **비차단 결함 1건을 고치지 않고 기록했다 — SPEC 자신의 표제 문장이 매칭되지 않는다**: `find_scene("파란 백라이트가 천천히 웨이브하는 씬 만들어줘")` — **spec.md §A·acceptance.md 시나리오 1·plan.md §B M8이 그대로 쓰는 문장** — 이 **양 축 `no_match`** 다. 귀속은 씬 계층이 아니라 **상류 어휘**다: `하는`(하다-용언 관형형)이 FXLIB 닫힌 어미 목록에 없고 `파란`이 LOOKLIB 별칭에 없다(라이브러리는 `푸른`). 어휘가 있는 문장(`달빛 웨이브`)에서는 두 축이 정확히 붙으므로 **체인 자체는 정상**이다. sync-phase에서 사용자가 **안 D(기록만 하고 넘긴다)** 를 확정했다 — 두 상류 모두 **PRESERVE**이고, 씬 권한 안에서 가능한 유일한 수정(씬 어미 목록 확장)은 `find_fx`와 갈리는 **행동 분기**를 만들며 M3 정정이 사본을 지운 방향과 정면으로 반대이기 때문이다. **문서가 자기 예시를 실행하지 못하는 상태를 알면서 출하한다.**
  - **sync-phase — 문서 스테일 소인 16건(정책 무변경 · 요구/인수/마일스톤 신설 0)**: run-phase가 M8에서 닫은 사실이 **선행 문서 5곳에 전파되지 않아** 아티팩트가 자기 자신과 모순이었다(M0 정리 의무 "미이행" 5곳 · 라이브 세션 "1회 소진" 2곳 · SHA `pending-backfill` 2건 · frontmatter `status: draft`). **append-only는 지켰다** — §E.2의 마일스톤 기록과 HISTORY의 v0.2.1/v0.2.2 측정치(`접두 행 6`)는 **한 글자도 고치지 않았고**, 고친 것은 전부 *현재 상태를 서술하는 표면*이다. ⚠️ **`draft → in-progress` 전이 누락**을 발견했으나 **소급 위조하지 않고 기록만 한다**(커밋이 이미 랜딩했고 이력을 고쳐 쓰는 것이 더 나쁘다).
  - **독립 sync-audit이 FAIL 0.80을 냈고 그 지적이 sync 자신의 감사 기록을 반증했다**: 문턱 0.85(Tier L). 역추적 폐쇄성(REQ 21/21 · AC 24/24)·append-only·PRESERVE·상류 상수 결합·M8 산출 수치는 **전부 반증에 실패**했으나, **sync가 쓴 감사 4축 중 3축이 거짓/미검증**이었다. **(P1)** *"현재형 주장 불일치 0건"* 은 `§E.2` 접두 행 블록 헤더가 **"이 6행"이라 적는데 블록은 8행**이라는 사실로 **grep 한 번에 반증**된다 — v0.2.3이 두 행을 주석 없이 삽입했고 sync가 놓쳤다(소인했다고 선언한 뒤 남았다는 점에서 더 나쁘다). **(P2)** *"`§C.0a`와 1:1 일치"* 는 `AC-SCENE-019` 행에서 거짓 — 그 AC는 §C.0a에서 M0 배정인데 근거 절은 M8 ③이다(두 표는 다른 질문에 답하며, "1:1"이라 단정하면 *M0 배정 AC가 M8에서야 닫혔다*는 기록할 가치가 있는 사실을 가린다). **(P2)** 근거로 댄 `plan.md §0`은 **존재하지 않는 절**이고, sync 자신이 세운 gitignore 표기 규율은 **9곳에서 미준수**다. **(P2)** 이 SPEC이 유일하게 고친 파일(`tools.py`)의 **줄 앵커 7계열이 전부 썩었고** 그중 하나가 `plan.md §A.5`의 **PRESERVE 좌표**라 후속 소유자가 엉뚱한 곳을 지키며 통과한다 — **심볼 앵커로 교체**했다. **(P2)** M8이 추가한 AC 판정 2건에 **폐쇄 어휘 판정 절이 없었다**(접두 행만 있어 `^` grep은 통과) — 절을 신설했다. **(P2)** 무범위 *"ruff 클린"* 이 실측(`check server/` 3 errors · `format --check` 20 files)과 어긋나 **범위를 명기**했다. 전건 정정하고 각 정정을 §E.4에 남겼다.
  - **머지 전 독립 코드 리뷰 — P1 3건이 3794 전건 통과 상태에서 살아 있었다**: 이 저장소는 `gh pr checks`가 **0건**이라 유일한 관문이 사람/독립 리뷰이고, PRECHK PR #7(P1 4건)·OVERLAP run-audit(P1 6건)이 같은 경고를 이미 두 번 했다. 읽기 전용 리뷰어 3인을 코드 슬라이스별로 병렬 투입했고 **전부 `incorrect` 판정**을 냈다. **(P1-1, 두 리뷰어가 독립 검출)** `compile_scene`이 fx의 **거부 게이트 `_refuse_unemitted_axes`를 우회**한다 — 라인 빌더만 재사용하고 게이트를 건너뛰어, **fx 로더가 수락하는** 자산(`relative` 보유 / `circle`+`phase_to`)을 `instantiate_fx`는 거부하는데 `compile_scene`은 **선언된 축을 조용히 버린 채 수락**한다. 1스텝 `Fx`는 **`Step` 라인이 없는 번들** — M0가 세 번 발화해 모션 0을 본 그 형상이다. 콘솔은 전 라인 `ok:true`를 내고 큐 내용은 읽히지 않으므로 **런타임 신호가 0**이다. **(P1-2)** 매칭기의 **다섯 번째 상태가 성공 형상으로 나간다** — `SceneMatch.fallback`이 `kind == FALLBACK`이라 축은 잡혔는데 그 조합의 씬이 없으면 `selected=null · fallback=false · reason=null`. 출하 자산에서 axis id **8개 중 6개**가 이 막다른 길이고(`"웨이브"`·`"펄스"`·`"말씀 회전"`), 모델은 payload에 없는 `scene_id`를 넘기라는 지시를 받아 **`run_commands` 손작성**(이 SPEC이 막으려는 실패)으로 몰린다. **(P1-3)** 미확인 (a) 주장을 **`기계 확인됨:` 표제 아래** 렌더한다 — 표제가 "확인됨"이라 말하고 바로 아래 줄이 "재조회를 수행하지 않았습니다"라고 말하며, 툴이 `requery=`를 넘기지 않으므로 **모든 생산 리포트가 그 모양**이었다.
  - **세 P1을 고치고 그 수정이 스위트에 보이지 않는다는 것을 실증했다**: 오케스트레이터가 세 결함을 **직접 재현한 뒤** 원천에서 고쳤다 — 컴파일러가 거부 게이트를 호출하며 **예외 타입만 번역(사유 코드 보존)**, `SceneMatch.fallback`을 `AxisMatch`와 **같은 정의**(`fallback_reason is not None`)로 맞추고 신규 사유 `NO_SCENE_COMPOSES_AXES` 신설(`kind`는 여전히 어느 축이 잡혔는지를 보고 — **두 사실을 분리**), `to_korean`이 주장의 **사실에 따라** 표제를 고르도록. 그리고 **고친 직후 379 passed · 죽은 테스트 0건** — 관측 가능한 동작이 두 군데 바뀌었는데 스위트가 침묵했다. 그래서 같은 커밋에서 테스트를 강화했고 **모든 신규 테스트는 수정을 되돌리면 죽는 것을 뮤테이션으로 실증**했다(거부 게이트 회귀는 주입 시 **15건 사망**, sha256 원복 대조). 리뷰가 함께 잡은 **공허한 테스트**도 닫았다 — 균일 집합의 "먼저" 위치·면제 재사용·시퀀스 섹션 거부 3경로·`;` 체인을 룩 값 라인으로 오인하는 판별식(`_speed_line`도 체인이다)·`!= CONFIRMED`로만 검사하던 BROKEN 분기·교차 호출 접힘의 `is_error` 확대. **반증에 실패한 것**: 결합 순서·`Step 1` 미발화·금지 `At Step` 형태·Store 플래그 부재(대소문자 무관)·충돌/미주장 열거의 정확성·단일 실행 경로 AST 스캔·실물 게이트+실물 락의 LiveLock 강등 — 전부 표적 뮤테이션으로 죽었다.
- **SPEC-COPILOT-OVERLAP-001** — 보수적 점유폭 상계로 구간 겹침 판정 재개 (`status: completed`, M0~M8 전 마일스톤 + **독립 run-audit 1회차**): 픽스처를 자기 DMX 점유폭에 잇는 조인 키가 없어도 **열거 가능한 모드 전체의 폭 최대값**을 상계로 삼아 *"겹침 없음"*을 증명하고, 증명되지 않으면 **충돌이 아니라 미확정**으로 보고한다. 신규 `server/prechk/footprint.py` + 신규 게이트 `server/tests/test_overlap_preserve.py` · 어휘 축 `overlap_basis` 4값 · **테스트 2758 → 2946 passed · 5 skipped · 0 failed** · **라이브 세션 0회** · PRESERVE 위반 0건. **저장소에서 처음으로 제안서가 아닌 곳에서 나온 SPEC이다** — 출처는 PRECHK의 독립 run-audit 지적 I-15이며 제안서에 `구간 겹침`·`점유폭`·`overlap` 문자열은 0건이다.
  - **M0 — 전제 판정(`GO: ASSUMPTION-34` · 코드 변경 0건 · 프로토타입 비커밋)**: 3단 순회(`Patch/FixtureTypes` → `DMXModes` → `DMXChannels`)가 `query_state` **하나로만** 도달하는지를 인메모리 프로토타입 arm 5개로 갈랐다. **`query_property`를 정의하지 않은 포트**에서 조회 6회로 완주해 상계 31을 냈고 호출된 포트 메서드 집합이 정확히 `{query_state}`였다. **arm B(대조군)가 없으면 arm A의 통과는 공허하다** — 프로퍼티 요구가 `AttributeError`로 즉시 드러남을 확인했다. 귀결: **`server/safety/**` 무변경 확정 · 신규 예외 지점 0건**(PRECHK가 조건부 예외를 받은 사유는 `prop`이 프로덕션 경로로 도달 불가였다는 것이며 `state`는 초크포인트 3층에 이미 있다). `ASSUMPTION-31`·`32`·`33`·`35`는 관측 없이 닫히지 않으므로 **`SKIP:`**으로 남겼다(접두 행 합 정확히 5행). **부수 확립**: 순회가 스냅샷이 스스로 보고하는 **풀 슬롯 `i`**만 쓰므로 식별자 해석 단계가 0단계이며 `FID`를 배제해야 했던 종류의 미결이 이 축에는 발생하지 않는다.
  - **M1 — 어휘 확장(13편집점 + 배선 3) · 무증상 단계를 구조로 없앴다**: `server/prechk/report.py`의 import 시점 가드가 **하드코딩 5-튜플**이라 신규 축을 빠뜨려도 **어떤 테스트도 실패하지 않았다** — 스위트가 못 잡는 유일한 단계였다. 그 루프를 `CLOSED_VOCABULARIES` 순회로 바꿔 *"빠뜨린다"*를 표현 불가능하게 만들고(양방향: 어휘에 표 없음 · 표에 어휘 없음) **구조 변경을 신규 축 도입 *이전에*** 해서 3단과 4단 사이에 무증상 창이 열리지 않게 했다. 레지스트리 **맨 끝 append**로 기존 5줄을 바이트 동일하게 남기고 재타이핑 정본 3단정(집합·키·**순서**)을 전부 유지했다. 뮤테이션 4건 전건 사망 — 그중 **append 위치를 맨 앞으로 옮기면 순서 단정만 실패한다**(집합 단정 단독이면 통과하므로 순서 단정을 유지한 값이 거기서 관측된다).
  - **M2 — 순회 모듈: 형상 3건이 거짓 안심을 형상으로 막는다**: **(1)** 순회는 `(완전성, 폭 집합)`을 돌려주고 `WalkOutcome`에 `bound` 필드가 **없다** — 부분 모드 집합의 `max`는 참 상계보다 **작아** 넓은 간격을 잘못 통과시키고, 나중에 붙는 플래그는 그것을 되돌리지 못한다(플래그는 *순회*에 붙고 판정은 *주소 쌍*에 붙어 따로 여행한다). 폴드는 완전성 참 분기 **안에서만** 일어나고 그것을 AST로 판정한다. **(2)** 1·2단은 목록 완전성, 3단은 **계수 존재성** — 실측 리그의 `DMXChannels` 열거는 항상 `truncated=true`이므로 술어를 통합하면 기능이 **통째로 죽는다**. **(3)** 예산 소진은 국소 표기가 아니라 **전역 무효**다. 사유 코드는 신설하지 않고 `REASON_UNRESOLVED`/`REASON_UNREACHABLE`을 재타이핑해 테스트가 원본과 등호로 고정한다(`tools` import은 하드 순환).
  - **M2의 뮤테이션이 테스트의 구멍을 잡았다 — 코드가 옳고 테스트가 못 재고 있었다**: 6건 중 5건 즉사, **3단 예산 소진을 국소 표기로 강등하는 뮤테이션이 34건 전건 통과 상태에서 살아남았다.** 기존 예산 테스트 둘의 예산이 **폭을 하나도 수집하기 전에** 소진되어 *"관측된 모드가 0개다"* 가드가 대신 통과시키고 있었다. 예산 4(3모드 중 2개를 읽고 세 번째에서 죽는다)로 재면 강등된 코드가 `complete=True`·폭 `(5, 7)`을 내고 폴드가 **7**을 답한다 — 참 상계 11보다 작은 상계이며 정확히 거짓 안심이다. **뮤테이션을 돌리지 않았다면 발견되지 않았다**(규율 16의 이번 사례).
  - **M3 — 상계 판정: 술어는 `간격 < 상계`이며 `이하`가 아니다**: 구간이 닫힌 끝이므로 `간격 == 상계`는 **증명 가능하게 깨끗하다**(주소 `a`에서 상계 폭이면 끝이 `a + 상계 − 1`이고 다음 시작이 `a + 상계`라 맞닿기만 한다). 경계 테스트를 **순회 층과 판정 층 두 곳**에 두었고 `<=`로 바꾸면 6건이 죽는다. 간격은 `_address_duplicates`의 그룹핑을 **추출한 키 집합** 위에서 유니버스 내부로만 계산한다(중복 주소가 한 번만 들어가 간격 0이 생기지 않는다 — 그것은 주소 중복 축의 일이다). **`type_mode_ok`를 요구하지 않는 것이 설계의 핵심**이며 요구하면 그 자체가 논증의 오해다. 주소에 **하한**을 도입해 `0.0`·`1.0`·`0.1`을 판독 실패로 분류하고 **상한은 넣지 않았다** — `ASSUMPTION-33`이 용량을 미확정으로 두므로 `1.65535`가 **통과함**을 단정해 그 부재를 기계로 고정했다. 미확정은 `range_overlaps`에 넣지 않고 `skipped_checks` 한 행으로 고지한다(침묵이 결함이고, 충돌로 세면 *"충돌 N건"*이 거짓으로 발화한다).
  - **M3이 "착수 시점에 살아 있던 뮤테이션"을 실측으로 확인하고 자기 리그의 공허함을 잡았다**: 조사가 `[코드]` 추론으로 적은 *"유니버스 키잉을 지워도 스위트가 통과한다"*를 직접 재현했다 — 라벨을 유지한 채 **주소 공간만** 붕괴시키면 기존 74건이 **전건 통과**하고 신규 테스트만 잡는다. 그 과정에서 **초안 리그(`1.500`·`2.001`)가 붕괴해도 답이 바뀌지 않음**을 발견했다(폭 40에서 구간 `500..539`와 `1..40`이 한 공간에서도 만나지 않는다). 초안 주석이 그 사실을 스스로 적으면서 리그를 바꾸지 않았고 그것은 **공허한 뮤테이션 테스트**였다 — `1.100`·`2.110`으로 고치고 *"같은 두 주소를 한 유니버스에 놓으면 두 축이 실제로 발화한다"*를 별도 테스트로 옮겨 비공허성을 산문에서 코드로 이동시켰다.
  - **M4 — 정확폭 우선 · 근거를 페이로드에 도달시켰다**: 폭을 **출처 경로와 함께** 담고(`ModeFootprint(path, width)`) `bound_source`가 `"<경로> childCount"`를 낸다. **선례가 경고다** — `FootprintPolicy.source`는 축이 출하된 이래 필드로만 있었고 페이로드에 도달한 적이 없어 소비자가 0건이다. 신규 최상위 키 하나에 7키를 싣고 **그 자리에 정확 키집합 단정을 신설했다**(기존 최상위 단정은 부분집합이라 키를 얹거나 빼도 아무것도 깨지지 않는다 — *"문서 무변경 가능한 자리 = 아무도 안 지키는 자리"*). 리그 전역 등급은 **수행된 비교 전체의 최약 등급**이다(`not_performed ≺ bound_inconclusive ≺ bound_proves_clear ≺ exact_widths`) — 3슬롯이 비교되지 않은 리그에 `bound_proves_clear`를 찍으면 미비교 슬롯이 비교된 슬롯에 업혀 간다. 정확폭 우선은 **두 층**에서 집행한다(정확폭 슬롯은 상계 대상에서 빠지고, **양 끝이 모두 정확폭인 간격**도 빠진다).
  - **M5 — 요약에 판정 근거를 도달시키고 한정을 같은 문장에 붙였다**: 페이로드에만 있는 등급은 아무도 읽지 않는다. `겹침 판정 근거: <라벨>(<관측 범위 한정>)`을 요약에 넣고 **한정을 등급과 같은 문장에** 두었다 — 별 문장이면 한 문장만 읽는 독자가 무한정 절반만 가져간다. 뮤테이션 2건이 1회차에 살아남아 테스트를 고쳤다: **(1)** *"정확폭에는 한정이 붙지 않는다"* 대조가 **`walk` 없는 리그**를 써서 공허했다(상계가 없으면 한정의 부재는 우선순위 규칙을 증명하지 않는다). **(2)** 한정의 모드 수를 상수 `2`로 바꿔도 통과했다 — 리그의 모드 수가 항상 2였다. 그리고 **라벨 부분문자열 충돌**을 잡았다: `"구간 겹침 판정 미수행"`이 `"겹침 판정 미수행"`을 포함하므로 단순 포함 단정은 `not_performed`에 대해 **등급이 한 번도 인쇄되지 않아도** 통과한다 — 접두 포함형으로 채널까지 고정했다.
  - **M6 — 툴 배선 · 계획을 실측이 두 번 정정했다**: 경로는 `rig_paths["fixture_types"]` 경유이며 순회 모듈에 `"Patch/FixtureTypes"` 리터럴 0건이다(오버라이드가 순회를 **실제로 이동시킴**을 단정한다). 섹션 가드는 **별도 상수를 신설**해 `create_macro` 분기 **밖에서 항상** 검사한다 — 기존 상수에 추가하면 **같은 오버라이드 누락이 인자에 따라 다른 결과**를 낸다. **(정정 1)** 누락을 **호출 거부로 만들 수 없었다** — 매크로 가드 메시지를 고정한 두 기존 테스트가 그 오버라이드를 쓰고, 더 근본적으로 거부는 이 툴의 존재 이유인 픽스처 재고를 버린다(같은 파일의 zero-target 분기가 이미 그 계열을 고쳤다). 섹션 이름을 리포트에 담고 등급을 `not_performed`로 낸다. **(정정 2)** 순회에 `sibling_answered`를 추가했다 — 첫 조회가 실패할 때 *"형제가 답했다"*는 **호출자만 아는 사실**이고 프로덕션은 두 실패를 같은 예외로 던진다. 없으면 툴 경로의 분류가 항상 `console_unreachable`이 되어 **설정 결함을 운영 조건으로** 보고한다.
  - **M6 — 선례 트립와이어는 갱신이 필요하지 않았다**: `plan.md`가 *"tools.py를 고치므로 같은 커밋에서 갱신한다"*고 못박았으나 **실측하니 갱신할 것이 없었다** — M6의 편집 전량이 PRECHK가 이미 연 hunk 영역 안에 떨어져 **old-side 경계 9개가 바이트 동일**했다(`--unified=0`에서 인접 삽입은 기존 hunk에 병합되고 트립와이어는 old-side만 본다). 선례 파일 변경 **0행**이며 이는 `AC-OVERLAP-019` ⑥의 *"1행에 한정"*보다 강하다. **갱신하지 않은 것을 갱신했다고 적지 않는다.**
  - **M7 — 이월된 PRESERVE 게이트를 상시 테스트로 집행했다**: 선행 SPEC의 게이트는 **1회성 수동 절차**였고 이 저장소는 CI가 0건이므로 **아무도 다시 돌리지 않는 게이트는 아무것도 지키지 않는다**. 신규 파일이 PRECHK BASE를 **단독 소유**한다 — 선례 파일은 다른 BASE에 묶여 있고 두 BASE를 한 모듈에 섞으면 게이트가 **엉뚱한 곳을 지키면서 통과한다**(두 상수가 정확히 **13** 어긋남을 단정으로 고정했고, 선례의 SHA는 **그 파일 소스에서 읽어 온다** — 재타이핑하면 드리프트와 자기참조가 생기며 실제로 초안이 자기 자신에서 실패했다). 비공허성을 전건 동반한다: 10경로 실재 + **분류를 목록에서 기계 도출**(손으로 적은 계수는 plan-audit가 P1으로 잡은 오류다) · 교차 술어가 심어둔 hunk를 실제로 잡음 · safety 삭제 1행은 **원문 대조와 구조적 독스트링 판정 둘 다** · `ruff` 대상을 `git diff`에서 도출.
  - **M8 — 종단 통합 · 라이브 0회 · 감사 1:1을 초크포인트로 관통시켰다**: 대역을 손으로 만들지 않고 **실제 `SafetyGate`**를 세워 `gate.state_port`를 툴에 넘겨 **감사 `state_query` 행 수 = 리그 조회 수** 등호를 단정했다(실패 조회도 1행 — 타임아웃도 OSC 송신 1건이다). 조회 계수는 착수 시점에 저장소 전체에 고정 단정이 0건이었으므로 **등호와 부등호를 둘 다** 신설했다(부등호만 두면 조회 1건이 영구히 조용히 늘어난다). 역방향 검증을 3등급으로 분리 기록했다 — **A**(모듈 부재 수집 오류 4파일 · 약한 증거이며 그렇게 적는다) · **B**(신규 툴 테스트 **19건이 수정 전 코드에서 실패**) · **C**(수정 전에도 통과하는 5건 + PRESERVE 게이트 23건을 **회귀 테스트가 아니라고 코드에 라벨**).
  - **불일치 4건을 덮지 않고 올렸다**(`progress.md` §5 `deviations`): **(1)** `AC-OVERLAP-021` ①의 4값 중 **`exact_widths`는 툴 표면에서 도달 불가능하다** — 출하된 핸들러가 `FootprintPolicy`를 만들지 않으며(AST 0건) 그 이유는 `ASSUMPTION-27` 부정이고 **본 SPEC의 상계 축이 존재하는 이유가 바로 그것**이다. 그 등급을 툴에서 내려면 기각된 축을 다시 켜야 하므로 범위 밖이며, **죽은 어휘는 아니다**(판정·리포트 계층에서 산출된다). **(2)** `AC-OVERLAP-019` ⑦의 *"트립와이어가 갱신되고"*는 전제가 성립하지 않았다. **(3)** D-3 가드가 오류를 내지 않는 형태. **(4)** 순회에 `sibling_answered` 추가. **인수 조건의 전제가 출하 형상과 어긋날 때 조건을 충족시키려 형상을 바꾸는 것이 결함이므로** 기록하고 후속 판단을 요청한다.
  - **뮤테이션 총계 36건 주입 · 33 killed · 3 생존(전건 테스트를 고쳐 닫음) · 1 주입 불가(형상 강화로 대체)**. 그 1건은 safety 삭제 행 단정을 계수로 약화하는 것인데 **효과를 보려면 PRESERVE 파일에서 의미 있는 줄을 함께 삭제해 커밋해야** 하고(`BASE..HEAD`는 커밋만 본다) 그것은 위반이다 — 억지 뮤테이션을 만들지 않고 삭제 행이 **구조적으로 독스트링인지**를 함께 단정해 원문을 모르는 의미 있는 삭제도 잡히게 했다. **정직한 잔여**: `ASSUMPTION-31`·`32`·`33`이 여전히 열려 있고 셋 다 다른 쇼파일을 요구하며, 본 SPEC은 **발화 억제가 아니라 한정 표현**으로 출하했다(`bound_proves_clear`가 항상 *"열거된 모드 N개에 한정한 판정"*을 함께 말한다) · `ruff check server/`는 착수 시점부터 3건 실패하고 그중 둘이 PRESERVE라 고치는 것이 위반이다 · `test_web_launcher` 사이드카 테스트가 15초 벽시계 데드라인으로 간헐 실패한다(본 SPEC과 무관) · 절단 계수 비교의 **4번째 사본**을 만들었고 D-8대로 수렴하지 않았다.
  - **`ASSUMPTION-27` 부정을 뒤집지 않는다**: PRECHK의 C-9는 *"폭이 **유일**한가"*를 물어 정당하게 부정했고(모드 4종 = 29·29·29·**31**) I-15는 *"폭에 **상계**가 있는가"*를 묻는다. 유일성은 조인을 불필요하게 만들지만 **상계도 같은 일을 한다** — 간격이 최대 폭보다 크면 어느 폭이든 겹치지 않는다. **더 약한 명제로 같은 결론에 도달하는 것이며 조인은 여전히 없다.**
  - **판정의 비대칭이 설계의 핵심이며 그것이 어휘 4값을 강제한다**: 상계 논증은 *"겹침 없음"*만 증명하고 *"겹침 있음"*은 증명하지 못한다 — 간격이 상계보다 작으면 그것은 충돌이 아니라 **미확정**이다. *"상계로 증명된 청결"*은 **충돌도 미수행도 아닌 제3의 결과**이므로 기존 두 축 어디에도 넣을 수 없다(넣으면 깨끗한 리그를 `충돌`로 라벨하거나 **수행된** 판정을 `미수행`이라 쓴다). **사용자 승인을 받아** 신규 축 `overlap_basis` 4값 + `SKIPPED_CHECK_KIND` 1값을 확장하며 **`COLLISION_KIND`와 `FIXTURE_VERDICT`는 무변경**이다. 이 저장소에 **닫힌 어휘 확장 선례는 0건**이므로(git 이력으로 확인 — 집합 리터럴이 한 번도 개정된 적이 없다) 절차를 문서화하는 것 자체가 산출물이다.
  - **술어는 `간격 < 상계`이며 `이하`가 아니다**: 구간이 닫힌 끝(`start + width - 1`)이므로 `간격 == 상계`는 **증명 가능하게 깨끗하다**. 선행 기록이 *"상계 이하라 미확정"*으로 적은 것은 off-by-one이며 **이 쇼파일에서는 42 > 31이라 두 표현이 같은 답을 내므로 오류가 잠복한다** — 간격이 정확히 상계인 리그에서 처음 드러나고 그때 깨끗한 리그를 미확정으로 보고한다.
  - **건전성 전제 3건이 전부 현 표면에서 관측 불가다 — 하나라도 거짓이면 상계가 상계가 아니다**: **(1)** 픽스처가 하나의 **연속 블록**을 점유한다(`Break*` 프로퍼티 전부 판독 불가이고 `Patch`가 한 쌍뿐이라 다중 브레이크면 `start + width - 1`이 첫 블록을 과대평가하고 둘째를 완전히 놓친다). **(2)** `DMXChannels` **자식 수 = DMX 슬롯 수**(`childCount` 29·31만 기록됐고 **자식 이름은 한 건도 기록되지 않았다** — 16비트 어트리뷰트가 2슬롯을 쓰면 상계가 과소평가된다). **(3)** **열거 완전성**(모드 집합이 불완전하면 `max`가 부분집합의 최대값이 되어 참 상계보다 작아지고 `bound_proves_clear`가 **거짓 양성**으로 발화한다 — 부분집합 29 vs 참값 31, 간격 30 리그에서 결론이 뒤집힌다). 셋 중 하나라도 미증명이면 **`bound_proves_clear`를 내지 않는 형상**으로 출하한다 — PRECHK가 `FID`를 배제하고 출하한 것과 같은 형태다.
  - **512는 필요하지 않다 — 필요한 것은 `B ≥ 467`이다**: 유니버스당 채널 상한에 대한 **저장소 근거가 0건**임을 범위 9곳 전수로 확인했고, 512를 그렇게 쓰는 저장소 유일 지점의 내용이 *"근거를 찾지 못했다"*다. 실측 리그에서 **판정이 갈리는 창은 `B ∈ [437, 466]` 30값뿐**이므로 전제를 훨씬 약한 명제로 쓰면 미검증 상수 도입을 피한다. 꼬리 초과는 Out of Scope이며 산술을 기록해 넘긴다(**용량을 런타임에 읽으려면 11번째 rig-path 키가 필요해 정확 10키 단정이 깨지는데, 폭 상계 축은 신규 키 0건으로 성립한다**).
  - **조사가 선행 기록의 오류·누락 5건을 정정했다**: 술어 off-by-one · 어휘 편집점이 4곳이 아니라 **13 + 배선 3** · §E.7이 dedupe 루프 BASE 범위 `(537, 582)`를 기록하지 않았음 · **`tools.py`도 삭제 1행이므로 *"순수 추가 = 삭제 0"* 규칙을 적용하면 즉시 실패한다** · 다음 SPEC BASE 기록이 1커밋 뒤처졌음. 그리고 선행 기록이 **추정을 단정으로 적은 것 1건**(`DmxUniverses` `childCount 1024`가 유니버스 개수라는 해석에 근거 병기 0건이며 자식 이름 `'DMX 2'`와 정합하지 않는다). **BASE는 두 개가 아니라 세 개**이며 선례 상수를 복사하면 게이트가 **주석 한복판과 루프 앞 13행**을 지킨다(수치 증명 기록).
  - **주소 공간에 검증이 0건이고 유니버스 서로소성에 살아 있는 뮤테이션 구멍이 있다**: 파서가 하한도 상한도 없어 `normalize_address("0.0")`이 `ok=True`를 낸다(실행 확인) — 오늘은 정확 일치 중복만 보므로 무해하고 **간격을 계산하는 순간 무의미한 주소가 무의미한 판정을 만든다.** 그리고 **구간 겹침 축을 두 유니버스로 밟는 테스트가 0건**이라 유니버스 키잉을 지워도 스위트가 통과한다 — 본 SPEC이 그 구멍을 닫는다.
  - **실행 형태 — 계약이 닫힌 뒤에만 병렬**: 조사는 **scout 4개 병렬**(충돌 0 · 정정 5건 + 신규 확립 4건), 정본 3종은 **단독 순차**(6문서는 한 설계의 투영이며 SONGCUE 지적 34건의 대부분이 문서 간 드리프트였다), `CONTRACT.md`로 계약을 고정한 뒤 `design`+`plan`을 **병렬 2**. **병렬이 값을 냈다 — 두 작성자가 계약의 빈틈 11건을 올렸고 그중 3건이 내가 방금 쓴 정본의 오류였다**(PRESERVE 경로 분류 · `AC` 하나의 BASE 미지정 · BASE 계수). 세 번째 BASE와 트립와이어 소속 마일스톤은 **계약을 쓸 때 내가 몰랐던 것**이다.
  - **독립 plan-audit 1회차 — FAIL 0.806 / 기준선 0.85 · 지적 19건(P1 3 · P2 9 · P3 7) 전건 처리**: 감사가 계수를 전건 재현했고(REQ 18 · AC 21 · 역추적 18/18 · 삼자 AC 배정 · BASE 상대 좌표의 끝점 원문과 +13 오프셋 · 술어 7문서 일관) **P1 3건은 전부 교차 정합 축**이며 내가 다 놓쳤다 — **(1)** `design.md`에 정정 전 경로 분류가 남아 `cross_document_drift: 0`이 거짓이었다(**원인은 숫자가 아니라 대조 축에 "계수 문구"가 없었던 것**이며 이제 대조 축 4종을 열거한다). **(2)** 한 인수 조건 항이 집행 마일스톤 없이 떠 있었고 **기계 충족 불가**였다(관측 불가 전제의 거짓을 코드가 감지할 수 없다 — 관측 가능한 것으로 좁히고 한정 표현 축으로 이관). **(3)** `plan.md`가 존재하지 않는 `progress.md` §F를 5곳에서 지목했다 — **자신이 LOOKLIB 선례를 인용해 막겠다고 선언한 바로 그 끊어진 참조**를 재생산했다. 가장 아픈 P2: *"`state_calls` 등호 단정 0건"*을 `[코드]` 등급으로 단정했는데 **그 단정을 깨는 테스트를 같은 세션에서 내가 직접 썼다** — 규율 18로 남겼다(전수 주장의 근거는 만든 사람의 기억이 아니라 **방금 돌린 명령의 출력**이다).
  - **독립 run-audit 1회차 — FAIL 0.714 / 기준선 0.85 · 지적 32건(P1 6 · P2 14 · P3 12) 전건 처리**: 작성자가 아닌 주체가 **폭 7 팬아웃**으로 채점했다(축별 슬라이스 6 + 격리 워크트리 뮤테이션 1 — 근거와 격리 실측은 `progress.md` §F.2). **P1 6건 전부가 2920개 스위트가 전건 통과하는 상태에서 살아 있었다.** 가장 무거운 것은 출하 코드다 — `_overlap_basis`가 `assessed`(**관측된** 픽스처)만 훑어 **미관측 개체가 등급에 기여하지 않았고**, `childCount=40` 선언·18개 열거 리그가 `missing_count=22`인 채 `bound_proves_clear`로 찍혔다. `CONTRACT.md` D-4가 결함으로 명명한 형상이자 `spec.md` §A 제약 4가 *"유일하게 가능한 오류"*라 적은 **거짓 "겹침 없음 증명"** 그 자체이며, 계측 census상 그 조합을 도는 테스트가 **0건**이었다. 나머지 5건은 코드가 옳고 **테스트가 못 재는** 형태였다 — D-6 가드가 *"레지스트리를 순회하되 루프 안에서 건너뛰는"* 1줄 우회에 무방비(대조: 그 우회 상태에서 라벨 없는 5번째 코드가 **import에 성공한다**) · 열거 불완전 무효화 3지점 무단정(셋 다 참 상계 31·산출 29·간격 30의 함정을 재현) · 예산 상한 단정이 자기참조라 `40 → 1e9`도 통과 · 픽스처 0개 리그 폴백 무단정.
  - **재주입이 감사자 자신의 수정에서 2건을 더 잡았다 — 결함을 고치는 것과 그 수정을 지키는 것은 별개의 작업이다**: P1 폐쇄 후 격리 워크트리에서 소스를 실제로 변형해 재주입하니 2건이 살아남았고 **둘 다 오케스트레이터 자신의 작업**이었다 — 미관측 개체 수정은 **코드를 고치고 게이트를 달지 않아** 되돌려도 아무것도 실패하지 않았고, `sibling_answered` 기본값 제거는 **모든 호출자가 인자를 넘기므로** 행태 테스트에 보이지 않았다(시그니처 단정으로 닫았다). 둘을 닫은 뒤 **8종 재주입 전건 killed**. 슬라이스도 같은 규율을 지켰다 — 예산 담당이 함께 시도한 단정이 등호에 가려 **실패 불가능**함을 발견하고 출하하지 않고 삭제했다.
  - **인수 조건 문언 3건을 감사 처분으로 개정했다**: **(1)** `AC-OVERLAP-021` ①이 4값 전부의 툴 도달을 요구했으나 `REQ-OVERLAP-013`이 `[Option]`(*"**Where** 정확폭이 **주어지면**"*)이라 어떤 요구도 툴이 폭을 **공급**할 의무를 지우지 않는다 — **지배 요구보다 강한 과잉 명세**였으므로 툴 표면 3값으로 좁혔다(죽은 어휘가 아니며 판정·리포트 계층이 산출한다). 작성자가 댄 근거(*"`spec.md` §D 범위 밖"*)는 §D 원문이 뒷받침하지 않아 **기각하고 근거를 교체했다.** **(2)** `AC-OVERLAP-019` ⑦의 *"트립와이어가 갱신되고"*는 전제가 성립하지 않았고, 실측은 더 강한 사실을 준다 — **갱신이 불필요했던 것이 아니라 갱신하면 단정이 깨진다.** **(3)** `AC-OVERLAP-017` ②의 *"판정 계층이 한국어를 만들지 않는다"*가 `CONTRACT.md` R-8(*"`observation_note`는 순한국어로 쓴다"*)과 **같은 정본 묶음 안에서 충족 불가능**이었다 — 불변식은 한국어 금지가 아니라 **라벨 소유권**이다.
  - **게이트 강화 5건과 계수 정정**: `AC-OVERLAP-014` ⑨가 **공허했다**(금지 토큰 스캐너 3종이 어휘의 **값**을 한 번도 보지 않았다 — 주장은 참이나 측정된 적이 없었다) · `AC-OVERLAP-018` ①이 명시한 *"AST 스캔"*이 실제로는 3-needle 부분문자열 스캔이라 **심은 라우트를 놓쳤다**(이제 라우트 8 · 클라이언트 메시지 타입 11 · 서버 이벤트 타입 15를 AST로 열거해 절대 튜플로 고정한다 — **저장소 최초의 라우트 계수 게이트**) · `"한정" in summary`가 한정과 **한정의 명시적 부정**을 구별하지 못했다 · `AC-OVERLAP-009` ②가 이름 붙인 10+9→17 리그가 **어디에도 없었다** · `AC-OVERLAP-002` ④의 비공허성 대조군 부재(그것 없이는 ③ 자신이 **공허하게 통과**함을 주입으로 실증했다). 계수 정정: M6 순증 `26 → 18`(인쇄된 등식 `2869+26≠2887`이었고 그 오기를 *"170 중 8건 재배치"*라는 **재측정 없는 서사**가 덮고 있었다) · 뮤테이션 총계 `36/33 → 39/36` · 손댄 파일 `10 → 11` · §6.3의 커밋수·증감을 **값 대신 측정 명령**으로 · 존재하지 않는 `acceptance.md` §D 인용 4곳을 실제 출처인 `.moai/specs/SPEC-COPILOT-PRECHK-001/acceptance.md:313`으로.
  - **머지 전 수렴 점검 4회차 — 계열은 소진되지 않았고, 뿌리 하나는 이 SPEC 밖이다**: 3회차까지 매 회차 새 P1이 나왔으므로 머지 전에 **수렴 여부만** 재기로 했다. 4회차도 P1을 냈고 **두 결함의 성격이 갈려 처분도 갈렸다.** **(C-1, 고쳤다)** 한 축이 겹침을 증명한 뒤에도 다른 축이 청결을 주장했다 — 앞 라운드가 세운 참여 검사를 `exact_set` 경로가 **우회했다**(같은 결함 계열이 두 번째 문으로 재입장). 수정은 `bound_slots and not exact_overlaps`이며 기존 단정 0건을 바꾼다. **출하 도달 불가**(`FootprintPolicy(enabled=True)`를 만드는 프로덕션 호출자 0건)임에도 고친 이유는 어휘가 공개 API이고 스위트가 상시 발화시키기 때문이다. **(C-2, 고칠 수 없다 — 이 SPEC 안전 논증의 가장 중요한 한계)** 완전성 술어가 **짧은 판독을 원리적으로 탐지하지 못한다**: 응답기가 선언 계수를 판독 배열 자체에서 파생하므로 계수와 목록이 항상 일치하고, **짧은 판독이 스스로를 "완전"이라 선언한다.** 앞 라운드의 수정은 `count == 0` 한 지점만 닫았고 같은 파생은 어떤 계수에서도 성립한다. 뿌리가 `console/lua/**`(PRESERVE)에 있어 형상이 아니라 **지식의 한계**이며, 한정 표현이 *"열거된 모드 N개에 한정"*을 항상 말하지만 **그 N이 참값보다 작을 수 있다는 것까지는 말하지 않는다.** 후속 SPEC이 집을 것: 응답기가 `Count()` 원값을 실어 짧은 판독이 자기 목록과 모순되게 할 것(인벤토리·매크로·상계 **세 축의 완전성 판정이 동시에 실질을 얻는다**).
  - **머지 판단의 근거는 "결함 0"이 아니라 "경계를 알았다"**: 결함 발견 곡선은 꺾이지 않았다(P1 3 → 6 → 2 → 1+1). 그러나 4회차가 **처음으로 경계를 그었다** — 출하 구성(`enabled=False`) 전수 훑기가 리그 약 **306,000개 · 청결 등급 1,244건에서 거짓 증명 0건**을 냈고, 남은 live 도달 가능 결함은 **하나이며 그 뿌리가 이 SPEC 밖에 있고 상속된 것**이다. *"더 찾으면 더 나온다"*에서 *"남은 것이 무엇이고 왜 여기서 못 고치는지 안다"*로 옮겼다.
- **SPEC-COPILOT-PRECHK-001** — 프리쇼 패치 점검 + 응답 확인 매크로 생성: 리그의 패치를 읽어 **관측 픽스처 전량 + 집계 2단**으로 보고하고 주소 중복·판독 실패·열거 완전성·**수행하지 않은 판정**까지 명시하는 신규 툴 `precheck_patch` (`status: completed`, M0~M8 전 마일스톤). AC-PRECHK-001~017 **17건 전량 PASS** — 소프트웨어 15건은 기계 검증(pytest), LIVE AC 2건(**AC-PRECHK-016** M0 프로브 · **AC-PRECHK-017** M8 종단)은 실제 grandMA3 onPC 2.4.2로 검증. 실행 형태는 **sub-agent 폭 1 → 폭 2 → 폭 1**(`progress.md` §F.1이 M0 실측 후 M4를 자립 슬라이스로 분리한 근거를 기록). **테스트 231건 신규 · 전체 2721 passed · 5 skipped · 0 failed.**
  - **M0 — 라이브 프로브(판정 6건: GO 4 · NEGATIVE 2, `REOPEN_SCOPE` 0건)**: 저작 착수 전 실물 콘솔에서 `ASSUMPTION-25`~`ASSUMPTION-30`을 닫힌 결과 어휘 4값으로 판정하고 접두 행 4종(합 6행)으로 기록했다. **유일한 블로킹이었던 `ASSUMPTION-26`(매크로 저작, 등급 T3·라이브 `OK` 0건)이 GO** — 다만 룰북의 2리터럴만으로는 실패하고 **`Store Macro <n>.<line>`(라인 개체 생성)이 프로퍼티 설정에 선행해야 한다**는 것이 실측으로 드러났다(순서를 어기면 `Illegal object`이며 그것은 "문법 없음"이 아니라 "대상 없음"이었다 — 부정 프로브로는 갈리지 않고 **생성 프로브 + 재조회만이** 갈랐다). `ASSUMPTION-25`는 19슬롯 전량 판독(실패 0건)이며 **사전 프로브가 놓친 19번째를 슬롯별 보강 조회로 채웠다**(`MMX 19` @ `2.401`). `ASSUMPTION-27`(픽스처→타입→모드→점유폭 연결)은 **후보를 전수로 닫은 뒤 NEGATIVE** — 구간 겹침만 빠지고 주소 중복은 어느 분기에서도 수행된다. 코드 변경 0건, 쇼파일 5지점 변경 **전건 원상 복구 + 재조회 일치**.
  - **M0가 드러낸 안전 관련 실측 2건**: **(1)** `prop … Index`가 **`ok=true`와 함께 `'function: 0x…'`**(Lua 함수 참조)를 반환한다 — `REQ-PRECHK-003`의 값 형태 검증이 이론적 방어가 아니라 이 쇼파일에서 재현되는 현상임을 실증하며, M2 형태검증기의 판별자는 **접두사**이고 특정 주소가 아니다. **(2)** `Assign … At Executor <N>`이 **`page × 100 + slot`을 단사로 사상하지 않는다** — `Executor 201`이 page 2가 아니라 **page 1의 인덱스 101을 조용히 덮었다**(`Cmd`는 `OK`를 냈다). page 1 익스큐터 1개가 실제로 덮여 같은 리터럴로 역전 복구했다. `ASSUMPTION-30`의 `page ≥ 2` 일반화가 부정인 근거이며, **접수 `OK`가 의도한 대상에 닿았음을 뜻하지 않는다**는 사례다.
  - **M0의 부수 폐쇄 3건(후속 SPEC용)**: `DataPool/PresetPools`가 **살아 있고**(14개, 각 풀 `class='Presets'`) `DataPool/Presets`·`AllPresets`는 사망 — 프리셋의 유일한 생존 경로다. `ASSUMPTION-7`(자식 풀 슬롯)이 **분기 (a) `probe_slots`로 충족됨**을 갭 풀(`DataPool/Pages/1`의 희소 `i` = 1·2·5·11·91·92·93·95·101)로 실측해 응답기 계약의 오래된 미확정을 닫았다 — `PROTOCOL.md`가 최악으로 적은 실패 모드("(a) 부재 + (b) 위치형 = 콘솔 측에서 탐지 불가능한 원래 결함")가 이 빌드에서는 발생하지 않는다. `ASSUMPTION-28`·`ASSUMPTION-29`도 GO(페이지 저작 · 빈 익스큐터 식별)로 BUSKWIZ의 DESCOPE를 닫았다.
  - **M1 — 초크포인트 프로퍼티 조회 (`server/safety/**` 조건부 예외, 사용자 승인)**: 픽스처 **주소는 프로퍼티에만** 있고 `prop`은 프로덕션 경로로 도달 불가였다(OSC 송신 표면 import 디렉터리가 테스트로 강제되고 `build_prop_query`에 프로덕션 소비자 0건). 우회 4종을 전수 배제한 뒤 **순수 추가 4지점**으로 한정 집행 — `ConsoleLink.query_property`(`query_state`와 동형) · `PropertyQueryPort` 신규 프로토콜 · 게이트 위임 + **감사 로그** · 오프라인 대역. `server/safety/console.py`는 hunk 3개 **전부 순수 추가(삭제 0행)**, `gate.py`는 추가 2개 + 독스트링 1행 정정이며 **기존 심볼·시그니처·스크리닝 의미론 변경 0건**. `REQ-MVP-029`의 단일 초크포인트 원칙은 강화된다 — 새 읽기도 감사된다.
  - **M2~M3 — 인벤토리 · 패치 정합성** (`server/prechk/{inventory,patch}.py` 신규): 완전성을 **`truncated` 플래그가 아니라 `node.childCount`와 읽은 개수의 비교**로 판정한다(`REQ-PRECHK-004`) — 조사 자신이 반환 수 18을 총수로 오독하고 그 위에 *"정합한 리그"*를 얹은 실증이 이 요구의 근거다. 절단되면 슬롯별 보강 조회로 상세를 늘리지만 **completeness를 `complete`로 승격하지 않는다**(인덱스 정의역 상한을 모른다 — `recovery_boundary`·`index_domain_unknown`을 페이로드에 노출한다). 주소는 `<유니버스>.<주소>` **정수 2개**로 정규화하고 파싱 불가는 기본값으로 채우지 않는다. 3중복을 쌍 3건으로 세지 않고, 다른 유니버스의 같은 주소는 충돌이 아니다. **불완전 입력에 정합성을 단정하지 않는다** — 미관측 픽스처는 `not_assessed`다.
  - **M4~M5 — 응답 확인 매크로 · 리포트** (`server/prechk/{macro,report}.py` 신규): 매크로 대상은 **그룹뿐**이며 `Fixture <n>` 생성은 0건이다 — 슬롯이 FID가 아니라 대부분의 리그에서 엉뚱한 기구를 **조용히** 선택하기 때문이고, 이 축소는 강제된 것이다. 그룹 1개당 점등·소등 1쌍이며 **쌍 수는 그룹 수에 연동**(상수 아님). 매크로 실행 `ok`는 **응답 증거가 아니다** — 반환에 응답 단언 필드를 두지 않고 `requires_human_visual_confirmation`을 클래스 상수로 고정한다. 판정 어휘는 **닫힌 5집합**이며 어휘 밖 코드는 라벨 조회에서 **명시적으로 실패**한다(`server/looks/report.py`의 통과 정책과 의도적으로 다르다 — 프리체크 판정은 콘솔에서 올라오는 자유 문자열이 아니다). 집계는 픽스처별 목록에서 계산되고 `skipped_checks`는 **두 생산자(패치·매크로)를 병합**한다.
  - **M6~M7 — 툴 배선 · 회귀/PRESERVE**: `precheck_patch`를 등재 3곳에 넣고 **디스패치로** 검증했다(dict 조회는 `TOOL_NAMES` 누락을 놓친다). 콘솔 발화는 **`run_commands` 경유**이므로 `bundle_gate.screen()`이 번들 전체를 먼저 본다 — `execution_port` 직접 접근·신규 REST 라우트·웹소켓 타입 **0건**(AST 스캔 + 비공허성). 스키마에 **리그 식별자 0개**이며 매크로 슬롯은 핸들러가 풀을 읽어 유도한다. PRESERVE diff는 `<BASE>..HEAD` 범위로만 검사한다 — **인자 없는 `git diff`는 위반이 커밋돼 있어도 0행을 내며 게이트가 통째로 무력해진다**(주입 실측으로 증명). 게이트 비공허성을 주입 3회로 실증하고 PRESERVE 10경로가 BASE에 실재함을 확인해 목록 오타 위양성을 배제했다.
  - **M8 — 종단 라이브**: 제품 조립 경로(`build_console_stack` + `build_toolset`)로 툴을 1회 종단 실행했다. **쓰기 0건**(`create_macro=false` · 세션 백업 끔 · `command` 감사 이벤트 0건)이며 증거는 툴 자기 목록이 아니라 **감사 로그 대조**다(`property_query` 95 · `state_query` 27 · `command` 0). **불완전 경로를 실물에서 밟았다** — 루트 열거가 절단되자 보강 조회가 19번째를 회수하고도(`recovered_count 1`) completeness를 승격하지 않았다. 재조회 불일치 0건 · 충돌 0건 · 집계=픽스처별 합 · 쇼파일 baseline 6경로 전항 일치.
  - **결함 6건을 스스로 잡아 고쳤다 — 각각 "그 단계가 없었으면 통과했다"**: **(1)** M6에서 `server.prechk.report.build_report`가 `server/looks/report.py`의 동명 심볼을 가려 `prepare_busking`이 잘못된 함수를 호출했다(busking 12건 실패) — 신규 파일만 돌린 결과는 통과였고 **전체 스위트가 잡았다**. **(2)** M8에서 `summary_ko`가 `missing_count` 0인데도 *"못 읽은 픽스처가 있다"*고 단정했다 — 인메모리 32건을 전부 통과했고 **라이브 종단 실행만이** 잡았다. **(3)** M0에서 `ASSUMPTION-27`을 후보 **부분집합** 위에서 부정 단정했다 — `REQ-PRECHK-010`이 금지하는 형태를 요구 작성자가 반복했고 **읽기 전용 scout**가 잡았다. **(4~6)** 그리고 아래 독립 감사가 셋을 더 잡았다.
  - **독립 run-audit 1회차 — FAIL 0.695 / 기준선 0.85 · 지적 14건 처리**: 작성자가 아닌 주체가 채점해 **P1 4건**을 냈고 **전부 M8이 밟지 않은 매크로 축**에 있었다. **(P1-1)** 매크로 소등 라인 `Group <n> At 0`이 **M0 미실측 리터럴**이었다(`REQ-PRECHK-012` 위반) — 게다가 대조 테스트가 페이로드를 비교 **전에 지우고** ON 라인만 필터해 **기계 게이트가 구조적으로 비껴갔다**. 보강 라이브 세션에서 측정해 정본에 등재하고 게이트를 전 페이즈 대조로 고쳤다. **(P1-2)** 그룹 풀 판독 실패를 *"리그에 그룹이 없다"*로 보고하고 `is_error=False`를 냈다 — M8이 잡은 결함과 **같은 계열**(코드는 방어 가능한데 사용자가 읽는 문자열이 거짓)이며 같은 SPEC의 인벤토리 계층이 문서로 못박은 규율을 정면으로 뒤집었다. **(P1-3)** `_free_macro_slot`이 `childCount`를 보지 않아 **절단된 매크로 풀에서 점유 슬롯을 골라 기존 매크로를 덮어쓸 수 있었다** — 본 SPEC의 **유일한 쓰기 경로**가 본 SPEC의 1급 요구(계수 비교)를 어긴 지점이다. **(P1-4)** `AC-PRECHK-014` ④의 LiveLock 강등이 구현·테스트 양쪽에 없었다(형제 툴 2개에는 있다). 회귀 테스트 10건을 신설하고 뮤테이션 5건으로 수정 전건의 방어를 확인했다.
  - **정직한 잔여 6건**: **(1)** `FID` 의미는 어떤 라이브 세션도 현재 쇼파일로 닫을 수 없다(슬롯 == FID) — 슬롯 ≠ FID로 패치된 쇼파일이 선행 조건이며 사용자 GUI 작업이다. 본 SPEC은 `FID`를 판정 근거에서 **배제한 형상으로** 출하하고 `미확정` 표시로만 싣는다. **(2)** 무응답 픽스처 자동 탐지는 **관측 경로가 존재하지 않아** DESCOPE다(응답기 동사 5종이 닫혀 있고 픽스처 하드웨어 피드백을 수집하는 코드가 없다) — 대신 사람이 눈으로 확인할 매크로를 준다. **(3)** 구간 겹침은 `ASSUMPTION-27` 부정으로 미수행이며 리포트가 그 축소를 명시한다. 감사가 추가로 열거한 후보 2건(콘솔측 프로브 플러그인 · **보수적 점유폭 상계**)은 미측정이며 후자는 라이브 없이 되살릴 여지가 있다. **(4)** 희소 풀에서 보강 조회 경계(`1..childCount` 캡)가 과소복구한다 — 이번 쇼파일이 조밀했을 뿐이다. **(5)** M2·M3·M4의 뮤테이션 20건은 **집계 수치만** 남아 개별 증거가 없다(워커 로그가 `.gitignore` 아래) — "검증됨"으로 세지 않는다. **(6)** `AC-PRECHK-015`의 PRESERVE 게이트는 1회성 수동 절차이며 상시 테스트로 박지 않았다 — 후속 SPEC이 PRECHK의 PRESERVE를 깨도 스위트가 잡지 못한다.
  - **PR #7 독립 코드 리뷰 — 지적 14건 중 13건 처리 · P1 4건은 머지 전에 고쳤다**: 이 저장소는 `gh pr checks`가 **0건**이므로 머지 전 유일한 관문이 사람 리뷰이고, 선행 run-audit는 **SPEC 프로세스** 축만 채점했다. 그래서 쓰기 경로와 판독 경로에 **독립 리뷰어 2명을 병렬로** 붙였고 둘 다 `overall: incorrect`를 냈다. **P1 4건 전부가 2721개 스위트가 전건 통과하는 상태에서 살아 있었다** — **(P1-1)** `_free_macro_slot`이 자식 0건 매크로 풀을 신뢰해 `Store Macro 1`이 **응답기 자신의 `Copilot Go` 매크로를 덮는다**(`M.safe_children`는 두 접근자가 모두 pcall 실패하면 빈 테이블을 돌려주고 `childCount`가 그 같은 빈 판독에서 파생되므로 **열거 전면 실패와 진짜 빈 풀이 한 페이로드**다). 수정 전 툴이 번들 전량을 `is_error=False`로 **실제 실행**했다. **(P1-2)** LiveLock이 **비오류 리포트**로 `macro.created=true`와 *"매크로를 실행해 눈으로 확인하라"*를 냈다 — 콘솔에 매크로가 없고, 락은 ANSWER이므로 `is_error`가 이 사실을 나를 수 없다. `macro.executed`를 신설했다. **(P1-3)** `root_was_short`가 *"자식이 슬롯 인덱스를 못 실었다"*를 *"루트 열거가 짧았다"*로 접어 **전량 반환된 입력에서도 거짓 문자열**을 냈다(`i` 생략은 `PROTOCOL.md` §4.2에 명시된 응답기 거동이며 이 계열 결함의 **4번째** 사례다). 술어를 계수-대-반환 비교로 좁히고 `incomplete` 라벨에서 원인 단정을 뺐다. **(P1-4)** 보강 스윕이 `ok=true`만 보고 노드 정체를 대조하지 않는데, **스윕을 촉발하는 바로 그 상태**(자식 중 하나도 슬롯 미확립)에서 응답기의 숫자 경로가 목록 위치로 열화한다 — 수정 전 `recovered_slots=(1,2,3)`을 **순수 위치로** 회수했다. 스윕에 확립된 슬롯 1개를 앵커로 요구한다.
  - **P2·P3 9건을 계약 무변경으로 닫았다**: 매크로 저작 후 **재조회로 효과 확인**(불일치를 `is_error`로 승격하지 **않는다** — 저장 후 정확 문자열 일치가 리그 1대·라인 1개에서만 실측됐으므로 콘솔이 정규화하면 모든 정상 사전점검이 오류가 되는 *허위경보* 버전이 된다) · 대상 그룹 0건이면 **슬롯을 유도하지 않는다**(쓰이지도 않을 슬롯의 판독 실패가 성공적으로 읽은 인벤토리를 버렸다) · `rig_paths` 섹션 누락을 이름으로 지목 · 자기 자식 수 **초과 관측 스냅샷 거부**(클램프가 `AC-PRECHK-003` 등식을 **거짓으로 닫았고** 런타임 검증이 0건이었다) · 포인터 판별을 **4종**으로 확장(근원이 함수가 아니라 `tostring` 폴백이며 Lua는 모든 비원시 타입을 `<type>: 0x…`로 만든다) · 보강 조회의 **타임아웃과 부재 분리** · **폭 부재 고지** + 폭 판정 단일 출처화 · `shape_invalid` 라벨 오기술 · 요약의 범위 한정어. **남은 2건은 닫힌 어휘 또는 리포트 스키마 확장을 요구해 계약 결정 대기이며 정밀도 문제이고 정보 손실이 아니다.**
  - **규율 16을 세웠다 — "스위트가 통과한다"는 "결함이 없다"가 아니다**: P1 4건이 전건 통과 상태에서 살아 있었으므로, 이 사이클부터 **신규 테스트가 수정 전 코드에서 실패함을 역방향으로 확인**하고(소스만 되돌려 실행 → 복원) **통과하는 테스트는 회귀 테스트가 아니라고 코드에 명시**한다(비공허성 보증 · 불변식 가드). 재조회를 **하지 않아야 하는** 3경로처럼 수정 전 공허하게 통과하는 테스트는 **직접 돌연변이를 넣어** 무엇을 막는지 증명한다. 신규 36건 중 **32건이 수정 전 실패**함을 확인했다. **전체 2758 passed · 5 skipped · 0 failed.**
  - **PR #7 squash 머지 (2026-07-30) · 새 `origin/main` = `b406a7b2bde856f0ecfb445885e6fe60693c68a5`**: 커밋 메시지를 믿지 않고 **신규 워크트리를 `origin/main`에 붙여 독립 검증**했다 — prechk 268 passed · 전체 2756 passed(차이 2건은 `.app` 번들 미빌드로 skip되는 tauri 테스트이며 2756+2=2758로 닫힌다) · PRESERVE 게이트 빈 출력 · `server/safety/` diff 파일집합이 승인 2파일뿐. **BASE가 이제 두 개이며 호환되지 않는다** — 다음 SPEC의 BASE는 새 main이고 **PRECHK의 PRESERVE 기준점은 `95687a0e…`로 영구 불변**이다(새 BASE로 검사하면 커밋 직후 항상 0행이라 게이트가 무력해진다). `progress.md` §E.9가 그 함정을 정본으로 적는다.
- **SPEC-COPILOT-FXLIB-001** — 이펙트 어휘 계층(FX 라이브러리): 시간축 움직임 패턴(페이저+MAtricks+원형 조합)을 폐쇄 어휘로 담아 채팅 지시를 매칭하고 시퀀스+큐로 인스턴스화 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB(정지 화면 어휘)의 시간축 자매편이며, 신규 패키지 `server/fx/`를 세우고 기존 `run_commands` → `gate.screen()` 단일 관문으로만 실행한다. AC-FXLIB-001~023 **23건 전량 PASS** — 소프트웨어 21건은 기계 검증(pytest), LIVE AC 2건(**AC-FXLIB-021** M0 프로브 · **AC-FXLIB-022** M7 종단)은 실제 grandMA3 onPC 2.4.2로 검증. **테스트 3240 passed + vitest 223 passed(13 files) — 신규 실패 0.**
  - **M0 — 라이브 프로브(판정 4건 전부 GO + `Accel`/`Decel` SKIP)**: 페이저는 2개 이상의 스텝을 요구하며 `<값>` → `Step 2` → `<값>` 형상이 확립됐다; `Relative`/`Phase`/`Speed`는 이미 존재하는 페이저를 **변형**할 뿐 생성하지 않는다(1스텝 위에서 발화하면 `ok:true` 전량에 모션 0이었다 — 실패 3회의 원인은 콘솔이 아니라 프로브였다). 이 실측이 v0.1.0의 "스텝 축은 선택 필드"를 철회시키고 스텝 축을 스키마 필수 1급 필드로 승격했다(v0.2.0 폴드인). **효과의 기계 증거 채널은 부정으로 확정** — 저장된 큐는 `Cue → Part(childCount 0)`에서 트리가 바닥나 빈 큐와 구별되지 않고, `Phase`/`Speed`/`CueFade`는 `"property not readable"`이다. 사람의 GUI 관측이 이 SPEC에서 효과의 유일한 증거 채널이며, 성공 경로를 포함한 **모든** 리포트가 그 한계를 무조건 명시한다(REQ-FXLIB-014 (c)).
  - **M1 — FX 스키마 + 로더**(`server/fx/{schema,loader}.py`, 86 tests): 스텝 축(길이 ≥2, attribute→값 매핑)을 필수 필드로 강제하고 로더 거부 4종(길이<2 / 스텝 간 attribute 집합 불일치 / 같은 attribute 두 스텝의 값 동일 / `steps` 없이 변형 축만 선언)을 개별 테스트로 고정. 뮤테이션 7/7 killed(지정 4 + 추가 3, 그중 1건은 `server.bridge` import 1줄 주입으로 `test_architecture.py` 자동 포섭을 실증).
  - **M2·M3·M4 — 병렬 웨이브 1**(`server/fx/{library,matching,instantiate,report}.py`, 275 tests): 라이브러리 12엔트리(패턴 6종 × 2) · 한국어 자연어 매칭 · 번들 빌더 + 값 라인 충돌 가드 2중(번들 내 구성 시점 + 교차 호출 실행 결과 시점) + 한국어 2단 리포트. 오케스트레이터가 커밋·`progress.md` 쓰기를 회수했다(같은 브랜치 동시 커밋의 git 인덱스 경합 회피). 뮤테이션 37/37 killed. PRESERVE diff 0건.
  - **M5 — 툴 표면**(`server/orchestrator/tools.py`, 78 tests): `find_fx`/`instantiate_fx` 등재, 룰북 byte-diff 0(발견성은 툴 스키마 설명이 전담), 제공자 중립(어댑터 import 0건). 뮤테이션 19/19 killed.
  - **M6 — 회귀 + 경계**(`server/tests/test_fx_boundary.py`, 38 tests): `server/fx/**` transport import 0건(AST 스캔), LiveLock 활성 시 실물 게이트 + 활성 잠금으로 16줄 전량 `proposal`(송신 0) 확인, `server/safety/**` diff 0. 전체 회귀 pytest 3239 + vitest 223(본 SPEC 최초 실행).
  - **M7 — 종단 라이브 검증**(2026-08-01, onPC 2.4.2, 코드 변경 0): 실물 게이트 스택(`build_console_stack` → `build_toolset`)으로 `pulse`(Dimmer)와 `sweep`(Pan)을 종단 발화. 감사 로그 18건이 계획 번들과 순서까지 동일(각 10줄 전량 `executed_ok`), GUI 관측 "파도처럼 순차적으로" / "좌우로 움직인다"로 두 판정 모두 확인. **ASSUMPTION-40 GO** — 스텝 생성 형상이 Dimmer 밖 Pan/Tilt에서도 성립. 라이브 세션은 정확히 **2회**(M0·M7, 사용자 사전 확정 ③ 그대로). 쇼파일 원상 복구 확인(재조회 `childCount` 17 == baseline, 잔여 0건).
  - **코드 결함 3건을 스스로 잡아 고쳤고(1~3), 그 위에 두 부류의 자기 정정이 반복됐다(4~5) — 전부 런타임에서 아무 신호도 내지 않았을 것들**: **(1)** M4가 자기 산출물에서 `circle`이 `diagonal`과 바이트 동일한 번들을 내는 결함을 발견했다 — 위상 축이 attribute 개수로 배분되고 있었다(M2의 커밋된 자산으로 자기 빌더를 재실행해서만 드러났다; 효과가 기계 검증 불가한 축이라 이 결함은 라이브에서 사람이 볼 때까지 아무 신호도 안 냈을 것이다). **(2)** 오케스트레이터가 `chase` 다중 attribute 위상 축퇴를 수정했다(커밋 `bd451df`) — R=0/G=180/B=360인데 360≡0이라 3색 체이스가 2상 점멸로 렌더되고 있었다; 산술로 판정 가능해 라이브 세션 없이 고쳤고, 한 바퀴를 도는 호에만(`span % 360 == 0`) 배타 끝점을 적용해 부분 호는 손대지 않았다. **(3)** 오케스트레이터가 `instantiate_fx`의 LiveLock 강등을 `is_error=True`→`False`로 정렬했다(커밋 `52d94de`) — 형제 툴 `prepare_busking`/`precheck_patch`는 이미 `False`였고, `True`는 공연 중 자기수정 루프가 같은 잠금에 재부딪히게 했다. **(4)** 뮤테이션 테스트가 마일스톤마다 반복해서 **테스트 자신의 결함**을 잡았다 — M1은 `match="x"`가 모든 메시지에 매칭돼 아무것도 판별 못 하는 패턴 10건을, M5는 서로 다른 두 거부 경로가 같은 `is_error=True`를 내 그 축만 본 단언을, M6는 이름이 "제안됐다"인데 실제로 발화된 번들도 통과시키던 테스트를 잡았다. **(5)** M0 자신이 두 차례 자기 정정을 했다 — 응답 못 받은 구간을 콘솔 결함으로 오판했다가 프로브 드라이버의 `kind` 불일치로 정정했고, 페이저 생성 실패 3회를 콘솔/룰북 결함으로 오판했다가 프로브가 스텝을 1개만 만든 자기 결함으로 정정했다; M7이 같은 성격의 오판을 세 번째로 피했다(패키지 앱용 감사 로그 경로를 개발 체크아웃 경로로 착각할 뻔했다가 결론 전에 자기 하네스를 먼저 의심해 잡았다).
  - **정직한 잔여**: ASSUMPTION-40은 `sweep`(Pan) 1종만 직접 관측했다 — `wave`/`circle`/`diagonal`/`chase`의 효과는 같은 축·같은 형상의 파생일 뿐 개별 라이브 관측은 없다. `Accel`/`Decel` 곡선은 스키마에 정의만 되고 v1 라이브러리는 값으로 쓰지 않는다(SKIP). **`@MX:ANCHOR` 상한 초과** — `tools.py` 6건이 `mx.yaml anchor_per_file: 3`을 넘는다(착수 시점 이미 4건, 본 SPEC이 2건 추가); AST 실측 결과 6개 핸들러 전부 fan_in 동률(Load 참조 1·직접 호출 0)이라 "최저 fan_in부터 강등" 처방이 축퇴한다 — sync-phase는 강등하지 않고 **`mx.yaml` 소유자의 설정 판단으로 이관한다**(정직한 해석은 "6건 중 3건이 틀렸다"가 아니라 "툴 레지스트리 파일에 per-file 상한이 구조적으로 맞지 않는다"이다). **design.md §5 한 줄 정합 필요** — M4 실측으로 `ChangeDestination Root`도 전 번들 공통 첫 줄임이 드러나 교차 호출 접힘이 §5가 적은 `Step 2`보다 한 줄 빠르게 시작한다(design.md는 manager-spec 소유라 여기서는 후속 발견으로만 기록한다).
- **SPEC-COPILOT-SONGCUE-001** — 송 구조 기반 큐리스트 초안 생성기: 곡 섹션 목록 한 벌로 **곡 1개 = 시퀀스 1개, 섹션 1개 = 큐 1개** 큐리스트를 승인 1회에 저작 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB의 룩 어휘와 BUSKWIZ의 번들·보고 형상을 **섹션 축**으로 소비한다. AC-SONGCUE-001~018 **18건 전량 PASS** — 소프트웨어 AC는 기계 검증(pytest), LIVE AC(017/018)는 실제 grandMA3 onPC 2.4.2로 검증. **실행 형태는 병렬 2트랙**(`progress.md` §F가 plan.md §G의 `sub-agent` 권고를 기각) — Orca 오케스트레이션 task DAG로 워커 2를 조율했다.
  - **M0 — 라이브 프로브(판정 5건, 전부 GO)**: 저작 착수 전 실물 콘솔에서 5전제를 실측해 **DESCOPE 0건**. **ASSUMPTION-21**(같은 시퀀스에 `Cue 2` 이상)이 유일한 블로킹이었고 GO로 M3의 기술적 차단을 해제 — `/Merge` 유무 **양 형태 모두** 새 큐 번호에 가산이고, **기존 큐 번호에 플래그 없는 `Store`는 `Not allowed`로 거부되며 쇼파일은 불변**이다(치환은 명시적 `/Overwrite`를 요구하므로 REQ-SONGCUE-010은 구조적으로 안전하다). ASSUMPTION-23은 시퀀스 풀이 **희소 인덱스를 명시**해 여집합이 계산 가능함을 확인 — BUSKWIZ가 익스큐터에서 데인 "비어 있음과 존재하지 않음이 구별 불가" 함정이 시퀀스 축에서는 재발하지 않았다. **ASSUMPTION-20(T5, 저장소 근거 0건)은 부정 프로브로는 판정 불가였고**(날조 키워드도 유효 키워드와 똑같이 `Illegal object`를 준다 — BUSKWIZ 측정 1의 함정) **생성 프로브가 판정을 갈랐다**(`Store Timecode 999` → 풀 childCount 0→1). ASSUMPTION-22는 `'Follow'`·`'Time'` **양쪽 수용**을 날조 토큰·날조 프로퍼티 대조군과 함께 확인해 `ok=True`의 변별력을 먼저 증명했다. ASSUMPTION-24는 86줄 **86/86** · 6.25s · 72.7ms/줄 · 열화 +1.1ms. 코드 변경 0건, 쇼파일 원상 복구.
  - **M0가 계획을 고치게 한 실측 4건**: **(F-1)** 큐 자식의 `i`는 큐 번호가 아니라 **나열 위치**다(`Cue 7`로 저장한 큐가 `i=5`로 보고됨) → AC-SONGCUE-018 측정 3이 재조회로 성립하지 않음. **(F-2)** 모든 시퀀스는 암묵 시스템 큐 2개(`OffCue`·`CueZero`)를 가져 **사용자 큐 수 = childCount − 2**. **(F-3)** 응답기 스냅샷에 `max_children = 24` 상한이 있어 풀이 24를 넘으면 여집합 계산이 무효 → M3가 `truncated` 참이면 거부한다. **(F-4)** 큐 번호는 커맨드로는 변별 가능(`Set Cue 7 …` OK vs `Set Cue 3 …` `Illegal object`).
  - **트랙 B — 응답기 v1.5.0 (`console/lua/**`, spec v0.2.0으로 범위 확장)**: M0가 `plan.md`(ASSUMPTION-22 GO면 `TrigTime` 의미론을 **반드시 측정**)와 `spec.md`(응답기 확장은 **범위 밖**, 한계 명시로 갈음)의 **모순**을 드러냈고, 사용자 승인으로 측정을 택해 PRESERVE에서 `console/lua/**` **한 항목만** 제외했다. 가산적 확장 2건 — 신규 동사 `prop`(실값 반환, 못 읽으면 `ok=false`), Cue 자식의 `cueNo`(**번호를 확신 못 하면 필드를 생략한다** — 추측 금지). 기존 `i`의 의미는 불변이라 다른 SPEC 소비자를 깨지 않는다. 이로써 **Gap 1 폐쇄 — `TrigTime`은 곡/시퀀스 시작 기준 절대 시각**(Cue1 `10`→readback `10.0`, Cue2 `14`→`14.0`; 상대 지연이면 `4.0`이어야 했다)이며 M4는 누적 절대값을 넣고 델타로 바꾸지 않는다. **F-1도 폐쇄**(`cueNo=7` 실측).
  - **M1~M2 — 섹션 입력 · 룩 매핑** (`server/looks/songcue.py` 신규): 시각 3형식(`mm:ss` · `mm:ss.mmm` · 초 실수)을 **밀리초 정수**로 정규화해 부동소수 비교를 남기지 않고, 역행·중복을 **각각** 거부하며 **정렬해서 진행하지 않는다**(임의 재배열 0건). 어휘는 `matching.py`의 `DYNAMICS_TERMS`·섹션어 밴드를 **import**하며 자체 매핑 리터럴 0건 — 검증은 raw grep이 아니라 **AST 식별자 스캔**이다(산문이 어휘를 설명할 수 있어 grep은 "표를 다시 적은 경우"와 "설명한 경우"를 구분하지 못한다). 밴드는 `tuple[int, ...]`로 유지해 점값으로 좁히지 않고, 미지 이름은 기본값으로 채우지 않고 "지정 필요"로 표시한다(섞인 입력에서 **있는 것만** 해석). 매핑은 `looks_for_genre`를 **호출**하고 재구현하지 않으며, 요구 다이내믹스에 룩이 없으면 **최근접으로 승격하지 않고** 미매핑으로 보고한다.
  - **M3 — 큐리스트 번들 (핵심 마일스톤, 하드 결함 3건)**: **큐 번호 원장**이 하드 결함 1을 닫는다 — 섹션마다 독립 계산하면 전부 `1`이 나오고, **건너뜀에도 번호를 당기지 않는** 것이 AC-SONGCUE-006 ②와 AC-SONGCUE-011을 동시에 참으로 만드는 유일한 형상이다. 결함의 실재와 해소를 **한 파일에서 함께** 고정했다(원장 없는 계산이 전부 1임을 별도 assert). 시퀀스 번호는 `DataPool/Sequences` 열거의 **여집합에서만** 고르고 하드코딩 0건이며 **조회 실패·절단 신호면 추측하지 않고 거부**한다. 금지 문법 스캔은 소스 grep이 아니라 **생성된 커맨드 튜플 전수**에 걸고 금지 형태를 **심어서** 스캐너의 실효성을 증명하며 `/Overwrite` assert는 **대소문자 무관**이다. 목적지 커맨드는 선두 **정확히 1회**로 개수와 **위치**를 둘 다 assert하고, 무손실은 문자열 assert가 아니라 **실제 `run_commands` 경로**로 확인한다(`skipped_already_executed` 0건). 모든 "0건" 스캔에 **비공허성 assert**를 동반한다.
  - **M4~M5 — 타이밍 · 보고 · 툴 배선**: M0가 두 축을 **모두 GO**로 확정했으므로 AC-SONGCUE-012는 구간 ①③이 발동하고 **②④는 `skip` 사유를 달아 보존**한다(삭제하지 않는다 — 후속 SPEC이 게이트를 다시 열 때의 출발점). 섹션 축 **2단 보고**(`server/looks/songcue_report.py` 신규)는 집계만 내지 않고 섹션별을 반드시 실으며, 미매핑 사유를 **부류로 구분**하고(어휘 미지 / 룩 부재 / 역할 미주소 — 사용자가 취할 조치가 다르다) **계획 시점의 건너뜀과 실행 시점의 중단 잔여를 합산하지 않는다**. 한계 문구는 산문이 아니라 **공개 상수 `PROPERTY_UNOBSERVED_NOTE`**이고 페이로드 `property_unobserved`와의 **상수 동일성**으로 기계 판정한다. 신규 툴 `prepare_songcue`는 기존 `run_commands` → `bundle_gate.screen()` 경로의 **호출자**이며 확인은 dict 조회가 아니라 **디스패치**로 한다(dict 조회만 하는 테스트는 `TOOL_NAMES` 누락을 놓친다). 리그는 핸들러가 직접 읽어 스키마에 그룹·풀·슬롯·**시퀀스 번호** 필드가 0개다.
  - **M6 — 회귀 · PRESERVE · 게이트 비공허성**: PRESERVE 게이트는 `git diff --stat <BASE>..HEAD -- <목록>`이며 **`<BASE>..HEAD` 범위는 협상 불가**다(인자 없는 `git diff`는 커밋 직후 항상 빈 출력이라 게이트가 통째로 무력해진다). **게이트 자체의 비공허성을 증명했다** — `server/looks/matching.py`에 공백을 주입해 커밋(`8e7aa57`)하니 게이트가 적발했고 revert(`12deea2`)로 원복했다. 목록 오타로 게이트가 조용히 통과하는 위양성을 이 절차가 배제한다. `tools.py`의 변경 hunk(old 시작줄 32·49·125·951·1222·1231)는 `_PROGRAMMER_STATE_COMMANDS`(`:234-238`)·dedupe 루프(`:524-569`) 어디에도 걸치지 않는다.
  - **M7 — 종단 라이브 검증(2026-07-29, onPC 2.4.2 / CopilotResponder v1.5.0)**: `prepare_songcue`를 통해 곡 1개를 종단 1회 생성. 감사 로그가 **33/33을 계획 번들과 순서까지 동일하게** 기록하고 전 행 `ok=true`, `skipped_already_executed` **0건** — 결정 G(코드 개정 없이 형상으로 회피)와 결정 H(충돌을 사전에 건너뜀)의 최종 증명. 재조회에서 시퀀스 1개에 사용자 큐 4개가 **`cueNo` 1·2·3·4로 서로 다른 번호로** 존재하고 이름이 일치한다 — **하드 결함 1의 실물 반증**. 보고 산술도 재조회 실측과 일치(`section_count=4`/`generated=4`/`unmapped=0`/`skipped=0`/`not_executed=0`/`failed=0`). 증거는 툴 자신의 목록이 아니라 **감사 로그와의 대조**다(툴 반환만으로 판정하면 세션이 스스로를 검증하는 순환이 된다 — BUSKWIZ M7의 교훈). 코드 변경 0건, 쇼파일 원상 복구.
  - **M7이 라이브에서만 잡은 결함 1건 — 즉시 수정**: 툴의 `report.requery.matched`가 `false`였다. 원인은 `_observed_cues`의 `child.get("cueNo", child.get("i"))` **`i` 폴백**이 `cueNo` 없는 `OffCue`를 나열 위치 `i=1`로 **추정**해 사용자 큐 1번과 충돌시킨 것이며, `matched`가 완전 일치 비교라 **구조적으로 참이 될 수 없었다**. 그 폴백은 응답기 v1.5.0의 계약("번호를 확신 못 하면 생략, **추측 금지**")을 정확히 되돌려 놓은 잔재였다. **유닛이 놓친 이유가 핵심이다** — `_requery_payload` 픽스처가 시스템 큐를 아예 넣지 않은 **이상화된 모양**이어서 M0의 F-2 실측이 픽스처로 전파되지 않았다. `i` 폴백을 제거하고 `cueNo >= 1` 필터로 교체(**표시 이름으로 걸러내지 않는다** — 이름에서 정체성을 끌어내는 것은 금지), 픽스처를 라이브 모양으로 교정, 회귀 테스트 신설. 검증은 폴백 복원 뮤테이션 + **실제 콘솔 페이로드**(시퀀스 11·20·1)로 이중 확인했다.
  - **정직한 잔여**: `CueFade`는 응답기 v1.5.0의 `prop` 경로에서도 `property not readable: CueFade`로 남아 REQ-SONGCUE-017의 한계 명시가 계속 유효하다; 매체 갭은 M7이 툴→게이트 경로로 실행해 대부분 닫혔으나 **`prop` 되읽기 자체는 게이트 미경유 직결**이다; `max_children = 24`의 실제 절단 거동은 미실측(현재 시퀀스 17)이라 M3의 거부 분기는 라이브 발동이 관측되지 않았다; `/Overwrite`는 끝내 발화하지 않아 "치환은 명시적 플래그를 요구한다"는 여전히 `Not allowed` 거부로부터의 **추론**이다; 라이브 원문 로그는 `.moai/state/`(gitignore) 아래에만 있어 커밋되는 사본은 `progress.md` §E.2의 표와 인용문이며 그래서 커맨드·응답 문자열을 요약 없이 전재했다.
  - **오케스트레이터 실수 2건 — 기록해 둔다**(`progress.md` §F 개정 절): **(1)** 트랙 B 설계 시 `plan.md`의 좁은 6파일 목록만 보고 **`spec.md §C`의 PRESERVE 정본을 읽지 않아** `console/lua` 변경을 지시했다. 위반은 워커의 일탈이 아니라 **내가 지시한 것**이고 발견자도 내가 아니라 M6 워커였다. **(2)** 트랙 B를 "M4를 막는 임계경로"로 규정한 것은 **과장**이었다 — SPEC 원설계는 관측을 포기하고 한계를 상수로 명시하는 것이었고 M4는 실제로 그것을 구현했다. 병렬 2트랙의 정당화 근거 하나가 사후적으로 약해졌다(트랙 분리 자체는 파일·언어가 겹치지 않아 유효했다).
  - **테스트**: `uv run pytest server/tests/ -q` **2490 passed · 5 skipped · 0 failed**. 착수 SHA(`38a6e7e`)에서 직접 실측한 기준선 **2423 passed · 3 skipped**에서 **+67**이며, `skipped` +2는 M4가 보존한 DESCOPE 축 2구간이다. songcue 테스트 6파일(`test_songcue_{sections,map,bundle,timing,report,tool}.py`) = 58 passed · 2 skipped. **뮤테이션 27건 전건 killed**(M1 4 · M2 3 · M3 6 · M4 6 · M5 5 · M6 2 · 결함 수정 1). 라이브 세션 2회(결정 D의 회계 그대로).

- **SPEC-COPILOT-BUSKWIZ-001** — 버스킹 준비 마법사: "이 리그로 록 버스킹 준비해줘" 한 마디로 장르 팔레트 일괄 생성 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB이 만든 룩 어휘를 **장르 단위**로 소비해, 한 장르의 룩 전량(7~9룩)을 **승인 1회로 끝나는 단일 번들**로 묶어 사용자 리그의 프리셋 풀에 저장한다. AC-BUSKWIZ-001~017 **17건 전량 PASS**(AC-012는 ② DESCOPE 경로) — 소프트웨어 AC는 기계 검증(pytest), LIVE AC(016/017)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M0 — 라이브 프로브(판정 4건)**: 저작 착수 전 실물 콘솔에서 익스큐터 축 3전제를 실측해 **전부 DESCOPE 판정**(ASSUMPTION-16 페이지·익스큐터 저작 문법 / 17 빈 익스큐터 판별 / 19 팔레트를 익스큐터에 얹는 문법). 결정적 근거는 17번 — 미점유 익스큐터 인덱스가 어느 주소형으로도 해석되지 않아 "비어 있는 익스큐터"를 식별하는 질의 경로가 존재하지 않는다. ASSUMPTION-18(단일 번들 상한 87줄 왕복)은 **GO**(87/87 executed_ok, 5.77s, 66.3ms/줄)로 M2의 기술적 차단을 해제. 코드 변경 0건.
  - **M1 — 장르 조회 계층** (`server/looks/busking.py` 신규): 장르 별칭 표는 `matching.py`의 것을 **그대로 재사용**(재정의하면 한/영 어휘가 두 곳에 살고 갈라진다). 해석 실패는 가장 비슷한 장르로 **승격되지 않는다** — 과신 매칭의 결과는 사용자가 원한 적 없는 팔레트가 쇼파일에 남는 것이다.
  - **M2 — 슬롯 원장 + 다중 룩 번들 빌더**: 하드 결함 2건을 닫았다. **(1) 슬롯 비전진** — 룩마다 청구한 슬롯·라벨을 누적해 다음 룩에 넘긴다(같은 PoolIndex로 3회 직접 호출하면 Color 슬롯이 `[1,1,1]`, 본 계층 통과 시 `[1,2,3]`). **(2) `ChangeDestination Root` dedupe 탈락** — 목적지를 번들 선두 1회만 남긴다. **`tools.py`의 dedupe 규칙은 고치지 않았다**(그 블록은 `run_commands`를 쓰는 모든 소비자가 공유한다) — 전역 실행 의미론 대신 번들 형상으로 회피했고, M7 라이브가 그 회피의 성립을 증명했다.
  - **M3 — 집계 보고 계층** (`server/looks/report.py` 신규): 집계 + **룩별 2단** 구조화 보고(생성·미매핑·건너뜀·룩별 판정·미실행). 51~87 커맨드 중 어느 룩이 죽었는지 집계만으로는 알 수 없기 때문이다. 미매핑의 단위는 `(룩, 역할)` **쌍**이며(리그를 1회만 해석하므로 역할 1종이 여러 룩에서 반복된다), 매칭 판정 3종과 **섹션 실패**를 부류로 분리한다 — 후자를 `no_match`로 보고하면 보지 않은 리그에 대한 주장이 된다. 한국어 라벨은 자산이 아니라 코드 표에 산다.
  - **결정 H — 값 라인 충돌 가드**: 같은 장르에 전체 속성 페이로드가 동일한 룩이 둘이면 값 라인이 문자열로 겹쳐 두 번째가 dedupe에 접히고, **빈 프로그래머 상태로 `Store`가 실행되는데 콘솔은 성공으로 답한다.** 거부(예외)가 아니라 **건너뛰기 + 사유 보고**로 막는다 — `_plan_stores`가 이미 "이 저장은 안전하게 일어날 수 없다"를 전부 `SkippedStore`로 답하는 선례를 따른 것이며, 룩 하나의 저작 결함으로 장르 전량을 죽이지 않는다. v1 출하 라이브러리에서는 발동하지 않는다(중복 0건).
  - **M4 — 툴 배선** (`prepare_busking`, `server/orchestrator/tools.py`): 기존 `run_commands` → `gate.screen()` 경로의 **호출자**이지 제2 실행 표면이 아니다(게이트·LiveLock·dedupe·감사 로그를 그 한 경로에서 상속). 리그는 모델 인자가 아니라 핸들러가 직접 읽는다 — 스키마에 그룹·풀·슬롯 필드 **0개**. `is_error` 규약: 알 수 없는 장르는 `True`(후보 목록으로 정정 가능), **저장 0건과 LiveLock 강등은 `False`**(답변인 결과이지 기술적 실패가 아니며, `True`면 자기수정 루프가 같은 잠금에 다시 부딪힌다).
  - **M5 — 익스큐터 ② DESCOPE 분기**: v1은 익스큐터·페이지 대상 커맨드를 **0건** 발화한다. 검증은 소스 grep이 아니라 **4장르가 실제 생성한 커맨드 전수** + AST `BinOp` 검사다(dotted form은 변수 조립으로만 발화되므로 소스에는 숫자가 없어 정규식이 결코 매치하지 않는다). GO 분기 테스트 3건은 **삭제하지 않고 skip 사유와 함께 보존** — 후속 SPEC의 출발점은 "문법이 없다"가 아니라 **"문법은 파싱되나 효과와 대상 식별이 미검증"**이다.
  - **M6 — 회귀 · PRESERVE · per-show 정적 금지 스캔**: 그룹·풀·슬롯·FID·익스큐터 번호가 커맨드 조립에 정적으로 들어가는 지점 0건(독스트링 제외 문자열 상수 + f-string 숫자 상수 + 리그 파라미터 숫자 기본값 3종 스캔, 전부 금지 형태 주입으로 비공허성 증명). PRESERVE 목록 diff 빈 출력, `tools.py`의 `_PROGRAMMER_STATE_COMMANDS`·`_is_programmer_state`·dedupe 블록 **바이트 단위 무변경**, 신규 YAML·JSON 자산 0개.
  - **M7 — 종단 라이브 검증(2026-07-28, onPC 2.4.2 / CopilotResponder v1.4.1)**: 감사 로그가 **61건을 계획 번들과 순서까지 동일하게** 기록하고 전 행 `ok=True`, `ChangeDestination Root` 1건·`ClearAll` 12건 전량 생존, `skipped_already_executed` **0건** — M2의 형상 회피가 실물에서 성립함의 최종 증명. 프리셋 18개가 4개 풀에 생성됐고 재조회에서 슬롯·라벨까지 18/18 일치. **슬롯 1을 건너뛰고 2부터 시작한 것이 원장이 실제로 동작한 증거**다(세 풀의 슬롯 1은 LOOKLIB M7이 남긴 기존 프리셋이 점유). 실물 리그가 역할 6종 중 2종만 주소해 **2룩 `none` + 6룩 `partial`**이 나왔고, 전부 초록인 세션보다 강한 증거로 M3의 2단 보고가 실물에서 값을 했다.
  - **정직한 잔여**: 응답기는 프리셋 **내용**을 읽지 못하므로 라이브 검증은 슬롯·라벨의 **존재** 수준이다(LOOKLIB M0 교차 발견의 계승); M7에서 게이트가 승인 요구 없이 번들을 통과시켜 **승인 채널이 실측되지 않았다**(게이트 보류 시 송신 0건은 M4가 유닛으로 소유); 익스큐터 축은 M0 판정에 따라 v1에서 통째로 닫혀 있고 페이지·익스큐터 저작 문법의 존부(G1)는 미측정으로 남는다; 값 라인 충돌 가드는 v1 라이브러리에서 발동하지 않아 **라이브 실증이 없다**(유닛·뮤테이션으로만 검증).
  - **뮤테이션 테스트로 잡은 자기 결함 3건**: 통과하던 테스트가 실제로는 아무것도 지키지 않던 공허한 단언 3건을 뮤테이션 단계가 드러냈다 — (M3) 렌더 문자열 부재 단언이 포맷 불일치로 무효, (결정 H) 번들을 둘 따로 만들어 원장이 공유되지 않음, (M1) raw 텍스트 스캔이 호출과 독스트링을 구분 못함. 전부 실효 단언으로 교체 후 뮤테이션 **19/19 검출**.
  - **테스트**: `uv run pytest server/tests/ -q` **2423 passed · 3 skipped · 0 failed**. 착수 SHA(`d176b81`)에서 직접 실측한 기준선 2291에서 **+132**이며, 6개 마일스톤의 증분(+34/+26/+27/+27/+10/+8)이 각 마일스톤 기록값과 한 건도 어긋나지 않고 닫힌다(이월 인용 0). 기존 비-clean 지점 2건(`test_web_dash.py:523` E501, `test_tools.py:567,598` format)은 baseline에서도 동일해 무관 재포맷을 피해 손대지 않았다.

- **SPEC-COPILOT-LOOKLIB-001** — 연출 어휘 계층(Look Library): 무드 지시를 콘솔 프리셋으로 실체화 (`status: completed`, M0~M7 전 마일스톤). "코러스에서 금색 톤으로 웅장하게" 수준의 추상 지시가 큐레이트된 룩(워십·록·발라드·EDM 4장르 × 32룩)으로 매칭되어, 사용자 리그의 실제 그룹에 바인딩된 MA3 프리셋으로 인스턴스화된다. AC-LOOKLIB-001~020 중 19건 PASS + 1건(AC-020, M0 라이브 프로브) 조건부 PASS — 소프트웨어 AC는 기계 검증(pytest/vitest), LIVE AC(020/014)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M0 — 라이브 프로브(판정 4건)**: 저작 착수 전 실물 콘솔에서 빔 attribute 문법(`Zoom`/`Iris` 수용 확인 → Focus/Beam 풀이 in-scope로 확정), 프리셋 풀 타입의 런타임 해석, 역할 명명 관례(이 쇼파일에서 2/6 매칭), `Store Preset`의 패밀리 스코프 캡처(구조적 추론 — 직접 관측은 아님)를 실측. 코드 변경 0건.
  - **M1~M3 — 스키마·로더·리졸버** (`server/looks/{schema,roles,loader,resolver}.py`): 컬러·강도 구체값 + 역할 기반 포지션 6종 폐쇄 어휘(백라이트·프론트·사이드·탑·배경·스페셜) + 정수 1~5 다이내믹스 축을 스키마로 정의; 리졸버는 `get_rig_context`의 groups 섹션에 실존하는 그룹만 후보로 쓰고, 미매핑을 `no_match`/`ambiguous`/`unaddressable`(신설) 3종으로 구분 보고한다 — 그룹 발명·번호 추측 금지.
  - **M2 — 내장 라이브러리** (`server/looks/library/*.yaml`): 워십·록·발라드·EDM 32룩, 각 장르가 다이내믹스 1~5 스펙트럼을 스팬.
  - **M4 — MA3 인스턴스화** (`server/looks/instantiate.py`): 풀 타입별 `Store Preset <pool>.<slot>` + `Label` 번들을 기존 `run_commands` → `gate.screen()` 단일 경로로만 발화; 런타임 빈 슬롯 탐색, 충돌/빈 슬롯 없음은 건너뛰고 사유별로 명시 보고, `/Overwrite` 미발화.
  - **M5 — 자연어 매칭** (`server/looks/matching.py`, 신규 툴 `find_looks`): 무드/장르/다이내믹스 축으로 매칭하고 실패 시 기존 룰북 무드 폴백으로 강등; 룩 데이터 본문은 고정 룰북 프리픽스에 담기지 않는다(byte-stability 계약 유지).
  - **M6 — 회귀 + 안전 경계**: LiveLock 활성 중 발화 0건(defense-in-depth로 확인), 미매핑 사유 3종 구분 유지, 단일 스크리닝 경로 회귀 확인.
  - **M7 — 배선 수정 + 종단 라이브 인수(2회차)**: 1차 라이브(2026-07-26)가 드러낸 배선 결함 — 인스턴스화/리졸버 층은 완성돼 있었으나 모델이 도달 가능한 툴이 없어 프리셋이 0건 생성됨 — 을 신규 툴 `instantiate_look` 등록(`server/orchestrator/tools.py`) + 룰북 3단계 절차 병합(find_looks → get_rig_context → run_commands, `server/rulebook/assets/v2.4.2/31_choreography_patterns.md`)으로 해소. 2차 라이브(2026-07-27)에서 "웅장한 금색 코러스로 가자" 지시가 실제로 프리셋 3개(Dimmer·Color·Focus 풀) 생성으로 이어짐을 감사 로그 + OSC 응답기 판독으로 확인.
  - **부수 수정**: `run_commands`의 번들 내 중복 제거가 `ClearAll` 등 프로그래머 상태 커맨드까지 병합해 격리 사이클을 침식하던 결함을 M4에서 발견 즉시 수정(프로그래머 상태 커맨드 면제 목록 도입, `server/orchestrator/tools.py`).
  - **정직한 잔여**: AC-LOOKLIB-020(M0)은 명세된 증거 매체 중 감사 로그·GUI 스크린샷 2종이 산출되지 않아 조건부 PASS로 남는다; AC-LOOKLIB-014(M7)는 명세된 GUI 스크린샷 대신 OSC 응답기 판독으로 대체 검증됐다; 빔 계열 값의 방향(Zoom/Iris 개방·폐쇄 어느 쪽이 좁은지)은 미측정; 풀 이름→패밀리 해석은 단일 쇼파일에서만 검증되어 사용자가 풀 이름을 바꾼 쇼파일에서의 재현성은 미확립; 역할 힌트 6종의 실전 적중률은 2/6에 머물며 그마저 M0와 동일 쇼파일의 재확인이라 넓은 표본이 아직 없음; 룰북 폴백 강등(REQ-LOOKLIB-017)은 프롬프트 계약이라 기계 테스트로 강제할 수 없음; 감사 로그 어휘에 툴 호출 이벤트 타입이 없어 `find_looks` 같은 read-only 툴 사용 여부는 로그로 관측 불가.
  - **테스트**: `.venv/bin/python -m pytest -q` 2291 passed, 0 failed (grandMA3 onPC 2.4.2 RUNNING, UDP 8000/9005 보유 상태에서 측정 — 환경 의존 테스트 포함 전량 green); `(cd ui && npx vitest run)` 223 passed (13 files).

- **SPEC-COPILOT-DASHUI-001** — 콘솔 상태 대시보드 + 코파일럿 분할 UI (`status: completed`, M1~M6 + M6-RC + 사용자 지시 UX 배치). 채팅 전용이던 코파일럿에 "콘솔이 지금 뭘 갖고 있는지" 항상 보이는 대시보드를 추가하고, 시퀀스/익스큐터/매크로 타일에서 직접 발화(게이트 경유)할 수 있게 한다. 17개 AC(AC-DASHUI-001~017) 전부 검증 — 소프트웨어 AC는 기계 검증(vitest/pytest), LIVE AC(014/015)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M1~M4 — 대시 카탈로그 프로토콜 + 서버 빌더 + 분할 UI 골격**: `server/web/dash.py`(신규 `dash_catalog_request`/`dash_catalog` 메시지, 실제 `no` 키잉·`path_not_resolved`/`console_unreachable` 실패 구분·`truncated`/`drilldown_capped`/`contents_unavailable` 플래그)·`server/web/messages.py`(프로토콜 양측 패리티, `target_kind` enum에 `macro` 추가)와 `ui/src/components/{DashBoard,PoolSection,PoolTile}.tsx` + `ui/src/App.tsx`(좌측 대시보드/우측 채팅 분할, 접기 지원)를 신설. 정보 전용 타일(그룹/프리셋/플러그인/픽스처 요약)은 구조적으로 발화 불가 — `target_kind` 필드 자체가 없다.
  - **M5 — 발화 경로 통합(게이트 + LiveLock)**: `useCopilotSocket.ts`의 `sendPanelExecute`/`sendPanelStop`/`sendDashRefresh`가 기존 `panel_execute`/`panel_stop` → `gate.screen()` 계약을 그대로 재사용해 시퀀스/익스큐터/매크로 타일 press를 배선; `connectResyncFrames()`가 모든 (재)접속에서 `panel_catalog_request`+`dash_catalog_request`+`status_request` 3종을 동시 dispatch(AC-DASHUI-017); LiveLock 활성 시 발화 가능 타일은 제안 전용으로 강등(송신 0건).
  - **M6 라이브 체크리스트 + 근본원인 규명(M6-RC)**: 실제 onPC 2.4.2 1차 라이브에서 익스큐터/매크로/LiveLock 발화 항목(②③⑤⑥)이 "발화 가능한 타깃이 쇼파일에 없다"는 이유로 미달성 판정됐으나, 사용자 지시로 근본 원인을 재조사해 실제 결함 2건을 실측 규명·수정: **(RC-1) 슬롯≠콘솔번호** — 대시보드가 주는 풀 슬롯 번호(1,5,11,…101)와 콘솔 주소 형식(`페이지×100+슬롯`, 슬롯1→`Executor 101`)이 다름을 8/8 실측 확정 → `_executor_candidates`가 [원시 슬롯, 페이지형] 순서로 이름 검증 후 확정 번호를 `meta.console_no`로 전달, UI(`dashPressTargetNo`)는 `meta.console_no`만 fail-closed로 사용해 오발을 구조적으로 봉쇄(AC-DASHUI-005 "해석된 콘솔 번호" 완결). **(RC-2) 응답기 회신의 조용한 증발** — MA3 커맨드라인이 ~2048바이트 초과 회신을 조용히 드롭(에러 없음)함을 실측(2000B 배달/2100B 드롭 스윕) → `console/lua/copilot_responder.lua` v1.4.1에서 `max_payload` 4000→1900으로 낮춰 재배포, 회귀 테스트 `server/tests/test_lua_responder_payload_budget.py` 추가. 수정 후 실제 onPC에서 `Go+`/`Off Executor 111` 왕복, `Macro 1` 게이트 경유 발화(감사 1:1), LiveLock 잠금 중 제안-전용 강등(송신 0건)을 전부 재검증 — AC-DASHUI-014 PASS로 상향(매크로 press는 양성 절반만 검증, 이 쇼파일에 블랙리스트 매크로가 없어 승인 카드 절반은 잔여 §E.5로 정직하게 기록).
  - **사용자 지시 UX 배치(post-M6)**: 콘솔-우선 레이아웃 역전 — 콘솔 상태 패널이 상시·주 영역, 채팅은 우측 접이식 420px 컬럼으로 전환; onPC 풀-WINDOW 모델 — 카테고리별 색상 창(그룹 파랑/프리셋 청록/매크로 빨강/플러그인 마젠타/익스큐터 황갈, live-amber와 미충돌)에 SQUARE 셀을 −/+로 단계 조절하고 창 우하단 ◢ 코너 드래그로 창 영역(가로×세로) 자체를 리사이즈(flex-wrap 모자이크 재배치); 프리셋 섹션은 사이트 선호에 따라 풀 21("All 1")만 표시(`VISIBLE_PRESET_POOL_NOS`); 매크로는 Off 형태가 없어 1회성 press가 running 상태에 영구 래치되던 결함을 수정(정지 어포던스가 있는 종류만 running 추적); `chat_response`에 `executed_ok`가 실리면 정확히 1회의 `dash_catalog_request`를 자동 dispatch(`dashResyncFrame`, 이벤트 기반 — 타이머 폴링 없음, REQ-DASHUI-021 준수).
  - **정직한 잔여**: AC-DASHUI-014 체크리스트 ⑤의 블랙리스트 매크로 승인 카드 절반은 이 쇼파일에 해당 매크로가 없어 미검증(§E.5); `panel.busy` 타일별 지속 표시는 acceptance.md 어떤 AC도 개별 검증 항목으로 열거하지 않아 이번 범위 밖(D-M5-3, 근접 확인은 승인 카드 경로로 간접 수행).
  - **테스트**: `(cd ui && npx vitest run)` 208/208 passed, `npx tsc --noEmit` exit 0; `.venv/bin/python -m pytest -q` 신규 실패 0건(M1 킥오프 기준선과 완전 동일, 잔존 실패는 모두 기존 환경적 실패); `pytest server/tests/test_architecture.py -q` 4/4 passed(신규 대시 모듈이 OSC 송신 표면을 import하지 않음을 재확인).

- **SPEC-COPILOT-EXECBODY-001** — 익스큐터 타일 1회 누름의 실제 마찰(승인 카드+`SaveShow`)을 실측으로 제거 (`status: completed`, M1~M6 전 마일스톤). EXECREF-001이 남긴 구조적 갭 — 콘솔 응답기가 익스큐터의 할당 시퀀스를 `Children()`으로 노출하지 못해(`childCount: 0`) 본문 해석이 원천 불가능했던 문제 — 를 응답기 확장으로 닫는다. 16개 AC(AC-EXECBODY-001~016) 전부 검증.
  - **M1 — 역주소 문제 조사 (GO)**: `console/lua/copilot_responder.lua`가 `"Executor <console_no>"` 형태의 주소를 `DataPool` 트리 워크 대신 네이티브 `ObjectList("Executor <no>")[1]` API로 직접 해석 가능함을 라이브 프로브로 확정(ASSUMPTION-10 GO) — 다중-페이지 오프셋 관례 하드코딩(ASSUMPTION-11, REQ-EXECBODY-007이 금지)을 회피하는 경로가 존재했다.
  - **M2 — 시퀀스 아이덴티티 노출** (`console/lua/copilot_responder.lua` `build_snapshot`): 익스큐터 노드에 한해 `node.sequenceNo` 필드를 가산적으로 추가 — 할당된 시퀀스의 풀 번호를, 표시 이름 파싱 없이(AC-EXECBODY-005) 기존 `SLOT_PROBES` 접근자로 노출한다. 기존 `{name, class, i}` 소비자는 무변경.
  - **M3 — 배포 + M4 — 안전 게이트 2단계 위임** (`server/safety/console.py` `StateBodyFetcher`): `"Executor <no>"` 참조를 만나면 1단계로 M2가 노출한 `sequenceNo`를 조회하고, 2단계로 그 번호를 `Sequence <no>` 참조로 취급해 이미 신뢰되는 시퀀스 본문 조회 경로(`DEFAULT_BODY_PATHS`)에 위임한다. 새 신뢰 경계를 만들지 않고 기존 경로로 가는 진입점만 추가.
  - **M5 — fail-closed 상속 + 빈 시퀀스 정합성**: 아이덴티티 조회 실패(미할당/타임아웃/프로퍼티 부재)는 계속 보류(`_hold`)됨을 회귀 재확인(AC-EXECBODY-007/008/011). `StateBodyFetcher`에 `allow_empty` 파라미터를 추가해, 큐 0개인 검증-확인된 빈 시퀀스는 "본문 없음"이 아니라 양성 통과로 구분(위험 커맨드가 빈 본문에 숨을 수 없으므로) — 직접 Macro/Plugin/Sequence 조회 경로의 기존 empty==unavailable 동작은 무변경.
  - **M6 — Lua `resolve_path` 갭 해소 + 배포 신뢰성 도구화**: `resolve_executor_address()` 신설로 `resolve_path()`가 `"Executor <n>"` 주소를 M1 API로 직접 분기 처리(그 외 경로는 기존 트리 워크 무변경). `server/tools/responder_roundtrip.py`에 `--expect-version` 플래그 추가 — 재-Import가 기존 동일 이름 플러그인을 실제로 갱신하지 못하는 배포 함정(실측됨)을, ping 응답 버전 대조 한 커맨드로 즉시 판정 가능하게 함. `console/lua/README.md` §2.1(배포 신뢰성) 신설.
  - **최종 라이브 인수 (AC-EXECBODY-010, 2026-07-24)**: 실제 grandMA3 onPC 2.4.2에서 채팅으로 "실행기 201번 실행해줘" → 승인 카드 0장, 감사 로그(`server/audit_logs/audit-20260723.jsonl`)가 정확히 3-이벤트 체인(익스큐터 정체 state_query → 할당 시퀀스 state_query → `Go+ Executor 201` 커맨드, 전부 `ok: true`)을 기록하고 이 구간에 승인/거부/`SaveShow` 이벤트 0건임을 확인. EXECREF-001·SHOWUI-001이 descope했던 역주소 갭이 최종적으로 닫혔다.
  - **테스트**: `pytest -q` 1739 passed/2 skipped/3 failed(기존 환경적 실패와 동일, 무관) + `npm run test`(ui) 98 passed + `test_architecture.py` 4 passed — OSC import 경계(`server/safety/**`) 기준선 대비 무변경.

- **SPEC-COPILOT-EXECREF-001** — 안전 게이트가 `Executor`를 인식 가능한 참조 타입으로 처음 인정 (`status: completed` — 본 SPEC의 범위는 처음부터 인식(S1)에 한정되어 있었고, 단일-press 복원(S2)은 이 SPEC의 일부가 아니라 별도 후속 SPEC으로 완전히 이연되었으므로, S1 완결로 이 SPEC 자체는 완료로 전환한다). plan-audit **PASS-WITH-DEBT(score 0.92)** — iteration 3/3(retry cap 도달), 유일 blocker는 MP-2(AC 표 서식 관례) 하나뿐이며 감사자가 "genuine unresolved defect가 아니라 rubric 정합 문제"로 명시 판정, 사용자가 PASS-with-debt를 명시적으로 수용(AskUserQuestion).
  - **인식 확장**(`server/safety/classify.py`): `RECOGNIZED_REFERENCE_TYPES`에 `"Executor"` 추가(REQ-EXECREF-001/002/003). `server/tests/test_safety_corpus.py`의 FN 코퍼스 참조타입 축을 `classify.RECOGNIZED_REFERENCE_TYPES`에서 동적으로 순회하도록 리팩터(REQ-EXECREF-011) — 하드코딩 리터럴 0건, 향후 타입 추가가 코퍼스를 자동 확장.
  - **동작은 변하지 않았다 (핵심 정직성 고지)**: 이 SPEC은 익스큐터 타일을 누를 때마다 뜨는 승인 카드 1장 + 쇼파일 저장 1회의 원인 — 게이트가 `Executor`를 해석 가능한 참조로 인정하지 않는다는 점 — 을 진단하고 **인식만** 교정했다. **실제 사용자 경험은 이전과 완전히 동일하다**: 타일을 눌러도 여전히 승인 카드가 1장 뜨고, 여전히 SaveShow가 발화한다. M1 단독은 무행동 변화(no-op)임이 라이브 검증으로 확인됨(design.md §2.1, AC-EXECREF-008/013) — 보류 사유 문자열이 `"unverifiable reference: no recognizable target object"`에서 `"no body path mapping for 'Executor N'"`로 바뀌었을 뿐, `hold=True`/승인 카드 발생/SaveShow 발화는 모두 그대로다.
  - **왜 여기서 멈췄나 — S2(단일-press 복원) DESCOPED**: 승인 카드 없는 단일-press 발화라는 원래 목표를 실제로 달성하려면 콘솔 실행기의 "본문 경로"를 조회할 방법이 있어야 하는데, 2026-07-23 라이브 프로브(`.moai/state/verify/showui-m6-resume/5-probe-body.log`)가 이를 구조적으로 불가능하다고 결정적으로 확인(childCount 0/4, 기존 갭 재확인). 사용자 결정(AskUserQuestion, 2026-07-23)으로 S2를 완전 이연 — 실제 실행 시 승인 카드를 없애려면 `console/lua/copilot_responder.lua`(콘솔측 응답기)를 손대야 하는데, 본 SPEC은 의도적으로 그 파일을 건드리지 않는다. 후속 SPEC `SPEC-COPILOT-EXECBODY-001`(권고, research.md §5.3, 미생성)을 추천.
  - **테스트**: pytest **1723 passed**(+132, 기준선 HEAD `0576553`의 1591 대비), 신규 실패 0건 — 유일 실패는 기존 환경적 실패(onPC UDP 9005 점유) 그대로. 라이브 재검증(AC-EXECREF-013③, 실제 onPC 대상, M1 커밋 이후 사후 수행): `cleared=False`, 승인 카드 여전히 생성, 콘솔 송신 0건 — M1 이전과 관측 동일함을 확인.
- **SPEC-COPILOT-SHOWUI-001** — 연출 컨트롤 패널(Show-Control Panel) 완료(`status: completed`). 채팅 옆 2컬럼 레이아웃에 실행기 윙의 연장을 목표로 한 타일 그리드를 구현 — M1~M6, 라이브 검증 1회(실제 grandMA3 onPC 2.4.2).
  - **⚠️ UI 표면은 DASHUI-001로 대체됨 — 현재 앱에 출하되지 않음**: 아래 "패널 UI" 항목이 기술하는 `ShowPanel`/`PanelTile` 컴포넌트(M4/M5, 커밋 857e9ed·09e2c4f)는 **다른 브랜치에서 병행 개발**된 것으로, DASHUI-001이 같은 자리(채팅 옆 콘솔 제어면)를 **신규 작성**으로 대체했다(DASHUI spec.md §88 "본 SPEC의 UI는 신규 작성이다", §128 "SHOWUI M4/M5 UI와의 병합 조정은 본 SPEC 범위 밖의 브랜치 전략 결정"). 두 세대를 한 화면에 병존시키는 병합을 시도했다가 되돌렸다 — 대체된 패널이 DASHUI가 이미 해결한 문제(익스큐터 주소 체계)를 낡은 전제로 다시 설명하는 문서 모순을 만들었기 때문. **최종본은 DASHUI-001의 대시보드 단일 세대**다. 아래 SHOWUI 기록은 그 시점의 사실로 보존하되, "패널 UI"·"v1 범위 축소" 서술은 현행 앱에 적용되지 않는다.
  - **살아남은 부분**: 아래 "프로토콜·서버" 항목(`server/web/panel.py`의 카탈로그/핀/membership + `panel_execute`/`panel_stop` 게이트 실행 경로)은 **폐기되지 않았다** — DASHUI-001의 대시보드 타일이 바로 그 경로를 재사용해 발화한다.
  - **패널 UI**(`ui/src/`): 카탈로그 자동 나열(시퀀스) + 채팅 연출 pin 타일, 그리드 순서는 핀 우선·append-only(정렬 없음). Live rail — running일 때만 amber 강조, 정지 상태는 조용함. **파괴적 발화-클래스**(All Off)는 arm→fire 정확히 2회 상호작용, **정지 클래스**(개별 Off)는 1회 press. `window.confirm` 0건 — 승인 카드/배너 패턴만 사용.
  - **프로토콜·서버**(`server/web/panel.py`, `server/web/messages.py`): 신규 WS 메시지 5종(`panel_execute`/`panel_stop`/`panel_pin`/`panel_unpin`/`panel_catalog_request`) + 이벤트 4종(`panel_catalog`/`panel_item_state`/`panel_busy`/`error(kind:"panel")`), 양측 allowlist 패리티(`PROTOCOL_VERSION` 1 유지). 실행은 전용 게이트 경유(`server/safety/` 단일 관문, OSC import 0건 — 아키텍처 테스트로 봉쇄), target은 parse-시점 정수 검증 + 카탈로그/핀 membership 검증(REQ-022) 후에만 번들 구성.
  - **핀 영속화**: `<user_data_dir>/panel_pins.json`, temp+`os.replace` 원자적 쓰기, credential-like 키 거부, 손상/부재 시 fail-open 빈 패널로 기동(다음 쓰기에서 재생성).
  - **fail-closed 재접속**: WS 단절 시 패널 running/busy 상태 소거(타일 목록은 서버 상태로 보존), 재접속 시 `panel_catalog_request`+`status_request` 재동기화 — running 자체는 의도적으로 재구축하지 않음(`PROTOCOL.md` 정정 반영).
  - **AC-SHOWUI-014 라이브 완결**: reply-port 드리프트(9005→9006) 유발 → HEALTH에서 실행 차단(감사 로그 `blocked` 기록, `kind=command` 0건) → UI 상단 오프라인 배너 + amber "⛔ 실행 차단됨" 패널 배너 + 핀 타일 비활성화(Go+/Off/ALL OFF 전부 disabled) — 9005 복귀 시 online 양성 대조까지 확인.
  - **실행기 타일 v1 범위 축소 → SPEC-COPILOT-EXECREF-001**: 드릴다운 실행기 타일(페이지 하위)을 카탈로그 소스에서 **구조적으로 제거**(fixtures 선례와 동일 패턴). 사유는 라이브 실측 — 콘솔 실번호가 `100 + i`(page-1, 8/8 실증)이고, i=101 타일이 무오류로 다른 오브젝트(Sequence 50)를 발화하는 silent wrong-object 충돌이 확인됨. 채팅 발화로 생성한 실행기 핀(정확한 콘솔 번호)은 영향 없음 — 정상 유지. 주소 수정(`console# = page*100 + i` 일반화) + 게이트 Executor 인식은 SPEC-COPILOT-EXECREF-001로 이연.
  - **테스트**: pytest 1591 passed(+onPC UDP 9005 점유로 인한 환경 실패 1건, 코드 회귀 아님) + vitest 176 passed. `panel.py` 커버리지 100%. `ruff check` 신규 0건.
  - amendment 이력: plan-audit iteration 1 FAIL 0.81 → v0.2.0 fold-in(F1~F6) → iteration 2 PASS 0.93 → v0.2.1 fix-forward(R1~R4) → sync-audit PASS 0.93(4-dim: Func 92/Sec 95/Craft 90/Consist 95).

- **SPEC-COPILOT-DEPLOY-001** — Stage-1 배포 가능한 arm64 macOS MVP 완료 (M1~M6 + M10, `status: in-progress` 유지 — Stage-2 M7~M9는 별도 kickoff로 이연되었으므로 아직 `completed`로 전환하지 않음).
  - **인앱 설정 + OS 자격 증명 저장**: `server/deploy/settings.py`(비민감 설정 — OSC 포트·플러그인 임포트 디렉터리·활성 프로바이더 — OS별 표준 사용자 config 경로 저장, 자격증명 유사 키 거부) + `server/deploy/keystore.py`(macOS Keychain/Windows Credential Manager 어댑터, 저장소 미가용/잠금/거부 시 평문 디스크 폴백 0 + 세션 한정 in-memory 폴백, 크래시/진단 덤프 env-scrub) + `server/web/settings_api.py`(`/api/settings`, `/api/keys` — 키 값은 절대 응답에 미포함) + `ui/src/components/SettingsPanel.tsx`.
  - **CopilotResponder provisioning**: `server/deploy/provisioning.py` + `server/web/provision_api.py`(`/api/provision/responder`) — 번들된 Lua responder(`console/lua/`)를 임포트 디렉터리로 설치 + onPC 로드/OSC 출력 포트 안내 UI(`ResponderGuide.tsx`); 앱이 직접 발행하는 `Import Plugin` 실행은 SPEC-COPILOT-MVP-001의 단일 안전 관문을 경유하고 감사 로그에 1:1 기록됨을 회귀 테스트로 증명(AC-DEPLOY-017).
  - **health/오류 UX**: 기존 HealthMonitor 3종 상태(online/console_offline/responder_degraded)에 원인+조치 한국어 안내 레이어(`healthGuidance`) 추가; 키 부재/무효는 기존 auth 오류 스크럽 경로로 raw SDK 원문 노출 없이 설정 UI로 유도.
  - **PyInstaller onedir 패키징**: `packaging/GrandMA3-Copilot.spec` + `entitlements.plist` + `sign.sh`(ad-hoc 기본, Developer ID 서명·notarization은 인증서 확보 시 코드 변경 없이 활성화) + `build.sh` — `dist/GrandMA3 Copilot.app`(arm64) 빌드 성공; `server/resources.py`가 frozen(`sys._MEIPASS`)/dev 자산 경로를 단일 리졸버로 통합.
  - **안전 불변식 보존 + local-only 검증**: 배포 셸을 감싼 구성에서도 단일 안전 관문 아키텍처·블랙리스트 승인 회귀·OSC 송신 표면 allowlist(fail-closed) 전부 green; `127.0.0.1` bind + localhost UDP + 원격 백엔드 미의존 + 오프라인 동작 확인.
  - **패키지 `.app` end-to-end 검증**: `packaging/verify_packaged_e2e.py` 8/8 PASS(기동→설정→provisioning→health→종료); 983 pytest + 기존 vitest 전부 green.
  - **환경-게이트 N/A**(이 빌드 호스트 한정, 코드 변경 없이 적합한 환경에서 활성화 가능): universal2(arm64+x86_64) 빌드, Windows x86_64 빌드, 실제 Developer-ID 서명·공증.
  - **Stage-2 이연**: Tauri v2 데스크톱 셸, Python 백엔드 sidecar 번들, 자동 업데이트, updater 재시작 안전상태 보존(M7~M9)은 별도 kickoff로 이연 — 본 항목은 Stage-1 배포 가능 MVP 마감만 반영한다.
  - **v0.3.0 라이브 E2E 하드닝(M14~M18, `status: in-progress` 유지)**: 2026-07-20 실제 grandMA3 onPC 2.4.2 + 실제 Gemini 라이브 데모에서 983-test 단위 스위트가 놓친 통합 결함 6건 중 5건(#2~#6)을 REQ-DEPLOY-028~032/AC-DEPLOY-019~023으로 fold-in 후 TDD 구현(결함 #1은 commit `1d65375`에서 이미 수정, 이력만 기록). 전체 스위트 `1017 passed`(983→1017, +34).
    - **M14 / #2 — 기동 시 활성 프로바이더 키 주입** (`server/web/serve.py` `build_runtime` + `server/deploy/keystore.py`): `build_runtime`이 OS 자격 증명 저장소의 활성 프로바이더 키를 프로바이더 클라이언트 생성 전에 프로세스 env로 주입; 기설정 env 키는 보존(덮어쓰지 않음); 저장소 미가용 시 REQ-DEPLOY-006a 세션 한정 폴백으로 강등. 신규 인스턴스가 "No API key"로 기동 실패하는 회귀를 봉쇄.
    - **M15 / #4 — 직전 생성 연출 상태 세션 추적**: 직전에 생성된 Seq/Exec를 세션에 캡처해 다음 턴에 주입 + "맹목적 수정보다 재생성" 유도 스티어를 추가, 후속 수정 지시가 엉뚱한 대상(예: `Seq 1`)이 아닌 방금 만든 대상으로 해소되도록 함.
    - **M16 / #5 — Gemini 캐시 만료 복구 + 키 오류 분류**: `403/404 "CachedContent not found"` 주입 시 캐시 재생성 후 1회 재시도; 키 누락 `ValueError`를 `unexpected`가 아닌 인증(auth) 오류로 분류.
    - **M17 / #3 — rig-context 실제 풀 번호 노출**: `get_rig_context`가 오브젝트마다 `{no, name}`을 방출해, 모델이 위치 인덱스가 아닌 실제(비연속 가능) 풀 번호를 참조하도록 함 — 존재하지 않는 오브젝트(예: `Group 3`) 생성 방지.
    - **M18 / #6 — OSC 수신 포트 동일-포트 재바인드 복구**: `SO_REUSEADDR` + 유한 재시도(포트 드리프트 없음, REQ-026)로 재바인드; 복구 불가 시 재설정 안내가 담긴 타입드 `ReceivePortInUseError`를 발생 — 이전에는 원시 크래시.
    - **테스트 인프라**: `server/tests/conftest.py`에 스위트 전역 in-memory keyring autouse fixture 추가 — M14가 실제 OS Keychain을 건드리지 않고 무인 `pytest` 실행을 복원.
    - **이연 항목**(진행 중 감사 추적, progress.md §E.4에 기록): (a) `#6` 앱 셸 안내 표시 — 브리지는 타입드 예외를 던지지만, `server/web`이 이를 잡아 표시하는 배선은 단일 OSC 관문 보안 불변식(AC-MVP-019, `server/web`의 `server.bridge` import 금지)과 상충해 보안 allowlist 결정 대기로 이연; (b) 5개 결함 전체의 라이브 onPC 재검증 — 단위 테스트가 배선을 증명하나 실하드웨어+실 Gemini 동작은 환경-게이트; (c) `console/lua/copilot_responder.lua:322`의 더 깊은 슬롯 정합성 — 진짜 갭 있는 rig에서 Lua 소스단 수정은 라이브-콘솔-게이트.
  - **v0.4.0 Stage-2 M7 — Tauri v2 데스크톱 셸(`status: in-progress` 유지, M8/M9 별도 kickoff 이연)**: Stage-1 PyInstaller onedir 백엔드를 sidecar로 재사용하는 네이티브 데스크톱 셸을 신규 구현(`src-tauri/`) — AC-DEPLOY-024~029 대응.
    - **M7.1 / AC-DEPLOY-025 — `/ws` Origin+per-launch-token 핸드셰이크** (`server/web/app.py`, `server/web/launcher.py`, `ui/src/useCopilotSocket.ts`): WebSocket `accept()` 이전에 Origin allowlist 대조 + `secrets.token_urlsafe` per-launch 토큰 상수-시간(`hmac.compare_digest`) 비교 게이트 삽입 — cross-site WebSocket hijacking(FEAS-9) 차단. 프로토콜 v1/`messages.py` 스키마 무변경.
    - **M7.2 / AC-DEPLOY-026 ③④ — 백엔드 parent-liveness watchdog** (`server/web/launcher.py`): Tauri force-quit 시(`RunEvent::Exit` 미발화) sidecar가 자기 그룹을 self-reap — PRIMARY(pipe/heartbeat EOF, 레이스 창 없음) + FALLBACK(`getppid()==1` 폴링, bounded ≤1s) 이중 트리거로 FEAS-5 Option C 백엔드 절반 완성.
    - **M7.3 / AC-DEPLOY-027 — SAFETY-2 교차언어 이중 스캔** (`server/tests/test_deploy_safety_invariants.py`, `packaging/verify_packaged_e2e.py`): Layer① `src-tauri/**/*.rs`+`Cargo.toml`/`Cargo.lock` deny-all 정적 스캔(부재/빈 트리 fail-closed, `files_scanned>0` assert) + Layer② 실제 설정 send_port wire-level 싱크(감사 로그 1:1 대조 + 양성 관측 assert + synthetic rogue 주입 증명) — 둘 다 vacuous-pass 차단 프루프 포함.
    - **M7.4a / AC-DEPLOY-024 — Tauri v2 셸 스캐폴드 + sidecar spawn** (`src-tauri/` 신규: `Cargo.toml`, `tauri.conf.json`, `src/main.rs`, `src/sidecar.rs`, `src/tray.rs`, `capabilities/default.json`): PyInstaller onedir 백엔드를 sidecar로 spawn, 네이티브 창이 동일 `ui/dist` 로드, 트레이+연결 상태 배지(M5 재사용), capabilities에서 sidecar-spawn 외 네트워크 플러그인 deny.
    - **M7.4b / AC-DEPLOY-025·026 ①② — Rust process-group kill + Stage-2 토큰 IPC** (`src-tauri/src/sidecar.rs`, `src-tauri/src/main.rs`): Unix setsid/setpgid 프로세스-그룹 kill을 authoritative teardown으로 배선(`RunEvent::Exit`), per-launch 토큰을 Tauri IPC로 웹뷰에 주입(디스크 미기록); 부수적으로 설정 영속화 부팅 수정(sidecar가 `settings.toml` 포트를 boot 시 실제로 준수) + 중복 로드 수정 + 허위 종료 다이얼로그 수정.
    - **M7.5 / AC-DEPLOY-027 Layer②** (`server/safety/console.py`, `server/safety/gate.py`, `packaging/wire_sink.py`): OSC 송신을 `DeploySend`/`ExecOutcome.sends`로 per-send 세분화해 wire-level 감사 1:1 대조 granularity를 1건 승인 의미론 보존한 채 완성.
    - **라이브 검증**: 패키지된 `.app`을 실제 grandMA3 onPC 2.4.2에 대해 실행 — health `online`, 백엔드가 설정된 UDP 9005에 바인드, 콘솔 피드백 왕복 확인. 이 라운드에서 발견·수정된 통합 결함(전부 수정 완료): sidecar 이름 해석 ENOENT, ready-line 레이스, declared-pid 신뢰 문제, 설정-무시-부팅(9000 바인드 vs 9005 설정), 문서 중복 로드, 종료 시 허위 "시작 실패" 다이얼로그, sidecar 부재 시 패닉(→ 가독 가능한 다이얼로그로 교체).
    - **환경-게이트 N/A**: Windows Job Object(KILL_ON_JOB_CLOSE) 경로, universal2 셸 빌드, 실제 notarization — 적합 환경 확보 시 코드 변경 없이 활성화.
    - **M8/M9 Stage-2-DEFERRED**: 자동 업데이트(M8)·코드 서명/공증(M9)은 본 kickoff 스코프 밖 — 별도 kickoff에서 AC 신설·검증. `status: in-progress` 유지.

- **SPEC-COPILOT-EVAL-001** — Phase 0 boardop 실사용 평가 및 격차 분석 완료 (문서 폴백 모드, REQ-EVAL-014). onPC 구동 실행 파일 미확보(베타 신청 대기)로 인해 문서·데모 영상 관찰 기반 평가로 전환(사용자 승인, AskUserQuestion 2026-07-16)하여 run-phase를 완결했다.
  - `.moai/project/research/boardop-eval-log.md` — 환경 기록(6종 중 5종 실측 + 1종 관찰 불가 명시) + 대표 시나리오 11종(S1~S10 + 파괴적 시나리오 S-D) 3태그 분류(관찰 기반 4 / 관찰 불가 7) 및 행별 근거 출처 인용.
  - `.moai/project/research/boardop-gap-analysis.md` — 4축(①한국어 UX ②안전장치 ③도구 커버리지 ④신뢰성) 전부 커버하는 격차 14건(Phase 1(MVP) 반영 11 / 보류 3 / 미반영 0) + FSL-1.1-Apache-2.0 라이선스 검토 결론(§3 — 내부 사용·평가 허용, 경쟁 제품 코드 재사용은 릴리스별 2년 제약, 아키텍처만 벤치마킹·clean-room 구현) + AC-EVAL-005 boardop 코드 미포함 4항목 이진 체크리스트 검수(§4, 전항목 0건).
  - AC-EVAL-001~006 전부 PASS(문서 폴백 경로), AC-EVAL-007(선택적 Claude/Gemini 비교)은 라이브 세션 미실행으로 평가 제외 — acceptance.md 기준 총 7개 AC 중 6개 평가·PASS, 1개 조건부 미평가.
  - amendment 이력: v0.2.0(LLM 듀얼 프로바이더 전략) → v0.3.0(문서 폴백 재조정, plan-audit delta FAIL 0.87) → v0.3.1(수정 후 delta re-audit PASS 0.95).

### Changed

- `.moai/project/tech.md` §5.1 — 단일 프로바이더(Anthropic 전용) 모델 전략 서술을 SPEC-COPILOT-MVP-001 REQ-MVP-038~041(프로바이더 교체 가능 추상화 계층, Claude/Gemini 듀얼 지원, 오류율 기반 기본 프로바이더 선정, 프로바이더별 캐싱) 반영으로 갱신하여 amended SPEC 상태와의 괴리를 해소.
