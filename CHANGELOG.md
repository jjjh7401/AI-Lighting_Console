# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- **SPEC-LDCLIMAX-001** — 정본 `docs/proposals/song-structure-lighting-standard.md` §6.1 이 규정한 코러스 찍는 액센트 일곱 수단 중 유일하게 구현이 없던 **색 스냅**이 기존 사다리(`zoom_pinch`/`blinder_or_flash`/`iris_pinch`[/`strobe_hit`])의 정규 회전 칸으로 편입됐고, §6 이 규정한 **절정 지속시간 상한**(최강 효과 1~4박, 백색 플래시 1~2박 뒤 통제된 룩으로 복귀)이 신설됐다. 계기는 큐 카드 t425(2026-09-20) — 두 결함 다 `server/looks/songcue.py` 를 직접 읽어 실측 확인했다(SPEC-LDACCENT-001, PR #472 병합 직후 `origin/main` `2258048b` 기준). 감독 결정(2026-09-20, 같은 날 2차 개정) — color_snap 은 기본값-거짓의 항상-꺼짐 스위치(`allow_color_snap`) 모델이 아니라, 기존 네 칸과 같은 자격으로 **무조건** 회전 후보에 포함되는 정규 칸이다.
  - **색 스냅이 회전 후보로 편입(REQ-LDCLIMAX-001~005)** — `_marking_accents`(`server/looks/songcue.py`)가 기존 칸 뒤 마지막 후보로 `color_snap`을 항상 붙인다(strobe를 붙이는 것과 같은 기법 — `_MARKING_ACCENTS` 상수 튜플 자체는 손대지 않는다). 새 셋째 판정 축(색 비교)이 `_accent_is_effective`에 추가되어, §7 "코러스 1의 색은 되돌아와야 한다"가 정한 팔레트 색 집합 밖의 색은 대상으로 고르지 않고(REQ-002), 직전 저장 큐와 색이 이미 같으면 무영향 칸으로 걸러진다(REQ-004, SPEC-LDACCENT-001의 무영향-칸 필터 재사용). 확정되면 그 큐의 페이드가 0으로 강제되고(REQ-003, 즉시 전환), 큐당 찍는 액센트 하나 규율(감독 결정 2026-09-12)은 색 스냅에도 그대로 적용된다(REQ-005). `songcue.py:109-110`의 옛 배제 독스트링("색 스냅도 뺀다 — §7 위반")은 이 SPEC의 정의(§7이 이미 요구하는 되돌아오는 색을 즉시 전환하는 것은 위반이 아니다)로 갱신했다.
  - **절정 지속시간 상한 — 복귀 큐 삽입(REQ-LDCLIMAX-006~011)** — 신설 후처리 패스 `_apply_climax_duration_cap`이 완성된 번들 직전에 `blinder_or_flash`(2박 상한)/`strobe_hit`(4박 상한)에만 적용된다(룩 자신의 값만 바꾸는 줌·아이리스·색 스냅에는 적용 안 함 — §6 밝기 예산이 이미 규율하는 축이라 별도 상한을 얹지 않는다). `beats_to_seconds`(`server/design/energy.py`)로 상한 시각을 계산해, 다음 자연 큐가 그 시각보다 늦게 오면 절정 큐의 사다리-오르기-전 기준 값을 재방출하는 복귀 큐를 삽입하고 후속 큐 번호를 1씩 민다(REQ-006~007). 자연 전환이 이미 상한을 지키면 삽입하지 않는 no-op이다(REQ-008). **BPM이 선언되지 않은 곡은 이 상한이 전혀 적용되지 않는다** — 반환 번들이 입력과 바이트 동일하다(REQ-009). 삽입은 조용히 끼워 넣지 않고 신규 `SongCueBundle.climax_returns` 필드(`SongCueClimaxReturn` 레코드)로 명시 보고한다(REQ-010, 기존 `SongCueWithheldMovement`류와 같은 "눈에 보이게 남긴다" 패턴) — 기존 큐의 값 결정 경로는 건드리지 않는다(REQ-011).
  - **`disable_color_snap` 임시 안전판(REQ-LDCLIMAX-012)** — 이 SPEC의 판정 수단은 로컬 pytest뿐이고 실기 콘솔 관측은 범위 밖이다. `build_songcue_bundle(..., disable_color_snap: bool = False)` 를 명시적으로 `True`로 넘기면 색 스냅만 후보에서 완전히 제외된다(다른 후보의 효과 판정에는 영향 없음). **이 스위치는 영구 설계가 아니다** — 첫 실기 콘솔 검증 세션까지의 임시 킬스위치이며, 그 세션이 결함 없이 통과하거나 발견된 결함이 후속 수정으로 닫히면 후속 커밋에서 제거한다.
  - **검증** — AC-LDCLIMAX-001~010 전부 PASS(REQ-001~012 PASS/FAIL 표는 `.moai/specs/SPEC-LDCLIMAX-001/progress.md` §E.2). 신규 테스트 13건 전량 RED→GREEN 확인(구현 전 `ImportError`/`TypeError`). 전체 회귀 `uv run pytest -q` → `13702 passed, 35 skipped`(착수 전 기준선 `13689 passed, 35 skipped` 대비 델타 +13, 회귀 0). SPEC-LDACCENT-001 골든 대조군(`TestHeadroomCaseIsUntouched`)과 `TestOneMarkingAccentPerCue`는 `disable_color_snap`을 넘기지 않고도(기본값 활성) 바이트 동일 유지 — 스위치가 꺼져서가 아니라 그 픽스처들이 회차 간 색이 고정이라 REQ-004(무영향 배제)가 매 회차 걸러내기 때문이다(실측으로 확인, 추측 아님). `ruff check`/`ruff format --check` 클린.
  - **범위 밖으로 남긴 것** — 정본 문서(§6/§6.1/§7) 문면 개정, 감독 인터뷰 경로(`AccentDecision`, 경로 A — 오늘 표시 전용, 무대 명령을 만들지 않음), `AccentDecision.hits` 필드 채우기, 줌·아이리스·색 스냅에 대한 별도 지속시간 상한, 실기 콘솔 육안 확인은 전부 이 SPEC의 범위 밖이다.

### Fixed

- **SPEC-LDRETURN-001** — SPEC-LDCLIMAX-001 이 신설한 절정 지속시간 상한의 복귀 큐 삽입이, 반복 룩(같은 `look_id`)이 다시 `blinder_or_flash`/`strobe_hit` 사다리 칸에 오르는 곡에서 구조적으로 거의 항상 실패하던 결함을 닫았다. 계기는 큐 카드 t427 — SPEC-LDCLIMAX-001 sync-auditor F4(2026-09-20, 브랜치 `WT-color-snap-climax@f6279509`) 후속. 원인: `blinder_or_flash`/`strobe_hit` 는 룩 자신의 값을 바꾸지 않으므로(`_rung_applied`, `server/looks/songcue.py:3259-3263`) 복귀 큐가 재방출하는 "사다리를 오르기 전 기준 값"은 같은 룩이 (경쟁 없이) 처음 저장됐을 때의 값 라인과 글자 그대로 같았고, `_guard_bundle_collision`(`songcue.py:2320`)이 이 충돌을 정확히 잡아 `SongCueBundleError` 를 던져 번들 조립 전체가 실패했다.
  - **복귀 큐 자신의 `Dimmer` 넛지(REQ-LDRETURN-001~005)** — 복귀 큐의 값 라인이 이미 번들 안(프로그래머 상태 명령 면제)에 있으면, `_yield_bundle`(`songcue.py:1950`)이 확립한 탐색 알고리즘을 재사용해 복귀 큐 자신의 `Dimmer` 를 `_HIT_STEP`(5) 단위로 `DARKNESS_FLOOR`(20) 쪽으로 반복 내리며 비충돌 값을 찾는다 — **단계 수 제한 없음**(감독 결정 2026-09-21, 옵션 A 채택: 절정 룩 고착이 사다리 회차와 정확히 같지 않은 값으로 복귀하는 것보다 나쁘다). 기존에 저장된 다른 큐(원본 절정 큐 포함)의 값은 바꾸지 않는다. `Dimmer` 축이 없거나 `DARKNESS_FLOOR` 까지 내려가도 비충돌 값을 못 찾으면 삽입을 유보하고 `SongCueWithheldClimaxReturn`(신규 레코드)으로 사유를 명시 보고한다(REQ-003/008) — 원래의 `SongCueClimaxReturn` 삽입 보고와 구분되는 별도 필드다.
  - **공개 진입점을 통한 성공 삽입 경로 확보(REQ-LDRETURN-006~007)** — `build_songcue_bundle(bpm=...)` 공개 진입점이 반복 룩 픽스처에서 `SongCueBundleError` 를 던지지 않고 성공적으로 번들을 반환한다는 것을 재는 시험을 신설해, sync-auditor F4 가 지적한 커버리지 공백("공개 진입점을 통한 삽입 성공을 재는 시험이 이 저장소 어디에도 없다")을 닫았다 — SPEC-LDCLIMAX-001 이 삽입 성공을 보이던 기존 시험은 전부 `_apply_climax_duration_cap` 을 직접 불러 가드를 우회하고 있었다. `bpm` 이 선언되지 않은 곡은 이 SPEC 이전과 바이트 동일하다(REQ-007, 회귀).
  - **검증** — AC-LDRETURN-001~008 전부 PASS(REQ-001~008 PASS/FAIL 표는 `.moai/specs/SPEC-LDRETURN-001/progress.md` §E.2). 전체 회귀 `uv run pytest -q` → `13707 passed, 35 skipped`(착수 전 기준선 `13705 passed, 35 skipped` 대비 델타 +2, 회귀 0). `ruff check`/`ruff format --check` 클린.
  - **프로세스 이탈 기록** — 이 SPEC 의 run-phase 는 PR 리뷰를 거치지 않고 `main` 에 직접 push 됐다(`38b76c19`, `3a700129`) — sync-audit 는 그 뒤 `main` 위에서 사후 수행됐다(sync-auditor 검증: Functionality 97 / Security 100 / Craft 90 / Consistency 75, PASS; CI `3a700129` conclusion `success`, run 35547648771). 정상적인 리뷰 경로가 아니었다는 점을 조용히 정상화하지 않고 기록으로 남긴다(`.moai/specs/SPEC-LDRETURN-001/progress.md` §E.4).
  - **범위 밖으로 남긴 것** — `bpm` 프로덕션 배선(`server/orchestrator/tools.py:3230`), `Dimmer` 외 축(Zoom/Iris/색)으로의 넛지 확장, 재번호 매기기의 교차 참조 갱신, 상대(rival) 값 넛지는 전부 이 SPEC 의 범위 밖이다.

- **SPEC-LDACCENT-001** — 찍는 액센트 사다리 회전이 무영향 칸(줌/아이리스 축이 없는 룩, 블라인더/스트로브 그룹이 없는 리그, §6 표에 없는 섹션 라벨)을 그대로 후보로 골라 `.ladder`에 실었던 결함을 닫았다. 카드 t424 — t382(PR #468) 자신의 판정 보고가 "effective-accent filter" 후속으로 예고했던 결함이다. 실측(FULL_RIG, 단일 축 룩, 후렴 7회): 무대 효과가 전혀 없는데도 `zoom_pinch`/`iris_pinch`/`blinder_or_flash`가 회차마다 사다리에 실렸다.
  - **효과-판정 필터(REQ-LDACCENT-001~003)** — 새 순수 함수 `_accent_is_effective`(`server/looks/songcue.py`)가 룩의 축 보유, 리그의 그룹 보유, 그 큐가 속한 섹션 라벨의 §6 행 보유(`intent_for_label`) 세 조건을 회전 이전에 판정한다. `_marking_accents`가 이 판정을 거쳐 회전 후보를 미리 좁힌 뒤 반환한다.
  - **건너뛰고 다음 후보로(REQ-LDACCENT-004)** — 회전이 무영향 칸을 가리켜도 같은 회전 순서 안에서 남은 유효 후보로 넘어간다 — 유효 후보가 있는 한 그 회차의 액센트 자체를 포기하지 않는다.
  - **유보 기록(REQ-LDACCENT-005~007)** — 유효 후보가 하나도 없으면 셀 이름을 지어내지 않고, 신규 `SongCueWithheldAccent`(`accent_withheld`/`withheld_accents`)가 어느 섹션·큐·사유로 못 채웠는지 담아 `SongCueBundle`/`SongCueSectionBundle`에 노출한다 — 기존 `withheld_movement`/`withheld_darkness`와 같은 보고 패턴.
  - **하나뿐인 액센트 불변식 유지(REQ-LDACCENT-008)** — 필터·유보 도입 뒤에도 큐당 찍는 액센트는 정확히 하나(감독 결정 2026-09-12) 그대로다.
  - **밝기 진행값은 바이트 동일** — `Dimmer` 값 진행(20/25/30/35/40/45/50)과 다른 속성 명령 바이트는 이 SPEC 전후로 변화 없음(AC-LDACCENT-004 골든 대조군). 달라지는 것은 `.ladder`의 셀 이름 보고와 신규 유보 기록뿐이다.
  - **검증** — AC-LDACCENT-001~006 전부 PASS. 전체 회귀 `uv run pytest -q` → `13689 passed, 35 skipped`(착수 전 기준선 `13685 passed, 35 skipped` 대비 회귀 0, 신규 통과 net +4). `ruff check`/`ruff format --check` 클린. subagent-boundary grep(`AskUserQuestion`) `server/looks/` 0건.
  - **범위 밖으로 남긴 것** — 밝기 구간이 이미 다른 큐로 소진된 네 번째 무영향 경로(`_unique_floor_climb` 소진)는 REQ-LDACCENT-007이 명시적 예외로 이름 붙여 미룬다. 실기 콘솔 육안 확인, 정본 문서(§6/§7.1) 개정, 회전 순서 자체 재설계도 이 SPEC의 범위 밖이다.

### Changed

- **SPEC-COPILOT-SONGCONFIRM-001** (제자리 개정, 카드 t417) — 배수 정정 무효 규칙을 REQ-003 규칙 3 / AC-003 (b) 로 명문화. 코드는 47da882(PR #445)에 이미 있었고 SPEC 이 한 조항 뒤져 있었다. 여유 상수 0.06 근거: 8곡 실측 오검출 0/8·검출 8/8(.moai/reports/d5-crowding-20260920/octave_fp.txt). 행동 변화 0, Tier M 상한 16/16 유지.

### Added

- **SPEC-COPILOT-COLORMODE-001** — 곡 인터뷰에 색 운용 방식을 확인하는 문항(Q2B_COLOR_USAGE)이 추가되어, 팔레트를 곡 전체에서 어떻게 쓸지가 감독의 명시적 확인을 거쳐 실제 팔레트 산출에 반영된다. 계기는 2026-09-13 조명감독 지시(「물론 곡과 조명감독의 스타일에 따라서 다르겠지. 그리고 조명감독의 확인을 받는게 좋을 것 같아.」, 카드 t404) — 기존 인터뷰(`DirectorInterview`)는 Q1(컨셉)→Q2(팔레트)→Q3(클라이맥스)→Q4(공간 스토리)→Q5(전환) 5문항 고정이었고, Q2에서 팔레트(어떤 색)는 확인하지만 그 색을 곡 전체에 걸쳐 어떻게 쓸지는 묻지 않았다. 선행 카드 t402(팔레트 회차 회전)/t403(악센트 웨이트 사다리)/t406(primary 우선 배치)는 모두 main 머지 완료 상태에서 시작.
  - **Q2B 색 운용 문항(REQ-COLORMODE-001~005)** — `STEP_ORDER`(`server/design/interview.py`)에 `Q2_PALETTE` 다음, `Q3_CLIMAX` 이전 `Q2B_COLOR_USAGE` 스텝을 삽입해 6단계 인터뷰로 확장했다. 카드는 정확히 3옵션: (기본, 첫 옵션) 「메인 컬러 중심 변조 + 임팩트에서 터뜨림」`"modulate"`, 「이 색 계열로만 간다」`"single"`, 「후렴마다 다른 포인트 색」`"per_chorus"`. 빈 답변은 첫 옵션(`"modulate"`)을 `confirmed=True`, 새 출처 상수 `SOURCE_DEFAULT_ACCEPTED`(`server/design/interview.py`)로 기록한다 — 다른 문항(Q1/Q3/Q4/Q5)의 빈 답변이 `confirmed=False`/`SOURCE_AUTO_DRAFT`로 재질의 카드를 만드는 기존 의미는 바뀌지 않는다(회귀 검증: AC-COLORMODE-016). 자유 텍스트는 단색/하나/only/single, 변조/기본/modulate/main, 후렴마다/포인트/per chorus/accent 키워드 그룹으로 해석되고, 어디에도 속하지 않으면 카드가 진행하지 않고 재질의한다(3회 상한 기존 경로 그대로).
  - **재시작 문구가 명시적 조회 테이블로 바뀜(REQ-COLORMODE-006/007/016)** — 기존 `"Q<N> 다시"`가 `STEP_ORDER[N-1]` 암묵 인덱싱에 의존했는데, Q2B 삽입으로 이 인덱싱이 깨지는 결함을 `_SONG_RESTART_STEP_BY_TOKEN`(`server/web/session.py`) 번호표 조회로 막았다. "Q3 다시"~"Q5 다시"는 삽입 이전과 동일하게 라우팅되고(회귀), "Q2B 다시" 또는 "색 운용 다시"가 신설되어 Q2B만 재확인받을 수 있다. "Q2 다시"는 `restart_from`의 접미 폐기 의미(spec.md D2)가 한 스텝 더 내려가 Q2B 답도 함께 폐기한다 — 팔레트가 바뀌면 그 팔레트를 어떻게 쓸지도 다시 확인받는 것이 맞다는 의도된 부수효과.
  - **`director_decisions` 축 + 분석 요약(REQ-COLORMODE-008~010)** — `DecisionAxis`/`_AXES`(`server/design/song_plan.py`)에 `"color_usage"`(`COLOR_USAGE_AXIS`)가 추가되어 타임라인 페이로드의 `director_decisions`에 `{step, axis: "color_usage", value, confirmed, source}` 항목이 실린다. default-accepted 경로는 재질의 카드를 만들지 않는다. 분석 요약(`ui/src/components/analysisSummary.ts` `buildColorLine`)이 이 축을 찾아 팔레트 문장 뒤에 운용 방식을 설명하는 한국어 절을 이어 붙이고, 출처가 default-accepted면 "(기본값 수용)" 표시를 붙인다.
  - **답이 실제 팔레트·큐 밀도에 반영(REQ-COLORMODE-011~015)** — `_section_palette_choice`(`server/web/session.py`)가 `color_usage` 파라미터를 받는다. `"modulate"`는 이 SPEC 이전 동작과 바이트 동일(회귀 fixture로 증명). `"single"`은 `palette_mode` 충돌 해소가 먼저 실행된 뒤 그 결과(base)를 `_arc_palette` 회전 없이 그대로 반환하며(`section_text` 직접 색상은 여전히 최우선), 출처 라벨은 `"section_single"`류로 구분된다. `"per_chorus"`는 연속된 두 후렴/피날레 회차가 같은 악센트를 갖지 않는 사다리(`_per_chorus_palette`)에서 색을 뽑되 `base[0]`(primary)는 항상 첫 칸에 유지하고, 후렴이 정확히 1개뿐이면 modulate와 동일한 산출을 낸다. 실측(sync-audit F5): `single`은 `_section_palette_sizes`를 `[2,2,2,2]`→`[1,1,1,1]`로 줄여 `_split_sections_for_density`의 실제 큐 개수를 5→3으로 줄이고, `per_chorus`는 색만 다르고 개수는 modulate와 동일하다 — 추측이 아니라 측정으로 확정.
  - **검증** — 16/16 AC 전부 PASS(`.moai/specs/SPEC-COPILOT-COLORMODE-001/progress.md` §E.2). 전체 회귀 `uv run pytest -q` → `13668 passed, 33 skipped, 0 failed`. UI `npm --prefix ui test -- --run` → `578/578 passed`. `npm --prefix ui run build` exit 0. `ruff check`/`ruff format --check` 클린. subagent-boundary grep(`AskUserQuestion`) 0건. sync-auditor 1차 감사는 FAIL이었다 — "Q3/Q4/Q5 다시" 및 "Q2B 다시"/"색 운용 다시"를 세션 레벨(`_song_run_interview` 실제 루프)이 아니라 인터뷰 엔진 API를 직접 호출하는 대체 시험으로 검증했던 결함을 뮤테이션 프로브로 확인(SURVIVED)하고, 세션-레벨 회귀 시험 5건 + 큐 밀도 시험 4건 + UI 결합 렌더링 시험 1건을 추가해 재감사 PASS(98/100/95/92)를 받았다.
  - **범위 밖으로 남긴 것** — `_per_chorus_palette` 사다리 색 순서가 실기 콘솔에서 실제로 다르게 보이는지는 육안 검증되지 않았다(감독 실기 확인 미실시). `SongTimeline.tsx`의 `DecisionRail` UI는 이미 축-무관 범용 렌더링이라 변경하지 않았다. Q2B에는 `pre_specified`(사전 지정) 옵션이 없다. 콘솔 송신 경로(`bundle_sender`)와 표준 10색 팔레트 확장은 이 SPEC의 대상이 아니다.
- **SPEC-LDRELEASE-001** — director apply 의 destination 예약 해제(release) 절차가 `partial` 로 끝난 원본 execution 에 한해 원자적으로 이전되도록 닫혔다. 근본 원인은 `ExecutionJournal._reserve_destination_locked`(`server/director/execution.py`)가 destination 예약을 **create-only** 로 영구 유지하고, `release_destination()`은 오늘 정확히 한 곳(`ApplyCoordinator.apply()` 의 송신-전 거부 분기)에서만 호출된다는 것 — `execute_bundles()`가 실제로 송신을 시도한 뒤 `partial`/`unknown` 으로 끝난 destination 은 저절로 풀리지 않아, `recovery_of` apply 가 원본이 점유한 슬롯을 재사용하지 못하고 새 슬롯(예: 9903→9904)에 써야 했다(`SPEC-LDSEND-001` M5 032 실기 관측, 2026-09-18, 큐 카드 t422). 이 SPEC 은 그 뒤에 남는 빈 자리만 채운다 — `SPEC-LDRECV-001`/`SPEC-LDSEND-001` 이 세운 재검사·직렬화·승인 소비 순서는 바꾸지 않는다.
  - **원자적 예약 이전(REQ-LDRELEASE-001)** — `_reserve_destination_locked` 에 `transfer_from` 파라미터를 추가했다. `recovery_of=<원본>` 을 실은 apply 가 그 원본이 점유한 destination 을 targeting 하고 원본 상태가 `partial` 이면, `begin_execution` 의 같은 transaction 안에서 예약을 원본에서 새 recovery execution 으로 원자적으로 이전하고 `TARGET_BUSY` 로 거부하지 않는다.
  - **`unknown` 원본은 거부(REQ-LDRELEASE-002)** — 원본 상태가 `unknown`(콘솔에 실제로 무엇이 도달했는지 불확실)이면 이전을 거부하고 `TARGET_BUSY` 를 반환한다 — 자동 이전 대상이 아니다.
  - **원본 행 불변(REQ-LDRELEASE-003)** — 이전이 일어나는 동안 원본 `executions` 행은 한 바이트도 바뀌지 않는다. `destination_reservations.reserved_by_execution_id`/`reserved_at` 만 바뀐다.
  - **전이 없는 경로는 오늘과 동일(REQ-LDRELEASE-004~006)** — `recovery_of` 없이 오거나 다른 destination 을 targeting 하면 기존 create-only 규칙(점유돼 있으면 무조건 `TARGET_BUSY`) 그대로다. 송신-전 거부(`cleared=False`) 도, 이전을 거치지 않은 apply 라면 오늘과 동일하게 즉시 `release_destination()` 이 호출된다.
  - **`recovery_required` 계약 문서화(REQ-LDRELEASE-007)** — `execute_bundles()` 의 `recovery_required` 플래그는 `partial` 결과(명시적 `failed` 포함)에 대해 오늘과 동일하게 `False` 로 남는다는 계약을 코드 변경 없이 characterization 회귀(`test_partial_with_explicit_failed_recovery_required_stays_false`)로 고정했다.
  - **전이-후-게이트-거부 복원(REQ-LDRELEASE-008)** — 예약을 넘겨받은 recovery apply 자체가 이어서 송신-전 거부되면, `ExecutionJournal.restore_destination()`(신규) 이 destination 을 완전 해제(`released`)하지 않고 원본 execution 에게 되돌린다(`reserved_by_execution_id`=원본, `status`=`reserved`로 유지) — 그래야 원본을 가리키는 다른 recovery 시도가 같은 슬롯을 다시 겨냥할 수 있다.
  - **검증** — AC-LDRELEASE-001~008 전부 PASS(9개 판정 명령, `9 passed`). 타겟 스위트(`test_director_execution_journal.py`/`test_director_ops_lifecycle.py`/`test_director_execution_failure.py`/`test_director_apply_rejection.py`) `73 passed`(baseline 65 → +8). 전량 스위트 `13619 passed, 35 skipped, 0 failed`(baseline 13611/35 대비 +8). `ruff check`/`ruff format --check` clean. sync-auditor 독립 재감사 PASS 91.8/100(harmonic mean, Functionality 95 · Security 95 · Craft 93 · Consistency 85).
  - **범위 밖으로 남긴 것** — 실기 콘솔에서 LDSEND-001 M5 032 시나리오(9903 재사용)를 재실행해 readback 으로 확인하는 것은 이 SPEC 의 로컬 판정 범위 밖이다 — 콘솔이 다음에 켜질 때 `Delete Sequence 9900/9901/9903/9904/9911` 정리와 함께 수행한다.

- **SPEC-LDWIRE-001** — director HTTP 층이 처음으로 실제 프로덕션 기동 경로에 배선됐다. `SPEC-LDRECV-001`·`SPEC-LDSEND-001`·`SPEC-LDSTORE-001`·`SPEC-LDCOMPILE-001` 네 SPEC 이 각자 완결한 조각(`DirectorApiDeps`/`build_director_router()`, `ApplyCoordinator`, `PipelineValidator`, `ContextSnapshot` 조립)이 실측(§1.1)으로 확인됐듯 `server/web/serve.py` `main()` 어디에서도 실제로 조립된 적이 없었다 — `WebDeps.director` 는 정의된 이후 오늘까지 기본값 `None` 그대로였고, director HTTP 표면(`/api/director/v1/...`) 전체가 배포된 앱에 마운트되지 않았다. 이 SPEC 은 그 조립만 다룬다 — 완료된 네 형제의 판단·검증 로직(승인 규칙·검증 규칙·apply 재검사·전송)은 바꾸지 않는다.
  - **운영 자격증명 발급(REQ-LDWIRE-001~003)** — `server/director/provision.py`(신규) `issue_operator_credential()` — 서버 부팅마다 `plan:apply` 를 포함한 human-only scope 를 가진 app-human `Credential` 을 새로 발급·등록한다(reissue-every-launch, 새 영속 스키마 없음). OS credential store 가 부팅 시점에 가용하지 않아도 기동을 중단하지 않고, 그 경우 모든 director route 는 기존 401 UNAUTHENTICATED fail-closed 경로로 응답한다.
  - **3축 실물 ContextProvider(REQ-LDWIRE-004/005)** — `RealContextProvider` — identity(콘솔 host/port)·expiry(정책 계산 TTL 윈도)·policy(SafetyRuleset 버전+규칙 목록) 세 축을 실제로 관측한다. 나머지 여섯 축(show/audio/group_membership/preset_content/compiler/capability)은 값을 지어내지 않고 `context.py` 의 정직한 미관측 상태(`unreadable`/`absent` 류)로 남긴다 — 이 정직한 부분관측 범위는 `@MX:DEBT`(`@MX:CEILING`+`@MX:UPGRADE`)로 코드에 명시했다.
  - **ValidationReport 영속화 + 실물 ValidationProvider(REQ-LDWIRE-006/007)** — `server/director/store.py`(EXTEND) + `server/director/migrations/004_validations.sql`(신규) — `validation_id` 로 `ValidationReport` 를 되찾는 저장소가 처음 생겼다(오늘까지 이 관계가 부재였다는 사실은 `store.py:38` 자신의 주석이 이미 예고했었다). `StoreValidationProvider` 는 부재·타 프로젝트 `validation_id` 를 정직하게 `NOT_FOUND` 로 거절한다.
  - **`DirectorApiDeps` 조립 + `serve.py` 기동 배선(REQ-LDWIRE-008~010)** — `build_director_deps()` 가 위 두 seam 과 이미 완료된 `ApplyCoordinator`/`ExecutionJournal`/`GateBundleSender` 를 하나의 `DirectorApiDeps` 로 묶고, `server/web/serve.py` 의 `WebDeps(...)` 호출에 `director=` 로 실제 주입한다. 단일-콘솔 배포 기준 프로젝트당 정확히 하나의 `DirectorApiDeps` 만 조립하며, `DirectorStore`/`ExecutionJournal` 의 `sqlite3.connect(check_same_thread=True)` 제약 때문에 FastAPI 이벤트루프 스레드에서만 호출되도록 배선했다(REQ-LDWIRE-010, thread-safety 확인됨 — `sqlite3.ProgrammingError` 0건).
  - **검증** — 전량 스위트 `13609 passed, 35 skipped, 0 failed`(이 워크트리 HEAD, 착수 전 기준선 13584/35 대비 +25). AC-LDWIRE-001~011 전부 PASS. director 범위 부분집합(`pytest -k director`) 728 passed. `provision.py` 커버리지 100%, `store.py` 99%, `director_api.py` 74%(신규 코드 미포함 — 기존 미배선 execution/feedback-proposals 503 분기만 미커버). 경계 grep: `server/director/`·`server/web/serve.py` 어디에도 `AskUserQuestion` 없음.
  - **범위 밖으로 남긴 것** — 6축 나머지 ContextProvider 관측 배선(후속 SPEC), `aud=director-mcp` MCP credential 발급(`SPEC-LDHOST-001` 소관), `ValidationProvider`/`ContextProvider` 의 의미 검사 확장.

- **SPEC-LDSEND-001** — `BundleSender` 프로토콜의 실물 구현과 로컬 관측 도구가 완결됐다(M1~M5). `SPEC-LDRECV-001`이 apply의 **거부 방향**(재검사·직렬화·journal)만 로컬로 닫고 실기 관측 없이 확정할 수 없다고 남긴 **승격 방향**(apply가 실제로 콘솔에 적용됐는가)을 잇는다 — 근본 원인은 `execute_bundles()`가 이미 있었지만 그 자리를 채우는 실물 송신기가 아직 없었다는 것 하나였다.
  - **M1(REQ-LDSEND-003·005·006·013·015)** — 인터페이스 확정. `server/orchestrator/ports.py`(EXTEND, `ExecutionResult.outcome` 필드) + `server/safety/gate.py`(EXTEND, `revoke_clearances()` — 호출 세션 자신의 클리어런스만 비움) + `server/director/execution.py`(EXTEND, 공유 함수 `run_director_apply()` 신설 — `ApplyCoordinator.apply()` → 전용 세션 바인딩 → `execute_bundles()` → 회수를 한 곳에 묶음) + `server/director/director_api.py`(EXTEND, `post_apply()`를 그 함수를 부르는 얇은 HTTP adapter로 교체, 행동 보존).
  - **M2(REQ-LDSEND-001·002·003·004)** — 실물 송신기. `server/orchestrator/bundle_sender.py`(신규, `GateBundleSender`) — 처음엔 `server/director/sender.py`로 만들었으나, `SPEC-LDSTORE-001`이 소유한 director 경계 시험(`test_director_boundary.py`)을 이름만 바꿔 우회한 결함을 사람 결정으로 되짚어 `server/orchestrator/` 로 옮겼다. 주입된 `execution_port.execute()`로만 명령을 보내고, 첫 미확인 결과 뒤 후속 명령을 중단한다.
  - **M3~M4(REQ-LDSEND-007~012·014)** — 로컬 관측 도구. `server/tools/director_apply_observe.py`(신규) — 콘솔 접근은 `build_console_stack()`으로만, apply 시나리오 실행은 공유 함수 `run_director_apply()`로만 수행하고 `director_api.py`(fastapi 의존)는 import하지 않는다. 자기 로컬 `DirectorStore`/`ApprovalRegistry` 인스턴스에서 공개 API로 진짜 `ApprovalBinding`을 발급해(`ldsend-observe-harness` 라벨) 스스로도 정당한 director-apply 호출자가 된다. 기본은 dry-run(세션 시작 백업 포함 콘솔 송신 0회), `--execute` 시나리오 021/024/032를 배선하고 destination 점유 확인·readback 재조회·백업 선행조건 관측·cleanup 명령 출력(전송은 사람이 직접)을 갖췄다. 무응답(`ConsoleSilentError`)을 "비어있음"으로 잘못 읽던 결함을 발견해 판독 불가(`exists=None`)로 정정했다.
  - **M4a·M5(onPC 실기 관측, 2026-09-18 사람 승인)** — AC-LDPLUGIN-021 승격부·024 관측부·032 실행부 세 콘솔 게이트 항목을 실기로 관측했다. AC-024/032의 실패 유발 명령은 `Store Sequence {n}`(점유된 destination에 맨몸 Store)으로 확정(2회 재현 일치) — 콘솔의 실제 거부 사유는 plan.md가 인용했던 `'Not allowed'`가 아니라 `User Canceled Command`였다(이 sync에서 정정). destination 존재/부재 응답 모양 확정(`ok:true`+`class: Sequence` / `ok:false` `path segment not found`), 백업 선행조건(REQ-014) 5회 전부 `ok`. 032 recovery가 원본과 다른 scratch destination에 쓰는 설계를 사람이 인정 — 원본 destination의 예약 해제 절차는 후속 카드 t422로 이관.
  - **검증** — 전량 스위트 `13584 passed, 35 skipped, 0 failed`(이 워크트리 HEAD, 착수 전 기준선 13515/35 대비 +69). sync-audit `.moai/reports/sync-audit/SPEC-LDSEND-001.md` PASS ≈91/100 — Functionality/Security must-pass 통과. F1(AC-LDSEND-006 역방향 시나리오 미재현)은 `test_run_director_apply.py`에 역방향 시험을 추가해 해소했다.
  - **범위 밖으로 남긴 것** — `server/web/serve.py` HTTP 라우팅·자격 발급 배선(카드 t421), `InterferenceDetector` 실물 구현, 032 recovery destination 예약 해제 절차(카드 t422), first-release go/no-go 판정(`SPEC-LDCERT-001`, 미작성).

- **SPEC-LDRECV-001** — Director 인증 receiver·사람 승인·실행·중재자 6개 마일스톤(M1~M6)이 전부 TDD로 완료됐다. `SPEC-LDPLUGIN-001`을 쪼갠 여섯 자식 중 이 SPEC이 소유한 8개 REQ/AC id(018-024, 032)가 전부 로컬로 닫혔다 — 승격부 3건(021·024·032 각각의 콘솔 관측 부분)만 실기 게이트로 남는다.
  - **M1(REQ-LDPLUGIN-018·019)** — 인증·ACL·Host/Origin 검증 + evidence 신뢰 경계·audio 외부 전송 동의 게이트. `server/director/auth.py`(신규) + `server/director/director_api.py`(신규, 계약 §3 공통 route HTTP 골격) + `server/web/app.py`(EXTEND, 조건부 router 등록). SQLite 스레드 친화성 위험을 발견해 7개 handler 전부 `async def`로 전환했다.
  - **M2(REQ-LDPLUGIN-020)** — 사람 승인/거절. `server/director/approvals.py`(신규, `ApprovalBinding` 12필드·만료 계산·무효화 판정·거절 오버레이) + `director_api.py`(EXTEND, `POST .../approvals`·`POST .../rejections`).
  - **M3(REQ-LDPLUGIN-021 §2.0-가·022)** — 공유 programmer 중재자. `server/safety/gate.py`(EXTEND, `execute_preapproved()` 신규 public 메서드) + `server/director/programmer_arbiter.py`(신규, `ProgrammerArbiter`). 기존 요구사항(REQ-SHOWUI-013, chat/panel 비busy 계약)과의 충돌을 발견해 Missing Inputs로 중단·사람 판단(옵션 1: panel을 중재자 범위에서 제외)을 거쳐 `server/web/panel.py`(EXTEND, `arbitrate=False`)로 해소했다.
  - **M4(REQ-LDPLUGIN-023)** — durable execution journal·idempotency. `server/director/execution.py`(신규, `ExecutionJournal`) + `server/director/migrations/002_execution_journal.sql`(신규). 같은 idempotency key+같은 request는 replay, 다른 request는 `IDEMPOTENCY_CONFLICT`(409). `isolation_level=None`에서 `with self._connection:`이 원자성을 보장하지 않는 결함을 발견해 `BEGIN IMMEDIATE` 기반 명시적 transaction으로 고쳤다.
  - **M5(REQ-LDPLUGIN-021·024)** — apply·실패 분류. `execution.py`(EXTEND, `ApplyCoordinator`+`execute_bundles()`) + `director_api.py`(EXTEND, `POST .../apply`). lock 안 재검사(승인 신선도→LiveLock→destination occupancy) 통과 후에만 `execute_preapproved()` 호출, k번째 실패/불확실 시 후속 bundle 미전송·`unknown`이 `partial`보다 우선.
  - **M6(REQ-LDPLUGIN-032)** — 운영 중단·recovery. `server/director/ops.py`(신규, `decommission_principal()`) + `execution.py`(EXTEND, `record_recovery_link`/`recovery_of`) + `server/director/migrations/003_ops_recovery.sql`(신규, `execution_recovery_links` 테이블). 인증 철회 후 신규 apply는 인증 단계에서 즉시 거부, journal은 삭제 API 없이 불변 보존, recovery는 별도 링크 테이블로만 원본과 연결.
  - **M1~M6 완료 후 다각도 검토(2026-09-17)** — apply 경로 핵심 안전 결함 7건(치명적 2건 포함)을 발견·수정했다: `compiled_digest` 미대조, 승인 재사용 가능, URL revision/plan_id 미검사, `TARGET_BUSY`가 `GATE_REJECTED`로 뭉개짐, 거부된 apply가 destination을 영구 점유. 기존 TDD 인프라를 확장해 신규 시험 10개로 고정했다.
  - **검증** — 전량 스위트 `13515 passed, 35 skipped, 0 failed`(이 워크트리 HEAD). ruff check/format clean.
  - **범위 밖으로 남긴 것** — `ValidationProvider`의 실제 저장소 구현(HTTP 노출)은 이 코드베이스에 아직 없다 — 후속 SPEC 소관. 콘솔 승격부 3건(AC-021·024·032 각 일부)은 실기 게이트가 남는다.

- **SPEC-LDSTORE-001** — 외부 `lighting-director` 플러그인이 만든 계획을 copilot이 받아 보관하고 근거를 되짚게 하는 저장층이 완결됐다(M1+M2+M3, `server/director/`). 이 층은 판단하지 않는다 — 스키마 검사·불변 저장·context 제공·검토된 지식 제공까지다. `SPEC-LDPLUGIN-001`을 쪼갠 여섯 자식 중 **의존 뿌리**로, 나머지 다섯이 소비하는 canonical plan/revision 자료구조와 `ContextSnapshot`을 제공한다.
  - **M1(REQ-LDPLUGIN-007·015)** — 교환 객체 strict parsing + 불변 저장·revision CAS·멱등. 계약 §9가 규정한 digest 값(`audio.sha256`·`context_digest`·`plan_digest`)을 정확히 재현했고, JCS(RFC 8785)를 stdlib 근사로 대체하지 않았다는 것을 회귀 시험으로 고정했다. `origin/main` `cde27445`로 머지.
  - **M2 — ContextSnapshot** — `server/director/context.py`(434줄), 9축 전부를 `context_digest`에 반영하도록 [HARD] 불변식으로 고정(자기 검토에서 발견한 안전 구멍 수정 — 4축만 반영했다면 preset 내용 변경 시 이전 승인이 유효하게 남는 상태였다). `cde27445`로 머지.
  - **M3(REQ-LDPLUGIN-027, 2026-09-16)** — design §3의 검토된 5주제(음악 근거 규칙·tracking 규칙·연출 case·rig·preset 규칙·적용·안전 규칙)를 `server/director/knowledge.py` + `knowledge_seed/`로 제공. `AC-LDPLUGIN-027`의 다섯 PASS 조건(주제 정확히 5개·bounded typed details·cursor staleness·미승인 지식 노출 0·"강제 법칙" 자기 라벨 금지) 전부를 시험 27개로 고정했다. feedback seed(scope binding)는 명시적으로 범위 밖 — `director-feedback` 패키지(우산 SPEC 소유)의 몫이다.
  - **독립 감사 없음** — `plan-auditor`는 이 저장소에서 착수 전 컨텍스트 초과(≈114K 토큰)로 죽는다(§F, 두 차례 재현 실측). 품질 판단은 전부 오케스트레이터 자기 검수다.
  - **검증** — 전량 스위트 `13376 passed, 35 skipped, 0 failed`(이 워크트리 HEAD). ruff check/format clean. `server/director/`에 `server.bridge.osc` import 없음, `songcue`/`_ARC_` 호출 없음(경계 grep).
  - **범위 밖으로 남긴 것** — HTTP 표면(`ld_submit_plan`/`ld_search_knowledge`의 `LDRECV` 몫), 의미 검사(계약 §11이 스키마로 보장되지 않는다고 열거한 것들), superseded 전이(승인 개념 필요, `LDRECV`와 함께), feedback seed·SQLite 영속화(M3는 정적 in-memory seed).

- **SPEC-LDCOMPILE-001** — Director 층 검증 파이프라인 6단(source identity → time/reference → tracking/FX → capability/fidelity → safety → approval freshness)이 전부 **실물 판정기**가 됐다(마일스톤 C1~C6, `server/director/validate/{pipeline,timing,simulate,conflict,capability,mib}.py` + `server/director/emit.py`). 이 층의 핵심 주장은 하나다 — 보존할 수 없으면 지원한다고 말하지 않는다. 착수 시점(2026-09-14 실측)엔 계약이 요구하는 축별 timing emitter 통로가 **0건**이었고, 이 SPEC 은 그 정직한 `blocking` 상태에서 출발해 실측된 것만 하나씩 열었다.
  - **C1~C5(검증 골격·시간 해석·simulation·충돌 검출·capability 거부·dark move/재진입 거부)** — 콘솔 없이 전부 로컬 pytest 로 닫힌다. AC-LDPLUGIN-014·011·010(계산부)·009(계산부)·008(거부부)·012(거부부)·013(거부부)이 여기서 초록이다. 전량 스위트 `13316 passed, 35 skipped, 0 failed`(C5 시점, `origin/main` HEAD `b3819fbf`).
  - **C6 — emitter 프로브 + 콘솔 관측(REQ-LDPLUGIN-013, `AC-LDPLUGIN-008/012/013` 콘솔 조건)** — `server/director/emit.py` 가 실측된 `CueFade` 만 낸다. 실기 콘솔 프로브로 `Set Cue <n> Sequence <seq> Property 'Preset2Fade'/'Preset2Delay' <값>` 문법(카드 t215 유래)을 재현해 **Preset2Fade/Preset2Delay 축 timing 쓰기를 관측했다** — 저장소 코드 판독으로는 0건이던 통로가 콘솔에서는 다른 이름으로 존재했다. **제한적 승격만 했다**: `AXIS_TIMING_OBSERVED = ("pan", "tilt")`는 pan==tilt(같은 값 요구)일 때만 재현 가능으로 인정하고, 값이 다르면 여전히 `unsupported`다 — 한 preset 값이 pan·tilt 를 가르지 못하는 콘솔 쪽 구조적 제약 때문이다. Position **값** 쓰기(fid·Attribute Pan/Tilt·Preset recall) 자체는 완전히 검증됐다(fid 501 실제 이동 확인, round 3/4). 전량 스위트 `13349 passed, 35 skipped, 0 failed`.
  - 🔴 **미해결 — 사람 판단이 필요한 후속 항목 (이 SPEC 이 해소했다고 주장하지 않는다)**: (1) **pan≠tilt 독립 timing** — `Preset2Fade`/`Preset2Delay` 는 CuePart 전체에 걸리는 값 하나뿐이라 두 축에 다른 값을 요구하는 요청은 여전히 재현 불가. (2) **`INDIVFADE`/`INDIVDELAY`/`IndividualTiming` enum 값** — 속성명은 유효하나(`Set…Property` 로 `ok:true`) 값 자체는 무효과(형제 속성 `PRESET2FADE`가 같은 경로에서 바뀌므로 경로 오류가 아니라 진짜 무효과)이며 `'On'` 은 `Illegal value` 로 거절됨 — 그랜드MA3 정식 문서 확인이 필요하다. (3) **`AC-LDPLUGIN-012`/`013` 의 P2 무대 관측(intensity=0 실측·terminal state 일치)** — 실기 콘솔에 물리로 접근 가능한 운영자 확인이 필요하며 이 SPEC 은 시도하지 않았다.
  - **경계 검사 전 마일스톤 통과**: OSC import 0 · 예술 producer import 0 · `server/lxseq`·`server/design`·`server/director/service.py` 무변경(형제 SPEC-LDSTORE-001 소유 seam 을 읽기만 함).
  - **독립 감사 없음** — `plan-auditor` 는 이 저장소에서 착수 전 컨텍스트 초과(≈114K 토큰)로 죽는다. 이 SPEC 의 품질 판단은 전부 orchestrator 자기 검수다.
  - **이 close 시점의 push 여부** — C1~C6 워크트리 커밋(`4c3ed064`·`50623f39`·`b3819fbf`·`e2593faf`)은 이미 PR #457~#460 로 머지됐다(`origin/main` 포함). 이 sync 커밋은 문서·frontmatter 전이만 다룬다.

### Changed

- **판정 하향 1건** — 「그룹 멤버십 판독은 플랫폼 성질상 원리적으로 불가」가 **미측정**으로 좁혀졌다. 그 판정이 내려진 표면에는 `introspect` 가 없었고 전제가 만료됐다(`SPEC-COPILOT-RESTORE-001/readability-survey.md` §A.2 → §A.5). 과거 릴리스 항목은 **고치지 않고 하향 표식만 덧붙였다**(감사 흔적 보존). 같은 단정이 저장소에 28곳 있으며 전수 분류표는 `overclaim-survey.md` 에 있다. **재측정은 그룹이 있는 쇼파일을 기다린다** — 결론(점유 슬롯 쓰기 금지)은 불변.

### Added

- **SPEC-COPILOT-CLASSIFYGAP-001** — 곡→콘솔 흐름이 실제로 쓰는 명령 네 개(`Store Sequence … /Merge`·`Store Cue`·`Store Group`·`Store Timecode`)가 분류 층에 하나도 안 걸리던 구멍을 4단계에 걸쳐 닫았다(칸반 카드 t299 P1~P3 + t325 P4, `blacklist.yaml` v4 → v8). 이전에는 이 명령들을 막는 방어가 호출 자리마다 손으로 붙인 `BatchRisk` 봉합뿐이었고, 실측(t321)으로는 열 자리 중 일곱이 그 봉합에만 의존했다.
  - **Phase 1** (PR #369, v4→v5) — `Store Group`·`Store Timecode` 를 폐집합에 추가.
  - **Phase 2** (PR #370, v5→v6) — `Store Sequence` 추가 + 그 확대가 드러낸 중복 카드 결함을 함께 닫음. `SEAL_DEFENCE` seal-only 자리 7 → 2.
  - **Phase 3** (PR #371, v6→v7) — `Store Cue` 추가로 이 SPEC 이 원래 목표한 네 명령을 전부 폐집합에 넣음.
  - **Phase 4** (PR #373, v7→v8, 카드 t325) — 남아 있던 `SEAL_DEFENCE` seal-only 두 자리(`_offer_fx_executor_assignment`·`_setlist_mode`)가 `Assign Sequence`·`Copy Sequence` 만 실어 나르던 것을 확인하고 두 오브젝트를 폐집합에 추가 — seal-only 2 → **0**.
  - **검증** — 전체 스위트 `12032 passed / 35 skipped / 0 failed`(sync-phase 재측정, main `197eb69`). 뮤테이션(신규 두 항목 되돌림) `9 failed / 12015 passed / 35 skipped` — 두 `SEAL_DEFENCE` 자리가 직접 죽는 것을 확인. AC-CG-001~010 전부 PASS, must-pass 넷(002·003·005·010) 포함.
  - **범위 밖으로 남긴 것** — `Store Page`·`Store Macro` 확대 비용 미측정(별도 후속 카드 필요), 실기(콘솔) 검증 0건(오프라인).

- **SPEC-COPILOT-SONGCONFIRM-001** — 곡 분석 확정이 이제 **닿는다**(Tier M, 칸반 카드 t273). MUSICSYNC-001 은 곡을 재고 확인 카드를 띄우고 BPM 을 확정하는 데까지 배달했지만, 사람이 체크한 구간은 버려졌고 확정 BPM 은 아무도 읽지 않았으며 `prepare_songcue` 는 세션을 볼 수 없었다(실브라우저 보고서 발견 3 + 같은 날 grep 실측). 이 회차는 기계가 아니라 **배관**을 고쳤다 — 새 통로·새 UI·새 카드 없이, 이미 있던 두 통로(세션 문맥 주입 · 도구 포트)로 확정을 모델과 도구에 잇는다.
  - **운영자가 할 수 있게 된 것** — 카드에서 체크를 풀고 「확인」을 누르면 고지가 BPM 문장 뒤에 「구간 N건 채택 · M건 제외. 이 곡의 큐 리스트를 만들려면 타임코드 번호와 함께 말씀해 주세요 — 확정한 구간을 그대로 씁니다.」라고 덧붙인다. 그다음 「이 곡으로 큐 리스트 만들어줘, 타임코드 5번」이라고 말하면 모델이 `prepare_songcue` 를 `sections` 없이 부르고 확정 구간이 그대로 큐가 된다 — 제외한 구간은 큐에 실리지 않는다. 다른 곡을 올리면 이전 확정은 무효가 되고, 기록이 있었을 때만 업로드 고지가 그 사실을 한 문장으로 말한다.
  - **① 답 판독과 불변 기록 (M1)** — `server/web/question.py` `parse_confirmed_sections`: UI 가 체크된 라벨을 `", "` 로 이어 되돌려 주므로 새 문법이 아니라 **라벨 왕복 대조**다(항목별 완전 일치 — 부분문자열 아님; 라벨이 하나도 없으면 전부 채택, 미응답·자유입력 표식이면 판정 없음). `section_label` 이 라벨의 단일 생산자(생산 호출자 3곳 → `@MX:ANCHOR` 1건 추가). `ConfirmedSongSection` · `ConfirmedSongAnalysis`(frozen dataclass — sha256 · 파일명 · 확정 시각 · `BpmResolution` · 구간 목록; 제외 구간도 `selected=False` 로 남는다). `server/web/session.py` `analyse_song_audio` 가 기록을 만들어 `_song_analysis` 에 들고 `song_analysis` 프로퍼티로 내준다 — 기록의 `bpm` 은 `song_bpm` 과 **같은 객체**(정본 하나). `upload_song_audio` 가 무효화한다.
  - **② 도달 (M2)** — 모델: `_session_context_note` 에 `Session context —` 문단 하나(파일명 · sha 앞 8자 · BPM · 채택 구간 전부 · 제외 개수, 그리고 `sections` 없이 `prepare_songcue` 를 부르되 `timecode_number` 는 운영자에게 받으라는 지시). `song_bpm` 프로퍼티의 첫 생산 판독자가 여기다. 도구: `server/orchestrator/tools.py` `SongAnalysisPort`(구조적 Protocol — `tools.py` 는 `server.web.session` 을 import 하지 않는다) + `_SongAnalysisView` 읽기 투과 뷰 + `build_toolset(song_analysis=…)`. `prepare_songcue` 는 `sections` 부재 + 기록 있음이면 채택 구간을 기본값으로 쓰고(중립 이름 `S<n>` · 시작 `m:ss.mmm` 손실 0 · dynamics = `d_level`) 결과에 `sections_source: "confirmed_analysis"` 를 싣는다. 명시 인자가 있으면 그것이 이기고 `sections_source: "explicit"` + `confirmed_analysis_mismatch`(개수 · 같은 자리 시작 시각 쌍 · 일치 여부)를 **보고**한다 — 오류가 아니다. 스키마 `required` 에서 `sections` 를 뺐다(`song_title` · `genre` · `timecode_number` 는 그대로 필수).
  - **하위 호환** — 기록이 없으면 전부 오늘과 바이트 동일: 오류 문자열 `'sections' must be a non-empty array of song sections` 그대로, 세션 노트 `None` 그대로, 미응답 갈래의 고지 그대로. 기존 시험 파일 5개 무수정. 콘솔 접촉 0(가짜 포트만). 보존 경계(`server/safety` · `server/audio` · `server/looks` · `server/design` · `ui/src` · `src-tauri`) diff 0행 · `timecode_slot_verdict` 변경 행 0(t270 소유, 건드리지 않았다).
  - **잰 것** — AC 16/16 PASS · FAIL 0. 전량 스위트 `11321 passed, 12 skipped`. `ruff check`/`format` 통과. 실브라우저 끝단 실측 1회(2026-09-06): WAV 업로드 → 카드(구간 3 채택 · 1 제외) → 「이 곡으로 큐 리스트 만들어줘 … 타임코드 5번」 → `prepare_songcue` 가 확정 구간 그대로 → 가짜 콘솔에 명령 26개(Sequence 4 큐 3개, TrigTime 0 / 15.952 / 36.014 + Timecode 5), 제외 구간 미반영 확인 — `reports/musicsync-e2e-song-to-cuelist-20260906.md`(주 체크아웃, git 미추적). 증거는 이 SPEC 디렉터리의 `progress.md` §E.2 · §E.3.
  - **열린 채로 두는 것** — 산문(「두 번째는 빼 줘」)은 구간 편집이 아니다(규칙 ② 전부 채택); 고치려면 다시 「분석」하거나 큐 요청 때 `sections` 를 명시한다. 큐 이름은 `S1 · S2 · S3` 중립 이름(룩 어휘 오인 방지) — 구간 이름 입력은 후속 카드 후보. 기록은 세션 수명이라 재시작·`history_restore` 로 되살아나지 않는다. 시트 `HEAD.BPM` 접합 · 인계 카드 생산 호출자는 MUSICSYNC-001 부채 그대로. 실기 콘솔 관측 0건(설계상).
- **SPEC-COPILOT-POOLEMPTY-001** — 타임코드가 **하나도 없는 쇼**에서 앱이 첫 타임코드를 영원히 만들지 못하던 결함을 고쳤다(Tier M, 칸반 카드 `t270`). 원인은 판정이 아니라 **채널**이었다: 응답기의 `M.safe_children` 은 「자식이 정말 없다」와 「자식을 못 읽었다」를 함수 **안에서는** 구별하면서 반환할 때 그 구별을 버렸고, 그래서 빈 풀과 죽은 풀이 `childCount 0` · `children []` · `ok true` 라는 **같은 페이로드**로 콘솔을 떠났다. 앱의 슬롯 판정은 그 페이로드를 받아 unknown 으로 닫을 수밖에 없었고, 타임코드 축을 통째로 내려놨다. 새 쇼는 언제나 타임코드가 0개이므로 이 결함은 **가장 흔한 상황에서만** 나타났다. **콘솔 쓰기 0** — 이 회차가 콘솔에 발화한 명령은 없다.
  - **① 응답기가 자기 열거를 말한다 (M1 · 1.6.4 → 1.6.5)** — `M.safe_children`(`console/lua/copilot_responder.lua`)이 항목 배열과 함께 **둘째 반환값** `"ok"` / `"failed"` 를 답하고, `build_snapshot` 이 그 값을 성공 회신의 `node.enumeration` 으로 싣는다. 새로 재는 것이 아니라 **이미 있던 정보를 내보내기만** 한 변경이다. 나머지 두 호출 지점(`find_child` · `set_plugin_source`)은 둘째 값을 받지 않아 Lua 가 버리므로 동작 불변이고, 마커는 배열 자체가 아니라 `node` 에 붙어 `#out` 과 `[i]` 순회도 그대로다. 와이어 버전 `"v": 1` 은 올리지 않았다 — `node` 에 키 하나를 더하는 **가산적** 변경이다. 규약은 `console/lua/PROTOCOL.md` §4.2 와 1.6.5 revision note 에 있다.
  - **② 앱은 마커가 「성공」이라 말할 때에만 완화한다 (M2)** — `timecode_slot_verdict`(`server/orchestrator/tools.py`)의 여섯 unknown 갈래 중 **`childCount == 0` 하나만** `node.enumeration == "ok"` 일 때 free 로 통과시킨다. 나머지 다섯(판독 예외 · 비-매핑 페이로드 · `truncated` · `childCount` 부재 · 짧은 열거)은 마커를 **읽지 않는다**. 🔴**하위 호환은 엄격한 방향으로만이다** — 마커가 없는 회신(1.6.5 미만 응답기)과 `"failed"` 마커는 오늘과 **바이트 동일한** unknown 판정과 **문구가 바뀌지 않은** 사유 문자열을 받는다. 완화가 아니라 조건부 해제다. 무조건 완화했다면 열거에 실패한 **살아 있는** 풀도 free 로 읽혀 `Store Timecode` 가 운영자의 쇼를 덮었을 것이다.
  - **③ 술어가 둘이던 것을 하나로 (REQ-016)** — 슬롯 판정은 툴셋 빌더 안의 **중첩 함수**라 임포트할 수 없었고, 그래서 `server/tools/musicsync_m3a_probe.py` 의 `slot_verdict` 는 같은 판정을 **재구현**하고 있었다. 두 술어가 갈리면 프로브의 판정이 앱 판정의 증거가 되지 못한다. 중첩 정의를 모듈 수준 `timecode_slot_verdict` 로 들어올리고(클로저가 밖에서 잡던 이름이 `SongCueTimingAxes` 하나뿐이라 폭발 반경이 함수 본문에 갇혔다), 프로브는 **호출만** 하는 어댑터가 됐다 — 판독 분기는 이제 프로브에 없다.
  - **④ 버전 핀 두 리터럴은 같은 커밋에서 움직였다** — `console/lua/copilot_responder.lua` 의 `VERSION` 과 `server/safety/responder_version.py` 의 `EXPECTED_RESPONDER_VERSION`(저장소 전체에서 **1행**). 검사가 Lua 파일에서 값을 읽어 대조하므로 한쪽만 올리면 빨강이 된다. 낮은 버전을 보고하는 응답기는 READBACK-002 가 세운 「응답기 버전 불일치 — 재임포트 필요」 갈래로 계속 막힌다 — 이 SPEC 은 그 분기를 **상속만** 했고 `server/safety/gate.py` 는 빈 diff 다.
  - **잰 것** — 오프라인 AC 13건(`AC-POOLEMPTY-001..012`·`015`) PASS · FAIL 0. 실제 `.lua` 를 lupa 로 구동해 빈 풀 `"ok"` / 두 접근자 모두 실패 `"failed"` / `Count`+`Ptr` 폴백 `"ok"` 를 각각 고정했고, 성공 회신 전수에서 마커 누락 0건을 셌다. 전량 스위트 `11298 passed, 12 skipped`. 프로브와 앱이 **같은 객체**임은 `is` 비교로, 어댑터가 갈리지 않았음은 12형상 판정 일치로 지켰다. 증거는 `.moai/specs/SPEC-COPILOT-POOLEMPTY-001/progress.md` §E.2·§E.3 에 있다.
  - ✅ **실기에서 확인됐다 (2026-09-06, M3) — 이 SPEC 은 닫혔다.** 운영자가 응답기 1.6.5 를 재임포트한 뒤(`responder_roundtrip --expect-version 1.6.5` → `live version=1.6.5` PASS), 빈 `DataPool/Timecodes` 의 raw 회신이 `{"childCount": 0, "enumeration": "ok", …}` 로 마커를 실어 왔고 슬롯 판정이 **free** 를 답했다. 같은 회차의 **음성 대조군**(열거되지 않는 경로)은 **unknown** 으로 닫혔다 — 대조군 없는 free 는 증거가 아니므로 두 팔을 함께 쐈고, 판정이 갈렸다는 것이 계기가 공허하지 않다는 증거다. 콘솔 쓰기 **0건**(ping 1 · state 조회 3). 감독의 `Delete Timecode 1` 이 `Illegal object` 로 거절된 것은 **풀이 이미 비어 있었다**는 뜻이며 이 회차가 원하던 조건 그 자체였다. 회차 노트 `docs/research/ma3-effects/16-poolempty-m3-live-verdict.md` · 원출력 `.moai/state/verify/t270-m3/verdict.json`. 대조군의 성격은 정직하게 적어 둔다 — 그것은 **판독-예외 갈래**이지 `enumeration: "failed"` 갈래가 아니며, 후자는 오프라인 lupa 검사가 덮는다.

- **SPEC-COPILOT-READBACK-001** — 되읽기를 값까지 밀어 본 회차다(Tier M, 칸반 카드 `table-serialize`). **답은 「값은 없다」였고, 그것이 이 SPEC 의 산출물이다.** 응답기가 테이블을 주소가 아니라 JSON 으로 직렬화하도록 고친 뒤(1.6.3 → **1.6.4**) 프리셋 개체의 프로퍼티를 전수로 훑었는데, 판독 가능한 **값 프로퍼티가 하나도 없었다**. 그래서 이 회차는 값 판독 기능을 만들지 않았다 — 없는 것 위에 기능을 얹으면 앱이 콘솔 대신 자기 팔레트를 보여 주는 지금의 결함이 한 겹 더 두꺼워질 뿐이다.
  - **① 응답기 테이블 직렬화 (M0 · R1)** — `M.safe_property` 가 테이블을 `tostring` 으로 넘겨 `table: 0x…` 라는 **주소**를 답하던 자리를 JSON 직렬화로 바꿨다. 오프라인에서는 실제 `.lua` 를 `lupa` 로 돌려 고정했고, 배포는 **재임포트 1회**(운영자·오케스트레이터 수행)로 끝냈다. 되돌리는 데 재임포트가 또 한 번 드는 결정이라 마일스톤 순서를 「쉬운 것부터」가 아니라 **「바꾸기 비싼 것부터」**로 잡았다.
  - **② 값 프로퍼티의 부재를 쟀다 (M2 · 읽기 전용)** — 프리셋 개체 프로퍼티 **138개 전수**(`total:138` · 도착 138 · 마지막 창 `truncated:false` · `paging:"complete"` — 세 값 정합). 판독 가능한 값 프로퍼티 **0건**. 🔴이 0 은 **「재지 못함」이 아니라 「없음」**이며, 그 구별은 음성 대조군이 세웠다 — `__NOSUCHPROP_CONTROL__` 이 `property not readable` 로 정확히 거절당했으므로 나머지 응답의 `ok` 는 계기 침묵이 아니다.
  - **③ 「1.6.4 가 답을 바꾼 자리」는 정확히 셋이다** — 1.6.3 에서 주소만 답하던 이름 중 `SELECTIONDATA`·`DEPENDENCIES`·`DOSHUFFLE` 셋만 JSON 으로 바뀌었고 나머지는 불변이다. 즉 직렬화 수정은 **의도한 표면만** 건드렸다.
  - **④ 클래스마다 다르다 — Preset 은 비었고 Part 는 차 있다 (M3)** — Preset 의 두 테이블은 `{}` 였지만 **Part 개체의 `DEPENDENCIES` 는 레코드 배열**이었다(`content_crc`·`key`·`name_crc`). Part 7개 표본에서 0~2건. 「테이블이 빈다」를 클래스 하나로 일반화하지 않으려고 표본을 넓힌 결과이고, **`SELECTIONDATA` 는 Part 에서도 전부 `{}`** 라 무엇이 이 축을 채우는지는 여전히 못 봤다.
  - 🔴 **소비자가 걸릴 두 가지를 미리 적어 둔다.** `content_crc`·`name_crc` 는 **부호 있는 64비트로 도착한다**(음수 실측). 그리고 빈 테이블이 `[]` 가 아니라 **`{}`** 로 오므로 `DEPENDENCIES` 를 항상 배열로 가정하면 빈 경우에 타입이 갈린다.
  - **팔레트 위장은 만들지 않았다** — `presets_api.py` 무변경. 콘솔 유래 값 필드를 신설할 근거가 측정으로 서지 않았으므로 신설하지 않았다. 없는 근거로 필드를 만들면 사용자는 앱이 저장해 둔 색을 콘솔 색으로 읽는다.
  - **잰 것** — AC 16건 중 PASS 10 · 단서부 PASS 3(`006` DEBT · `007` BLOCKER · `015` FINDING) · 판정 미기록 3(`008`·`011`·`012` — 미발견 경로라 공허 충족) · **FAIL 0**. 전량 스위트 `10992 passed, 12 skipped, 0 failed`. 라이브 계기는 `ping` 으로 직접 잰 `1.6.4`(main 문면을 배포 증거로 쓰지 않았다). 증거는 `.moai/specs/SPEC-COPILOT-READBACK-001/progress.md` §E.2·§E.3 과 `docs/research/ma3-effects/10-preset-property-readback-sweep.md` 에 있다.
  - 🔴 **콘솔 쓰기 셈을 정정한다 — 「이 회차가 콘솔에 아무것도 안 보냈다」는 거짓이다.** 측정 프로브의 쓰기는 **0건**이 맞고 정적·실측 두 갈래로 지켰다(`introspect_probe.py:139 attempt_session_backup=False` · 감사 로그 19행 전부 `introspect`/`props`, `backup` 0행). 그러나 **전량 스위트를 한 번 돌릴 때마다 `SaveShow` 가 2건 발사된다**(2/2 재현, 이 회차 2회 실행 = 4건, 전부 `ok:false`). 기전은 `build_console_stack` 의 `attempt_session_backup` 기본값이 `True` 라는 것이고, **어느 테스트인지는 특정하지 못했다.** 의도한 쓰기(재임포트 1회)와 스위트의 부수 발사는 다른 것이며, 후자를 셈에서 빼면 예산 보고가 거짓말이 된다.
  - 🔴 **열린 채로 출하하는 것들.** ⓐ `REFERENCES` 페이로드는 해독하지 않았고 Preset 회신에서 240자에 절단된다 · ⓑ Part·Preset 이외 클래스(Cue·Sequence·Group)의 테이블 형상 미관측 · ⓒ 의존 3건 이상인 Part 를 못 찾아 **절단은 산술이지 측정이 아니다**(87바이트/엔트리) · ⓓ 표본이 한 시퀀스(`Sequences/3`)에 몰려 있어 잰 넓이는 「Part 클래스」가 아니라 「이 시퀀스의 Part」다 · ⓔ 쇼 지문은 노화한다 — 재현할 때 경로 존재부터 다시 재라.

- **SPEC-COPILOT-MUSICSYNC-001** — 음악의 시간축을 앱에 들여왔다(Tier L, 마일스톤 넷). **시트가 이미 가진 시각을 잃지 않고 받고**(M1), **곡을 재서 숫자를 뽑되 확정은 사람에게 맡기고**(M2), **콘솔 타임코드는 어디까지 열리는지 먼저 재고**(M3-a) **녹화 동사는 앱이 쏘지 않고 운영자에게 넘긴다**(M3-b). 세 층의 콘솔 쓰기 권한이 서로 다르다는 것이 이 SPEC 의 뼈대이고, 그래서 마일스톤 순서를 「쉬운 것부터」가 아니라 **「되돌리기 비싼 것부터」**로 잡았다. AC 25건 중 PASS 22 · 단서부 PASS 1(`016` DEBT) · 판정행 미기록 2(`020`·`021` — 근거는 프로브 노트) · **FAIL 0**.
  - **① 시트 시간열이 큐의 발사 시각이 된다 (M1)** — `TC In`/`TC Out` 을 읽어 `TrigType`/`TrigTime` 두 줄을 **기존 승인 번들 하나에** 얹는다(승인 요청 추가 0건 · 조회 증가 0건). 판독 결과를 `int | None` 이 아니라 **다섯 갈래 종류**(`ok`·`preroll`·`needs_check`·`blank`·`malformed`)로 돌린 것이 이 마일스톤의 핵심이다 — `None` 은 언젠가 `or 0` 을 만나고, 그 순간 그 큐는 **첫 박에 발사된다**. 미확정 큐는 숫자를 아예 안 들고 두 줄도 안 나간다. PRE-ROLL 음수는 `TrigTime` 으로는 나가되 타임라인 투사에서는 **빠지고, 빠졌다고 말한다**(`TimestampedSection.start_ms` 의 `minimum=0` 계약을 완화하는 대신 소비자를 나눴다). 정본 곡파일 두 장 18큐 전량 회귀.
  - **② 곡을 재는 것은 DSP, 정하는 것은 사람 (M2)** — WAV/FLAC 최대 8 MiB 를 업로드하면 `server/audio/analyze.py` 가 BPM·구간 경계·온셋·RMS·D 등급을 내고, 그 숫자는 **확인 카드**(`QuestionRequest`, 스키마 무변경)로 사람에게 간다. LLM 이 숫자를 정하는 경로는 없다 — 카드 빌더 소스에 `provider`·`ask_user`·`llm` **0건**. BPM 우선순위는 확정 > 시트 > 기본값이고 `FX-Rate` 역산은 **대조 전용**(어느 분기에서도 채택되지 않는다 — 역산은 「사이클당 박수」를 모르면 일의적이지 않다). 분석 층의 **콘솔 접촉 0건**은 AST 스캔으로 기계 고정했고, 주입한 위반 4종이 전부 잡힌다.
  - **③ 재생 문법은 증명되지 않았고, 그래서 인계 목록에 없다 (M3-a · M3-b)** — 격리 슬롯에서 프로브 5건·조회 11회·쓰기 8건으로 잰 결과, `TrackGroup 1` 아래는 **열렸으나**(`childCount 2`, `MarkerTrack` + `Track`) 재생 후보 4종은 **효과가 관측되지 않았다**. 설계서 §5 의 **갈래 B** 다. 그래서 `Record Timecode` 는 앱이 쏘지 않고 `QuestionRequest.commands[]` 를 타고 운영자에게 간다(앱 발화 전수 grep **0건** · `blacklist.yaml` diff 0줄). 되읽기 검증은 **검증 1회당 조회 4회 상한**이 코드 상수로 잠겨 있고, 상한을 넘긴 조회는 **보내지 않고 거절**된다 — 예산을 넘겨야 나오는 답은 답이 아니라 무결론이기 때문이다.
  - **④ 판정 어휘에 `verified` 가 없다** — `songcue_timecode` 가 낼 수 있는 판정은 `unverified`·`SongCueTimingSkip`·`inconclusive` 셋뿐이다. 2026-09-05 실기 리허설(응답기 1.6.4 · 쓰기 0 · 조회 3/4)은 세 축이 전부 일치했는데도 판정이 **`unverified`** 로 남았다 — 풀 childCount 1→2, 이름 `MSYNCPROBE` 일치, `TrackGroup 1` 자식 2까지가 이 채널이 말할 수 있는 전부이고, **트랙 아래 이벤트 내용은 못 읽었다**. `truncated:true` 는 세 자리 어디에 넣어도 판정을 `inconclusive` 로 떨어뜨리며 산출물에 「전부 읽었다」가 0건이다.
  - 🔴 **소비자가 걸릴 자리를 미리 적어 둔다.** ⓐ **`build_timecode_handoff_card` 에 생산 호출자가 아직 없다** — 카드는 세워졌지만 세션 질문 채널로 띄우는 배선이 없어 지금은 tool payload 로만 노출된다(실측: 저장소 전수에서 호출자는 테스트뿐). ⓑ **`analyse_song_audio` 의 생산 호출자는 sync 대조에서 0건으로 드러나 같은 날 배선했다(#313)** — 새 프레임 `song_audio_analyse`(`app.py:585`)와 UI 「분석」 버튼이 분석 → 확인 카드 → BPM 확정 사슬을 잇는다. 발견 시점엔 업로드가 저장에서 멈춰 있었다. ⓒ **시트 `HEAD.BPM` → 분석 접합은 안 배선했다** — `analyse_song_audio` 가 `sheet_bpm` 을 인자로 받지만 M1 임포터의 값을 넘기는 호출자는 `tools.py` 에 있어야 하고 그 파일은 M2 의 PRESERVE 목록이었다. ⓓ **번들이 213.7 MB 커진다**(65,564 KiB → 274,240 KiB, `du -sk` 실측) — 판정선 300MB 미만이라 폴백은 선택되지 않았지만, 폴백 스위치 자체는 구현·검증돼 있다.
  - 🔴 **열린 채로 출하하는 것들.** ⓐ **B4** — 재생 명령의 효과는 이 채널로 미관측(갈래 B). ⓑ **B9** — 음수 `TrigTime` 을 콘솔이 받아들이는지는 **여전히 미측정**이다. M1 이 잰 것은 콘솔의 답이 아니라 **거절을 받았을 때의 우리 거동**이다. ⓒ **이벤트 내용 축** — 녹화된 이벤트가 계획과 일치한다는 주장은 하지 않는다. ⓓ **빈 타임코드 풀에서는 앱이 첫 타임코드를 못 만든다**(`_timecode_slot_verdict` 의 `childCount 0 → unknown`) — 이 회차는 감독이 손으로 `Timecode 1` 을 만든 뒤라 진행됐다. 규칙은 **안 건드렸다**(카드 t270). ⓔ **AC-MUSICSYNC-016 의 문구 절은 생산 지점에서만 판정했다** — 「BPM 미지정, 120 기본값」 문구는 `_build_q5` 의 중복 제거 루프에서 버려져 오늘 사용자 출력에 애초에 닿지 않는다(실측). 그 공백은 이 SPEC 범위 밖이라 고치지 않았다. ⓕ **분석기는 합성 트랙으로만 쟀다** — 구간 분절기가 에너지 기반이라 세기가 그대로인 음색·화성 전환은 못 잡는데, 합성 픽스처는 세기 계단으로 경계를 만들므로 이 한계가 픽스처에서는 드러나지 않는다. ⓖ **동결 앱 안에서 librosa 가 실제로 도는지는 안 쟀다** — 잰 것은 정적 수집과 `--self-check` 부팅까지다.
  - **잰 것** — 전량 스위트 `11257 passed, 12 skipped`(배선 #313 뒤 재측정), UI `506 passed`, `ruff check`/`format` 통과, `tsc` exit 0. 콘솔 예산: M1 승인 번들 밖 쓰기 0 · 조회 증가 0 · M2 쓰기 0 · 조회 0 · M3-a 쓰기 8/8 · 조회 11/12 · 프로브 5/5 · M3-b 오프라인 0/0, 실기 되읽기 쓰기 0 · 조회 3/4. 증거는 `.moai/specs/SPEC-COPILOT-MUSICSYNC-001/progress.md` §E.2·§E.3 과 `docs/research/ma3-effects/14-musicsync-m3a-timecode-probe-run2.md` · `15-musicsync-m3b-rehearsal-verify.md` 에 있다.

- **SPEC-COPILOT-READBACK-002** — 되읽기 채널이 **믿을 만한지**를 다룬다(Tier M, 칸반 카드 `readback002`). 두 구멍을 닫았다 — 채널이 자기 버전을 모르던 것과, 이름을 물으면 핸들로 답하던 것. **결함은 기계가 아니라 배관이었다**: 번역기도 버전을 싣는 응답기도 이미 옳게 동작했고, 고친 것은 **도착한 값을 버리는 자리**와 **이미 있는 인자를 안 넘기는 자리** 둘이다. **콘솔 쓰기 0** — 필요한 값은 이미 매 하트비트에 도착하고 있었고 타입명 표는 읽기 조회 하나로 얻는다.
  - **① 응답기 버전 게이트 (R3)** — `ConsoleLink.ping` 은 회신에 실려 온 `version` 을 **매 하트비트마다 버리고** `bool` 만 돌려줬다. 이 저장소에는 이미 live-1.6.1 / main-1.6.2 전례가 기록돼 있다 — 즉 「main 에 있다」는 「콘솔에 있다」가 아닌데, 그것을 구별할 수 있는 값이 도착해서 버려지고 있었다. 이제 링크가 그 값을 보존하고(`ConsolePort.ping()` 시그니처는 **안 넓혔다** — 모든 `FakeConsole` 로 파급되기 때문이다), 기대 버전은 `server/safety/responder_version.py` 의 **단일 상수** `EXPECTED_RESPONDER_VERSION = "1.6.3"` 이 소유한다. 테스트는 리터럴을 다시 적는 대신 `console/lua/copilot_responder.lua` 에서 값을 읽어 대조한다 — 두 출처가 조용히 갈릴 수 없다.
  - **② 낮은 버전과 미인식을 가른다** — 운영자가 취할 행동이 다르기 때문이다. 낮은 버전(`blocked_responder_version_mismatch`, 「응답기 버전 불일치 — 재임포트 필요」)은 **재임포트**로 끝나지만, 파싱 불가나 기대보다 **높은** 값(`blocked_responder_version_unrecognized`, 「응답기 버전 미인식 — 확인 필요」)은 무엇이 도는지 모른다는 뜻이므로 **조사**가 먼저다. 같은 배너에 담으면 사유를 거짓말한다 — **미인식 갈래는 재임포트를 권하지 않는다.** 두 상태는 `SafetyGate._check_health` 의 **분기로** 들어가 감사 기록 · `ScreenDecision` 형상 · 전송 시점 재검사를 **그대로 상속**하고, UI 라벨/가이던스(`ui/src/protocol.ts`)와 `server/web/PROTOCOL.md` 의 `health` 열거까지 따라갔다.
  - 🔴 **버전 부재(`None`)는 차단하지 않는다 — 의도된 구멍이다.** `pong` 에 `version` 이 없는 회신은 이 저장소의 오프라인 하네스가 **실제로 보내는 형태**다(2026-09-04 실측: `test_safety_console.py` 의 `_echo_send` 외 3파일). 이 채널로는 「버전 없는 응답기」와 「버전을 재지 않는 호출자」를 구별할 수 없고, **구별할 수 없는 것을 차단 사유로 쓰면 재지 않은 것을 잰 것처럼 보고하게 된다.** 그래서 열어 뒀다.
  - **③ 타입명 번역 배관 (R4) — 일곱 자리를 한 집합으로 고쳤다.** `read_inventory(..., type_names=)` kwarg 는 **이미 존재했고 이미 한 곳에서 옳게 쓰이고 있었다**(`tools.py:6356`). 나머지 **여섯**(`:3014, :3206, :3397, :4039, :4433, :4624`)이 그것을 빠뜨려, VWX diff 는 핸들을 도면 타입명과 fuzzy 대조하다 **모든 타입에서 `console_count=0`** 을 세고(리그에 86대가 있어도 「콘솔에 아무것도 없다」로 읽힌다) 패치 시트는 `FixtureType 12` 라는 핸들을 사람이 읽는 서류에 그대로 인쇄했다. **하나만 고치면 나머지가 같은 결함으로 남아 있다는 사실이 안 보이므로** 여섯을 한 집합으로 감사하고 한 집합으로 고쳤다. 계기는 줄 단위 grep 이 아니라 **`ast` 호출 계수**다 — 정본 폭 상한 100 이 다섯 자리를 여러 줄로 쪼개서 올바른 구현 뒤에도 `grep -c` 가 `2` 를 답한다(실측). 최종: `calls 7` · `with type_names 7`, 두 목록이 동일.
  - **④ 시트가 스스로 읽지 않는다** — `build_patch_sheet` 는 `type_names` 를 **매개변수로 받고** 표를 자기가 읽지 않는다. 읽기는 호출자(`server/web/paperwork_api.py` · `server/paperwork/bundle.py` · 시트 툴)로 밀려났다. 근거는 비준된 조회 예산 규칙이다 — 번역 헬퍼는 매핑이 아니라 **읽은 결과**를 받는다. 렌더러는 `fixture_type_untranslated` 를 **가시적으로 표면화**한다: 판독 경계에서 산 정직성이 인쇄 경계에서 버려지면 시트를 읽는 사람은 번역 실패와 번역 성공을 구별할 수 없다.
  - **⑤ 조회 증가분을 일곱 자리 전부에 계상했다** — 지점당 최대 **+1**(`read_fixture_type_names` 의 루트 목록 조회 1회)이 상한이고, 실측 결과 다섯 자리가 `+1`, 두 자리가 `+0` 이다. `+0` 인 두 자리는 **사유가 서로 다르다**: `_verify` 는 상위 `patch_fixtures` 가 진입부에서 한 번 읽은 표를 **나눠 쓰고**(한 핸들러의 두 자리를 한 번이 덮는다), `import_lxseq_patch` 는 애초에 수리 대상이 아니라 **원래부터 옳던 유일한 자리**다. 상한 초과는 **부정 대조군**(N+2 → FAIL)으로 지켰다.
  - **⑥ 번역 기계는 무변경이다** — `server/prechk/**` · `server/vwx/**` 0-diff(REQ-READBACK2-010). 계기 둘을 따로 걸었다: 커밋 이력(`git diff --stat`, 빈 출력)과 작업 트리(`diff -r`, `exit=0` · `.py` 파일 수 양쪽 28). **계기가 눈멀지 않았다는 대조군**으로 반드시 달라야 하는 `server/orchestrator` 에 같은 명령을 쏘아 `exit=1` 을 받았다 — 위 두 `exit=0` 은 실측된 무변경이지 계기의 침묵이 아니다.
  - **잰 것** — AC 16건 전건 PASS(M0 `AC-001..006` · M1 `AC-007..013b` · M2 `AC-014·015`), 뮤테이션 **13/13 RED**(M0 7 + M1 6), 전량 스위트 `10979 passed · 12 skipped · 0 failed`, UI `506 passed`, `ruff check`/`format` 통과, `tsc exit 0`. 콘솔 쓰기 **0**(`FakeConsole.executed` 오프라인 + 라이브 세션 미개설). 증거는 `.moai/specs/SPEC-COPILOT-READBACK-002/progress.md` §E.2·§E.3 과 `.moai/state/verify/readback002/pins-full.log` 에 있다.
  - 🔴 **미검증 3건은 열린 채 출하된다.** ⓐ **제3 핸들 형태** — 번역 방아쇠는 `^FixtureType (\d+)$` 이고, 여섯 소비자를 고쳐도 **미관측 제3 형태는 표식 없이 통과**한다(코드 자신이 그렇게 적어 뒀다). 이 SPEC 은 그 구멍을 **메우지 않고 명시**한다 — 「없다」가 아니라 **「안 쟀다」**. ⓑ **라이브 응답기 버전** — 이 SPEC 은 콘솔에 한 번도 접촉하지 않았다. 게이트가 실기에서 어느 버전을 보는지는 안 쟀다. ⓒ **원격 CI** — 브랜치가 원격에 없어 깨끗한 환경의 전수 실행을 관측하지 않았다. 위 수치는 전부 로컬 워크트리 실측이다.
  - **오케스트레이터 판정 2건** — M0·M1 이 각각 미해결 차단을 **자체 승인하지 않고 올렸고, 그 판단이 옳았다**. 안전 초크포인트 파일 집합 핀은 **감독 승인**으로(넓힌 값은 핀 자기 기준에서 재측정 — 배차서의 첫 측정이 `gate.py` 증가 2줄을 놓쳤고 **핀이 그것을 잡아냈다**), 비준된 조회 예산 등식 핀은 오케스트레이터 판정으로 해소했다. 등식은 **완화하지 않았다** — `<=` 로 바꾸거나 단언을 지우는 대신 `_TYPE_NAME_LIST_READS = 1` 을 신설해 **이름으로** 더했으므로 다음 독자가 늘어난 조회가 무엇인지 볼 수 있다. 실제 상한 `PRECHK_FOOTPRINT_QUERY_CAP == 40` 은 불변이다.

- **SPEC-COPILOT-RECVOBS-001** — 승격 헬퍼 `recv_frame` 의 **엄격함이 관측 가능해졌다**(칸반 카드 `t57`). **테스트 전용 변경이고 제품 코드는 0줄이다** — 서버·콘솔·UI 동작은 하나도 안 바뀌었고 사용자가 볼 수 있는 차이도 없다. 바뀐 파일은 `server/tests/conftest.py`(+47)와 `server/tests/test_ws_wait_guard.py`(+125/-5) 둘뿐이다.
  - **문제** — `recv_frame` 은 기대 밖 프레임을 **돌려주고** `drain_until` 은 **조용히 버린다**. 그런데 한 장만 읽는 자리는 행복 경로에서 두 헬퍼가 똑같이 동작해서, t54 가 옮긴 66자리 중 **1자리**에서만 헬퍼 선택이 뮤테이션으로 갈렸다. 나머지 65자리는 어느 쪽을 써도 초록이라 「옳게 골랐다」가 검사로 지켜지지 않았다.
  - **한 일** — `fire_status_listeners(deps)` 를 승격했다. 등록된 status 리스너를 타이머를 기다리지 않고 **지금** 부른다. 지어낸 경로가 아니라 `_heartbeat_loop`(`app.py:255-256`)이 도는 **같은 집합의 같은 콜러블**이고, 조건 없는 생산 호출자는 응답 진단 완료 콜백(`serve.py:438`)이다. 그 위에 관측 검사 4건을 세웠다 — 한 장 읽기 · 모아 읽기 · 음성 대조군 · 순서 결정성. 65자리는 **하나도 안 건드렸다.**
  - **잰 것** — 레버 치환 두 자리 다 **KILLED**, 퇴화 치환은 SURVIVED(치환 자체의 성질이지 검사의 결함이 아니다). 순서 20/20 을 3회, 음성 대조군 20/20. 전량 스위트 `10337 passed, 12 skipped, 0 failed`. 독립 감사 판정 **PASS-WITH-DEBT 86.4**, must-pass 2축 통과. 증거는 `.moai/reports/t57/` 에 있다.
  - 🔴 **65자리 부채는 안 갚았다.** 이 카드가 세운 것은 엄격함이 **한 번, 결정적으로** 관측된다는 사실뿐이다. 나머지 자리의 판정은 후속 카드 `t99` 로 넘어갔다 — `AC-RECVOBS-016`(close 시점 census 재실행 + 수집형 자리 연언 재판정)이 run 단계에서 발화해 「영구 폐쇄」 주장을 철회시킨 결과다.

- **SPEC-COPILOT-PARITY-001** — 가짜 콘솔 ↔ 실물 콘솔 괴리 **D2(핸들↔이름) 정합**과 번짐 범위 실측(Tier M, TDD, 칸반 카드 `t11`). 실기 콘솔은 픽스처의 `FixtureType` 프로퍼티로 타입 **이름**이 아니라 `FixtureType <슬롯>` 형태의 **객체 핸들**을 돌려주는데, 매퍼는 그 값을 이름과 직접 비교한다. 비교가 항상 어긋나 「이미 패치됨」 갈래가 실기에서 **한 번도 발화하지 않았고**, 재실행 86행이 `fid_occupied`(FID 충돌)로 잘못 분류됐다. 코드 변경은 `server/prechk/{inventory,mode_read}.py` · `server/orchestrator/tools.py` + 테스트 2파일이며, **봉쇄 구역(`console/lua` · `server/safety` · `server/vwx` · `server/paperwork` · `server/rulebook/assets` · `ui`) 0-diff**.
  - **① 판독 경계에서 정방향만 번역** — 슬롯→이름 대응표로 핸들을 이름으로 바꾼다. **역방향(이름→슬롯)은 하지 않는다**: 라이브 관측상 `Robin Spiider`가 슬롯 4와 12에 중복 등록돼 있어 역방향은 모호하지만, **정방향은 두 슬롯이 같은 이름을 내므로 그 모호성에 걸리지 않는다**(전용 테스트로 고정). 값이 이미 이름이면 번역은 항등이며, 라이브러리에 **없는 이름도 그대로 통과한다** — 번역기는 이름을 검증하는 자리가 아니다.
  - **② 추측 금지 · 미번역 사유를 구조화 필드로** — 대응표를 못 얻었거나 핸들의 슬롯이 표에 없으면 **추측한 이름으로 채우지 않는다.** 원값을 보존하고 사유를 남긴다(`no_type_table` / `slot_absent`). 두 사유를 가르는 이유는 전자가 **고칠 수 있는 순서 결함**(그 경로가 트리를 안 읽었다)이고 후자가 **리그 사실**(트리를 읽었는데 그 슬롯이 없다)이기 때문이다 — 한 표식으로 뭉치면 진단이 되지 않는다.
  - **③ 판독을 호출자가 넘긴다** — `read_inventory`는 타입 트리를 **스스로 읽지 않고** 호출자가 넘긴 표만 쓴다. **정정: 「질의 순증 0」이라고 적었으나 툴 수준에서는 참이 아니다** — 툴이 호출당 타입 트리를 1회 더 읽고, `read_type_mode_widths` 가 확정 타입 수만큼 **같은 루트를 이미 읽고 쌍까지 만든 뒤 버린다**(`mode_read.py`). 순증 0 이 성립하는 것은 **질의 예산 가드가 사는 `prechk` 경로**까지이며, 진짜 판독 공유는 후속 재료다. 중간 설계였던 「핸들이 보일 때만 트리를 읽는」 지연 판독은 `server/prechk`의 **질의 예산 가드 5건을 RED로 만들어 폐기**했다. 단정을 넓히는 대신 설계를 바꿨고, 가드 5건은 **원래 수치 그대로** 초록이다(`git diff --name-only 5c988bd..aabcdde -- server/tests/test_prechk_tool.py` → 빈 출력 — 「안 넓혔다」가 기계적으로 참이다). 대가는 **순서 의존**이며, 트리를 안 읽은 경로는 표가 없어 번역이 조용히 사라지는 대신 `no_type_table`로 표시된다(전용 테스트 + 뮤테이션으로 고정).
  - **관측 — 「정방향을 넣으면 초록이 된다」는 목표였지 전제가 아니었다.** SPEC이 앞머리에 「아무도 관측한 적이 없다」로 못박은 항목이며, 이 카드가 그 관측을 만들었다: 번역 전 `Counter({'fid_occupied': 86})` → 후 `Counter({'already_patched': 86})`, occupant의 타입 값이 `'FixtureType 1'` → `'ETC S4 LED S3 Lustr X8'`. 기본값(이름) 경로는 불변.
  - **뮤테이션 4종 전부 KILLED(최종 코드 기준)** — 설계가 바뀌었으므로 1차 결과는 폐기하고 다시 돌렸다. 번역기 항등화 · 표 부재 조용히 통과 · 대응표 미공급 · 슬롯 부재 조용히 통과. **표 부재 뮤테이션은 순서-의존 테스트가, 슬롯 부재 뮤테이션은 미번역 표식 테스트가 각각 「한 건만」 죽였다** — 그 테스트가 그 성질의 유일한 판별자라는 뜻이다. 복구는 초록이 아니라 `shasum -a 256 -c` 와 `grep -c "MUT-" → 0`으로 증명했다.
  - **번역 실패 사유를 다섯 갈래로 가른다(독립 감사 D-1 · N-1 수정)** — 초판은 사유가 둘이라, **타입 트리 판독이 실패한 경우까지 「그 슬롯이 라이브러리에 없다」(`slot_absent`)로 보고**했다. 그 라벨을 읽은 사람은 리그를 손보러 가지만 실제로 필요한 것은 **재조회**다 — 이 카드가 다루는 결함이 정확히 「라벨이 틀려 사람을 엉뚱한 곳으로 보낸다」인데 수정 자체가 같은 실수를 했다. 이제 **다섯**으로 가른다 — `no_type_table`(이 경로가 트리를 아예 안 읽었다 — 호출자 결함) · `type_tree_unreadable`(읽었으나 콘솔이 답하지 않았다 — 재조회할 자리) · `slot_absent`(트리가 **전수** 답했고 그 슬롯이 없다 — **유일한 리그 사실**) · `slot_unseen_listing_unconfirmed`(목록이 **전수임을 확인할 수 없어**(절단 · 자식 0개 · 부분집합) 그 슬롯을 못 봤다) · `listing_shape_invalid`(트리가 답했으나 쓸 수 있는 항목을 하나도 열거하지 못했다 — 고칠 곳은 리그가 아니라 판독 쪽이다). **뒤의 셋은 모두 「목록을 믿을 수 없다」는 뜻이고, 어느 하나라도 `slot_absent` 로 뭉치면 사람을 리그로 보낸다.** 넷째는 커밋 전 자체 점검에서, 다섯째는 독립 감사가 배선 7갈래를 실측해 찾았다 — 이 저장소는 이미 「절단이 무효화하는 것은 부정 결론뿐이다」를 규약으로 갖고 있고(결함 D1 계열), `slot_absent` 가 바로 그 부정 결론이다. 이 열거는 **실측된 갈래에 근거하며**, 여섯째는 새 측정이 있을 때 세운다. 판독 경계는 이제 **표가 아니라 판독 결과 자체를 받아** 이 구분을 호출자가 버릴 수 없게 한다(빈 표만 넘기면 「못 읽었다」와 「없다」가 구별되지 않는다).
  - **⚠️ AC 전건 통과가 실기 D2 해소의 증거는 아니다** — 이 SPEC에는 **실기 AC가 하나도 없다**(설계이지 누락이 아니다; 카드를 콘솔 일정에 묶지 않으려고 실기 항목을 후속으로 밀었다). 오프라인이 보증하는 것은 **「핸들이 들어오는 갈래에서 코드가 이름으로 번역한다」**까지이며, **「실기 콘솔이 실제로 그 형식의 핸들을 준다」는 가정으로 남는다.** 핸들 형식 `FixtureType <슬롯>`은 라이브 관측(한 번의 판독에서 86행·8개 슬롯이 모두 같은 형식)에 근거하나 **다른 클래스·다른 판독 경로의 형식은 재지 않았다.** 형식이 다르면 번역이 실패해 미번역 경로로 떨어진다 — 조용히 틀리지는 않으나 **D2는 닫히지 않는다.**
  - **⚠️ 번짐 3건은 열린 채 출하된다** — 같은 값을 소비하는 13자리(C1~C13)를 실측해 분류했고(측정 11 · 미측정 2), **3자리가 실제로 어긋난다.** 원인은 하나로 보인다 — `read_inventory` 호출 **8곳 중 대응표를 넘기는 것은 lxseq 경로 1곳뿐**이라 나머지는 핸들을 그대로 받는다. **다만 「표를 넘기면 함께 닫힌다」는 측정하지 않았다.**
    - **`server/vwx/diff.py` 수량 대조 — 감독에게 거짓 경보를 낸다.** 콘솔 타입 값이 설계 타입명과 fuzzy 대조되는데 핸들은 절대 매치되지 않아(`fuzzy_type_equal('Source 4 LED…','FixtureType 10')` → `False`) **모든 타입이 `console_count=0`으로 보고된다.** 실기에 86대가 있어도 「콘솔에 아무것도 없다」로 읽힌다.
    - **`server/paperwork` 패치 시트 — 사람이 읽는 서류에 핸들이 인쇄된다.** 렌더 실측: `['1','KEY 101','1','1','FixtureType 10','Mode 1']`.
    - 나머지 자리는 **어긋나지 않음을 실측으로 확인**했다. 특히 `server/vwx/apply.py`는 `_TYPE_DISPLAY_INDEX`로 **핸들 형태를 진작 처리하고 있었다** — 그 정규식은 이번에 `server/prechk/inventory.py`에 추가한 것과 **바이트 동일**하며, 이 **모듈 간 지식 불균형**(형식이 드리프트하면 두 곳을 따로 고쳐야 하고 한쪽만 고치면 조용히 갈린다)은 이번 카드에서 고치지 않고 후속 재료로 남긴다.
  - **부수 발견 — 가짜 콘솔끼리 서로 다른 콘솔을 모델링하고 있었다.** `server/tests/test_prechk_tool.py`의 가짜는 `"FixtureType 1"`(핸들, 실물 충실)을, `test_lxseq_tool.py`의 가짜는 타입 **이름**을 돌려준다. **D2가 lxseq에서만 안 보인 이유가 이것이다 — 옆 모듈의 가짜는 진작 옳았다.** 이 대조는 **실기 없이 오프라인으로 가능**하며, 이번 카드가 그 축을 한 건 실증했다.
  - **검증(sync 세션 직접 재측정)** — `uv run pytest server/tests -q` → **9709 passed · 8 skipped · 1 warning**(143.25s), 실패 0. 손대기 전 기준선 **9691**(코드 변경 0행 상태에서 측정) 대비 **+18 = 신규 테스트 수와 정확히 일치**(M1 12건 + 신설 판독의 오류 갈래 2건 + 감사 D-1 회귀 1건 + 절단 회귀 1건 + 감사 N-1 회귀 1건 + 그 대조군 1건). UI 스위트 **496 passed 불변**. `server/prechk` 커버리지 `TOTAL 1141 36 97%`. ⚠️ **이 저장소에는 테스트 CI가 없다** — 전체 로컬 실행이 유일한 회귀 증거다.

- **SPEC-COPILOT-LXSEQ-001** — LX-SEQ 연계 **1단계(패치)**: 조명감독 산출물인 RIG 팩 **패치 CSV 한 장**을 읽어 grandMA3 onPC에 픽스처를 패치한다(Tier M, TDD, 칸반 카드 `t9`). 신규 코드는 **파서 + 매퍼 + 툴 등재 1종**뿐이고, 콘솔 조작·안전 가드·Lua 생성·실행·재조회는 전부 기존 `resolve_fixture_type` → `patch_fixtures` 경로를 그대로 쓴다. **기존 툴 계약은 0-diff** — `git diff 453846a..HEAD -- server/orchestrator/tools.py`의 삭제 줄 수가 0(순수 추가)이고, 봉쇄 구역(`console/lua` · `server/safety` · `server/prechk` · `server/vwx` · `server/paperwork` · `server/rulebook/assets`) 대조는 빈 출력이다. 툴 33종 → **34종**.
  - **① 파서 `server/lxseq/parser.py`** — 9열(`FID,Group,FixtureType,Mode,Ch,Universe,Address,AddrRange,Position`)을 열 이름으로 찾고(순서 뒤섞임·BOM 흡수), 실물 86행을 거부 0·제외 0으로 읽는다. 거부 부류는 `non_integer_field` · `universe_overflow` · `address_out_of_range` · `addr_range_mismatch` · `duplicate_fid` · `address_overlap_in_file` **6종**이며, 중복·겹침은 **관여한 행 전부**를 거부한다. `Ch` = 0 은 **거부가 아니다** — DMX 를 점유하지 않는 수동 운용 장비 등은 `zero_channels` **제외행**으로 따로 센다(정본 `spec.md` REQ-LXSEQ-002). **FID는 주소가 아니다** — FID를 3종으로 오프셋해도 계획된 자리 집합이 불변임을 단언하고, 소스에 FID를 주소 산술에 쓰는 표현이 없는지 AST로 훑는다(스캔 함수 자체의 비공허성 테스트 동반).
  - **② 매퍼 `server/lxseq/mapper.py`** — 타입 이름은 **추측하지 않고** `resolve_fixture_type`에 묻는다(서로 다른 타입 8종에 8회, 행 86회가 아니다). 모드는 **콘솔에서 실측한 폭**으로 확정하고, 폭이 동률이면 라벨 토큰으로, 둘 다 실패하면 그 타입의 행을 만들지 않고 `mode_unresolved`로 건너뛴다 — 질문 카드를 띄우지 않고 목록을 사용자에게 돌려주어 `mode_overrides`로 다시 부르게 한다. 점유 판정은 자리 → FID 순서로 보아 `address_occupied` · `already_patched` · `fid_occupied`를 가르며, **어느 경우에도 덮어쓰지 않는다.** 런은 타입·모드·유니버스·Group·주소 연속성으로 갈린다(기본 `group` 모드 12런, `type` 모드 9런, 둘 다 86대).
  - **③ 툴 `import_lxseq_patch`** — 기본 `action='preview'`는 **콘솔에 아무것도 쓰지 않고**, `apply`는 런을 순서대로 `patch_fixtures`에 위임한다. **바이트는 파일에서만 온다** — 툴 설명문·인자 설명·응답 `guidance` 3중으로 "채팅에 붙여넣은 CSV 본문을 base64로 만들지 마라"를 싣고(개행·공백이 조용히 깨져 잘못된 자리에 패치되며 이 앱에 실행 취소는 없다), 받은 바이트의 sha256과 길이를 페이로드에 실어 원본 대조가 가능하다. 한 런이 `created`가 아니면 그 자리에서 멈추고 나머지는 `not_attempted`로 보고한다 — **자동 재시도는 없다**(중복 리그가 생긴다). 같은 파일을 다시 넣으면 런 0개로 끝난다.
  - **④ 실기 확인(M4) — 감독 onPC 2.4.2에 86대 실제 패치.** `preview` 12런 86대·건너뜀 0 → `apply` 12런 전량 `created`, 생성 합 86(5분 3초) → **툴과 독립된 재조회**에서 `child_count 86`(패치 전 `0`), 표본 프로퍼티 대조 일치. 판독 채널과 **쓰기 채널을 각각** 날조 대조군으로 먼저 검증했다(판독 대조군은 매 실행 선행이며 인자로 끌 수 없다). 첫 `apply`는 **0대 생성 fail-closed**로 끝났는데, 원인은 제품이 아니라 하네스였다 — `plugin_import_dir`를 플래그로만 받아 현장 설정과 다른 경로를 썼다. 플래그 기본값을 하드코딩하는 대신 앱과 **같은 이음매**(`resolve_effective_settings`)를 쓰도록 고쳐, 현장 설정이 바뀌면 하네스가 따라간다. 이때도 설계대로 0번 런에서 멈췄고 독립 재조회 `child_count 0`이 **아무것도 쓰이지 않았음**을 확인했다.
  - **결함 D1(수정 완료, `d48d4b7`)** — 매퍼의 콘솔 판독 게이트가 저장소 정본 규약보다 엄격했다. `server/vwx/apply.py::console_read_caveat`가 「열거는 절단됐으나 선언된 자식을 전부 관측한 판독」과 「진짜 짧은 판독」을 명시적으로 가르는데, 매퍼는 `completeness` 라벨만 보아 둘을 함께 막았다 — **실기 절단선은 19대**이므로 리그가 그 선을 넘으면 툴이 무력해진다. 새 정책을 만든 것이 아니라 형제 호출부 둘과 같은 잣대(`console_read_caveat(...)["kind"]`)로 맞췄고, `missing_count > 0`인 진짜 짧은 판독은 그대로 막힌다. **오프라인에서 안 잡힌 이유는 「가짜 콘솔이 절단되지 않아서」**이므로 `FakeConsole`에 절단 변형(`truncate_at` — `childCount`는 진짜 총계, `children`만 짧아진다)을 먼저 넣고 테스트 5건을 세웠다. 뮤테이션 3회로 양방향 확인(잣대를 되돌리면 3건 RED · 검사를 통째로 없애면 5건 RED · 절단 변형을 무력화하면 비공허성 1건 RED). 라이브 재검증에서 `complete_enough_to_judge_absence`가 `False` → `True`로 열렸다(쓰기 0건).
  - **결함 D2(미수정, 후속 카드 `t11`로 분리 — 감독 결정)** — 실기 콘솔의 `occupant.fixture_type`은 이름이 아니라 `FixtureType <슬롯>` 형태의 **객체 핸들**이다. 매퍼의 `already_patched` 갈래는 이름 대조를 요구하므로 실기에서 **절대 참이 되지 않고**, 재실행 86행이 `already_patched`가 아니라 `fid_occupied`로 나간다. D1과 정확히 같은 계열의 **가짜↔실물 괴리**다. 영향은 D1보다 작다 — `fid_occupied`도 건너뛰므로 **쓰기는 0건이고 중복은 생기지 않는다**. 다만 라벨이 지시하는 다음 행동이 「다른 FID로 다시 패치하라」라, M3에서 라벨 순서를 고친 이유가 실기에서는 아직 살아 있지 않다. 수정은 라이브러리 판독을 매퍼에 새로 주입하는 **설계 변경**이고, 역방향(이름→슬롯)은 `Robin Spiider`가 슬롯 4와 12에 **중복 등록**돼 있어 모호하다(슬롯 12가 무엇이 다른지는 **못 읽었다** — 추정하지 않는다).
  - **AC 17건 중 16 PASS · AC-LXSEQ-016 FAIL** — 016의 5개 행 중 4개(preview 계획 · 런마다 `created` · 재조회 86 확인 · 재실행 쓰기 0건)는 PASS이고, 「재실행 사유가 `already_patched` 86건」 한 줄이 FAIL이다(사유는 D2). **정정**: 처음에는 이 AC를 "PASS-WITH-DEBT · FAIL 0"으로 적었으나 독립 감사가 반박했고 그쪽이 맞다 — `acceptance.md`는 그런 등급을 정의한 적이 없고 AC-016 ⑤는 실패 행이 있으면 M4를 PASS로 닫지 않는다고 명시한다. **따라서 이 SPEC은 `implemented`에서 멈추며 `completed`가 아니다**(`acceptance.md` §D DoD가 AC-016 미충족 상태의 `implemented`를 허용한다). 독립 감사는 두 번 돌았다. **수정 전**(`2b328c6`) **FAIL 0.80**(Functionality 0.70 must-pass 미달 · Security 0.90 · Craft 0.86 · Consistency 0.88) → 감사 후속 수정 **뒤**(`07bc93b`) 재판정 **FAIL 0.86**(0.80 · 0.91 · 0.90 · 0.91, +0.062). **여전히 FAIL인 동인은 오직 AC-LXSEQ-016 한 줄**이며 그것은 이 카드가 손대기로 한 항목이 아니다(D2 → `t11`, 감독 결정) — 겨냥한 F3·F4·DOC-2·F2 네 건은 재감사자가 **뮤테이션을 양방향으로 직접 재현해** 전부 닫혔음을 확인했고, 상승분 전부가 거기서 나온다. 차단 결함 0건 · 신규 코드 결함 0건. 보고서 원문 2종은 `.moai/reports/`·`.moai/state/`가 `.gitignore` 대상이라 **저장소에 없다** — 판정 요약은 `progress.md` §E.4 「독립 감사」가 정본이다. **AC-LXSEQ-012 주**: ③의 "질문 카드로 **모드**를 묻는 시나리오"는 이 설계에서 도달 불가다 — 매퍼가 위임 전에 모드를 확정하므로 `patch_fixtures`의 모드 질문 가지에 닿지 않는다(오히려 정상 성질). 도달 가능한 질문 카드는 「Patch 편집기를 열어 달라」 하나뿐이라 관측 계약 3가지를 그 경로로 검증했다.
  - **감사 후속 — 공허한 검사 3건 수정(감독 결정으로 t9 안에서 처리)** — 독립 감사 2건이 소인한 것은 「테스트가 없다」가 아니라 **「그 테스트가 무엇을 재는가」**였다. 셋 다 스위트를 내내 초록으로 두고 있었다.
    - **F3 — 매퍼의 자기 보고를 재던 단언.** `assert len(plan.types_requested) == 8`이 재는 값은 `mapper.py`가 `records`에서 **파생시킨 것**이라, 해석표를 행마다(86회) 조회해도 8종 그대로다. 테스트 이름이 주장하는 것과 단언이 재는 것이 서로 다른 물건이었다. 조회를 기록하는 Mapping 을 주입해 **실제 조회 횟수**를 재도록 교정. **뮤테이션**(타입 루프를 행 루프로): 새 단언 `assert 86 == 8` **RED**, 같은 뮤테이션 아래 옛 단언은 `True`로 **그대로 통과** — 옛 단언이 이 결함을 원리적으로 볼 수 없었음이 기계로 증명됐다.
    - **F4 — 부분 겹침에 테스트가 없었다.** 기존 겹침 테스트는 두 행을 **같은 시작 주소**로 옮긴다. 뒤 장비가 앞 장비 구간 **안쪽에서** 시작하는 경우(`grep -c '104\|105'` → `0`)는 검사가 없었다. 픽스처 실측(FID 104 `1.037–048` · 105 `1.049–060`, 인접하되 겹치지 않음)을 비공허성으로 고정한 뒤 105를 주소 40으로 옮기는 테스트를 추가. **뮤테이션**(겹침 판정을 시작점 동일로 좁힘): `1 failed, 1 passed` — 기존 테스트는 통과했고 신규만 `assert 0 == 2`로 빨개졌다.
    - **DOC-2 — AC-LXSEQ-013의 검증 명령이 테스트를 0건 수집했다.** `-k "idempotent"` → **`29 deselected`**. 이 파일의 어떤 테스트 이름에도 `idempotent`가 없다. AC-LXSEQ-011이 「수집 0건이면 게이트가 공허하므로 FAIL로 친다」고 못박은 그 형태다. 선택자를 교체해 **`5 passed, 24 deselected`**, 수집된 5건의 이름까지 기록했다. `acceptance.md` 본문은 정본상 `manager-spec` 소유이므로 **소유 경계를 넘은 사실을 해당 줄과 `progress.md` 양쪽에 남겼다.**
    - 두 뮤테이션 모두 실행 직후 복구했고 `git diff --quiet -- server/lxseq/` 빈 출력으로 확인했다. 뮤테이션 원문·관측 출력 전문은 `progress.md` §E.2 「감사 후속」이 정본이다.
  - **검증(sync 세션 직접 재측정)** — 수정 전 `uv run pytest server/tests -q` **9681 passed · 8 skipped · 1 warning**(141.64s, exit 0) → 수정 후 **9682 passed · 8 skipped · 1 warning**(142.05s, exit 0), 양쪽 다 실패 0. 증가분 **+1 = F4 신규 테스트 1건**과 정확히 일치한다(F3은 재작성이라 개수 불변, DOC-2는 문서). plan-phase 기준선 9596 → M1 9614 → M2 9649 → M3 9676 → D1 9681 → 감사 후속 9682로, 매 단계 증가분이 신규 테스트 수와 일치한다. **커버리지 `TOTAL 346 5 99%`**(`--cov=server.lxseq`) — §D DoD 기준 85% 대비 PASS이며, **이 값은 이번 sync 세션이 처음 실측했다**(run 세션도 재지 않았다). ⚠️ **이 저장소에는 테스트 CI가 없다** — `.github/workflows/`에 `label-sync.yml` 하나뿐이라 브랜치에도 PR에도 테스트를 도는 워크플로가 없고, **전체 로컬 실행이 이 카드의 유일한 회귀 증거다.** ⚠️ 증거 파일은 `.moai/state/`(`.gitignore:203`)·`.moai/reports/`(`:247`) 아래라 **저장소에 없다** — 경로만 인용하면 남이 열 수 없으므로 **요약 본문이 정본**이다.
  - **툴 등재는 4지점이 아니라 6지점이다** — `TOOL_NAMES` · 핸들러 클로저 · `ToolDefinition` · `handlers` 맵에 더해, `server/orchestrator/runner.py`의 `_TOOL_TASKS` 한국어 작업 이름과 `server/tests/test_songcue_bundle.py`의 헝크 트립와이어가 **반드시 따라온다**. 계획의 파일 목록이 이 두 자리를 빠뜨려 1회차 전체 스위트가 3건 빨갛게 났다(무관한 실패가 아니라 전부 신규 등재가 원인). 다음 툴 등재 카드는 처음부터 6지점으로 잡을 것.
  - **남은 열린 항목(닫지 않음)** — ① D2(핸들↔이름) → 카드 `t11` ② UI 파일 선택기 배선 → 카드 `t10`(지금은 로컬 하네스 `server/tools/lxseq_e2e.py`가 파일을 읽어 인자를 채운다) ③ `REQ-LXSEQ-005` 토큰 분할 규칙 미정의 ④ plan-audit optional O4~O7 미적용(재량) ⑤ 뮤테이션이 AC 묶음당 1~2회라 **개별 테스트 판별력 일부 미증명** — 특히 `AC-LXSEQ-002` 5건 중 판별력이 증명된 것은 1건뿐이고, 판정 4종(`zero_channels`(제외) · `non_integer_field` · `duplicate_fid` · `address_overlap_in_file`)에는 전용 뮤테이션을 하지 않았다 ⑥ **런 경계의 연속성 조건을 검사하는 테스트는 `test_runs_split_when_a_row_in_the_middle_is_skipped` 1건뿐**이라 이 테스트가 지워지면 연속성 회귀가 조용히 통과한다 ⑦ `already_patched` 판정 순서를 지키는 것은 툴 테스트 2건뿐이며 **매퍼 단위에는 순서 단언이 없다** ⑧ 가짜 콘솔은 타입마다 모드를 1개만 주므로 폭 동률(라벨 토큰) 갈래와 `mode_overrides` 재호출은 툴 계층에서 미검증(매퍼 단위에는 있다).

- **SPEC-COPILOT-PRESETGUARD-001** — Position 프리셋(풀 2) **비가역 덮어쓰기 가드 · 저장 되읽기 · 재생성 경로**(Tier M, TDD, `status: implemented`). `Store Preset 2.n`은 점유 슬롯을 경고 없이 덮어쓰는데 이 앱에는 undo도 restore 경로도 없고 `Delete`는 블랙리스트다 — **덮어쓴 프리셋은 사라진다.** 안전 게이트(`server/safety/`)는 명령의 위험도를 분류하지만 이 명령은 위험 분류상 평범하므로, 가드는 세션 계층(`server/web/session.py`)에 산다. 코드 변경은 `server/web/session.py` · `server/tests/test_web_session.py` **2파일뿐**.
  - **① 점유 검사를 시작 번호의 출처와 무관하게 수행** — 종전에는 운영자가 번호를 시스템에 맡겼을 때(else 갈래)만 검사했고, 번호를 직접 지목하면(`"21번부터"`) 검사가 **한 번도 실행되지 않았다.** *가장 확신을 갖고 행동한 운영자가 가장 보호받지 못한다.* 가드를 갈래 밖으로 옮겨 명시 번호 경로와 질문 카드 자유 입력 경로가 동일하게 검사되고, 충돌 시 **어느 슬롯이 사라지는지 번호로** 묻는다.
  - **② 저장 후 풀 되읽기** — 종전 저장 루프는 `run_commands`를 던지고 *"저장 요청했습니다"* 라고만 보고했다. 이제 풀을 한 번 되읽어 형용사가 아닌 **산술**(착지/누락 슬롯 번호)로 보고한다. 승인 게이트가 보류한 슬롯은 `proposal`로 분류해 결함으로 세지 않는다 — 그러지 않으면 게이트가 켜진 정상 환경에서 상시 거짓 경보가 나고, 무시되는 경보는 진짜 미착지까지 함께 가린다.
  - **③ 재생성(제자리 갱신) 입구 신설** — *"지금 배치로 다시 잡아줘"* 가 기존 구간을 갱신해 그 프리셋을 참조하는 모든 큐가 재조준된다. 종전에는 핸들러가 없어 이 말이 신규 저장 경로로 흘렀고, **운영자는 프리셋이 갱신됐다고 믿는데 실제로는 새 구간에 저장**됐다.
  - **설계 불변 2가지** — **확인 카드 하나, 호출자 둘**(①과 ③이 같은 판정 로직을 공유해 한쪽만 fail-closed가 되는 드리프트를 차단) · **미상 ≠ 빈칸**(풀 판독 실패를 `set()`으로 접으면 *"검사했고 비어 있었다"* 로 위장된다 — 신규 저장은 고지 후 진행하고 재생성은 저장하지 않는 **의도된 비대칭**).
  - **독립 리뷰 반영** — 리뷰 NOT-A-PASS 판정의 결함 F1~F9 수정(`ca0294c`), 이후 R4(재생성 동사에서 `저장` 토큰 제거)·R2·R3 추가 수정. **R2**: 동의 판정을 완전 일치에서 **어절 단위 형태 정규화**로 교체 — `진행해주세요`·`좋아요`·`넵`이 승낙으로 인식되고, 네 번째 상태 `unrecognised`가 *"못 알아들음"* 과 *"거절함"* 을 분리해 회신이 원인을 오귀속하지 않는다(`취소하지 마`가 거절로 뒤집히지 않음). 공허했던 비공허성 테스트(구현 자신의 토큰 목록을 파라미터화해 결코 실패할 수 없던 것)를 손으로 적은 독립 코퍼스 14문장으로 교체. **R3**: `다시(?:\s+\S{1,6})?\s*(?:잡|만들|생성|갱신)`으로 사이 어절 하나를 허용해 `"다시 한번 잡아줘"`가 저장 경로로 새던 결함을 닫았다.
  - **검증(sync 세션 직접 재측정)** — `uv run pytest server/tests -q` **8929 passed · 8 skipped**(141.44s) · `uv run ruff check server/` **선재 2건 외 신규 0**(E501, `server/safety/console.py`) · `ruff format --check` 실패 2파일(`server/safety/console.py` · `server/web/preview.py`)은 **base `f5cfe15`에서도 동일하게 실패**함을 base 사본 추출로 확인 → **신규 0**(봉쇄 구역이라 재서식하지 않음) · 범위 봉쇄 `server/spatial/` · `server/orchestrator/tools.py` · `server/safety/` **byte-diff 0**(f5cfe15..HEAD). **여기까지가 sync 세션의 직접 측정이다.** 뮤테이션 1라운드 10/10 · 2라운드 12/12(생존 0)는 **run 세션이 실행한 것**이며 sync 세션은 로그(`.moai/state/verify/presetguard/mutations.log` — git HEAD·추적파일 clean 각인)를 읽어 확인만 했다. 이후 독립 감사(sync-auditor)가 자체 뮤테이션 5건을 별도로 작성·실행해 하니스를 우회 검증했다(아래 감사 항목).
  - **독립 감사(sync-auditor) — PASS-WITH-DEBT, 조화평균 0.83** (기능 0.93 · 보안 0.95 · 완성도 0.78 · 일관성 0.70). AC 14건을 **한 건씩 개별 명령으로** 검증해 전건 PASS·미검증 0건, 전체 스위트 자체 재현(8929 passed · 8 skipped · 142.49s), 헝크핀 트립와이어 `test_songcue_bundle.py` 20 passed. 감사자가 **자체 뮤테이션 5건**을 작성해 가드를 직접 파괴 검증했다 — 가드 본문을 상수 반환으로 치환하니 25건 RED, 저장 루프를 무효화하니 37건 RED, 승낙/판독/되읽기의 `None` 접기 3종도 전부 RED. 이로써 `acceptance.md`가 이 SPEC 최대 위양성 함정이라 경고한 **스텁 함정이 실제로 회피됐음이 기계적으로 증명됐다**(스텁 리그였다면 가드를 지워도 전건 통과했을 것). 감사 후 `git diff --stat HEAD` 공백 — 추적 파일 무변경. 지적된 결함은 **전부 문서 결함이며 코드 결함 0건**이고, 그중 sync 세션이 만든 3건(무출처 라이브 주장 · 뮤테이션 수치 귀속 오류 · 봉쇄 설명 오류)은 이 커밋에서 교정했다.
  - ⚠️ **`ca0294c` 커밋 메시지의 검증 주장은 부정확하다** — 본문이 *"뮤테이션 19건 전건 RED"* 라 적지만 당시 로그가 증거하던 것은 16건이었다(하니스가 사람의 `| tee` 부착에 의존해 5회 중 3회만 남았고, 첫 회는 덮어쓰기였다). 게다가 그 실행들은 커밋 직전 `ruff format` 패스보다 먼저 돌아 **커밋된 바이트에 대한 관측이 아니었다.** 발행된 이력을 고쳐 부정확한 주장을 감추는 것이 주장 자체보다 나쁘므로 **커밋은 재작성하지 않았다** — 정정 전문은 `progress.md` §E.2에 있고, 위 검증 수치는 하니스가 스스로 로그를 append하도록 고친 뒤 커밋 트리에서 **재실행한** 결과다. 승계 교훈: *사람이 파이프를 붙이는 것에 의존하는 증거는 언젠가 빈다.*
  - **남은 열린 항목(닫지 않음)** — ① 동의어 3종 `그래`·`덮어써줘`·`ㅇㅇ`이 여전히 미인식(직접 측정 확인, 쓰기 0이므로 안전 방향이나 사용성 간극) ② P4′ 후보 — `basic_position_presets()`가 좌표 확인된 **모든** 픽스처를 대상으로 삼는데 `pointing.py`는 무빙헤드 조준용이라 PAR·블라인더 같은 고정 등기구가 쓸 수 없는 Pan/Tilt를 받는다 ③ **본 SPEC 범위 밖** — `get_spatial_context`의 서술형 도구설명에 rotation이 **0회** 등장해(첫 등장은 `include_rotation` 파라미터 스키마 안) 모델이 옵션을 발견하지 못하고 Lua 플러그인을 즉흥 작성한다(도구설명의 rotation 0회는 sync 세션이 직접 확인했다. 이로 인한 Lua 즉흥 작성은 lead 세션이 전달한 관측이며 **이 저장소에 출처 기록이 없다** — 재현 전까지 미확인으로 다룰 것). 수정은 `tools.py`를 건드리므로 봉쇄 구역 밖 별도 커밋 + `test_songcue_bundle.py` 헝크핀 재측정이 필요하다.

- **감독 타임라인 워크플로우 완성** (SPEC-COPILOT-SONGSTD-001 후속, 핸드오프 2026-08-15 우선순위 1~6 전부) — 자연어 곡 브리프가 콘솔 저장·공연 배치·리허설 수정까지 한 흐름으로 이어진다. 콘솔 쓰기는 전부 승인 게이트 뒤에서만, PLAN 단계는 콘솔 무접촉 불변.
  - **① PLAN 편집 + 큐 추가/삭제** — 승인 카드에서 "수정"을 답한 계획이 세션에 보존되어(`_pending_song_plan`) "큐 4 삭제"·"큐 2와 3 사이에 브레이크 추가"·"큐 3을 Center로"가 콘솔 명령 0건으로 재합성된다. 삭제/삽입 시 재질의·페이드·FX 오버라이드 인덱스 자동 재매핑.
  - **② 편집 어휘 확장** — 디머(D1~5·어둡게/밝게), 컬러(팔레트 색 단어 치환), 페이드(`SectionDecision.fade_override` — 컴포저가 D레벨 예산 대신 사용), FX on/off(차단은 감사 기록), 시간 이동(m:ss, 이웃 교차 시 큐 재정렬), 저장 대상 시퀀스 변경("시퀀스 320으로 변경").
  - **③ 자동 버전 스냅샷** — 승인 실행 뒤(verified·readback_failed 모두) 방금 보낸 projection을 라이브러리에 `이름 (자동 vN)`으로 자동 저장. N은 기존 스탬프 max+1, 수동 저장·비정형 이름은 번호 계산 제외.
  - **④ 셋리스트 모드** — 라이브러리 곡(이름별 최신 버전)을 `Copy Sequence <원본> At <210+10i>`(원본 보존) + `Assign … At Executor <101+i>`로 일괄 배분. 대상 슬롯 빈자리 사전 점검(점유 시 검증된 대체 시작 번호 카드), Executor 페이지 슬롯 창(x01~x99) 가드, 실행 후 `Executor <n>` 정체 프로브(`node.sequenceNo`)로 readback 검증.
  - **⑤ 리허설 편집** — "지금 이 큐를 무대 중앙으로": 큐 번호 없이 시퀀스 핸들의 `CurrentCue`(T-H3 실측 경로)로 재생 위치를 판별하고, 라이브 출력이 즉시 바뀌므로 매회 명시 승인 카드 뒤에만 `/Merge`. 정지 상태·plan-only 타임라인·타임라인 밖 큐는 전부 거부.
  - **⑥ 레이어 매핑 실사용** (결함 6 종결) — 감독이 확인한 'back' 역할 그룹이 점등 섹션 큐마다 `Group <n> ; Dimmer At <key×0.8>` 한 줄로 실제 명령에 반영(전체 키 디머 뒤·Store 앞, last-wins). RG5 준수: 멤버십(fid)은 주장하지 않고 그룹 번호 주소만.
  - **오류 예방 파이프라인** (사용자 방향 2026-08-16: "문제가 생기면 거절 대신 해결 질문을") — 첫 질문부터 콘솔 검증 번호만 제안(시퀀스 `(비어 있음)`·프리셋 `(2.21~2.30 저장 확인됨)`·타임코드 빈 슬롯), 승인 카드에 검증된 영향 요약(`시퀀스 120: 비어 있음 확인 · 덮어쓰기: 없음`), 점유/거부 시 빈 슬롯을 제안하고 즉시 재저장하는 복구 카드(최대 3회), Store 실패 시 계획 보존 + ClearAll 정리, 보류 계획의 새로고침 생존(`PendingSongPlanStore` — 서버 재시작만 소실).
  - **탐침 정직성 3계** — fail-closed(조회 실패 슬롯은 절대 '비어 있음'으로 제안 안 함) · 폴백 카드에 진단 표기(`점유 X곳 · 조회 실패 Y곳`) · **실측 교정 2건**: 빈 슬롯 조회의 `path segment not found` 오류는 부재의 확답이므로 'empty'(이것을 '실패'로 읽어 검증 제안이 구조적으로 불가능했던 결함의 근본 수정), 풀 자식 슬롯 번호는 `"no"`가 아니라 `"i"` 키(PROTOCOL §4.2). flapping은 1회 자동 재탐침.
  - **감독 화면 현장어 정비** — 질감→전환 방식(컷/페이드), 색감 충돌→베이스 컬러 결정, TrigTime→큐 타임(자동 진행), 타임코드 N→N번 슬롯, 승인 카드 리뷰 요약 한글화(영어 린트 덤프→"확인 포인트 N건 … 의도한 포인트면 그대로 승인하세요"). 콘솔 명령 문법은 불변.
  - **실측 수정 4건** — 팔레트 힌트가 한 덩어리 문자열로 실려 L5가 전 큐 오탐(`'블루와 화이트'` → 토큰화 + 조사 제거), 힌트 정규식의 문장 경계 이탈, `Store Preset` 인라인 이름이 라벨로 적용되지 않음(별도 `Label` 명령 동반 — 무명 프리셋 21.x 재발 방지), 점유 시퀀스에 옵션 없는 `Store`가 `Not allowed`로 반거부되던 사고 경로를 사전점검+복구로 대체.
  - 검증: 서버 8,789 · UI 450 테스트 통과, 발라드/팝 브리프 시나리오 재현 스크립트로 카드 10장 흐름 검증, 실기 onPC 2.4.2에서 프리셋 검증 라벨·복구 카드·영향 요약 관측 확인. `research/ma3-effects-phaser` → `main` 머지(`c2aec1d`).

- **SPEC-COPILOT-IMGLAYOUT-001** — 이미지 기반 배치 제안: 조명감독이 채팅에 첨부한 배치 스케치의 **구조**(패턴·겹·대수·대칭)와 이미지 안 텍스트 주석을 읽어 배치 계획을 제안한다(`status: completed`, M1~M5 + 라이브 비전 스모크 통과 — gemini-3.5-flash 10.8s, 주석 OCR 3종 전부 정확·픽셀 추정 0건). WS 메시지 `layout_image_upload`(MIME `image/png`·`image/jpeg`·`image/webp`, 디코드 후 5MB 상한) → 세션 보관(`ChatSession._layout_image`, 최신 1장·교체형) → 툴 `analyse_layout_image`(비전 1회 호출, 콘솔로 0 exec verbs) → 구조 사양 JSON(`pattern`/`layers`/`symmetry`/`confidence`/`annotations`[원문+해석 쌍]/`unresolved`) → 미해결 수치는 기존 `question_request` 카드로 한 번에 하나씩 확인 → 실행은 기존 `arrange_fixtures`(백업→쓰기→재검증) 경로 그대로. M2가 `ImageAttachment`/`UserMessage.images`를 어댑터 3종에 배선했고(`claude_code`는 이미지 첨부 시 정직 거부 텍스트를 반환 — REQ-IMGLAYOUT-006), M4가 세션의 단일 활성 프로바이더를 그대로 `vision_provider`로 주입해(`server/llm/factory.py`가 프로바이더를 정확히 1개만 빌드하므로 별도 비전 프로바이더 설정이 없다) 그 정직 거부가 비전 호출에도 자동 승계된다. M5 통합 테스트(`server/tests/test_imglayout_flow.py`) 1건이 업로드→분석→질문→배치 전체 흐름을 mock provider 스크립트로 왕복 검증한다.
  - **비목표(v1)**: 이미지에서 픽셀 비율로 수치를 추정하지 않는다(수치 출처는 이미지 텍스트 주석 또는 조명감독 답변뿐) · CAD 도면 좌표 추출(MVR/CSV import 몫) · 픽스처 회전(orientation) 쓰기 · 이미지 저장/영속화(업로드는 WS 세션 수명) · claude_code 어댑터의 이미지 지원.
  - **후속 처리(2026-08-16)**: ① **`triangle` 프리셋** — 스모크가 삼각형 스케치를 정확히 읽고도 어휘 밖이라 `rows`로 강등하던 간극을 닫았다. `_LAYOUT_PATTERNS`·비전 프롬프트에 `triangle`, `interpreted` 키에 `side`(한 변 길이), `spatial/presets.py`에 정삼각형 프리셋(무게중심=origin, 꼭짓점 90/210/330°, 둘레 등간격·apex 시작·반시계, `side` 기본 3.0m — 3의 배수 대수는 꼭짓점에 정확히 착지), `arrange_fixtures` 스키마에 `side` 파라미터(`SPATIAL_PRESETS` 3 → 4, `ARRANGE_PRESETS` 4 → 5). 수치 규율 동일: `side`의 출처는 이미지 주석 또는 감독의 답뿐. ② **업로드 UX** — 드래그앤드롭·클립보드 붙여넣기(버튼과 같은 라우터 경유), 두 번째 업로드에 "이전 이미지를 교체했습니다" notice(세션은 1장 보관 — 침묵 교체는 다른 스케치로 분석된 계획을 승인하게 만든다). ③ **confidence 노출 규율** — `analyse_layout_image` 설명이 제시 시 confidence 원문 명시 + medium/low면 경고 선행을 요구.
  - **후속 수정(2026-08-16, sync-phase)**: 첨부 파일 선택창에서 이미지가 비활성으로 뜨던 결함 — `accept`가 MIME만 나열해 일부 브라우저/OS 파일 피커가 이미지를 회색 처리했다. 확장자(`.png,.jpg,.jpeg,.webp`)를 명시하고 MIME은 폴백으로 유지 (`ebadf92`).
  - **독립 리뷰(2026-08-15, 머지 후)**: 품질·보안 리뷰어 2종 병렬 — **Critical/High/Major 0건**, 승인 게이트 우회 없음(analyse_layout_image는 실행 포트 무접근, 쓰기는 run_commands 게이트 상속 실측). Low 3(무프레이밍 재주입·비전 응답 값 타입 미검증·매직바이트 미검사)·Info 2는 기록만 하고 미수정(사용자 결정) — `reports/imglayout-independent-review-20260815.md`.
  - **남은 잔여(의도적 미실행)**: 실기 1턴 왕복(업로드→분석→질문→승인 카드 렌더→arrange→재검증) — WRITEGATE M2와 같은 이유로 사람 입회 없이 돌리지 않는다(실제 쇼파일 좌표 쓰기의 무인 실행은 검증이 아니라 결함의 재현).
- **SPEC-COPILOT-FXGEN-001** — 파라메트릭 페이저 생성 + 프리셋 All 저장: 정형 라이브러리만으로는 못 만드는 이펙트를 자연어로 조립한다 (`status: completed`, V1~V7 라이브 실측 + E2E 스모크). onPC 2.4.2 라이브 검증(콘솔 프로브 + 오퍼레이터 GUI 관측 5건)으로 게이트 5축을 열었다 — ① **Accel/Decel 곡선** (`Step <k> At Accel -100`을 **스텝 열 완성 후** 발화하면 사인 페이드가 실제로 보인다 — M0 SKIP의 원인은 문법이 아니라 발화 순서였다) ② **Relative 스텝 값** (`At Relative <n>` — 현재 위치 위에 올라타는 이펙트) ③ **SpeedMaster 바인딩** (`At SpeedMaster <n>` — 마스터 페이더로 라이브 변속; 고정 Speed와의 동시 지정은 미측정이라 두 계층에서 거부) ④ **Width/Measure** (짧은 펄스 + 루프 스케일) ⑤ **3스텝 + 채널별 phase 스프레드** (레인보우 웨이브). 스키마·로더·빌더가 5축을 방출하고 라이브러리 엔트리 7종이 추가됐다(12 → **19**).
  - **compose_fx 신설** (`TOOL_NAMES` 31 → **32**) — 모델이 스텝·축을 인자로 넘기면 **출하 라이브러리와 동일한 로더 스키마**로 검증하고, 동일한 `run_commands` 단일 관문(`_deliver_fx_plan`)으로 발화한다. find_fx 폴백("정형 이펙트에 없음")이 죽은 골목이 아니라 조립 경로로 이어진다.
  - **프리셋 목적지** — `instantiate_fx`/`compose_fx`에 `destination: "preset"` 추가. `Store Preset <pool>.<slot> '<label>' /Universal`(실측 리터럴)로 "All" 풀에 멀티스텝 프리셋을 저장한다. 풀 번호("All 1"이 기본 쇼파일에서 21이었다는 것 포함)와 빈 슬롯은 **항상 재조회 실측** — 하드코딩·추측 금지, 잘린 목록은 거부(`preset_pool_truncated` 등 사유 4종). **증거 2계**: 풀 목록 readback = 기계 증거(저장 성립), 프리셋 내용은 여전히 기계 판독 불가(childCount 0) → 효과는 사람 관측.
  - **룰북 자산 `33_effect_editors.md`** (허가된 추가 2호) — Phaser Editor / Recipe Editor / MAtricks Editor 세 패러다임의 라우팅 표 + 검증 어휘 + **레시피 쓰기 금지 경계**(EditRecipe/Cook 계열은 리포지토리 실측 0건 + `Cook /Overwrite` 파괴 위험 → 읽기 전용, 가까운 검증 대안 = 프리셋 목적지 제시). fx 도구명을 지명할 수 있는 유일한 자산으로 경계 테스트를 갱신했다.
  - **E2E 스모크 실측** — 실제 앱·실제 `/ws`·실제 게이트·실제 onPC로 *"그룹 11에 부드럽게 숨쉬는 사인 딤머 이펙트를 새로 만들어서 프리셋 All에 저장해줘"* 를 끝까지 실행: 모델이 compose 경로 선택 → 실측 문법 그대로 방출(`Step 1 At Accel -100` … `Store Preset 21.1 'Breath FX' /Universal`) → 전 커맨드 executed_ok → 풀 readback에 `21.1 "Breath FX"` 확인.
  - **연구 코퍼스** — `docs/research/ma3-effects/00~07`(오케스트레이션 병렬 리서치 7건 + 종합 계획): 페이저 기초 · 팬틸트 · 컬러/디머/빔 · 프로그래매틱 · Phaser Editor · Recipe Editor · MAtricks Editor. Recipe/StepCreator/Shape 쓰기 경로·attribute별 독립 속도·Grid 서브선택은 Out of Scope로 명시(스펙 §D).
  - **라이브 운용 경화 (2026-08-15~16 실전 테스트 후속)** — 오퍼레이터 확인 3건으로 잔여 게이트 전량 확정(컬러 축 Accel/Decel 사인 · 프리셋-리콜 경유 Relative 중심추종 · MAtricks 형상의 프리셋 고착 — `31_choreography_patterns.md` OBSERVED EFFECT 항, append-only grant로 라인 인용 보존). **라벨 3종 수정**: ① `Store Sequence … Cue 1 '<이름>'`은 큐만 명명 → `Label Sequence` 별도 방출 ② `Store Preset … '<이름>' /Universal` 인라인 이름은 접수만 되고 미적용 → `Label Preset` 별도 방출 ③ 콘솔 타일이 한글 라벨 미표시 → 비ASCII 라벨을 fx_id 파생 영어명으로 자동 대체(`sweep-soft-wide` → `Soft Wide Sweep`) + 도구 label 파라미터 ENGLISH ONLY 규칙. **STOP AT THE STORE 규율**: 라벨 프리셋으로 저장하고 멈춤 — 손으로 쓴 recall/큐/Assign/`Go+` 후속 번들 금지(라이브 관측: 게이트 거부 + 승인 카드 2장 낭비), 응답은 두 문장 + '시퀀스(큐)에도 저장할까요?' 단일 질문(선택은 감독 몫). **현장 어휘 확장**: 라이브러리 19종 전체에 실무 표현 alias 추가("무빙 살랑살랑"·"웨이브 태워줘"·"따닥따닥"·"제자리에서 돌려줘"·"템포 물려줘" 등) — 매칭 스모크 11/11 적중. compose_fx 설명의 표현 불가 예문('팬은 천천히 틸트는 빠르게' — fx 전역 단일 속도원과 불일치)을 정정하고 per-attribute 속도 요청은 정직하게 밝히고 나눠 받도록 명시.

- **진행 순서 보드 ↔ 셋리스트 연동** — 셋리스트 배분(`셋리스트 모드`)이 실행되면 그 슬롯 순서가 큐 진행 모니터의 계획 순서가 된다(`planned_show_order`: 실행된 셋리스트 > 감독 타임라인 단곡 > 빈 계획). 순서는 `SongTimelineStore`에 디스크 영속되어 서버 재시작을 견딘다.

- **3D 배치 요청 경로** — `arrange_fixtures`에 `elevation` 프리셋을 추가했다. 현재 x/y를 보존한 채 지정한 절대 z 높이만 기록하며, 쓰기 전 좌표 백업·승인·읽기 검증·복원 번들을 그대로 적용한다. 고정 프롬프트도 `3D`·`레이아웃`·`시뮬레이션`·`배치`·`이동` 요청을 공간 좌표 읽기와 배치 도구로 연결한다.
- **SPEC-COPILOT-WRITEGATE-001** — 쓰기 경로 무결성: 좌표 기록이 **사람 확인 없이 쇼파일을 변형하던 구멍**을 게이트 분류 한 곳에서 닫는다 (`status: in-progress`, M1 분류·회귀 착지 + M3 문서 동기화). `Set Fixture … Pos*`가 `server/safety/classify.py:241` 종단 반환으로 `safe`였으므로 게이트가 **승인 카드를 띄우지 않았고** showfile 백업 규칙 ③(`server/safety/gate.py:362-365`)도 **발동하지 않았다**. **대가는 가설이 아니라 관측이다** — SPATIAL `progress.md §E.2.20` 결함 2에서 사용자가 요청하지 않은 좌표 기록 **54건**이 그 사이에 사람 없이 콘솔에 도달했고, 같은 턴의 `Go+ Page 1.202`는 **정상적으로 카드를 띄웠다**(즉 **게이트는 건강했고 쓰기만 그 그물을 통과했다**). 수정의 형상은 `server/safety/blacklist.yaml` **version 1 → 2** + 폐쇄집합 엔트리 **정확히 1건**(`"Set Fixture"`) 추가이며 **코드 변경 0행**이다 — `classify.py` · `ruleset.py` · `gate.py` · `expand.py` 전부 무변경이고 신규 `category` 값도 도입하지 않는다(신규 값은 expand-or-hold와 배포 스캔 두 소비처에서 조용히 열린다). 이로써 `AC-SPATIAL-031` 전체와 `REQ-SPATIAL-024`의 **승인 흐름** 절, 그리고 `REQ-SPATIAL-020`의 **규칙 ③ 연동** 절이 `[DEFERRED]`에서 해소된다. 실측 **19종 커맨드 형태 전수 · 불일치 0건**: 부호 소실형 `Set Fixture 11 Posx -3.5` · 무동작형 `Set Fixture 11 Posx - 3.5` · 3자 축약 `Set Fixture 11 Pos -3.5` · 범위형 `Set Fixture 1 Thru 18 Posz '5.0'` · 방향축 `Set Fixture 11 Rotx '90.0'` · 매크로 본문에 밀입국한 `Set Macro 1.1 Property 'Command' "Set Fixture 11 Posx '5.0'"` 전부 `risky=True` · `category="blacklisted"` · `matched_entry="Set Fixture"`로 잡히고, `Set Selection MAtricks 'PhaseFromX' 0` · `Set Macro … Property 'Command' '<safe>'` · `Store`/`Assign`/`Copy`/`Label` 전 계열은 **`safe` 그대로**다.
  - **저장소 역사상 `blacklist.yaml` 리터럴의 첫 개정이다**: 이 파일 헤더(`:2-5`)는 처음부터 *"they change ONLY via a revision of this file with a version bump"* 를 규정해 왔고 **이번이 그 절차의 첫 실행**이다. `classify.py:32-43`이 SPEC-COPILOT-EXECREF-001의 `RECOGNIZED_REFERENCE_TYPES` 개정에 부여한 것과 **동일한 무게**로 다뤘다(*"weighted the same as a blacklist.yaml revision"* — 그 주석이 예고한 대칭을 이 개정이 처음으로 실제 성립시킨다). OVERLAP-001 `research.md:365-367`이 *"이 저장소의 닫힌 집합 리터럴은 **한 번도 개정된 적이 없다** … 확장 선례 **0건**. 이 SPEC이 선례를 만든다 → **절차를 문서화하는 것 자체가 산출물이어야 한다**"* 고 기록했고, 그 요구를 여기서 이행한다. 헤더가 함께 약속한 *"a revision here auto-extends the FN corpora"* 도 실측으로 성립했다 — 신규 엔트리가 `직접`/`번들`/`간접-매크로` 3변형을 자동 획득하고 전건 통과했다.
  - **측정 규율 — 문면의 "10건"은 실제 5건이었다**: `[DEFERRED]` 사유는 *"기존 테스트 **10**건이 깨진다"* 고 적었으나 그것은 **되돌린 시점의 과대 계상**이며 본 설계의 파탄 집합이 아니다. 실측 **5건**이고 전부 유형이 판별된다 — ① 실패 메시지가 **스스로 교체를 지시하는** 일부러 심은 트립와이어(*"must be replaced by the approval-flow assertions it was standing in for"* — 삭제가 아니라 승인 흐름 단정으로 **교체**하며, 같은 테스트가 함께 붙들던 MAtricks 불변은 **유지**한다. 트립와이어를 걷을 때 딸린 보호까지 걷는 것이 이 종류의 전형적 사고다) ② 문면이 *"any change creates review friction on purpose"* 를 선언한 **의도된 마찰 핀**(고정값 6 → 7 + 개정 근거 주석) ③④ **제품이 옳고 픽스처가 낡은** 하네스 2건(기본 `DenyAllApprovalPort`가 **올바르게** 막았다 — 승인 포트 주입) ⑤ 타 SPEC 소유 불변식(`test_overlap_preserve.py`, 비준 그랜트 3겹). **측정 함정**: ⑤는 워킹트리가 아니라 `_PRECHK_BASE..HEAD` **커밋 범위**를 읽으므로 **커밋하기 전에는 실패가 보이지 않는다** — 수정 전 전체 스위트가 `4 failed · 4716 passed · 7 skipped`로 4건만 보였던 이유이며, SCENE-001 M7이 이미 같은 함정을 기록했다(*"git 상태에 의존하는 테스트는 커밋 후 한 번 더 돌린다"*).
  - **일부러 가져가지 않은 범위 — `Store Group` risky 화(사용자 결정 2026-08-05 범위 분리)**: `Store`는 `server/measurement/corpus.yaml`의 *"non-risky verbs only"* 불변식과 **21 시나리오 중 10건 · 대표 작업 10종 중 5종**에서 충돌하고(`cue-store-1/2` · `group-create-1/2/3` · `macro-create-1/2` · `page-setup-1` · `preset-store-1/2`), DEPLOY 테스트 **3건**이 `Store Group 3`을 **정캐논 안전 픽스처**로 고정한다(`test_deploy_gate_e2e::test_registered_non_destructive_plugin_passes_audited` · `test_deploy_pipeline::test_non_destructive_plugin_registers_unflagged` · `test_deploy_scan::test_safe_commands_yield_no_findings` — `test_deploy_pipeline.py:26` `SAFE_SOURCE`). **머지 전 리뷰 정정 — 종전 문면의 "13건 · 7종"은 틀렸다**: 패치 자신의 `_would_be_held` 술어로 `load_corpus()`를 전수 재측정하면 엔트리 `Store`는 **10/21 시나리오 · 5/10 작업종**, 실제로 이름 붙일 리터럴인 `Store Group`은 **3/21 시나리오 · 1/10 작업종**(`group-create-1/2/3`)이다. "13"은 `Store|Set|Assign|Copy` 쓰기동사 정규식이 센 **커맨드 13건**(13 시나리오 · 6 작업종)을 `Store`의 시나리오 수로 옮겨 적은 것이며, `Store`를 포함하는 **어떤 동사 합집합도** 13/7에 도달하지 못한다(코퍼스 천장은 **17/8** — 21 중 4 시나리오에 `mock.commands`가 없다). 반면 같은 문장의 **DEPLOY 3건은 `Store Group`에 대해 정확하다** — **두 반쪽이 서로 다른 엔트리에 대해 계산됐다**는 것이 오류의 기제다. **결정 자체는 그대로 선다**: `group_create`는 AC-MVP-001 대표 10종 중 하나이고 DEPLOY 픽스처 충돌은 코퍼스 규모와 무관하다. 과장된 것은 **크기뿐**이다. 그리고 **`Set`은 아무것과도 충돌하지 않는다** — 코퍼스에 `Set` 토큰이 **0개**(전수 grep)이므로 `server/measurement/**`는 **한 바이트도 고치지 않는다**. **남는 손실을 정직하게 적는다**: 손으로 쓴 `Store Group`의 무승인 경로는 이번 창에 **열린 채로 남는다.** 이는 관측 사고가 아니라 가설이며(관측된 54건은 **전부 좌표 기록**이다), GROUPGEN의 툴 계층 seam이 덮는 것은 조립기 경로뿐이다. 본 SPEC이 폐쇄집합 개정 절차의 선례를 만들었으므로 후속 SPEC은 절차를 재발명하지 않고 **엔트리 추가와 코퍼스 갈래 결정만** 다루면 된다.
  - **M2 라이브 1턴은 일부러 돌리지 않았다(`SKIP: CONDITION_NOT_MET` · ASSUMPTION-68)**: M2는 사용자의 **실제 쇼파일에 좌표를 쓴다.** 사람이 없는 자리에서 그것을 하는 것은 **본 SPEC이 존재하는 이유가 되는 바로 그 행위**이므로, 무인 실행은 검증이 아니라 결함 2의 재현이다. 성공 판정은 애초에 구조에 걸려 있고(`REQ-WRITEGATE-014` — *"성공은 구조에 걸고 라이브는 보조 증거다"*) **구조적 기준은 M2 없이 전부 충족됐다**. 남는 미지수는 **정확히 하나** — 승인 카드가 라이브에서 **렌더되는가**다. 분류 · 백업 규칙 ③ 발동 · 거부 시 송신 0건은 전부 단위로 고정돼 있고, 사용자 입회 세션에서만 닫을 수 있는 관측 1건이 후속으로 넘어간다.

- **SPEC-COPILOT-SPATIAL-001** — 공간 인식 연출: 앱이 픽스처가 **어디에 있는지** 알게 된다 (`status: completed` v0.3.0, M0~M6 전 마일스톤 + 사람 관측 5회). 종전 rig context의 픽스처 스냅샷은 `{no, name}` 두 축뿐이라 *"왼쪽에서 오른쪽으로 웨이브"* 가 **1행×30대와 3행×10대에서 같은 커맨드**였다. 본 SPEC이 그 채널을 양방향으로 연다 — **READ**(`get_spatial_context`: 패치 3D `posx/posy/posz` 판독 → 행 검출·정렬 4종) · **WRITE**(`arrange_fixtures`: 프리셋 3종 `grid`/`row`/`circle` 좌표 계산 후 기록). 신규 패키지 `server/spatial/`(schema · rows · sorting · presets · choreography) + 룰북 자산 `32_spatial_design.md`(라이브 확인 문법만) + 툴 2종, `TOOL_NAMES` 18 → **20**. 검증: pytest **4511 passed · 5 skipped · 0 failed**(기준선 4246 대비 신규 실패 **0**) · vitest **350 passed** · 뮤테이션 전건 RED(M1 8종 · M4 7종 · Z축 2종 · Gemini 스키마 1종) · 라이브 기록 **8회 전부 복구, 쇼파일 잔여 0**. **핵심 원리**: 웨이브 방향은 좌표가 아니라 **선택 순서**가 정한다 — 정렬된 가산 선택(`Fixture a + Fixture b + …`) 위에 페이저를 얹는다.
  - **실사용 확인 — 실제 앱·실제 `/ws`·실제 Gemini·실제 게이트·실제 onPC**: *"모든 장비를 5미터 높이에 자연스럽게 배치하고 컬러와 딤머 이펙트로 연출"* 을 끝까지 실행했다. 5m 높이 ✅ · 컬러+딤머 ✅ · **"자연스럽게" → 모델이 `arrange_fixtures`를 fid 부분집합으로 4번 호출해 테이퍼를 합성**(`1–6→y=3.0` · `7–11→y=1.0` · `12–15→y=−1.0` · `16–18→y=−3.0`) — `grid`는 등길이 행만 내므로 **단일 프리셋으로는 만들 수 없는 형상**이다. 폐쇄 프리셋 3종이 조합으로 열린 형상을 낸다는 설계 의도의 실측 확인. 한 턴으로는 `loop_limit`(비용 상한 `max_model_calls=12`)에 걸려 **부분 실행을 정직하게 보고**했다.
  - **⚠ 결함 1 — 절단된 리그에서 모델이 불완전성을 알리지 않았다**: 스냅샷이 `childCount 19` / 반환 18 / `truncated: true`이므로 18대만 배치되고 **fid 19만 원점에 남았다**. **툴은 제 몫을 다했다** — 절단 보고 ✅ · 좌표 발명 **0** ✅ · 설명문이 이미 *"the list is NOT the whole rig — **say so**"* 를 명령형으로 적고 있다. 그런데 모델은 **금지된 바로 그것**을 했다(받은 일부에 대한 좌우 정렬 제시 + 불완전성 침묵). **툴 결함이 아니라 모델 준수 갭이며, 툴 설명은 지시일 뿐 강제가 아니다** — `server/looks/**`가 쓰는 방식의 상속된 한계다. 구조적 강제(부분 리그 상태값 또는 정렬 결과 보류)를 후속 과제로 등록했다.
  - **⚠ 결함 2 — 요청하지 않은 쇼파일 변형 54건이 무승인 통과했다**: 해당 턴의 지시는 *연출만*이었는데 모델이 대화 이력에 남은 미완의 "자연스럽게 배치" 목표를 이어 완성했다. 의도는 합리적이나 **그 사이에 사람이 없었다.** `Set Fixture … Pos*`가 AC-031 `[DEFERRED]`로 `safe`이므로 게이트가 카드 없이 통과시키고 showfile 백업 규칙 ③도 발동하지 않았다. 같은 턴의 `Go+ Page 1.202`는 **정상적으로 카드를 띄웠다** — 즉 **게이트는 건강하고 좌표 기록만 그 그물을 통과한다.** AC-031 되돌림의 대가가 가설이 아니라 **관측 사례**로 실현된 것이며 후속 SPEC 우선순위 근거다. 남은 방어선(원좌표 백업·재조회 검증·복원 번들·범위 봉쇄)은 설계대로 작동해 복구도 그 경로로 했다.
  - **⚠ Z축 미검증 축을 사용자 지적으로 발견하고 고쳤다**: `no_spatial_spread`가 **5m 수직 열**에 대해 거짓을 말하며 진짜 축퇴 리그와 같은 신호를 냈다 — x/y만 보고 z를 보지 않았다. 수직벽·수직열 2배열을 라이브로 관측해 수정했고 뮤테이션 2종 RED. 앱 E2E에서 전 픽스처 z=5.0일 때 `vertical_span = 0.0` + x 확산 1행 고신뢰로 **필드가 모델에게 정확히 전달되는 것**까지 확인했다.
  - **⚠ 소급 발견 — 정렬 어휘 `left_to_right`의 기준이 house(객석)다**: MA3 공식 축은 **+x = stage left**(*"Stage right will be negative numbers"*)이고 무대 관례상 **stage 기준과 house 기준은 정반대**다. x 오름차순이므로 `left_to_right` = house left → house right = **stage RIGHT → stage LEFT**. 조명 디자이너가 *"stage left에서 stage right로"* 라고 하면 **이 정렬의 역방향**이다. **동작·라이브 판정은 전부 유효하고**(P8 관측에서 사용자가 본 "왼쪽"이 최소 x였다) **낱말이 기준을 말하지 않는 것**이 결함이다. 출하된 폐쇄 집합이라 개명하지 않고 `spec.md §C.4`·`design.md §3.2`에 기준을 명기했다(sync-phase). `SPATIAL_ROW_ORDER`의 *"stage front to back"* 도 비표준 낱말로 정정 기록(표준은 `Downstage → Upstage`). 후속 GROUPGEN-001은 **기준을 이름에 박아** 회피한다(`GEO Stage Right N`) — 두 SPEC이 같은 좌표를 같은 방향으로 읽는 것을 sync에서 실측 확인했다.
  - **선재 결함 1건 수정**(본 SPEC 범위 밖, 커밋 `a5fa16a`): Gemini가 `additionalProperties`를 거부해 **모든 턴이 400으로 죽고 있었다**. base `4d298b8`에서 11종이 이미 보유 — 원인이 본 SPEC이 아님을 워크트리 실측으로 확인했고, 앱이 기동 후 무응답이라 고쳤다.
  - **`[DEFERRED]` 3건**(후속 SPEC 소유): ① **Layout 기록**(REQ-003 · AC-003) — ASSUMPTION-55 실측 근거, v1은 3D-only ② **AC-031 risky 분류** — `server/safety/` PRESERVE 경계. 구현했다가 되돌렸다(기존 테스트 10건 · `corpus.yaml`의 *"non-risky verbs only"* 불변식) ③ **REQ-024 승인 흐름** — ②에 종속. `server/safety/**` byte-diff **0**. REQ-024의 *"확인 없이 콘솔에 도달하는 경로는 없다"* 는 **감사 의미로는 참이고 승인 의미로는 오늘 거짓**이며, 한 문장을 두 뜻으로 읽을 수 있게 둔 것이 결함 2를 놓친 문면상의 원인이다.

- **SPEC-COPILOT-GROUPGEN-001** — 배치 인식 그룹 생성: 배치의 위상(topology)을 판별해 그 성격에 맞는 폐쇄 어휘로 그룹을 만든다 — **이름은 결과이고 판별이 본체다.** SPATIAL의 선택 순서(런타임 상태·`ClearAll`로 소멸)가 표현할 수 없던 "뒷줄만"·"바깥 링만"을 쇼파일 **영속** 자산으로 만든다 (`status: completed`, M0~M6 전 마일스톤 + 사람 무대 관측 GO). 신규 `server/spatial/topology.py`(위상 6종) · `server/spatial/naming.py`(폐쇄 어휘, `GEO` 접두) · `server/spatial/fixture_type.py`(장비 종류 축, 보수적 범위) · `server/groupgen/write.py`(순수 그룹 쓰기 계획 조립) + 툴 2종(`classify_arrangement_topology` · `create_arrangement_groups`), `TOOL_NAMES` 20 → **22**. 검증: pytest **4676 passed · 7 skipped · 0 failed**(run 킥오프 재측정 기준선 `4511 passed · 5 skipped` 대비 신규 실패 **0**) · vitest **350 passed**(UI byte 무변경) · ruff 3건 전부 기준선 부채 · `server/safety` byte-diff **0** · 룰북 byte-diff **0** · 뮤테이션 **6/6 RED**. M6 라이브 E2E: 3×6 그리드 → `grid` · 2겹 동심원 → `concentric[6,12]` · 좌우 분할 → `lateral_split[9,9]` · 전대 원점 → `None`+저신뢰(REQ-004, 위상을 발명하지 않는다). 사람 무대 관측 **GO**(`Group 2` 내륜 6대 / `Group 3` 외륜 12대, 의도대로 갈렸다).
  - **M0 — 게이트 A NEGATIVE는 응답기 한계가 아니라 MA3 플랫폼 한계였다**: 접근자 경로(`props COUNT`)로 그룹 멤버십을 실제로 읽으니 실사용 그룹 4개 전부 `COUNT` = **0**. 같은 배치의 날조 대조군(`ZzzBogus`)은 `ok:false`인데 `COUNT`는 `ok:true`이므로 이 `0`은 오류가 아니라 **실제 판독값** — 오브젝트 트리 `childCount: 0`과 정확히 일치한다. `introspect`가 Group 속성 **101개**를 열거해도 멤버십 필드가 없다. 정직한 천장: 101 중 73은 payload 절단으로 **미확인**(`introspect`에 offset 인자 없음). → **멤버십 검증은 이 플랫폼에서 원리적으로 불가**하다고 기록하며, `REQ-023`의 GO 분기는 도달 불가 분기로 명시했다.
    - ⚠️ **2026-08-24 하향 (원문 보존 — 위 항목은 한 글자도 고치지 않았다)**: 마지막 절의 「멤버십 검증은 이 플랫폼에서 **원리적으로 불가**」는 **측정보다 넓다.** 측정된 것은 *「시도한 채널(`props COUNT` · `state` · `introspect` 101필드)에서 안 보인다, 그중 73은 절단으로 미확인」* 이고 — 그 천장은 같은 줄이 스스로 적어 뒀다 — 적힌 것은 **플랫폼 성질**이다. 그 도약만 좁힌다: 판정은 **미측정**이며 `SPEC-COPILOT-RESTORE-001/readability-survey.md` §A.2 가 전제 만료를 지적하고 §A.5 가 하향했다. **측정과 날조 대조군(`ZzzBogus`)은 그대로 유효하다** — 취소되는 것은 범위뿐이다.
  - **M0 — 점유 슬롯 `Store`가 GUI 모달을 띄운다**: 조용히 덮지 않고 **더 나쁜 방식**으로 덮는다 — 무인이면 `ok:false "User Canceled Command"`, 사람이 확인 다이얼로그의 OK를 누르면 **덮인다**. 앱 관점에서 비결정적이고 **라이브 콘솔 UI를 블로킹**할 수 있는 운영 위험이며 본 세션이 처음 관측했다. `REQ-022`(점유 슬롯 정적 차단)의 근거를 "조용한 덮어쓰기"에서 "조작자 의존 + 모달 위험"으로 강화했다.
  - **M0 — `Store Group`이 안전 게이트에서 `safe`로 분류된다**: 승인 카드 없이 콘솔로 나간다(`blacklist.yaml`은 `Delete`와 `Store /overwrite`만 담을 뿐 무플래그 `Store`는 없다). `D-Q4`(안전 게이트 확장, `REQ-031`)를 경험적으로 정당화했다.
  - **REQ-024 정정 — 절단 가드가 틀린 곳에 있었다**: 명시 fid 목록 쓰기를 목록 절단(`childCount 19` vs 반환 18)으로 전면 거부해 ~18대를 초과하는 리그 전부에서 실사용 불가였다. 가드를 **쓰기 경로에서 판별 경로로** 이동하고 `coverage{judged, of, complete}` + `topology_partial` 필드를 신설해 "39대 중 18대"를 표기 가능하게 했다.
  - **M6 — 미러 아티팩트(이 SPEC이 고치려던 결함의 거울상)**: 좌우 대칭 평면 배치(x=±3…±11, y=z=0)에서 원점 반지름이 `|x|`로 붕괴해 모든 반지름이 정확히 한 쌍을 갖는다. `concentric`이 완벽 분리로 score **20.0**을 얻어 `lateral_split`(0.75)을 압도하고 **"9개의 2대짜리 링"**을 답했다 — research §3(2겹 동심원을 9행으로 오독)과 같은 등급의 오답이다. 수정: 모든 반지름 버킷이 정확히 2이고 `bilateral_pairs`가 고신뢰면 대칭의 대수적 서명이므로 반지름 판독을 경합에서 강등. 뮤테이션 RED로 증명(강등 제거 → 1 failed).
  - **M6 — `bilateral_pairs`가 경합에서 이겼다**: 미러 아티팩트를 고치자 대칭 신호가 승자가 됐는데, `D-Q10`은 대칭이 그룹이 되지 않는다고 못박았고 `naming.py`에 어휘가 없어 선택되면 산출 그룹 **0**이 됐다(조작자에게는 "아무것도 못 찾았다"로 읽히지만 실제로는 이름 붙이면 안 되는 대칭 리그를 찾은 것). 수정: `scored`에서 제외하되 `candidates`에는 그대로 보고.
  - **M6 — 슬롯 ≠ fid**: 리그가 39대로 커진 뒤 첫 시도가 목록에 안 보이는 fid를 노렸고, `arrange_fixtures`가 *"18 of 18 targets could not be read, so NOTHING was written"*로 **fail-closed 거부**했다 — 백업 없는 좌표 쓰기를 막은 이 거부 자체가 M6 증거다.
  - **어휘 2건 소인**: `SR Boom N`(리깅 하드웨어 — spec.md §D는 `Electric N`만 차용 허용) → `Stage Right/Left N`. `Low Side/High Side`(`roles.py`의 사이드 role 힌트 `Side`와 단어경계 충돌 + 사이드라이트 시스템 어휘) → `Low/High`. 둘 다 같은 오류 계열이다.
  - **REQ-031 게이트 확장 → 툴 계층 강제로 정정**: `server/safety/classify.py` 확장을 구현했다가 되돌렸다 — 기존 테스트 10건이 깨졌고, `server/measurement/corpus.yaml` 헤더가 "non-risky verbs only"를 불변식으로 선언하며 그 코퍼스 첫 `task_type`이 `group_create`(AC-MVP-001 10개 대표 작업 중 하나)였다. 사용자가 파급 0(툴 계층 승인 강제)을 선택. `server/safety/**` byte-diff **0**.
  - **알려진 천장**: 본 SPEC 툴을 경유하지 않는 채팅 경로의 직접 `Store Group` 발화는 여전히 무승인(spec.md §C.1.1). 좌표 쓰기도 여전히 무승인(AC-SPATIAL-031 DEFERRED, 라이브 재현). `corpus.yaml`의 `group_create`가 `Label Group 3 "Vocal"` — 큰따옴표를 쓰는데 `protocol.py:109`가 거부하는 형태(mock 전용이라 미노출된 기존 잠재 결함).
  - **개정 v0.5.0 — 머지 전 독립 리뷰 4인이 P0 1건을 잡아 머지를 멈췄다**: PR을 열고 **머지 전에** 리뷰어 4인을 파일 무교차 4축(안전 경계·위상 정확성·툴 표면·증거 정합성)으로 병렬 투입했다(SCENE-001 선례). **P0 — 승인 무결성 붕괴**(리뷰어 2인이 서로 다른 축에서 **독립 수렴**): `_is_programmer_state`가 `Fixture 7`은 dedupe에서 면제하지만 **가산 체인 `Fixture 1 + Fixture 2 + Fixture 3`은 면제하지 않아**(실측 `False`), 승인된 계획을 한 번들로 넘긴 `create_arrangement_groups`에서 동일 fid 그룹의 둘째 선택 줄이 `skipped_already_executed`로 탈락하고 **`Store Group N`이 빈 프로그래머에서 발화**했다 — **사람이 승인한 카드와 콘솔이 받은 것이 다르다.** 제조사·모델 1:1 리그(대부분의 실사용 리그)에서 두 축의 fid 튜플이 바이트 동일하므로 평범한 경우에 터지고, **v0.4.0 개정이 축을 전부 보고하게 만들어 도달 범위를 넓혔다.** 멤버십이 판독 불가라 **탐지·복구가 원리적으로 불가**하며, 쓰기를 반영하는 콘솔에서는 `status:"created"`·`slot_exists:true`·`name_verified:true`로 **빈 그룹을 긍정 확인**한다. 결정적 근거: 저장소가 같은 번들 형상을 **이미 세 곳에서 가드**한다(`looks/busking.py`·`scene/compile.py`·`fx/instantiate.py`의 `@MX:ANCHOR`가 *"the drop is silent, because `Store` still runs and the console still answers ok"* 를 명시) — **판단 미스가 아니라 알려진 패턴을 놓친 것**이다. 수정은 두 겹: 그룹당 번들 분리 + 새 `ExecutionContext`(교차 호출 누적도 함께 닫힘), 그리고 `write.py`가 자기 줄을 스스로 분류하는 `guard_bundle_collision`(면제 집합 동일성을 테스트가 단정). **`_PROGRAMMER_STATE_COMMANDS` 자체는 넓히지 않았다** — 넓히면 looks/scene/fx로 파급된다. **P1 3건**: ① **미러 아티팩트 강등이 리그 지문에 걸려 있었다** — 여분 장비 1대만 추가하면 "9개 2대짜리 링"이 재발했다(같은 결함을 두 번 놓쳤다: 첫 수정이 *증상이 난 리그*를 고정했을 뿐 *원인*을 고정하지 않았다). 진짜 현상은 *평면이 평평해 `hypot(x,y)`가 `|x|`로 붕괴한다*이므로 붕괴 조건 1개로 교체 — 평면 미러-바 스윕 `concentric` **6/15 → 0/15**. ② **`median_gap > 0` 가드가 완벽 정렬 행 리그의 depth 점수를 0.0으로** 만들었다(정렬 3×10 = **0.0** vs 창립 오독 리그 2겹 동심원 = **36.6**, 역전). **도달 범위가 이 PR 자신**이다 — `arrange_fixtures` grid 프리셋이 행당 y를 완전 정렬해 쓰므로 앱이 직접 쓴 리그를 자기 분류기가 오독하며, `vertical_levels`가 어휘와 **파티션**까지 가로챘다(`depth_rows[5,5,5]` → `vertical_levels[10,5]`). 절 삭제 + **깊이 우선 정책**(사용자 도메인 결정: 전기바는 '앞/중/뒤'로 부르고 트림은 부수적). ③ **AC 인용 35건 중 20건이 해석 불가** — plan-phase 이름을 구현 후 반영하지 않아 AC 문면대로 돌리면 `no tests ran`이 났다(41건으로 정리, 미해석 0). **자기 정정 1건**: v0.4.0에서 코디네이터가 W8의 AC-043 뮤테이션 "3 failed"를 과대라 정정했는데 **그 정정이 틀렸다**(파일 범위 3 · `-k` 좁은 범위 2) — 검증하는 쪽이 더 좁은 범위로 재고 차이를 상대 오류로 귀속시킨 형태다. **범위 밖 발견**: `SPEC-COPILOT-INTROSPECT-001` 6파일(1,271줄)이 `git add -A`로 쓸려 들어와 있었다 — `spec/introspect-001`은 main+15커밋 타 작업자 브랜치이고 우리 사본은 전부 낡았다. SPATIAL 자신의 progress.md가 *"이 브랜치에서 untracked"* 라 적고 있어 **문서가 트리와 모순되며 문서 쪽이 옳았다**. 제거했다. 검증: pytest **4716 passed · 7 skipped · 0 failed**(+34) · vitest 350 · `server/safety` byte-diff **0**.
    - ⚠️ **2026-08-24 하향 (원문 보존)**: 「멤버십이 판독 불가라 **탐지·복구가 원리적으로 불가**」의 전제는 위 M0 항목의 판정이며, 그 판정이 **미측정**으로 하향됐다(§A.2·§A.5). **P0 의 위험도는 안 낮아진다** — 미측정이어도 탐지·복구 경로는 지금 없다.
  - **개정 v0.4.0 — 잔여 SKIP 2건을 라이브로 닫으며 결함 2건이 더 드러났다** (in-place amendment: `completed -> in-progress -> completed`, `amendment_of` 자기참조, HISTORY `## Amendments`). **(1) 타입 축 GO(ASSUMPTION-67 종결)**: 리그가 이종이 되며(`fid 1~19` Robin MMX Spot · `fid 20~39` Robin LEDBeam 350, 제조사 전부 `Robe`) `type_name` 축이 처음으로 실제 2분할됐다. 이 실측이 `fixture_type.py`의 두 결함을 드러냈다 — **축별 침묵 영성**(분할 없는 축이 빈 튜플로 조용히 사라져 "균일해서 없음"과 "읽기 실패"를 호출자가 구분 불가) → `FixtureTypeAxisReport`로 **모든 축을 매 호출 명시 보고**(`uniform_across_rig`/`all_values_unreadable`), 그리고 **전체-호출 raise가 부분 결손을 삼킴**(장비 1대의 빈 구조 필드가 읽을 수 있는 38대의 축을 통째로 죽임) → `FixtureTypeUnreadable` **항목별 강등**으로 변경(키 누락·타입 오류는 여전히 raise). 뮤테이션 RED 실측: 축 보고 제거 → 2 failed · 빈 문자열 raise 복원 → 2 failed. **(2) 실제 Gemini 턴 GO**: `/ws`에 한국어 지시 1건(*"지금 배치에 맞게 그룹 잡아줘"*)으로 모델이 **위상 분류 툴을 스스로 선택**했고(감사 로그 `property_query` 72건이 `read_spatial_fixtures` 서명과 일치 + 슬롯 실측 `state_query` 2건), **폐쇄 어휘를 그대로 사용**(`GEO Inner`/`GEO Outer`, 창작 이름 0건), 무엇보다 **`coverage{judged:18, of:39}`·`topology_partial` 공시가 자연어로 사용자에게 도달**했다(*"전체 39대 중 나머지 21대는 … 제외되었습니다"*). 승인 카드를 거부하니 **쓰기 0건**(`commands: []`, 턴 후 그룹 풀 = 기준선 동일). ⚠ 새 위험 1건 기록: 모델이 원시 명령 체인을 산문으로 적으므로 승인 시 `create_arrangement_groups` 대신 `run_commands`로 흘릴 경로가 열려 있다(툴 설명 조종 문제 — 다음 SPEC 후보). 검증: pytest **4682 passed · 7 skipped · 0 failed** · `server/safety`·룰북 byte-diff **0**.

- **SPEC-COPILOT-SCENE-001** — 씬 컴파일러: 룩(정적 값) + 이펙트(스텝 열) + 타이밍을 **하나의 큐**로 합성 (`status: completed`, M0~M8 전 마일스톤 + **머지 전 독립 리뷰 4인 병렬** · **PR #10 squash 머지 2026-08-02, 새 `origin/main` = `85611a1`**). LOOKLIB(정지 화면 어휘)·FXLIB(시간축 어휘)이 세운 "의도→메모리 파이프라인"의 **2단계**이며, FXLIB이 `spec.md:42`·`:70`·`:140` 세 곳에서 명시적으로 예약해 둔 좌석을 이행한다. 신규 패키지 `server/scene/`(schema · loader · matching · compile · report · library) + 툴 2종(`find_scene` · `compile_scene`)을 기존 `run_commands` → `gate.screen()` 단일 관문으로만 배선했다. AC-SCENE-001~024 **24건 전량 PASS** — 소프트웨어 22건은 기계 검증(pytest), LIVE AC 2건(**AC-SCENE-019** M0 프로브 정리 · **AC-SCENE-021** M8 종단)은 실제 grandMA3 onPC 2.4.2로 검증. **테스트 3432 → 3927 passed · 5 skipped**(정본 baseline `3c701b1` 기준 신규 **495** — run-phase 362 + 머지 전 리뷰 117 + 재주입·가드 스윕이 낸 구멍 폐쇄 16, 신규 실패 0) + vitest **223 passed / 13 files**(기준선 동일 — sync에서 직접 재실측) · **뮤테이션 47/47 killed(survived 0)** · **라이브 세션 2/2** · PRESERVE diff 빈 출력 · 닫힌 툴 집합 11 → **13**.
  - **M0 — 라이브 프로브가 사용자 확정 정책을 무너뜨렸고, 폴백하지 않은 것이 정답이었다**: 트래킹 정책 D1(전 큐 `/CueOnly`)의 전제인 `ASSUMPTION-41`(플래그 접수)이 **기계 채널 소진**으로 `CONDITION_NOT_MET`이 됐다 — 날조 대조군 **`/CueOnlyy`(고의 오타)가 `ok`를 받고 저장까지 됐고**, 그 큐가 기대한 이름·`cueNo`를 그대로 가져 **재조회 채널마저 비변별**이었다. M0 플랜이 적어 둔 대비책(*"`ok`가 무력하면 재조회에 의존한다"*)이 **함께 무너진 것**이며 이는 플랜이 예상하지 못한 상태다. 에이전트가 무플래그로 조용히 진행했다면 사용자는 정책이 바뀐 사실조차 몰랐을 것이므로 **중단 + 블로커 보고**했고(`plan.md §A.3` 예외 — 사용자 확정 정책의 전제가 무너지면 축소가 아니라 중단이다), 이 규율은 v0.2.0에서 **`REQ-SCENE-021` (b)로 요구화**됐다. 판정 5건은 접두 행 6행으로 기계 판독 가능하게 기록했다(`^` 앵커 grep — `SKIP:` 1 · `DESCOPE:` 1 · `GO:` 3 · `REOPEN:` 1).
  - **M0의 부수 발견 3건 — 실패한 프로브가 더 중요한 사실을 냈다**: **(1)** 저장 순서를 역전시키려는 후속 프로브 D″가 실패하면서 **큐 생성이 실질 append-only**임이 드러났다 — 이미 존재하는 큐보다 낮은 번호는 **플래그 무관** 거부된다(`Not allowed`). 그러면 `/CueOnly`의 보정 대상("다음 큐")이 저장 시점에 **영원히 존재하지 않으므로** `ASSUMPTION-42`의 A=B(대조군이 갈리지 않음)가 설명된다. ⚠️ 룰북 `:56`의 **소수 번호 삽입** 서술과 표면상 충돌하며 소수는 **미측정**이다 — *"삽입 일반 불가"*로 확대 해석하지 않는다. **(2)** 거부 메시지가 원인마다 다르다(점유 큐 재저장 `User Canceled Command` · 역순 저장 `Not allowed`) — **메시지 리터럴을 단정 근거로 쓰면 안 된다.** **(3)** `DataPool/Sequences` 재조회가 childCount **24**에 반환 **18** + `truncated: True` — `REQ-SCENE-013` (d)의 자동배정 거부 가드가 **가상의 방어가 아니라 실재 조건에 대한 방어**임이 확인됐다.
  - **사용자 재결정 — 미발화 커맨드 대신 구조적 회피**: `/CueOnly`를 폐기하고 **속성 집합 균일화 + 미주장 속성 전수 열거**(옵션 D)를 채택했다. 신규 미발화 커맨드 **0개**(FXLIB이 *"신규 시퀀스 Cue 1만"*으로 택한 회피와 같은 계열)이고 *"모든 씬의 값 라인 속성 집합이 동일하다"*는 **정적으로 단정 가능**하다. 근거 조사(룩 32개 전수 파싱)가 코어 4 = **32/32** · 유니온 6 · Zoom 16/32 · Iris 8/32를 냈고 **자산 편집 0건 · PRESERVE 무교차**를 확정했다. `Zoom`/`Iris`는 방향 미측정이라 균일 보장 밖이며 **채움값을 발명하지 않는다.**
  - **개정이 닫지 못한 축을 §D에 명시했다 — Pan/Tilt**: 룩은 Pan/Tilt를 **구조적으로** 가질 수 없고(`server/looks/loader.py:105-110`) fx 12개 중 **8개가 Pan/Tilt 전용**이다. movement 씬 뒤에는 위치가 트래킹되며 룩 값 라인이 덮을 수단이 없다 — **적지 않으면 *"구조적 회피"* 라는 서술 자체가 과잉 주장**이 되므로 제외 항목으로 명시하고 미주장 열거(`REQ-SCENE-014` (d))가 **매 컴파일마다** 노출한다.
  - **관측 천장은 개정으로 바뀌지 않았다 — 이것이 이 SPEC의 중심 AC다**: 트래킹 전파는 **관측 주체가 없다**(`ui/src/components/ExecutionPreviewCard.tsx:61`). *"균일 집합을 발화했다"* 는 정적으로 확인되지만 *"그래서 트래킹이 무해해졌다"* 는 **관측 불가**다. `AC-SCENE-015`가 주장 5종을 각각 **자기 상수**로 분리 강제한다 — (a) 산출물 · (a′) 균일성 · (b) 효과 · (c) 트래킹 무해화 · (d) 미주장 열거. (b)는 **전 경로 무조건** 실리고 (c)는 침묵하지 않고 **추론을 명시적으로 거절**한다(*"위 균일성 확인은 무엇을 발화했는지에 대한 사실이며 트래킹 무해화의 증거가 아닙니다"*) — 독자가 기본값으로 그 추론을 하기 때문이다. 배치도 분리해 두 주장 사이에 **반드시 표제가 하나 들어간다.**
  - **M5 뮤테이션 ④가 부분 일치 검사의 공허함을 실증했다**: payload가 상수 대신 **"직접" 한 단어만 빠진 거의 같은 인라인 산문**을 싣게 하자 `"사람" in text` 부분 일치로는 **통과한다**(실제로 확인). 동일성 검사만 잡는다 — 그 간극을 고정한 테스트를 스위트에 남겼다(`test_a_substring_check_would_miss_a_truncated_notice`). ⑥에서는 **위양성 하나를 잡아 고쳤다**: `"이월됐" not in ...`은 문면 자신의 부정문(*"이월됐다는 뜻이 아닙니다"*)까지 금지하므로 단정형 금지 + 부정문 존재로 바꿨다.
  - **이음매에서만 보이는 결함이 두 번 나왔다 — 슬라이스 테스트로는 둘 다 잡히지 않는다**: **(1)** M2/M3/M4 병렬 웨이브에서 M3가 상류 공개 상수 `server.fx.matching.PATTERN_ALIASES`의 **오늘 27/27 완전히 동일한 사본**을 들고 있었다(대칭차 `[]`) — **같으니까 증상이 0이고** 상류가 별칭 하나만 더하면 씬 매칭만 조용히 몸란다. 사본을 지우고 읽기 import로 교체했다(정정도 **슬라이스 소유자에게 되돌려 디스패치** — 코디네이터가 남의 파일을 고치지 않는다). 반대로 `_PARTICLES`/`_ENDINGS` 중복은 **위반이 아니라고 판정**했다 — looks·fx가 각자 자기 본을 가진 확립된 선례이며 **사본이라고 다 같은 사본이 아니다**. **(2)** M6에서 `compile.py`가 부르는 fx `select_sequence_number`가 **`FxInstantiationError`를 던져** 툴이 구조화 에러 대신 예외로 죽었다. 고친 위치는 툴이 아니라 **컴파일 계층**이다 — 툴에 `except FxInstantiationError`를 더하면 씬을 쓰는 모든 호출자가 fx 예외를 알아야 정확해진다. **사유 코드는 그대로 전달하고 예외 타입만 씬의 것으로 바꾼다.**
  - **값 라인 판별식을 이음매 테스트가 정정했다**: *"`;` 체인이면 룩 값 라인"* 은 **틀렸다** — `_speed_line`도 `;` 체인이다(`Attribute 'Pan' At Speed 112 ; …`). 판별식은 **모든 세그먼트가 절대값(`At <수>`) 형태인 `;` 체인**이며, 이 오판은 실제로 발생했고 fx 단독 씬이 잡아냈다. 리포트의 균일성 주장도 같은 판별식을 쓴다 — **컴파일러를 믿지 않고 산출 문자열을 다시 읽는다**(주장의 내용이 *"빌더가 정렬을 의도했다"* 가 아니라 *"번들이 실제로 그 순서를 담았다"* 이기 때문).
  - **M7 — 커밋 전에는 보이지 않는 테스트가 있다**: `test_songcue_bundle.py`의 `_TOOLS_EXPECTED_HUNK_OLD_STARTS`는 `git diff <BASE>..HEAD`를 읽는 **의도적 트립와이어**다. M6 커밋 **직전** 전체 회귀는 3766 passed로 그린이었고 **직후** 실패했다 — 변경이 아직 `HEAD`에 없었기 때문이다. **git 상태에 의존하는 테스트는 커밋 후 한 번 더 돌린다.** 스냅샷에 old-start **15**를 의도적으로 추가하되 이 가드의 진짜 불변식인 **보호구역 무침범 단정**(`(234,238)` · `(524,569)`)은 손대지 않았다.
  - **강제 코드를 빼도 오늘은 아무 일도 일어나지 않는다 — 그래서 픽스처를 주입한다**: 룩 자산 **32/32가 이미 코어 4를 이 순서로** 담고 있어 균일 정렬 도입은 **바이트 무변화**다. M4 뮤테이션 ⑰이 이를 **실측으로 입증**했다 — 정렬 로직을 제거하자 32개 전수 스윕은 **전부 통과한 채** 반전 픽스처 테스트만 죽었다. 같은 이유로 상류 `_values_line`의 **인자 순서 보존**을 따로 고정했다(상류가 내부 정렬을 도입하면 씬의 균일 강제가 **무음 no-op**이 된다). 충돌 열거 비공허성도 재료 규율이 있다 — **movement fx로 세우면 정답도 ∅이라 통과해 버리므로** dimmer/color fx로만 세운다.
  - **M8 — 종단 라이브(게이트 경유) · 감사 채널의 변별력을 먼저 세웠다**: `ok`를 어느 축의 증거로 쓰기 전에 날조 대조군 **`ZzzNotACommand 999`** 가 `failed`·`Illegal object`·`ok=false`로 갈리는지 확인했다 — M0의 store-플래그 축(`ok`도 재조회도 비변별)과 정반대이며 그래서 이 세션의 기계 주장은 **등급이 높다**. `truncated` 가드가 실물 조건에서 **발화 0건으로 거부**(`reason "sequence_truncated"` · 감사 델타 `state_query` 2건, `command` **0건**)했고, 사용자 GUI 정리 후(191~197 삭제 · childCount 24→17 · `truncated` True→False) 종단 2회를 발화했다. **두 씬이 서로의 대조군**이다 — `SCN GOLD PULSE`(dimmer fx)는 충돌 열거 `[Dimmer]`·미주장 `[Iris, Pan, Tilt]`, `SCN MOON RISE`(movement fx)는 충돌 **`[]`**·미주장 `[Iris, Pan, Zoom]`. **두 열거가 씬마다 실제로 달라졌다.**
  - **효과 관측은 `ok`가 아니라 출력값의 시간 변화로 잰다**: 정지 화면 1장으로는 모션을 주장할 수 없으므로 **시계열 표본 8장 + 픽셀 차분**으로 쟀다. 1회차는 Dimmer가 프레임마다 변하고(4.10·3.67·4.42·…) RGB는 **0.00 ×7 고정**, 2회차는 PanTilt가 변하고(1.46·1.38·…) Dimmer·RGB가 **0.00 ×7 고정** — **fx가 구동하는 축만 움직이고 룩의 정지 값은 그대로다.** 한 순간에 픽스처마다 값이 다르므로(`3·14·26·37·48·59·70·81·92`) 균일 점멸이 아니라 **위상 분산(웨이브)** 이고, 룩이 낸 `Dimmer 88`이 6~92를 순회하므로 **충돌 축에서 이펙트가 이겼다**. **한계를 함께 적는다**: 관측 채널은 **GUI 시트 판독**이지 무대 실물이 아니고, 표본은 **이산**(~1.5s 간격)이라 파형의 정확한 주기·형태는 주장하지 않는다. **이것은 M0 판정의 재측정이 아니다** — 같은 형상이 다른 맥락에서 어긋나지 않았다는 기록이다.
  - **비차단 결함 1건을 고치지 않고 기록했다 — SPEC 자신의 표제 문장이 매칭되지 않는다**: `find_scene("파란 백라이트가 천천히 웨이브하는 씬 만들어줘")` — **spec.md §A·acceptance.md 시나리오 1·plan.md §B M8이 그대로 쓰는 문장** — 이 **양 축 `no_match`** 다. 귀속은 씬 계층이 아니라 **상류 어휘**다: `하는`(하다-용언 관형형)이 FXLIB 닫힌 어미 목록에 없고 `파란`이 LOOKLIB 별칭에 없다(라이브러리는 `푸른`). 어휘가 있는 문장(`달빛 웨이브`)에서는 두 축이 정확히 붙으므로 **체인 자체는 정상**이다. sync-phase에서 사용자가 **안 D(기록만 하고 넘긴다)** 를 확정했다 — 두 상류 모두 **PRESERVE**이고, 씬 권한 안에서 가능한 유일한 수정(씬 어미 목록 확장)은 `find_fx`와 갈리는 **행동 분기**를 만들며 M3 정정이 사본을 지운 방향과 정면으로 반대이기 때문이다. **문서가 자기 예시를 실행하지 못하는 상태를 알면서 출하한다.**
  - **sync-phase — 문서 스테일 소인 16건(정책 무변경 · 요구/인수/마일스톤 신설 0)**: run-phase가 M8에서 닫은 사실이 **선행 문서 5곳에 전파되지 않아** 아티팩트가 자기 자신과 모순이었다(M0 정리 의무 "미이행" 5곳 · 라이브 세션 "1회 소진" 2곳 · SHA `pending-backfill` 2건 · frontmatter `status: draft`). **append-only는 지켰다** — §E.2의 마일스톤 기록과 HISTORY의 v0.2.1/v0.2.2 측정치(`접두 행 6`)는 **한 글자도 고치지 않았고**, 고친 것은 전부 *현재 상태를 서술하는 표면*이다. ⚠️ **`draft → in-progress` 전이 누락**을 발견했으나 **소급 위조하지 않고 기록만 한다**(커밋이 이미 랜딩했고 이력을 고쳐 쓰는 것이 더 나쁘다).
  - **독립 sync-audit이 FAIL 0.80을 냈고 그 지적이 sync 자신의 감사 기록을 반증했다**: 문턱 0.85(Tier L). 역추적 폐쇄성(REQ 21/21 · AC 24/24)·append-only·PRESERVE·상류 상수 결합·M8 산출 수치는 **전부 반증에 실패**했으나, **sync가 쓴 감사 4축 중 3축이 거짓/미검증**이었다. **(P1)** *"현재형 주장 불일치 0건"* 은 `§E.2` 접두 행 블록 헤더가 **"이 6행"이라 적는데 블록은 8행**이라는 사실로 **grep 한 번에 반증**된다 — v0.2.3이 두 행을 주석 없이 삽입했고 sync가 놓쳤다(소인했다고 선언한 뒤 남았다는 점에서 더 나쁘다). **(P2)** *"`§C.0a`와 1:1 일치"* 는 `AC-SCENE-019` 행에서 거짓 — 그 AC는 §C.0a에서 M0 배정인데 근거 절은 M8 ③이다(두 표는 다른 질문에 답하며, "1:1"이라 단정하면 *M0 배정 AC가 M8에서야 닫혔다*는 기록할 가치가 있는 사실을 가린다). **(P2)** 근거로 댄 `plan.md §0`은 **존재하지 않는 절**이고, sync 자신이 세운 gitignore 표기 규율은 **9곳에서 미준수**다. **(P2)** 이 SPEC이 유일하게 고친 파일(`tools.py`)의 **줄 앵커 7계열이 전부 썩었고** 그중 하나가 `plan.md §A.5`의 **PRESERVE 좌표**라 후속 소유자가 엉뚱한 곳을 지키며 통과한다 — **심볼 앵커로 교체**했다. **(P2)** M8이 추가한 AC 판정 2건에 **폐쇄 어휘 판정 절이 없었다**(접두 행만 있어 `^` grep은 통과) — 절을 신설했다. **(P2)** 무범위 *"ruff 클린"* 이 실측(`check server/` 3 errors · `format --check` 20 files)과 어긋나 **범위를 명기**했다. 전건 정정하고 각 정정을 §E.4에 남겼다.
  - **머지 전 독립 코드 리뷰 — P1 3건이 3794 전건 통과 상태에서 살아 있었다**: 이 저장소는 `gh pr checks`가 **0건**이라 유일한 관문이 사람/독립 리뷰이고, PRECHK PR #7(P1 4건)·OVERLAP run-audit(P1 6건)이 같은 경고를 이미 두 번 했다. 읽기 전용 리뷰어 3인을 코드 슬라이스별로 병렬 투입했고 **전부 `incorrect` 판정**을 냈다. **(P1-1, 두 리뷰어가 독립 검출)** `compile_scene`이 fx의 **거부 게이트 `_refuse_unemitted_axes`를 우회**한다 — 라인 빌더만 재사용하고 게이트를 건너뛰어, **fx 로더가 수락하는** 자산(`relative` 보유 / `circle`+`phase_to`)을 `instantiate_fx`는 거부하는데 `compile_scene`은 **선언된 축을 조용히 버린 채 수락**한다. 1스텝 `Fx`는 **`Step` 라인이 없는 번들** — M0가 세 번 발화해 모션 0을 본 그 형상이다. 콘솔은 전 라인 `ok:true`를 내고 큐 내용은 읽히지 않으므로 **런타임 신호가 0**이다. **(P1-2)** 매칭기의 **다섯 번째 상태가 성공 형상으로 나간다** — `SceneMatch.fallback`이 `kind == FALLBACK`이라 축은 잡혔는데 그 조합의 씬이 없으면 `selected=null · fallback=false · reason=null`. 출하 자산에서 axis id **8개 중 6개**가 이 막다른 길이고(`"웨이브"`·`"펄스"`·`"말씀 회전"`), 모델은 payload에 없는 `scene_id`를 넘기라는 지시를 받아 **`run_commands` 손작성**(이 SPEC이 막으려는 실패)으로 몰린다. **(P1-3)** 미확인 (a) 주장을 **`기계 확인됨:` 표제 아래** 렌더한다 — 표제가 "확인됨"이라 말하고 바로 아래 줄이 "재조회를 수행하지 않았습니다"라고 말하며, 툴이 `requery=`를 넘기지 않으므로 **모든 생산 리포트가 그 모양**이었다.
  - **세 P1을 고치고 그 수정이 스위트에 보이지 않는다는 것을 실증했다**: 오케스트레이터가 세 결함을 **직접 재현한 뒤** 원천에서 고쳤다 — 컴파일러가 거부 게이트를 호출하며 **예외 타입만 번역(사유 코드 보존)**, `SceneMatch.fallback`을 `AxisMatch`와 **같은 정의**(`fallback_reason is not None`)로 맞추고 신규 사유 `NO_SCENE_COMPOSES_AXES` 신설(`kind`는 여전히 어느 축이 잡혔는지를 보고 — **두 사실을 분리**), `to_korean`이 주장의 **사실에 따라** 표제를 고르도록. 그리고 **고친 직후 379 passed · 죽은 테스트 0건** — 관측 가능한 동작이 두 군데 바뀌었는데 스위트가 침묵했다. 그래서 같은 커밋에서 테스트를 강화했고 **모든 신규 테스트는 수정을 되돌리면 죽는 것을 뮤테이션으로 실증**했다(거부 게이트 회귀는 주입 시 **15건 사망**, sha256 원복 대조). 리뷰가 함께 잡은 **공허한 테스트**도 닫았다 — 균일 집합의 "먼저" 위치·면제 재사용·시퀀스 섹션 거부 3경로·`;` 체인을 룩 값 라인으로 오인하는 판별식(`_speed_line`도 체인이다)·`!= CONFIRMED`로만 검사하던 BROKEN 분기·교차 호출 접힘의 `is_error` 확대. **반증에 실패한 것**: 결합 순서·`Step 1` 미발화·금지 `At Step` 형태·Store 플래그 부재(대소문자 무관)·충돌/미주장 열거의 정확성·단일 실행 경로 AST 스캔·실물 게이트+실물 락의 LiveLock 강등 — 전부 표적 뮤테이션으로 죽었다.
- **SPEC-COPILOT-OVERLAP-001** — 보수적 점유폭 상계로 구간 겹침 판정 재개 (`status: completed`, M0~M8 전 마일스톤 + **독립 run-audit 1회차**): 픽스처를 자기 DMX 점유폭에 잇는 조인 키가 없어도 **열거 가능한 모드 전체의 폭 최대값**을 상계로 삼아 *"겹침 없음"*을 증명하고, 증명되지 않으면 **충돌이 아니라 미확정**으로 보고한다. 신규 `server/prechk/footprint.py` + 신규 게이트 `server/tests/test_overlap_preserve.py` · 어휘 축 `overlap_basis` 4값 · **테스트 2758 → 2946 passed · 5 skipped · 0 failed** · **라이브 세션 0회** · PRESERVE 위반 0건. **저장소에서 처음으로 제안서가 아닌 곳에서 나온 SPEC이다** — 출처는 PRECHK의 독립 run-audit 지적 I-15이며 제안서에 `구간 겹침`·`점유폭`·`overlap` 문자열은 0건이다.
  - **M0 — 전제 판정(`GO: ASSUMPTION-34` · 코드 변경 0건 · 프로토타입 비커밋)**: 3단 순회(`Patch/FixtureTypes` → `DMXModes` → `DMXChannels`)가 `query_state` **하나로만** 도달하는지를 인메모리 프로토타입 arm 5개로 갈랐다. **`query_property`를 정의하지 않은 포트**에서 조회 6회로 완주해 상계 31을 냈고 호출된 포트 메서드 집합이 정확히 `{query_state}`였다. **arm B(대조군)가 없으면 arm A의 통과는 공허하다** — 프로퍼티 요구가 `AttributeError`로 즉시 드러남을 확인했다. 귀결: **`server/safety/**` 무변경 확정 · 신규 예외 지점 0건**(PRECHK가 조건부 예외를 받은 사유는 `prop`이 프로덕션 경로로 도달 불가였다는 것이며 `state`는 초크포인트 3층에 이미 있다). `ASSUMPTION-31`·`32`·`33`·`35`는 관측 없이 닫히지 않으므로 **`SKIP:`**으로 남겼다(접두 행 합 정확히 5행). **부수 확립**: 순회가 스냅샷이 스스로 보고하는 **풀 슬롯 `i`**만 쓰므로 식별자 해석 단계가 0단계이며 `FID`를 배제해야 했던 종류의 미결이 이 축에는 발생하지 않는다.
  - **M1 — 어휘 확장(13편집점 + 배선 3) · 무증상 단계를 구조로 없앴다**: `server/prechk/report.py`의 import 시점 가드가 **하드코딩 5-튜플**이라 신규 축을 빠뜨려도 **어떤 테스트도 실패하지 않았다** — 스위트가 못 잡는 유일한 단계였다. 그 루프를 `CLOSED_VOCABULARIES` 순회로 바꿔 *"빠뜨린다"*를 표현 불가능하게 만들고(양방향: 어휘에 표 없음 · 표에 어휘 없음) **구조 변경을 신규 축 도입 *이전에*** 해서 3단과 4단 사이에 무증상 창이 열리지 않게 했다. 레지스트리 **맨 끝 append**로 기존 5줄을 바이트 동일하게 남기고 재타이핑 정본 3단정(집합·키·**순서**)을 전부 유지했다. 뮤테이션 4건 전건 사망 — 그중 **append 위치를 맨 앞으로 옮기면 순서 단정만 실패한다**(집합 단정 단독이면 통과하므로 순서 단정을 유지한 값이 거기서 관측된다).
  - **M2 — 순회 모듈: 형상 3건이 거짓 안심을 형상으로 막는다**: **(1)** 순회는 `(완전성, 폭 집합)`을 돌려주고 `WalkOutcome`에 `bound` 필드가 **없다** — 부분 모드 집합의 `max`는 참 상계보다 **작아** 넓은 간격을 잘못 통과시키고, 나중에 붙는 플래그는 그것을 되돌리지 못한다(플래그는 *순회*에 붙고 판정은 *주소 쌍*에 붙어 따로 여행한다). 폴드는 완전성 참 분기 **안에서만** 일어나고 그것을 AST로 판정한다. **(2)** 1·2단은 목록 완전성, 3단은 **계수 존재성** — 실측 리그의 `DMXChannels` 열거는 항상 `truncated=true`이므로 술어를 통합하면 기능이 **통째로 죽는다**. **(3)** 예산 소진은 국소 표기가 아니라 **전역 무효**다. 사유 코드는 신설하지 않고 `REASON_UNRESOLVED`/`REASON_UNREACHABLE`을 재타이핑해 테스트가 원본과 등호로 고정한다(`tools` import은 하드 순환).
  - **M2의 뮤테이션이 테스트의 구멍을 잡았다 — 코드가 옳고 테스트가 못 재고 있었다**: 6건 중 5건 즉사, **3단 예산 소진을 국소 표기로 강등하는 뮤테이션이 34건 전건 통과 상태에서 살아남았다.** 기존 예산 테스트 둘의 예산이 **폭을 하나도 수집하기 전에** 소진되어 *"관측된 모드가 0개다"* 가드가 대신 통과시키고 있었다. 예산 4(3모드 중 2개를 읽고 세 번째에서 죽는다)로 재면 강등된 코드가 `complete=True`·폭 `(5, 7)`을 내고 폴드가 **7**을 답한다 — 참 상계 11보다 작은 상계이며 정확히 거짓 안심이다. **뮤테이션을 돌리지 않았다면 발견되지 않았다**(규율 16의 이번 사례).
  - **M3 — 상계 판정: 술어는 `간격 < 상계`이며 `이하`가 아니다**: 구간이 닫힌 끝이므로 `간격 == 상계`는 **증명 가능하게 깨끗하다**(주소 `a`에서 상계 폭이면 끝이 `a + 상계 − 1`이고 다음 시작이 `a + 상계`라 맞닿기만 한다). 경계 테스트를 **순회 층과 판정 층 두 곳**에 두었고 `<=`로 바꾸면 6건이 죽는다. 간격은 `_address_duplicates`의 그룹핑을 **추출한 키 집합** 위에서 유니버스 내부로만 계산한다(중복 주소가 한 번만 들어가 간격 0이 생기지 않는다 — 그것은 주소 중복 축의 일이다). **`type_mode_ok`를 요구하지 않는 것이 설계의 핵심**이며 요구하면 그 자체가 논증의 오해다. 주소에 **하한**을 도입해 `0.0`·`1.0`·`0.1`을 판독 실패로 분류하고 **상한은 넣지 않았다** — `ASSUMPTION-33`이 용량을 미확정으로 두므로 `1.65535`가 **통과함**을 단정해 그 부재를 기계로 고정했다. 미확정은 `range_overlaps`에 넣지 않고 `skipped_checks` 한 행으로 고지한다(침묵이 결함이고, 충돌로 세면 *"충돌 N건"*이 거짓으로 발화한다).
  - **M3이 "착수 시점에 살아 있던 뮤테이션"을 실측으로 확인하고 자기 리그의 공허함을 잡았다**: 조사가 `[코드]` 추론으로 적은 *"유니버스 키잉을 지워도 스위트가 통과한다"*를 직접 재현했다 — 라벨을 유지한 채 **주소 공간만** 붕괴시키면 기존 74건이 **전건 통과**하고 신규 테스트만 잡는다. 그 과정에서 **초안 리그(`1.500`·`2.001`)가 붕괴해도 답이 바뀌지 않음**을 발견했다(폭 40에서 구간 `500..539`와 `1..40`이 한 공간에서도 만나지 않는다). 초안 주석이 그 사실을 스스로 적으면서 리그를 바꾸지 않았고 그것은 **공허한 뮤테이션 테스트**였다 — `1.100`·`2.110`으로 고치고 *"같은 두 주소를 한 유니버스에 놓으면 두 축이 실제로 발화한다"*를 별도 테스트로 옮겨 비공허성을 산문에서 코드로 이동시켰다.
  - **M4 — 정확폭 우선 · 근거를 페이로드에 도달시켰다**: 폭을 **출처 경로와 함께** 담고(`ModeFootprint(path, width)`) `bound_source`가 `"<경로> childCount"`를 낸다. **선례가 경고다** — `FootprintPolicy.source`는 축이 출하된 이래 필드로만 있었고 페이로드에 도달한 적이 없어 소비자가 0건이다. 신규 최상위 키 하나에 7키를 싣고 **그 자리에 정확 키집합 단정을 신설했다**(기존 최상위 단정은 부분집합이라 키를 얹거나 빼도 아무것도 깨지지 않는다 — *"문서 무변경 가능한 자리 = 아무도 안 지키는 자리"*). 리그 전역 등급은 **수행된 비교 전체의 최약 등급**이다(`not_performed ≺ bound_inconclusive ≺ bound_proves_clear ≺ exact_widths`) — 3슬롯이 비교되지 않은 리그에 `bound_proves_clear`를 찍으면 미비교 슬롯이 비교된 슬롯에 업혀 간다. 정확폭 우선은 **두 층**에서 집행한다(정확폭 슬롯은 상계 대상에서 빠지고, **양 끝이 모두 정확폭인 간격**도 빠진다).
  - **M5 — 요약에 판정 근거를 도달시키고 한정을 같은 문장에 붙였다**: 페이로드에만 있는 등급은 아무도 읽지 않는다. `겹침 판정 근거: <라벨>(<관측 범위 한정>)`을 요약에 넣고 **한정을 등급과 같은 문장에** 두었다 — 별 문장이면 한 문장만 읽는 독자가 무한정 절반만 가져간다. 뮤테이션 2건이 1회차에 살아남아 테스트를 고쳤다: **(1)** *"정확폭에는 한정이 붙지 않는다"* 대조가 **`walk` 없는 리그**를 써서 공허했다(상계가 없으면 한정의 부재는 우선순위 규칙을 증명하지 않는다). **(2)** 한정의 모드 수를 상수 `2`로 바꿔도 통과했다 — 리그의 모드 수가 항상 2였다. 그리고 **라벨 부분문자열 충돌**을 잡았다: `"구간 겹침 판정 미수행"`이 `"겹침 판정 미수행"`을 포함하므로 단순 포함 단정은 `not_performed`에 대해 **등급이 한 번도 인쇄되지 않아도** 통과한다 — 접두 포함형으로 채널까지 고정했다.
  - **M6 — 툴 배선 · 계획을 실측이 두 번 정정했다**: 경로는 `rig_paths["fixture_types"]` 경유이며 순회 모듈에 `"Patch/FixtureTypes"` 리터럴 0건이다(오버라이드가 순회를 **실제로 이동시킴**을 단정한다). 섹션 가드는 **별도 상수를 신설**해 `create_macro` 분기 **밖에서 항상** 검사한다 — 기존 상수에 추가하면 **같은 오버라이드 누락이 인자에 따라 다른 결과**를 낸다. **(정정 1)** 누락을 **호출 거부로 만들 수 없었다** — 매크로 가드 메시지를 고정한 두 기존 테스트가 그 오버라이드를 쓰고, 더 근본적으로 거부는 이 툴의 존재 이유인 픽스처 재고를 버린다(같은 파일의 zero-target 분기가 이미 그 계열을 고쳤다). 섹션 이름을 리포트에 담고 등급을 `not_performed`로 낸다. **(정정 2)** 순회에 `sibling_answered`를 추가했다 — 첫 조회가 실패할 때 *"형제가 답했다"*는 **호출자만 아는 사실**이고 프로덕션은 두 실패를 같은 예외로 던진다. 없으면 툴 경로의 분류가 항상 `console_unreachable`이 되어 **설정 결함을 운영 조건으로** 보고한다.
  - **M6 — 선례 트립와이어는 갱신이 필요하지 않았다**: `plan.md`가 *"tools.py를 고치므로 같은 커밋에서 갱신한다"*고 못박았으나 **실측하니 갱신할 것이 없었다** — M6의 편집 전량이 PRECHK가 이미 연 hunk 영역 안에 떨어져 **old-side 경계 9개가 바이트 동일**했다(`--unified=0`에서 인접 삽입은 기존 hunk에 병합되고 트립와이어는 old-side만 본다). 선례 파일 변경 **0행**이며 이는 `AC-OVERLAP-019` ⑥의 *"1행에 한정"*보다 강하다. **갱신하지 않은 것을 갱신했다고 적지 않는다.**
  - **M7 — 이월된 PRESERVE 게이트를 상시 테스트로 집행했다**: 선행 SPEC의 게이트는 **1회성 수동 절차**였고 이 저장소는 CI가 0건이므로 **아무도 다시 돌리지 않는 게이트는 아무것도 지키지 않는다**. 신규 파일이 PRECHK BASE를 **단독 소유**한다 — 선례 파일은 다른 BASE에 묶여 있고 두 BASE를 한 모듈에 섞으면 게이트가 **엉뚱한 곳을 지키면서 통과한다**(두 상수가 정확히 **13** 어긋남을 단정으로 고정했고, 선례의 SHA는 **그 파일 소스에서 읽어 온다** — 재타이핑하면 드리프트와 자기참조가 생기며 실제로 초안이 자기 자신에서 실패했다). 비공허성을 전건 동반한다: 10경로 실재 + **분류를 목록에서 기계 도출**(손으로 적은 계수는 plan-audit가 P1으로 잡은 오류다) · 교차 술어가 심어둔 hunk를 실제로 잡음 · safety 삭제 1행은 **원문 대조와 구조적 독스트링 판정 둘 다** · `ruff` 대상을 `git diff`에서 도출.
  - **M8 — 종단 통합 · 라이브 0회 · 감사 1:1을 초크포인트로 관통시켰다**: 대역을 손으로 만들지 않고 **실제 `SafetyGate`**를 세워 `gate.state_port`를 툴에 넘겨 **감사 `state_query` 행 수 = 리그 조회 수** 등호를 단정했다(실패 조회도 1행 — 타임아웃도 OSC 송신 1건이다). 조회 계수는 착수 시점에 저장소 전체에 고정 단정이 0건이었으므로 **등호와 부등호를 둘 다** 신설했다(부등호만 두면 조회 1건이 영구히 조용히 늘어난다). 역방향 검증을 3등급으로 분리 기록했다 — **A**(모듈 부재 수집 오류 4파일 · 약한 증거이며 그렇게 적는다) · **B**(신규 툴 테스트 **19건이 수정 전 코드에서 실패**) · **C**(수정 전에도 통과하는 5건 + PRESERVE 게이트 23건을 **회귀 테스트가 아니라고 코드에 라벨**).
  - **불일치 4건을 덮지 않고 올렸다**(`progress.md` §5 `deviations`): **(1)** `AC-OVERLAP-021` ①의 4값 중 **`exact_widths`는 툴 표면에서 도달 불가능하다** — 출하된 핸들러가 `FootprintPolicy`를 만들지 않으며(AST 0건) 그 이유는 `ASSUMPTION-27` 부정이고 **본 SPEC의 상계 축이 존재하는 이유가 바로 그것**이다. 그 등급을 툴에서 내려면 기각된 축을 다시 켜야 하므로 범위 밖이며, **죽은 어휘는 아니다**(판정·리포트 계층에서 산출된다). **(2)** `AC-OVERLAP-019` ⑦의 *"트립와이어가 갱신되고"*는 전제가 성립하지 않았다. **(3)** D-3 가드가 오류를 내지 않는 형태. **(4)** 순회에 `sibling_answered` 추가. **인수 조건의 전제가 출하 형상과 어긋날 때 조건을 충족시키려 형상을 바꾸는 것이 결함이므로** 기록하고 후속 판단을 요청한다.
  - **뮤테이션 총계 36건 주입 · 33 killed · 3 생존(전건 테스트를 고쳐 닫음) · 1 주입 불가(형상 강화로 대체)**. 그 1건은 safety 삭제 행 단정을 계수로 약화하는 것인데 **효과를 보려면 PRESERVE 파일에서 의미 있는 줄을 함께 삭제해 커밋해야** 하고(`BASE..HEAD`는 커밋만 본다) 그것은 위반이다 — 억지 뮤테이션을 만들지 않고 삭제 행이 **구조적으로 독스트링인지**를 함께 단정해 원문을 모르는 의미 있는 삭제도 잡히게 했다. **정직한 잔여**: `ASSUMPTION-31`·`32`·`33`이 여전히 열려 있고 셋 다 다른 쇼파일을 요구하며, 본 SPEC은 **발화 억제가 아니라 한정 표현**으로 출하했다(`bound_proves_clear`가 항상 *"열거된 모드 N개에 한정한 판정"*을 함께 말한다) · `ruff check server/`는 착수 시점부터 3건 실패하고 그중 둘이 PRESERVE라 고치는 것이 위반이다 · `test_web_launcher` 사이드카 테스트가 15초 벽시계 데드라인으로 간헐 실패한다(본 SPEC과 무관) · 절단 계수 비교의 **4번째 사본**을 만들었고 D-8대로 수렴하지 않았다.
  - **`ASSUMPTION-27` 부정을 뒤집지 않는다**: PRECHK의 C-9는 *"폭이 **유일**한가"*를 물어 정당하게 부정했고(모드 4종 = 29·29·29·**31**) I-15는 *"폭에 **상계**가 있는가"*를 묻는다. 유일성은 조인을 불필요하게 만들지만 **상계도 같은 일을 한다** — 간격이 최대 폭보다 크면 어느 폭이든 겹치지 않는다. **더 약한 명제로 같은 결론에 도달하는 것이며 조인은 여전히 없다.**
  - **판정의 비대칭이 설계의 핵심이며 그것이 어휘 4값을 강제한다**: 상계 논증은 *"겹침 없음"*만 증명하고 *"겹침 있음"*은 증명하지 못한다 — 간격이 상계보다 작으면 그것은 충돌이 아니라 **미확정**이다. *"상계로 증명된 청결"*은 **충돌도 미수행도 아닌 제3의 결과**이므로 기존 두 축 어디에도 넣을 수 없다(넣으면 깨끗한 리그를 `충돌`로 라벨하거나 **수행된** 판정을 `미수행`이라 쓴다). **사용자 승인을 받아** 신규 축 `overlap_basis` 4값 + `SKIPPED_CHECK_KIND` 1값을 확장하며 **`COLLISION_KIND`와 `FIXTURE_VERDICT`는 무변경**이다. 이 저장소에 **닫힌 어휘 확장 선례는 0건**이므로(git 이력으로 확인 — 집합 리터럴이 한 번도 개정된 적이 없다) 절차를 문서화하는 것 자체가 산출물이다.
  - **술어는 `간격 < 상계`이며 `이하`가 아니다**: 구간이 닫힌 끝(`start + width - 1`)이므로 `간격 == 상계`는 **증명 가능하게 깨끗하다**. 선행 기록이 *"상계 이하라 미확정"*으로 적은 것은 off-by-one이며 **이 쇼파일에서는 42 > 31이라 두 표현이 같은 답을 내므로 오류가 잠복한다** — 간격이 정확히 상계인 리그에서 처음 드러나고 그때 깨끗한 리그를 미확정으로 보고한다.
  - **건전성 전제 3건이 전부 현 표면에서 관측 불가다 — 하나라도 거짓이면 상계가 상계가 아니다**: **(1)** 픽스처가 하나의 **연속 블록**을 점유한다(`Break*` 프로퍼티 전부 판독 불가이고 `Patch`가 한 쌍뿐이라 다중 브레이크면 `start + width - 1`이 첫 블록을 과대평가하고 둘째를 완전히 놓친다). **(2)** `DMXChannels` **자식 수 = DMX 슬롯 수**(`childCount` 29·31만 기록됐고 **자식 이름은 한 건도 기록되지 않았다** — 16비트 어트리뷰트가 2슬롯을 쓰면 상계가 과소평가된다). **(3)** **열거 완전성**(모드 집합이 불완전하면 `max`가 부분집합의 최대값이 되어 참 상계보다 작아지고 `bound_proves_clear`가 **거짓 양성**으로 발화한다 — 부분집합 29 vs 참값 31, 간격 30 리그에서 결론이 뒤집힌다). 셋 중 하나라도 미증명이면 **`bound_proves_clear`를 내지 않는 형상**으로 출하한다 — PRECHK가 `FID`를 배제하고 출하한 것과 같은 형태다.
  - **512는 필요하지 않다 — 필요한 것은 `B ≥ 467`이다**: 유니버스당 채널 상한에 대한 **저장소 근거가 0건**임을 범위 9곳 전수로 확인했고, 512를 그렇게 쓰는 저장소 유일 지점의 내용이 *"근거를 찾지 못했다"*다. 실측 리그에서 **판정이 갈리는 창은 `B ∈ [437, 466]` 30값뿐**이므로 전제를 훨씬 약한 명제로 쓰면 미검증 상수 도입을 피한다. 꼬리 초과는 Out of Scope이며 산술을 기록해 넘긴다(**용량을 런타임에 읽으려면 11번째 rig-path 키가 필요해 정확 10키 단정이 깨지는데, 폭 상계 축은 신규 키 0건으로 성립한다**).
  - **조사가 선행 기록의 오류·누락 5건을 정정했다**: 술어 off-by-one · 어휘 편집점이 4곳이 아니라 **13 + 배선 3** · §E.7이 dedupe 루프 BASE 범위 `(537, 582)`를 기록하지 않았음 · **`tools.py`도 삭제 1행이므로 *"순수 추가 = 삭제 0"* 규칙을 적용하면 즉시 실패한다** · 다음 SPEC BASE 기록이 1커밋 뒤처졌음. 그리고 선행 기록이 **추정을 단정으로 적은 것 1건**(`DmxUniverses` `childCount 1024`가 유니버스 개수라는 해석에 근거 병기 0건이며 자식 이름 `'DMX 2'`와 정합하지 않는다). **BASE는 두 개가 아니라 세 개**이며 선례 상수를 복사하면 게이트가 **주석 한복판과 루프 앞 13행**을 지킨다(수치 증명 기록).
  - **주소 공간에 검증이 0건이고 유니버스 서로소성에 살아 있는 뮤테이션 구멍이 있다**: 파서가 하한도 상한도 없어 `normalize_address("0.0")`이 `ok=True`를 낸다(실행 확인) — 오늘은 정확 일치 중복만 보므로 무해하고 **간격을 계산하는 순간 무의미한 주소가 무의미한 판정을 만든다.** 그리고 **구간 겹침 축을 두 유니버스로 밟는 테스트가 0건**이라 유니버스 키잉을 지워도 스위트가 통과한다 — 본 SPEC이 그 구멍을 닫는다.
  - **실행 형태 — 계약이 닫힌 뒤에만 병렬**: 조사는 **scout 4개 병렬**(충돌 0 · 정정 5건 + 신규 확립 4건), 정본 3종은 **단독 순차**(6문서는 한 설계의 투영이며 SONGCUE 지적 34건의 대부분이 문서 간 드리프트였다), `CONTRACT.md`로 계약을 고정한 뒤 `design`+`plan`을 **병렬 2**. **병렬이 값을 냈다 — 두 작성자가 계약의 빈틈 11건을 올렸고 그중 3건이 내가 방금 쓴 정본의 오류였다**(PRESERVE 경로 분류 · `AC` 하나의 BASE 미지정 · BASE 계수). 세 번째 BASE와 트립와이어 소속 마일스톤은 **계약을 쓸 때 내가 몰랐던 것**이다.
  - **독립 plan-audit 1회차 — FAIL 0.806 / 기준선 0.85 · 지적 19건(P1 3 · P2 9 · P3 7) 전건 처리**: 감사가 계수를 전건 재현했고(REQ 18 · AC 21 · 역추적 18/18 · 삼자 AC 배정 · BASE 상대 좌표의 끝점 원문과 +13 오프셋 · 술어 7문서 일관) **P1 3건은 전부 교차 정합 축**이며 내가 다 놓쳤다 — **(1)** `design.md`에 정정 전 경로 분류가 남아 `cross_document_drift: 0`이 거짓이었다(**원인은 숫자가 아니라 대조 축에 "계수 문구"가 없었던 것**이며 이제 대조 축 4종을 열거한다). **(2)** 한 인수 조건 항이 집행 마일스톤 없이 떠 있었고 **기계 충족 불가**였다(관측 불가 전제의 거짓을 코드가 감지할 수 없다 — 관측 가능한 것으로 좁히고 한정 표현 축으로 이관). **(3)** `plan.md`가 존재하지 않는 `progress.md` §F를 5곳에서 지목했다 — **자신이 LOOKLIB 선례를 인용해 막겠다고 선언한 바로 그 끊어진 참조**를 재생산했다. 가장 아픈 P2: *"`state_calls` 등호 단정 0건"*을 `[코드]` 등급으로 단정했는데 **그 단정을 깨는 테스트를 같은 세션에서 내가 직접 썼다** — 규율 18로 남겼다(전수 주장의 근거는 만든 사람의 기억이 아니라 **방금 돌린 명령의 출력**이다).
  - **독립 run-audit 1회차 — FAIL 0.714 / 기준선 0.85 · 지적 32건(P1 6 · P2 14 · P3 12) 전건 처리**: 작성자가 아닌 주체가 **폭 7 팬아웃**으로 채점했다(축별 슬라이스 6 + 격리 워크트리 뮤테이션 1 — 근거와 격리 실측은 `progress.md` §F.2). **P1 6건 전부가 2920개 스위트가 전건 통과하는 상태에서 살아 있었다.** 가장 무거운 것은 출하 코드다 — `_overlap_basis`가 `assessed`(**관측된** 픽스처)만 훑어 **미관측 개체가 등급에 기여하지 않았고**, `childCount=40` 선언·18개 열거 리그가 `missing_count=22`인 채 `bound_proves_clear`로 찍혔다. `CONTRACT.md` D-4가 결함으로 명명한 형상이자 `spec.md` §A 제약 4가 *"유일하게 가능한 오류"*라 적은 **거짓 "겹침 없음 증명"** 그 자체이며, 계측 census상 그 조합을 도는 테스트가 **0건**이었다. 나머지 5건은 코드가 옳고 **테스트가 못 재는** 형태였다 — D-6 가드가 *"레지스트리를 순회하되 루프 안에서 건너뛰는"* 1줄 우회에 무방비(대조: 그 우회 상태에서 라벨 없는 5번째 코드가 **import에 성공한다**) · 열거 불완전 무효화 3지점 무단정(셋 다 참 상계 31·산출 29·간격 30의 함정을 재현) · 예산 상한 단정이 자기참조라 `40 → 1e9`도 통과 · 픽스처 0개 리그 폴백 무단정.
  - **재주입이 감사자 자신의 수정에서 2건을 더 잡았다 — 결함을 고치는 것과 그 수정을 지키는 것은 별개의 작업이다**: P1 폐쇄 후 격리 워크트리에서 소스를 실제로 변형해 재주입하니 2건이 살아남았고 **둘 다 오케스트레이터 자신의 작업**이었다 — 미관측 개체 수정은 **코드를 고치고 게이트를 달지 않아** 되돌려도 아무것도 실패하지 않았고, `sibling_answered` 기본값 제거는 **모든 호출자가 인자를 넘기므로** 행태 테스트에 보이지 않았다(시그니처 단정으로 닫았다). 둘을 닫은 뒤 **8종 재주입 전건 killed**. 슬라이스도 같은 규율을 지켰다 — 예산 담당이 함께 시도한 단정이 등호에 가려 **실패 불가능**함을 발견하고 출하하지 않고 삭제했다.
  - **인수 조건 문언 3건을 감사 처분으로 개정했다**: **(1)** `AC-OVERLAP-021` ①이 4값 전부의 툴 도달을 요구했으나 `REQ-OVERLAP-013`이 `[Option]`(*"**Where** 정확폭이 **주어지면**"*)이라 어떤 요구도 툴이 폭을 **공급**할 의무를 지우지 않는다 — **지배 요구보다 강한 과잉 명세**였으므로 툴 표면 3값으로 좁혔다(죽은 어휘가 아니며 판정·리포트 계층이 산출한다). 작성자가 댄 근거(*"`spec.md` §D 범위 밖"*)는 §D 원문이 뒷받침하지 않아 **기각하고 근거를 교체했다.** **(2)** `AC-OVERLAP-019` ⑦의 *"트립와이어가 갱신되고"*는 전제가 성립하지 않았고, 실측은 더 강한 사실을 준다 — **갱신이 불필요했던 것이 아니라 갱신하면 단정이 깨진다.** **(3)** `AC-OVERLAP-017` ②의 *"판정 계층이 한국어를 만들지 않는다"*가 `CONTRACT.md` R-8(*"`observation_note`는 순한국어로 쓴다"*)과 **같은 정본 묶음 안에서 충족 불가능**이었다 — 불변식은 한국어 금지가 아니라 **라벨 소유권**이다.
  - **게이트 강화 5건과 계수 정정**: `AC-OVERLAP-014` ⑨가 **공허했다**(금지 토큰 스캐너 3종이 어휘의 **값**을 한 번도 보지 않았다 — 주장은 참이나 측정된 적이 없었다) · `AC-OVERLAP-018` ①이 명시한 *"AST 스캔"*이 실제로는 3-needle 부분문자열 스캔이라 **심은 라우트를 놓쳤다**(이제 라우트 8 · 클라이언트 메시지 타입 11 · 서버 이벤트 타입 15를 AST로 열거해 절대 튜플로 고정한다 — **저장소 최초의 라우트 계수 게이트**) · `"한정" in summary`가 한정과 **한정의 명시적 부정**을 구별하지 못했다 · `AC-OVERLAP-009` ②가 이름 붙인 10+9→17 리그가 **어디에도 없었다** · `AC-OVERLAP-002` ④의 비공허성 대조군 부재(그것 없이는 ③ 자신이 **공허하게 통과**함을 주입으로 실증했다). 계수 정정: M6 순증 `26 → 18`(인쇄된 등식 `2869+26≠2887`이었고 그 오기를 *"170 중 8건 재배치"*라는 **재측정 없는 서사**가 덮고 있었다) · 뮤테이션 총계 `36/33 → 39/36` · 손댄 파일 `10 → 11` · §6.3의 커밋수·증감을 **값 대신 측정 명령**으로 · 존재하지 않는 `acceptance.md` §D 인용 4곳을 실제 출처인 `.moai/specs/SPEC-COPILOT-PRECHK-001/acceptance.md:313`으로.
  - **머지 전 수렴 점검 4회차 — 계열은 소진되지 않았고, 뿌리 하나는 이 SPEC 밖이다**: 3회차까지 매 회차 새 P1이 나왔으므로 머지 전에 **수렴 여부만** 재기로 했다. 4회차도 P1을 냈고 **두 결함의 성격이 갈려 처분도 갈렸다.** **(C-1, 고쳤다)** 한 축이 겹침을 증명한 뒤에도 다른 축이 청결을 주장했다 — 앞 라운드가 세운 참여 검사를 `exact_set` 경로가 **우회했다**(같은 결함 계열이 두 번째 문으로 재입장). 수정은 `bound_slots and not exact_overlaps`이며 기존 단정 0건을 바꾼다. **출하 도달 불가**(`FootprintPolicy(enabled=True)`를 만드는 프로덕션 호출자 0건)임에도 고친 이유는 어휘가 공개 API이고 스위트가 상시 발화시키기 때문이다. **(C-2, 고칠 수 없다 — 이 SPEC 안전 논증의 가장 중요한 한계)** 완전성 술어가 **짧은 판독을 원리적으로 탐지하지 못한다**: 응답기가 선언 계수를 판독 배열 자체에서 파생하므로 계수와 목록이 항상 일치하고, **짧은 판독이 스스로를 "완전"이라 선언한다.** 앞 라운드의 수정은 `count == 0` 한 지점만 닫았고 같은 파생은 어떤 계수에서도 성립한다. 뿌리가 `console/lua/**`(PRESERVE)에 있어 형상이 아니라 **지식의 한계**이며, 한정 표현이 *"열거된 모드 N개에 한정"*을 항상 말하지만 **그 N이 참값보다 작을 수 있다는 것까지는 말하지 않는다.** 후속 SPEC이 집을 것: 응답기가 `Count()` 원값을 실어 짧은 판독이 자기 목록과 모순되게 할 것(인벤토리·매크로·상계 **세 축의 완전성 판정이 동시에 실질을 얻는다**).
  - **머지 판단의 근거는 "결함 0"이 아니라 "경계를 알았다"**: 결함 발견 곡선은 꺾이지 않았다(P1 3 → 6 → 2 → 1+1). 그러나 4회차가 **처음으로 경계를 그었다** — 출하 구성(`enabled=False`) 전수 훑기가 리그 약 **306,000개 · 청결 등급 1,244건에서 거짓 증명 0건**을 냈고, 남은 live 도달 가능 결함은 **하나이며 그 뿌리가 이 SPEC 밖에 있고 상속된 것**이다. *"더 찾으면 더 나온다"*에서 *"남은 것이 무엇이고 왜 여기서 못 고치는지 안다"*로 옮겼다.
- **SPEC-COPILOT-PRECHK-001** — 프리쇼 패치 점검 + 응답 확인 매크로 생성: 리그의 패치를 읽어 **관측 픽스처 전량 + 집계 2단**으로 보고하고 주소 중복·판독 실패·열거 완전성·**수행하지 않은 판정**까지 명시하는 신규 툴 `precheck_patch` (`status: completed`, M0~M8 전 마일스톤). AC-PRECHK-001~017 **17건 전량 PASS** — 소프트웨어 15건은 기계 검증(pytest), LIVE AC 2건(**AC-PRECHK-016** M0 프로브 · **AC-PRECHK-017** M8 종단)은 실제 grandMA3 onPC 2.4.2로 검증. 실행 형태는 **sub-agent 폭 1 → 폭 2 → 폭 1**(`progress.md` §F.1이 M0 실측 후 M4를 자립 슬라이스로 분리한 근거를 기록). **테스트 231건 신규 · 전체 2721 passed · 5 skipped · 0 failed.**
  - **M0 — 라이브 프로브(판정 6건: GO 4 · NEGATIVE 2, `REOPEN_SCOPE` 0건)**: 저작 착수 전 실물 콘솔에서 `ASSUMPTION-25`~`ASSUMPTION-30`을 닫힌 결과 어휘 4값으로 판정하고 접두 행 4종(합 6행)으로 기록했다. **유일한 블로킹이었던 `ASSUMPTION-26`(매크로 저작, 등급 T3·라이브 `OK` 0건)이 GO** — 다만 룰북의 2리터럴만으로는 실패하고 **`Store Macro <n>.<line>`(라인 개체 생성)이 프로퍼티 설정에 선행해야 한다**는 것이 실측으로 드러났다(순서를 어기면 `Illegal object`이며 그것은 "문법 없음"이 아니라 "대상 없음"이었다 — 부정 프로브로는 갈리지 않고 **생성 프로브 + 재조회만이** 갈랐다). `ASSUMPTION-25`는 19슬롯 전량 판독(실패 0건)이며 **사전 프로브가 놓친 19번째를 슬롯별 보강 조회로 채웠다**(`MMX 19` @ `2.401`). `ASSUMPTION-27`(픽스처→타입→모드→점유폭 연결)은 **후보를 전수로 닫은 뒤 NEGATIVE** — 구간 겹침만 빠지고 주소 중복은 어느 분기에서도 수행된다. 코드 변경 0건, 쇼파일 5지점 변경 **전건 원상 복구 + 재조회 일치**.
  - **M0가 드러낸 안전 관련 실측 2건**: **(1)** `prop … Index`가 **`ok=true`와 함께 `'function: 0x…'`**(Lua 함수 참조)를 반환한다 — `REQ-PRECHK-003`의 값 형태 검증이 이론적 방어가 아니라 이 쇼파일에서 재현되는 현상임을 실증하며, M2 형태검증기의 판별자는 **접두사**이고 특정 주소가 아니다. **(2)** `Assign … At Executor <N>`이 **`page × 100 + slot`을 단사로 사상하지 않는다** — `Executor 201`이 page 2가 아니라 **page 1의 인덱스 101을 조용히 덮었다**(`Cmd`는 `OK`를 냈다). page 1 익스큐터 1개가 실제로 덮여 같은 리터럴로 역전 복구했다. `ASSUMPTION-30`의 `page ≥ 2` 일반화가 부정인 근거이며, **접수 `OK`가 의도한 대상에 닿았음을 뜻하지 않는다**는 사례다.
  - **M0의 부수 폐쇄 3건(후속 SPEC용)**: `DataPool/PresetPools`가 **살아 있고**(14개, 각 풀 `class='Presets'`) `DataPool/Presets`·`AllPresets`는 사망 — 프리셋의 유일한 생존 경로다. `ASSUMPTION-7`(자식 풀 슬롯)이 **분기 (a) `probe_slots`로 충족됨**을 갭 풀(`DataPool/Pages/1`의 희소 `i` = 1·2·5·11·91·92·93·95·101)로 실측해 응답기 계약의 오래된 미확정을 닫았다 — `PROTOCOL.md`가 최악으로 적은 실패 모드("(a) 부재 + (b) 위치형 = 콘솔 측에서 탐지 불가능한 원래 결함")가 이 빌드에서는 발생하지 않는다. `ASSUMPTION-28`·`ASSUMPTION-29`도 GO(페이지 저작 · 빈 익스큐터 식별)로 BUSKWIZ의 DESCOPE를 닫았다.
  - **M1 — 초크포인트 프로퍼티 조회 (`server/safety/**` 조건부 예외, 사용자 승인)**: 픽스처 **주소는 프로퍼티에만** 있고 `prop`은 프로덕션 경로로 도달 불가였다(OSC 송신 표면 import 디렉터리가 테스트로 강제되고 `build_prop_query`에 프로덕션 소비자 0건). 우회 4종을 전수 배제한 뒤 **순수 추가 4지점**으로 한정 집행 — `ConsoleLink.query_property`(`query_state`와 동형) · `PropertyQueryPort` 신규 프로토콜 · 게이트 위임 + **감사 로그** · 오프라인 대역. `server/safety/console.py`는 hunk 3개 **전부 순수 추가(삭제 0행)**, `gate.py`는 추가 2개 + 독스트링 1행 정정이며 **기존 심볼·시그니처·스크리닝 의미론 변경 0건**. `REQ-MVP-029`의 단일 초크포인트 원칙은 강화된다 — 새 읽기도 감사된다.
  - **M2~M3 — 인벤토리 · 패치 정합성** (`server/prechk/{inventory,patch}.py` 신규): 완전성을 **`truncated` 플래그가 아니라 `node.childCount`와 읽은 개수의 비교**로 판정한다(`REQ-PRECHK-004`) — 조사 자신이 반환 수 18을 총수로 오독하고 그 위에 *"정합한 리그"*를 얹은 실증이 이 요구의 근거다. 절단되면 슬롯별 보강 조회로 상세를 늘리지만 **completeness를 `complete`로 승격하지 않는다**(인덱스 정의역 상한을 모른다 — `recovery_boundary`·`index_domain_unknown`을 페이로드에 노출한다). 주소는 `<유니버스>.<주소>` **정수 2개**로 정규화하고 파싱 불가는 기본값으로 채우지 않는다. 3중복을 쌍 3건으로 세지 않고, 다른 유니버스의 같은 주소는 충돌이 아니다. **불완전 입력에 정합성을 단정하지 않는다** — 미관측 픽스처는 `not_assessed`다.
  - **M4~M5 — 응답 확인 매크로 · 리포트** (`server/prechk/{macro,report}.py` 신규): 매크로 대상은 **그룹뿐**이며 `Fixture <n>` 생성은 0건이다 — 슬롯이 FID가 아니라 대부분의 리그에서 엉뚱한 기구를 **조용히** 선택하기 때문이고, 이 축소는 강제된 것이다. 그룹 1개당 점등·소등 1쌍이며 **쌍 수는 그룹 수에 연동**(상수 아님). 매크로 실행 `ok`는 **응답 증거가 아니다** — 반환에 응답 단언 필드를 두지 않고 `requires_human_visual_confirmation`을 클래스 상수로 고정한다. 판정 어휘는 **닫힌 5집합**이며 어휘 밖 코드는 라벨 조회에서 **명시적으로 실패**한다(`server/looks/report.py`의 통과 정책과 의도적으로 다르다 — 프리체크 판정은 콘솔에서 올라오는 자유 문자열이 아니다). 집계는 픽스처별 목록에서 계산되고 `skipped_checks`는 **두 생산자(패치·매크로)를 병합**한다.
  - **M6~M7 — 툴 배선 · 회귀/PRESERVE**: `precheck_patch`를 등재 3곳에 넣고 **디스패치로** 검증했다(dict 조회는 `TOOL_NAMES` 누락을 놓친다). 콘솔 발화는 **`run_commands` 경유**이므로 `bundle_gate.screen()`이 번들 전체를 먼저 본다 — `execution_port` 직접 접근·신규 REST 라우트·웹소켓 타입 **0건**(AST 스캔 + 비공허성). 스키마에 **리그 식별자 0개**이며 매크로 슬롯은 핸들러가 풀을 읽어 유도한다. PRESERVE diff는 `<BASE>..HEAD` 범위로만 검사한다 — **인자 없는 `git diff`는 위반이 커밋돼 있어도 0행을 내며 게이트가 통째로 무력해진다**(주입 실측으로 증명). 게이트 비공허성을 주입 3회로 실증하고 PRESERVE 10경로가 BASE에 실재함을 확인해 목록 오타 위양성을 배제했다.
  - **M8 — 종단 라이브**: 제품 조립 경로(`build_console_stack` + `build_toolset`)로 툴을 1회 종단 실행했다. **쓰기 0건**(`create_macro=false` · 세션 백업 끔 · `command` 감사 이벤트 0건)이며 증거는 툴 자기 목록이 아니라 **감사 로그 대조**다(`property_query` 95 · `state_query` 27 · `command` 0). **불완전 경로를 실물에서 밟았다** — 루트 열거가 절단되자 보강 조회가 19번째를 회수하고도(`recovered_count 1`) completeness를 승격하지 않았다. 재조회 불일치 0건 · 충돌 0건 · 집계=픽스처별 합 · 쇼파일 baseline 6경로 전항 일치.
  - **결함 6건을 스스로 잡아 고쳤다 — 각각 "그 단계가 없었으면 통과했다"**: **(1)** M6에서 `server.prechk.report.build_report`가 `server/looks/report.py`의 동명 심볼을 가려 `prepare_busking`이 잘못된 함수를 호출했다(busking 12건 실패) — 신규 파일만 돌린 결과는 통과였고 **전체 스위트가 잡았다**. **(2)** M8에서 `summary_ko`가 `missing_count` 0인데도 *"못 읽은 픽스처가 있다"*고 단정했다 — 인메모리 32건을 전부 통과했고 **라이브 종단 실행만이** 잡았다. **(3)** M0에서 `ASSUMPTION-27`을 후보 **부분집합** 위에서 부정 단정했다 — `REQ-PRECHK-010`이 금지하는 형태를 요구 작성자가 반복했고 **읽기 전용 scout**가 잡았다. **(4~6)** 그리고 아래 독립 감사가 셋을 더 잡았다.
  - **독립 run-audit 1회차 — FAIL 0.695 / 기준선 0.85 · 지적 14건 처리**: 작성자가 아닌 주체가 채점해 **P1 4건**을 냈고 **전부 M8이 밟지 않은 매크로 축**에 있었다. **(P1-1)** 매크로 소등 라인 `Group <n> At 0`이 **M0 미실측 리터럴**이었다(`REQ-PRECHK-012` 위반) — 게다가 대조 테스트가 페이로드를 비교 **전에 지우고** ON 라인만 필터해 **기계 게이트가 구조적으로 비껴갔다**. 보강 라이브 세션에서 측정해 정본에 등재하고 게이트를 전 페이즈 대조로 고쳤다. **(P1-2)** 그룹 풀 판독 실패를 *"리그에 그룹이 없다"*로 보고하고 `is_error=False`를 냈다 — M8이 잡은 결함과 **같은 계열**(코드는 방어 가능한데 사용자가 읽는 문자열이 거짓)이며 같은 SPEC의 인벤토리 계층이 문서로 못박은 규율을 정면으로 뒤집었다. **(P1-3)** `_free_macro_slot`이 `childCount`를 보지 않아 **절단된 매크로 풀에서 점유 슬롯을 골라 기존 매크로를 덮어쓸 수 있었다** — 본 SPEC의 **유일한 쓰기 경로**가 본 SPEC의 1급 요구(계수 비교)를 어긴 지점이다. **(P1-4)** `AC-PRECHK-014` ④의 LiveLock 강등이 구현·테스트 양쪽에 없었다(형제 툴 2개에는 있다). 회귀 테스트 10건을 신설하고 뮤테이션 5건으로 수정 전건의 방어를 확인했다.
  - **정직한 잔여 6건**: **(1)** `FID` 의미는 어떤 라이브 세션도 현재 쇼파일로 닫을 수 없다(슬롯 == FID) — 슬롯 ≠ FID로 패치된 쇼파일이 선행 조건이며 사용자 GUI 작업이다. 본 SPEC은 `FID`를 판정 근거에서 **배제한 형상으로** 출하하고 `미확정` 표시로만 싣는다. **(2)** 무응답 픽스처 자동 탐지는 **관측 경로가 존재하지 않아** DESCOPE다(응답기 동사 5종이 닫혀 있고 픽스처 하드웨어 피드백을 수집하는 코드가 없다) — 대신 사람이 눈으로 확인할 매크로를 준다. **(3)** 구간 겹침은 `ASSUMPTION-27` 부정으로 미수행이며 리포트가 그 축소를 명시한다. 감사가 추가로 열거한 후보 2건(콘솔측 프로브 플러그인 · **보수적 점유폭 상계**)은 미측정이며 후자는 라이브 없이 되살릴 여지가 있다. **(4)** 희소 풀에서 보강 조회 경계(`1..childCount` 캡)가 과소복구한다 — 이번 쇼파일이 조밀했을 뿐이다. **(5)** M2·M3·M4의 뮤테이션 20건은 **집계 수치만** 남아 개별 증거가 없다(워커 로그가 `.gitignore` 아래) — "검증됨"으로 세지 않는다. **(6)** `AC-PRECHK-015`의 PRESERVE 게이트는 1회성 수동 절차이며 상시 테스트로 박지 않았다 — 후속 SPEC이 PRECHK의 PRESERVE를 깨도 스위트가 잡지 못한다.
  - **PR #7 독립 코드 리뷰 — 지적 14건 중 13건 처리 · P1 4건은 머지 전에 고쳤다**: 이 저장소는 `gh pr checks`가 **0건**이므로 머지 전 유일한 관문이 사람 리뷰이고, 선행 run-audit는 **SPEC 프로세스** 축만 채점했다. 그래서 쓰기 경로와 판독 경로에 **독립 리뷰어 2명을 병렬로** 붙였고 둘 다 `overall: incorrect`를 냈다. **P1 4건 전부가 2721개 스위트가 전건 통과하는 상태에서 살아 있었다** — **(P1-1)** `_free_macro_slot`이 자식 0건 매크로 풀을 신뢰해 `Store Macro 1`이 **응답기 자신의 `Copilot Go` 매크로를 덮는다**(`M.safe_children`는 두 접근자가 모두 pcall 실패하면 빈 테이블을 돌려주고 `childCount`가 그 같은 빈 판독에서 파생되므로 **열거 전면 실패와 진짜 빈 풀이 한 페이로드**다). 수정 전 툴이 번들 전량을 `is_error=False`로 **실제 실행**했다. **(P1-2)** LiveLock이 **비오류 리포트**로 `macro.created=true`와 *"매크로를 실행해 눈으로 확인하라"*를 냈다 — 콘솔에 매크로가 없고, 락은 ANSWER이므로 `is_error`가 이 사실을 나를 수 없다. `macro.executed`를 신설했다. **(P1-3)** `root_was_short`가 *"자식이 슬롯 인덱스를 못 실었다"*를 *"루트 열거가 짧았다"*로 접어 **전량 반환된 입력에서도 거짓 문자열**을 냈다(`i` 생략은 `PROTOCOL.md` §4.2에 명시된 응답기 거동이며 이 계열 결함의 **4번째** 사례다). 술어를 계수-대-반환 비교로 좁히고 `incomplete` 라벨에서 원인 단정을 뺐다. **(P1-4)** 보강 스윕이 `ok=true`만 보고 노드 정체를 대조하지 않는데, **스윕을 촉발하는 바로 그 상태**(자식 중 하나도 슬롯 미확립)에서 응답기의 숫자 경로가 목록 위치로 열화한다 — 수정 전 `recovered_slots=(1,2,3)`을 **순수 위치로** 회수했다. 스윕에 확립된 슬롯 1개를 앵커로 요구한다.
  - **P2·P3 9건을 계약 무변경으로 닫았다**: 매크로 저작 후 **재조회로 효과 확인**(불일치를 `is_error`로 승격하지 **않는다** — 저장 후 정확 문자열 일치가 리그 1대·라인 1개에서만 실측됐으므로 콘솔이 정규화하면 모든 정상 사전점검이 오류가 되는 *허위경보* 버전이 된다) · 대상 그룹 0건이면 **슬롯을 유도하지 않는다**(쓰이지도 않을 슬롯의 판독 실패가 성공적으로 읽은 인벤토리를 버렸다) · `rig_paths` 섹션 누락을 이름으로 지목 · 자기 자식 수 **초과 관측 스냅샷 거부**(클램프가 `AC-PRECHK-003` 등식을 **거짓으로 닫았고** 런타임 검증이 0건이었다) · 포인터 판별을 **4종**으로 확장(근원이 함수가 아니라 `tostring` 폴백이며 Lua는 모든 비원시 타입을 `<type>: 0x…`로 만든다) · 보강 조회의 **타임아웃과 부재 분리** · **폭 부재 고지** + 폭 판정 단일 출처화 · `shape_invalid` 라벨 오기술 · 요약의 범위 한정어. **남은 2건은 닫힌 어휘 또는 리포트 스키마 확장을 요구해 계약 결정 대기이며 정밀도 문제이고 정보 손실이 아니다.**
  - **규율 16을 세웠다 — "스위트가 통과한다"는 "결함이 없다"가 아니다**: P1 4건이 전건 통과 상태에서 살아 있었으므로, 이 사이클부터 **신규 테스트가 수정 전 코드에서 실패함을 역방향으로 확인**하고(소스만 되돌려 실행 → 복원) **통과하는 테스트는 회귀 테스트가 아니라고 코드에 명시**한다(비공허성 보증 · 불변식 가드). 재조회를 **하지 않아야 하는** 3경로처럼 수정 전 공허하게 통과하는 테스트는 **직접 돌연변이를 넣어** 무엇을 막는지 증명한다. 신규 36건 중 **32건이 수정 전 실패**함을 확인했다. **전체 2758 passed · 5 skipped · 0 failed.**
  - **PR #7 squash 머지 (2026-07-30) · 새 `origin/main` = `b406a7b2bde856f0ecfb445885e6fe60693c68a5`**: 커밋 메시지를 믿지 않고 **신규 워크트리를 `origin/main`에 붙여 독립 검증**했다 — prechk 268 passed · 전체 2756 passed(차이 2건은 `.app` 번들 미빌드로 skip되는 tauri 테스트이며 2756+2=2758로 닫힌다) · PRESERVE 게이트 빈 출력 · `server/safety/` diff 파일집합이 승인 2파일뿐. **BASE가 이제 두 개이며 호환되지 않는다** — 다음 SPEC의 BASE는 새 main이고 **PRECHK의 PRESERVE 기준점은 `95687a0e…`로 영구 불변**이다(새 BASE로 검사하면 커밋 직후 항상 0행이라 게이트가 무력해진다). `progress.md` §E.9가 그 함정을 정본으로 적는다.
- **SPEC-COPILOT-FXLIB-001** — 이펙트 어휘 계층(FX 라이브러리): 시간축 움직임 패턴(페이저+MAtricks+원형 조합)을 폐쇄 어휘로 담아 채팅 지시를 매칭하고 시퀀스+큐로 인스턴스화 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB(정지 화면 어휘)의 시간축 자매편이며, 신규 패키지 `server/fx/`를 세우고 기존 `run_commands` → `gate.screen()` 단일 관문으로만 실행한다. AC-FXLIB-001~023 **23건 전량 PASS** — 소프트웨어 21건은 기계 검증(pytest), LIVE AC 2건(**AC-FXLIB-021** M0 프로브 · **AC-FXLIB-022** M7 종단)은 실제 grandMA3 onPC 2.4.2로 검증. **테스트 3240 passed + vitest 223 passed(13 files) — 신규 실패 0.**
  - **M0 — 라이브 프로브(판정 4건 전부 GO + `Accel`/`Decel` SKIP)**: 페이저는 2개 이상의 스텝을 요구하며 `<값>` → `Step 2` → `<값>` 형상이 확립됐다; `Relative`/`Phase`/`Speed`는 이미 존재하는 페이저를 **변형**할 뿐 생성하지 않는다(1스텝 위에서 발화하면 `ok:true` 전량에 모션 0이었다 — 실패 3회의 원인은 콘솔이 아니라 프로브였다). 이 실측이 v0.1.0의 "스텝 축은 선택 필드"를 철회시키고 스텝 축을 스키마 필수 1급 필드로 승격했다(v0.2.0 폴드인). **효과의 기계 증거 채널은 부정으로 확정** — 저장된 큐는 `Cue → Part(childCount 0)`에서 트리가 바닥나 빈 큐와 구별되지 않고, `Phase`/`Speed`/`CueFade`는 `"property not readable"`이다. 사람의 GUI 관측이 이 SPEC에서 효과의 유일한 증거 채널이며, 성공 경로를 포함한 **모든** 리포트가 그 한계를 무조건 명시한다(REQ-FXLIB-014 (c)).
  - **M1 — FX 스키마 + 로더**(`server/fx/{schema,loader}.py`, 86 tests): 스텝 축(길이 ≥2, attribute→값 매핑)을 필수 필드로 강제하고 로더 거부 4종(길이<2 / 스텝 간 attribute 집합 불일치 / 같은 attribute 두 스텝의 값 동일 / `steps` 없이 변형 축만 선언)을 개별 테스트로 고정. 뮤테이션 7/7 killed(지정 4 + 추가 3, 그중 1건은 `server.bridge` import 1줄 주입으로 `test_architecture.py` 자동 포섭을 실증).
  - **M2·M3·M4 — 병렬 웨이브 1**(`server/fx/{library,matching,instantiate,report}.py`, 275 tests): 라이브러리 12엔트리(패턴 6종 × 2) · 한국어 자연어 매칭 · 번들 빌더 + 값 라인 충돌 가드 2중(번들 내 구성 시점 + 교차 호출 실행 결과 시점) + 한국어 2단 리포트. 오케스트레이터가 커밋·`progress.md` 쓰기를 회수했다(같은 브랜치 동시 커밋의 git 인덱스 경합 회피). 뮤테이션 37/37 killed. PRESERVE diff 0건.
  - **M5 — 툴 표면**(`server/orchestrator/tools.py`, 78 tests): `find_fx`/`instantiate_fx` 등재, 룰북 byte-diff 0(발견성은 툴 스키마 설명이 전담), 제공자 중립(어댑터 import 0건). 뮤테이션 19/19 killed.
  - **M6 — 회귀 + 경계**(`server/tests/test_fx_boundary.py`, 38 tests): `server/fx/**` transport import 0건(AST 스캔), LiveLock 활성 시 실물 게이트 + 활성 잠금으로 16줄 전량 `proposal`(송신 0) 확인, `server/safety/**` diff 0. 전체 회귀 pytest 3239 + vitest 223(본 SPEC 최초 실행).
  - **M7 — 종단 라이브 검증**(2026-08-01, onPC 2.4.2, 코드 변경 0): 실물 게이트 스택(`build_console_stack` → `build_toolset`)으로 `pulse`(Dimmer)와 `sweep`(Pan)을 종단 발화. 감사 로그 18건이 계획 번들과 순서까지 동일(각 10줄 전량 `executed_ok`), GUI 관측 "파도처럼 순차적으로" / "좌우로 움직인다"로 두 판정 모두 확인. **ASSUMPTION-40 GO** — 스텝 생성 형상이 Dimmer 밖 Pan/Tilt에서도 성립. 라이브 세션은 정확히 **2회**(M0·M7, 사용자 사전 확정 ③ 그대로). 쇼파일 원상 복구 확인(재조회 `childCount` 17 == baseline, 잔여 0건).
  - **코드 결함 3건을 스스로 잡아 고쳤고(1~3), 그 위에 두 부류의 자기 정정이 반복됐다(4~5) — 전부 런타임에서 아무 신호도 내지 않았을 것들**: **(1)** M4가 자기 산출물에서 `circle`이 `diagonal`과 바이트 동일한 번들을 내는 결함을 발견했다 — 위상 축이 attribute 개수로 배분되고 있었다(M2의 커밋된 자산으로 자기 빌더를 재실행해서만 드러났다; 효과가 기계 검증 불가한 축이라 이 결함은 라이브에서 사람이 볼 때까지 아무 신호도 안 냈을 것이다). **(2)** 오케스트레이터가 `chase` 다중 attribute 위상 축퇴를 수정했다(커밋 `bd451df`) — R=0/G=180/B=360인데 360≡0이라 3색 체이스가 2상 점멸로 렌더되고 있었다; 산술로 판정 가능해 라이브 세션 없이 고쳤고, 한 바퀴를 도는 호에만(`span % 360 == 0`) 배타 끝점을 적용해 부분 호는 손대지 않았다. **(3)** 오케스트레이터가 `instantiate_fx`의 LiveLock 강등을 `is_error=True`→`False`로 정렬했다(커밋 `52d94de`) — 형제 툴 `prepare_busking`/`precheck_patch`는 이미 `False`였고, `True`는 공연 중 자기수정 루프가 같은 잠금에 재부딪히게 했다. **(4)** 뮤테이션 테스트가 마일스톤마다 반복해서 **테스트 자신의 결함**을 잡았다 — M1은 `match="x"`가 모든 메시지에 매칭돼 아무것도 판별 못 하는 패턴 10건을, M5는 서로 다른 두 거부 경로가 같은 `is_error=True`를 내 그 축만 본 단언을, M6는 이름이 "제안됐다"인데 실제로 발화된 번들도 통과시키던 테스트를 잡았다. **(5)** M0 자신이 두 차례 자기 정정을 했다 — 응답 못 받은 구간을 콘솔 결함으로 오판했다가 프로브 드라이버의 `kind` 불일치로 정정했고, 페이저 생성 실패 3회를 콘솔/룰북 결함으로 오판했다가 프로브가 스텝을 1개만 만든 자기 결함으로 정정했다; M7이 같은 성격의 오판을 세 번째로 피했다(패키지 앱용 감사 로그 경로를 개발 체크아웃 경로로 착각할 뻔했다가 결론 전에 자기 하네스를 먼저 의심해 잡았다).
  - **정직한 잔여**: ASSUMPTION-40은 `sweep`(Pan) 1종만 직접 관측했다 — `wave`/`circle`/`diagonal`/`chase`의 효과는 같은 축·같은 형상의 파생일 뿐 개별 라이브 관측은 없다. `Accel`/`Decel` 곡선은 스키마에 정의만 되고 v1 라이브러리는 값으로 쓰지 않는다(SKIP). **`@MX:ANCHOR` 상한 초과** — `tools.py` 6건이 `mx.yaml anchor_per_file: 3`을 넘는다(착수 시점 이미 4건, 본 SPEC이 2건 추가); AST 실측 결과 6개 핸들러 전부 fan_in 동률(Load 참조 1·직접 호출 0)이라 "최저 fan_in부터 강등" 처방이 축퇴한다 — sync-phase는 강등하지 않고 **`mx.yaml` 소유자의 설정 판단으로 이관한다**(정직한 해석은 "6건 중 3건이 틀렸다"가 아니라 "툴 레지스트리 파일에 per-file 상한이 구조적으로 맞지 않는다"이다). **design.md §5 한 줄 정합 필요** — M4 실측으로 `ChangeDestination Root`도 전 번들 공통 첫 줄임이 드러나 교차 호출 접힘이 §5가 적은 `Step 2`보다 한 줄 빠르게 시작한다(design.md는 manager-spec 소유라 여기서는 후속 발견으로만 기록한다).
- **SPEC-COPILOT-SONGCUE-001** — 송 구조 기반 큐리스트 초안 생성기: 곡 섹션 목록 한 벌로 **곡 1개 = 시퀀스 1개, 섹션 1개 = 큐 1개** 큐리스트를 승인 1회에 저작 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB의 룩 어휘와 BUSKWIZ의 번들·보고 형상을 **섹션 축**으로 소비한다. AC-SONGCUE-001~018 **18건 전량 PASS** — 소프트웨어 AC는 기계 검증(pytest), LIVE AC(017/018)는 실제 grandMA3 onPC 2.4.2로 검증. **실행 형태는 병렬 2트랙**(`progress.md` §F가 plan.md §G의 `sub-agent` 권고를 기각) — Orca 오케스트레이션 task DAG로 워커 2를 조율했다.
  - **M0 — 라이브 프로브(판정 5건, 전부 GO)**: 저작 착수 전 실물 콘솔에서 5전제를 실측해 **DESCOPE 0건**. **ASSUMPTION-21**(같은 시퀀스에 `Cue 2` 이상)이 유일한 블로킹이었고 GO로 M3의 기술적 차단을 해제 — `/Merge` 유무 **양 형태 모두** 새 큐 번호에 가산이고, **기존 큐 번호에 플래그 없는 `Store`는 `Not allowed`로 거부되며 쇼파일은 불변**이다(치환은 명시적 `/Overwrite`를 요구하므로 REQ-SONGCUE-010은 구조적으로 안전하다). ASSUMPTION-23은 시퀀스 풀이 **희소 인덱스를 명시**해 여집합이 계산 가능함을 확인 — BUSKWIZ가 익스큐터에서 데인 "비어 있음과 존재하지 않음이 구별 불가" 함정이 시퀀스 축에서는 재발하지 않았다. **ASSUMPTION-20(T5, 저장소 근거 0건)은 부정 프로브로는 판정 불가였고**(날조 키워드도 유효 키워드와 똑같이 `Illegal object`를 준다 — BUSKWIZ 측정 1의 함정) **생성 프로브가 판정을 갈랐다**(`Store Timecode 999` → 풀 childCount 0→1). ASSUMPTION-22는 `'Follow'`·`'Time'` **양쪽 수용**을 날조 토큰·날조 프로퍼티 대조군과 함께 확인해 `ok=True`의 변별력을 먼저 증명했다. ASSUMPTION-24는 86줄 **86/86** · 6.25s · 72.7ms/줄 · 열화 +1.1ms. 코드 변경 0건, 쇼파일 원상 복구.
  - **M0가 계획을 고치게 한 실측 4건**: **(F-1)** 큐 자식의 `i`는 큐 번호가 아니라 **나열 위치**다(`Cue 7`로 저장한 큐가 `i=5`로 보고됨) → AC-SONGCUE-018 측정 3이 재조회로 성립하지 않음. **(F-2)** 모든 시퀀스는 암묵 시스템 큐 2개(`OffCue`·`CueZero`)를 가져 **사용자 큐 수 = childCount − 2**. **(F-3)** 응답기 스냅샷에 `max_children = 24` 상한이 있어 풀이 24를 넘으면 여집합 계산이 무효 → M3가 `truncated` 참이면 거부한다. **(F-4)** 큐 번호는 커맨드로는 변별 가능(`Set Cue 7 …` OK vs `Set Cue 3 …` `Illegal object`).
  - **트랙 B — 응답기 v1.5.0 (`console/lua/**`, spec v0.2.0으로 범위 확장)**: M0가 `plan.md`(ASSUMPTION-22 GO면 `TrigTime` 의미론을 **반드시 측정**)와 `spec.md`(응답기 확장은 **범위 밖**, 한계 명시로 갈음)의 **모순**을 드러냈고, 사용자 승인으로 측정을 택해 PRESERVE에서 `console/lua/**` **한 항목만** 제외했다. 가산적 확장 2건 — 신규 동사 `prop`(실값 반환, 못 읽으면 `ok=false`), Cue 자식의 `cueNo`(**번호를 확신 못 하면 필드를 생략한다** — 추측 금지). 기존 `i`의 의미는 불변이라 다른 SPEC 소비자를 깨지 않는다. 이로써 **Gap 1 폐쇄 — `TrigTime`은 곡/시퀀스 시작 기준 절대 시각**(Cue1 `10`→readback `10.0`, Cue2 `14`→`14.0`; 상대 지연이면 `4.0`이어야 했다)이며 M4는 누적 절대값을 넣고 델타로 바꾸지 않는다. **F-1도 폐쇄**(`cueNo=7` 실측).
  - **M1~M2 — 섹션 입력 · 룩 매핑** (`server/looks/songcue.py` 신규): 시각 3형식(`mm:ss` · `mm:ss.mmm` · 초 실수)을 **밀리초 정수**로 정규화해 부동소수 비교를 남기지 않고, 역행·중복을 **각각** 거부하며 **정렬해서 진행하지 않는다**(임의 재배열 0건). 어휘는 `matching.py`의 `DYNAMICS_TERMS`·섹션어 밴드를 **import**하며 자체 매핑 리터럴 0건 — 검증은 raw grep이 아니라 **AST 식별자 스캔**이다(산문이 어휘를 설명할 수 있어 grep은 "표를 다시 적은 경우"와 "설명한 경우"를 구분하지 못한다). 밴드는 `tuple[int, ...]`로 유지해 점값으로 좁히지 않고, 미지 이름은 기본값으로 채우지 않고 "지정 필요"로 표시한다(섞인 입력에서 **있는 것만** 해석). 매핑은 `looks_for_genre`를 **호출**하고 재구현하지 않으며, 요구 다이내믹스에 룩이 없으면 **최근접으로 승격하지 않고** 미매핑으로 보고한다.
  - **M3 — 큐리스트 번들 (핵심 마일스톤, 하드 결함 3건)**: **큐 번호 원장**이 하드 결함 1을 닫는다 — 섹션마다 독립 계산하면 전부 `1`이 나오고, **건너뜀에도 번호를 당기지 않는** 것이 AC-SONGCUE-006 ②와 AC-SONGCUE-011을 동시에 참으로 만드는 유일한 형상이다. 결함의 실재와 해소를 **한 파일에서 함께** 고정했다(원장 없는 계산이 전부 1임을 별도 assert). 시퀀스 번호는 `DataPool/Sequences` 열거의 **여집합에서만** 고르고 하드코딩 0건이며 **조회 실패·절단 신호면 추측하지 않고 거부**한다. 금지 문법 스캔은 소스 grep이 아니라 **생성된 커맨드 튜플 전수**에 걸고 금지 형태를 **심어서** 스캐너의 실효성을 증명하며 `/Overwrite` assert는 **대소문자 무관**이다. 목적지 커맨드는 선두 **정확히 1회**로 개수와 **위치**를 둘 다 assert하고, 무손실은 문자열 assert가 아니라 **실제 `run_commands` 경로**로 확인한다(`skipped_already_executed` 0건). 모든 "0건" 스캔에 **비공허성 assert**를 동반한다.
  - **M4~M5 — 타이밍 · 보고 · 툴 배선**: M0가 두 축을 **모두 GO**로 확정했으므로 AC-SONGCUE-012는 구간 ①③이 발동하고 **②④는 `skip` 사유를 달아 보존**한다(삭제하지 않는다 — 후속 SPEC이 게이트를 다시 열 때의 출발점). 섹션 축 **2단 보고**(`server/looks/songcue_report.py` 신규)는 집계만 내지 않고 섹션별을 반드시 실으며, 미매핑 사유를 **부류로 구분**하고(어휘 미지 / 룩 부재 / 역할 미주소 — 사용자가 취할 조치가 다르다) **계획 시점의 건너뜀과 실행 시점의 중단 잔여를 합산하지 않는다**. 한계 문구는 산문이 아니라 **공개 상수 `PROPERTY_UNOBSERVED_NOTE`**이고 페이로드 `property_unobserved`와의 **상수 동일성**으로 기계 판정한다. 신규 툴 `prepare_songcue`는 기존 `run_commands` → `bundle_gate.screen()` 경로의 **호출자**이며 확인은 dict 조회가 아니라 **디스패치**로 한다(dict 조회만 하는 테스트는 `TOOL_NAMES` 누락을 놓친다). 리그는 핸들러가 직접 읽어 스키마에 그룹·풀·슬롯·**시퀀스 번호** 필드가 0개다.
  - **M6 — 회귀 · PRESERVE · 게이트 비공허성**: PRESERVE 게이트는 `git diff --stat <BASE>..HEAD -- <목록>`이며 **`<BASE>..HEAD` 범위는 협상 불가**다(인자 없는 `git diff`는 커밋 직후 항상 빈 출력이라 게이트가 통째로 무력해진다). **게이트 자체의 비공허성을 증명했다** — `server/looks/matching.py`에 공백을 주입해 커밋(`8e7aa57`)하니 게이트가 적발했고 revert(`12deea2`)로 원복했다. 목록 오타로 게이트가 조용히 통과하는 위양성을 이 절차가 배제한다. `tools.py`의 변경 hunk(old 시작줄 32·49·125·951·1222·1231)는 `_PROGRAMMER_STATE_COMMANDS`(`:234-238`)·dedupe 루프(`:524-569`) 어디에도 걸치지 않는다.
  - **M7 — 종단 라이브 검증(2026-07-29, onPC 2.4.2 / CopilotResponder v1.5.0)**: `prepare_songcue`를 통해 곡 1개를 종단 1회 생성. 감사 로그가 **33/33을 계획 번들과 순서까지 동일하게** 기록하고 전 행 `ok=true`, `skipped_already_executed` **0건** — 결정 G(코드 개정 없이 형상으로 회피)와 결정 H(충돌을 사전에 건너뜀)의 최종 증명. 재조회에서 시퀀스 1개에 사용자 큐 4개가 **`cueNo` 1·2·3·4로 서로 다른 번호로** 존재하고 이름이 일치한다 — **하드 결함 1의 실물 반증**. 보고 산술도 재조회 실측과 일치(`section_count=4`/`generated=4`/`unmapped=0`/`skipped=0`/`not_executed=0`/`failed=0`). 증거는 툴 자신의 목록이 아니라 **감사 로그와의 대조**다(툴 반환만으로 판정하면 세션이 스스로를 검증하는 순환이 된다 — BUSKWIZ M7의 교훈). 코드 변경 0건, 쇼파일 원상 복구.
  - **M7이 라이브에서만 잡은 결함 1건 — 즉시 수정**: 툴의 `report.requery.matched`가 `false`였다. 원인은 `_observed_cues`의 `child.get("cueNo", child.get("i"))` **`i` 폴백**이 `cueNo` 없는 `OffCue`를 나열 위치 `i=1`로 **추정**해 사용자 큐 1번과 충돌시킨 것이며, `matched`가 완전 일치 비교라 **구조적으로 참이 될 수 없었다**. 그 폴백은 응답기 v1.5.0의 계약("번호를 확신 못 하면 생략, **추측 금지**")을 정확히 되돌려 놓은 잔재였다. **유닛이 놓친 이유가 핵심이다** — `_requery_payload` 픽스처가 시스템 큐를 아예 넣지 않은 **이상화된 모양**이어서 M0의 F-2 실측이 픽스처로 전파되지 않았다. `i` 폴백을 제거하고 `cueNo >= 1` 필터로 교체(**표시 이름으로 걸러내지 않는다** — 이름에서 정체성을 끌어내는 것은 금지), 픽스처를 라이브 모양으로 교정, 회귀 테스트 신설. 검증은 폴백 복원 뮤테이션 + **실제 콘솔 페이로드**(시퀀스 11·20·1)로 이중 확인했다.
  - **정직한 잔여**: `CueFade`는 응답기 v1.5.0의 `prop` 경로에서도 `property not readable: CueFade`로 남아 REQ-SONGCUE-017의 한계 명시가 계속 유효하다; 매체 갭은 M7이 툴→게이트 경로로 실행해 대부분 닫혔으나 **`prop` 되읽기 자체는 게이트 미경유 직결**이다; `max_children = 24`의 실제 절단 거동은 미실측(현재 시퀀스 17)이라 M3의 거부 분기는 라이브 발동이 관측되지 않았다; `/Overwrite`는 끝내 발화하지 않아 "치환은 명시적 플래그를 요구한다"는 여전히 `Not allowed` 거부로부터의 **추론**이다; 라이브 원문 로그는 `.moai/state/`(gitignore) 아래에만 있어 커밋되는 사본은 `progress.md` §E.2의 표와 인용문이며 그래서 커맨드·응답 문자열을 요약 없이 전재했다.
  - **오케스트레이터 실수 2건 — 기록해 둔다**(`progress.md` §F 개정 절): **(1)** 트랙 B 설계 시 `plan.md`의 좁은 6파일 목록만 보고 **`spec.md §C`의 PRESERVE 정본을 읽지 않아** `console/lua` 변경을 지시했다. 위반은 워커의 일탈이 아니라 **내가 지시한 것**이고 발견자도 내가 아니라 M6 워커였다. **(2)** 트랙 B를 "M4를 막는 임계경로"로 규정한 것은 **과장**이었다 — SPEC 원설계는 관측을 포기하고 한계를 상수로 명시하는 것이었고 M4는 실제로 그것을 구현했다. 병렬 2트랙의 정당화 근거 하나가 사후적으로 약해졌다(트랙 분리 자체는 파일·언어가 겹치지 않아 유효했다).
  - **테스트**: `uv run pytest server/tests/ -q` **2490 passed · 5 skipped · 0 failed**. 착수 SHA(`38a6e7e`)에서 직접 실측한 기준선 **2423 passed · 3 skipped**에서 **+67**이며, `skipped` +2는 M4가 보존한 DESCOPE 축 2구간이다. songcue 테스트 6파일(`test_songcue_{sections,map,bundle,timing,report,tool}.py`) = 58 passed · 2 skipped. **뮤테이션 27건 전건 killed**(M1 4 · M2 3 · M3 6 · M4 6 · M5 5 · M6 2 · 결함 수정 1). 라이브 세션 2회(결정 D의 회계 그대로).

- **SPEC-COPILOT-BUSKWIZ-001** — 버스킹 준비 마법사: "이 리그로 록 버스킹 준비해줘" 한 마디로 장르 팔레트 일괄 생성 (`status: completed`, M0~M7 전 마일스톤). LOOKLIB이 만든 룩 어휘를 **장르 단위**로 소비해, 한 장르의 룩 전량(7~9룩)을 **승인 1회로 끝나는 단일 번들**로 묶어 사용자 리그의 프리셋 풀에 저장한다. AC-BUSKWIZ-001~017 **17건 전량 PASS**(AC-012는 ② DESCOPE 경로) — 소프트웨어 AC는 기계 검증(pytest), LIVE AC(016/017)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M0 — 라이브 프로브(판정 4건)**: 저작 착수 전 실물 콘솔에서 익스큐터 축 3전제를 실측해 **전부 DESCOPE 판정**(ASSUMPTION-16 페이지·익스큐터 저작 문법 / 17 빈 익스큐터 판별 / 19 팔레트를 익스큐터에 얹는 문법). 결정적 근거는 17번 — 미점유 익스큐터 인덱스가 어느 주소형으로도 해석되지 않아 "비어 있는 익스큐터"를 식별하는 질의 경로가 존재하지 않는다. ASSUMPTION-18(단일 번들 상한 87줄 왕복)은 **GO**(87/87 executed_ok, 5.77s, 66.3ms/줄)로 M2의 기술적 차단을 해제. 코드 변경 0건.
  - **M1 — 장르 조회 계층** (`server/looks/busking.py` 신규): 장르 별칭 표는 `matching.py`의 것을 **그대로 재사용**(재정의하면 한/영 어휘가 두 곳에 살고 갈라진다). 해석 실패는 가장 비슷한 장르로 **승격되지 않는다** — 과신 매칭의 결과는 사용자가 원한 적 없는 팔레트가 쇼파일에 남는 것이다.
  - **M2 — 슬롯 원장 + 다중 룩 번들 빌더**: 하드 결함 2건을 닫았다. **(1) 슬롯 비전진** — 룩마다 청구한 슬롯·라벨을 누적해 다음 룩에 넘긴다(같은 PoolIndex로 3회 직접 호출하면 Color 슬롯이 `[1,1,1]`, 본 계층 통과 시 `[1,2,3]`). **(2) `ChangeDestination Root` dedupe 탈락** — 목적지를 번들 선두 1회만 남긴다. **`tools.py`의 dedupe 규칙은 고치지 않았다**(그 블록은 `run_commands`를 쓰는 모든 소비자가 공유한다) — 전역 실행 의미론 대신 번들 형상으로 회피했고, M7 라이브가 그 회피의 성립을 증명했다.
  - **M3 — 집계 보고 계층** (`server/looks/report.py` 신규): 집계 + **룩별 2단** 구조화 보고(생성·미매핑·건너뜀·룩별 판정·미실행). 51~87 커맨드 중 어느 룩이 죽었는지 집계만으로는 알 수 없기 때문이다. 미매핑의 단위는 `(룩, 역할)` **쌍**이며(리그를 1회만 해석하므로 역할 1종이 여러 룩에서 반복된다), 매칭 판정 3종과 **섹션 실패**를 부류로 분리한다 — 후자를 `no_match`로 보고하면 보지 않은 리그에 대한 주장이 된다. 한국어 라벨은 자산이 아니라 코드 표에 산다.
  - **결정 H — 값 라인 충돌 가드**: 같은 장르에 전체 속성 페이로드가 동일한 룩이 둘이면 값 라인이 문자열로 겹쳐 두 번째가 dedupe에 접히고, **빈 프로그래머 상태로 `Store`가 실행되는데 콘솔은 성공으로 답한다.** 거부(예외)가 아니라 **건너뛰기 + 사유 보고**로 막는다 — `_plan_stores`가 이미 "이 저장은 안전하게 일어날 수 없다"를 전부 `SkippedStore`로 답하는 선례를 따른 것이며, 룩 하나의 저작 결함으로 장르 전량을 죽이지 않는다. v1 출하 라이브러리에서는 발동하지 않는다(중복 0건).
  - **M4 — 툴 배선** (`prepare_busking`, `server/orchestrator/tools.py`): 기존 `run_commands` → `gate.screen()` 경로의 **호출자**이지 제2 실행 표면이 아니다(게이트·LiveLock·dedupe·감사 로그를 그 한 경로에서 상속). 리그는 모델 인자가 아니라 핸들러가 직접 읽는다 — 스키마에 그룹·풀·슬롯 필드 **0개**. `is_error` 규약: 알 수 없는 장르는 `True`(후보 목록으로 정정 가능), **저장 0건과 LiveLock 강등은 `False`**(답변인 결과이지 기술적 실패가 아니며, `True`면 자기수정 루프가 같은 잠금에 다시 부딪힌다).
  - **M5 — 익스큐터 ② DESCOPE 분기**: v1은 익스큐터·페이지 대상 커맨드를 **0건** 발화한다. 검증은 소스 grep이 아니라 **4장르가 실제 생성한 커맨드 전수** + AST `BinOp` 검사다(dotted form은 변수 조립으로만 발화되므로 소스에는 숫자가 없어 정규식이 결코 매치하지 않는다). GO 분기 테스트 3건은 **삭제하지 않고 skip 사유와 함께 보존** — 후속 SPEC의 출발점은 "문법이 없다"가 아니라 **"문법은 파싱되나 효과와 대상 식별이 미검증"**이다.
  - **M6 — 회귀 · PRESERVE · per-show 정적 금지 스캔**: 그룹·풀·슬롯·FID·익스큐터 번호가 커맨드 조립에 정적으로 들어가는 지점 0건(독스트링 제외 문자열 상수 + f-string 숫자 상수 + 리그 파라미터 숫자 기본값 3종 스캔, 전부 금지 형태 주입으로 비공허성 증명). PRESERVE 목록 diff 빈 출력, `tools.py`의 `_PROGRAMMER_STATE_COMMANDS`·`_is_programmer_state`·dedupe 블록 **바이트 단위 무변경**, 신규 YAML·JSON 자산 0개.
  - **M7 — 종단 라이브 검증(2026-07-28, onPC 2.4.2 / CopilotResponder v1.4.1)**: 감사 로그가 **61건을 계획 번들과 순서까지 동일하게** 기록하고 전 행 `ok=True`, `ChangeDestination Root` 1건·`ClearAll` 12건 전량 생존, `skipped_already_executed` **0건** — M2의 형상 회피가 실물에서 성립함의 최종 증명. 프리셋 18개가 4개 풀에 생성됐고 재조회에서 슬롯·라벨까지 18/18 일치. **슬롯 1을 건너뛰고 2부터 시작한 것이 원장이 실제로 동작한 증거**다(세 풀의 슬롯 1은 LOOKLIB M7이 남긴 기존 프리셋이 점유). 실물 리그가 역할 6종 중 2종만 주소해 **2룩 `none` + 6룩 `partial`**이 나왔고, 전부 초록인 세션보다 강한 증거로 M3의 2단 보고가 실물에서 값을 했다.
  - **정직한 잔여**: 응답기는 프리셋 **내용**을 읽지 못하므로 라이브 검증은 슬롯·라벨의 **존재** 수준이다(LOOKLIB M0 교차 발견의 계승); M7에서 게이트가 승인 요구 없이 번들을 통과시켜 **승인 채널이 실측되지 않았다**(게이트 보류 시 송신 0건은 M4가 유닛으로 소유); 익스큐터 축은 M0 판정에 따라 v1에서 통째로 닫혀 있고 페이지·익스큐터 저작 문법의 존부(G1)는 미측정으로 남는다; 값 라인 충돌 가드는 v1 라이브러리에서 발동하지 않아 **라이브 실증이 없다**(유닛·뮤테이션으로만 검증).
  - **뮤테이션 테스트로 잡은 자기 결함 3건**: 통과하던 테스트가 실제로는 아무것도 지키지 않던 공허한 단언 3건을 뮤테이션 단계가 드러냈다 — (M3) 렌더 문자열 부재 단언이 포맷 불일치로 무효, (결정 H) 번들을 둘 따로 만들어 원장이 공유되지 않음, (M1) raw 텍스트 스캔이 호출과 독스트링을 구분 못함. 전부 실효 단언으로 교체 후 뮤테이션 **19/19 검출**.
  - **테스트**: `uv run pytest server/tests/ -q` **2423 passed · 3 skipped · 0 failed**. 착수 SHA(`d176b81`)에서 직접 실측한 기준선 2291에서 **+132**이며, 6개 마일스톤의 증분(+34/+26/+27/+27/+10/+8)이 각 마일스톤 기록값과 한 건도 어긋나지 않고 닫힌다(이월 인용 0). 기존 비-clean 지점 2건(`test_web_dash.py:523` E501, `test_tools.py:567,598` format)은 baseline에서도 동일해 무관 재포맷을 피해 손대지 않았다.

- **SPEC-COPILOT-LOOKLIB-001** — 연출 어휘 계층(Look Library): 무드 지시를 콘솔 프리셋으로 실체화 (`status: completed`, M0~M7 전 마일스톤). "코러스에서 금색 톤으로 웅장하게" 수준의 추상 지시가 큐레이트된 룩(워십·록·발라드·EDM 4장르 × 32룩)으로 매칭되어, 사용자 리그의 실제 그룹에 바인딩된 MA3 프리셋으로 인스턴스화된다. AC-LOOKLIB-001~020 중 19건 PASS + 1건(AC-020, M0 라이브 프로브) 조건부 PASS — 소프트웨어 AC는 기계 검증(pytest/vitest), LIVE AC(020/014)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M0 — 라이브 프로브(판정 4건)**: 저작 착수 전 실물 콘솔에서 빔 attribute 문법(`Zoom`/`Iris` 수용 확인 → Focus/Beam 풀이 in-scope로 확정), 프리셋 풀 타입의 런타임 해석, 역할 명명 관례(이 쇼파일에서 2/6 매칭), `Store Preset`의 패밀리 스코프 캡처(구조적 추론 — 직접 관측은 아님)를 실측. 코드 변경 0건.
  - **M1~M3 — 스키마·로더·리졸버** (`server/looks/{schema,roles,loader,resolver}.py`): 컬러·강도 구체값 + 역할 기반 포지션 6종 폐쇄 어휘(백라이트·프론트·사이드·탑·배경·스페셜) + 정수 1~5 다이내믹스 축을 스키마로 정의; 리졸버는 `get_rig_context`의 groups 섹션에 실존하는 그룹만 후보로 쓰고, 미매핑을 `no_match`/`ambiguous`/`unaddressable`(신설) 3종으로 구분 보고한다 — 그룹 발명·번호 추측 금지.
  - **M2 — 내장 라이브러리** (`server/looks/library/*.yaml`): 워십·록·발라드·EDM 32룩, 각 장르가 다이내믹스 1~5 스펙트럼을 스팬.
  - **M4 — MA3 인스턴스화** (`server/looks/instantiate.py`): 풀 타입별 `Store Preset <pool>.<slot>` + `Label` 번들을 기존 `run_commands` → `gate.screen()` 단일 경로로만 발화; 런타임 빈 슬롯 탐색, 충돌/빈 슬롯 없음은 건너뛰고 사유별로 명시 보고, `/Overwrite` 미발화.
  - **M5 — 자연어 매칭** (`server/looks/matching.py`, 신규 툴 `find_looks`): 무드/장르/다이내믹스 축으로 매칭하고 실패 시 기존 룰북 무드 폴백으로 강등; 룩 데이터 본문은 고정 룰북 프리픽스에 담기지 않는다(byte-stability 계약 유지).
  - **M6 — 회귀 + 안전 경계**: LiveLock 활성 중 발화 0건(defense-in-depth로 확인), 미매핑 사유 3종 구분 유지, 단일 스크리닝 경로 회귀 확인.
  - **M7 — 배선 수정 + 종단 라이브 인수(2회차)**: 1차 라이브(2026-07-26)가 드러낸 배선 결함 — 인스턴스화/리졸버 층은 완성돼 있었으나 모델이 도달 가능한 툴이 없어 프리셋이 0건 생성됨 — 을 신규 툴 `instantiate_look` 등록(`server/orchestrator/tools.py`) + 룰북 3단계 절차 병합(find_looks → get_rig_context → run_commands, `server/rulebook/assets/v2.4.2/31_choreography_patterns.md`)으로 해소. 2차 라이브(2026-07-27)에서 "웅장한 금색 코러스로 가자" 지시가 실제로 프리셋 3개(Dimmer·Color·Focus 풀) 생성으로 이어짐을 감사 로그 + OSC 응답기 판독으로 확인.
  - **부수 수정**: `run_commands`의 번들 내 중복 제거가 `ClearAll` 등 프로그래머 상태 커맨드까지 병합해 격리 사이클을 침식하던 결함을 M4에서 발견 즉시 수정(프로그래머 상태 커맨드 면제 목록 도입, `server/orchestrator/tools.py`).
  - **정직한 잔여**: AC-LOOKLIB-020(M0)은 명세된 증거 매체 중 감사 로그·GUI 스크린샷 2종이 산출되지 않아 조건부 PASS로 남는다; AC-LOOKLIB-014(M7)는 명세된 GUI 스크린샷 대신 OSC 응답기 판독으로 대체 검증됐다; 빔 계열 값의 방향(Zoom/Iris 개방·폐쇄 어느 쪽이 좁은지)은 미측정; 풀 이름→패밀리 해석은 단일 쇼파일에서만 검증되어 사용자가 풀 이름을 바꾼 쇼파일에서의 재현성은 미확립; 역할 힌트 6종의 실전 적중률은 2/6에 머물며 그마저 M0와 동일 쇼파일의 재확인이라 넓은 표본이 아직 없음; 룰북 폴백 강등(REQ-LOOKLIB-017)은 프롬프트 계약이라 기계 테스트로 강제할 수 없음; 감사 로그 어휘에 툴 호출 이벤트 타입이 없어 `find_looks` 같은 read-only 툴 사용 여부는 로그로 관측 불가.
  - **테스트**: `.venv/bin/python -m pytest -q` 2291 passed, 0 failed (grandMA3 onPC 2.4.2 RUNNING, UDP 8000/9005 보유 상태에서 측정 — 환경 의존 테스트 포함 전량 green); `(cd ui && npx vitest run)` 223 passed (13 files).

- **SPEC-COPILOT-DASHUI-001** — 콘솔 상태 대시보드 + 코파일럿 분할 UI (`status: completed`, M1~M6 + M6-RC + 사용자 지시 UX 배치). 채팅 전용이던 코파일럿에 "콘솔이 지금 뭘 갖고 있는지" 항상 보이는 대시보드를 추가하고, 시퀀스/익스큐터/매크로 타일에서 직접 발화(게이트 경유)할 수 있게 한다. 17개 AC(AC-DASHUI-001~017) 전부 검증 — 소프트웨어 AC는 기계 검증(vitest/pytest), LIVE AC(014/015)는 실제 grandMA3 onPC 2.4.2로 검증.
  - **M1~M4 — 대시 카탈로그 프로토콜 + 서버 빌더 + 분할 UI 골격**: `server/web/dash.py`(신규 `dash_catalog_request`/`dash_catalog` 메시지, 실제 `no` 키잉·`path_not_resolved`/`console_unreachable` 실패 구분·`truncated`/`drilldown_capped`/`contents_unavailable` 플래그)·`server/web/messages.py`(프로토콜 양측 패리티, `target_kind` enum에 `macro` 추가)와 `ui/src/components/{DashBoard,PoolSection,PoolTile}.tsx` + `ui/src/App.tsx`(좌측 대시보드/우측 채팅 분할, 접기 지원)를 신설. 정보 전용 타일(그룹/프리셋/플러그인/픽스처 요약)은 구조적으로 발화 불가 — `target_kind` 필드 자체가 없다.
  - **M5 — 발화 경로 통합(게이트 + LiveLock)**: `useCopilotSocket.ts`의 `sendPanelExecute`/`sendPanelStop`/`sendDashRefresh`가 기존 `panel_execute`/`panel_stop` → `gate.screen()` 계약을 그대로 재사용해 시퀀스/익스큐터/매크로 타일 press를 배선; `connectResyncFrames()`가 모든 (재)접속에서 `panel_catalog_request`+`dash_catalog_request`+`status_request` 3종을 동시 dispatch(AC-DASHUI-017); LiveLock 활성 시 발화 가능 타일은 제안 전용으로 강등(송신 0건).
  - **M6 라이브 체크리스트 + 근본원인 규명(M6-RC)**: 실제 onPC 2.4.2 1차 라이브에서 익스큐터/매크로/LiveLock 발화 항목(②③⑤⑥)이 "발화 가능한 타깃이 쇼파일에 없다"는 이유로 미달성 판정됐으나, 사용자 지시로 근본 원인을 재조사해 실제 결함 2건을 실측 규명·수정: **(RC-1) 슬롯≠콘솔번호** — 대시보드가 주는 풀 슬롯 번호(1,5,11,…101)와 콘솔 주소 형식(`페이지×100+슬롯`, 슬롯1→`Executor 101`)이 다름을 8/8 실측 확정 → `_executor_candidates`가 [원시 슬롯, 페이지형] 순서로 이름 검증 후 확정 번호를 `meta.console_no`로 전달, UI(`dashPressTargetNo`)는 `meta.console_no`만 fail-closed로 사용해 오발을 구조적으로 봉쇄(AC-DASHUI-005 "해석된 콘솔 번호" 완결). **(RC-2) 응답기 회신의 조용한 증발** — MA3 커맨드라인이 ~2048바이트 초과 회신을 조용히 드롭(에러 없음)함을 실측(2000B 배달/2100B 드롭 스윕) → `console/lua/copilot_responder.lua` v1.4.1에서 `max_payload` 4000→1900으로 낮춰 재배포, 회귀 테스트 `server/tests/test_lua_responder_payload_budget.py` 추가. 수정 후 실제 onPC에서 `Go+`/`Off Executor 111` 왕복, `Macro 1` 게이트 경유 발화(감사 1:1), LiveLock 잠금 중 제안-전용 강등(송신 0건)을 전부 재검증 — AC-DASHUI-014 PASS로 상향(매크로 press는 양성 절반만 검증, 이 쇼파일에 블랙리스트 매크로가 없어 승인 카드 절반은 잔여 §E.5로 정직하게 기록).
  - **사용자 지시 UX 배치(post-M6)**: 콘솔-우선 레이아웃 역전 — 콘솔 상태 패널이 상시·주 영역, 채팅은 우측 접이식 420px 컬럼으로 전환; onPC 풀-WINDOW 모델 — 카테고리별 색상 창(그룹 파랑/프리셋 청록/매크로 빨강/플러그인 마젠타/익스큐터 황갈, live-amber와 미충돌)에 SQUARE 셀을 −/+로 단계 조절하고 창 우하단 ◢ 코너 드래그로 창 영역(가로×세로) 자체를 리사이즈(flex-wrap 모자이크 재배치); 프리셋 섹션은 사이트 선호에 따라 풀 21("All 1")만 표시(`VISIBLE_PRESET_POOL_NOS`); 매크로는 Off 형태가 없어 1회성 press가 running 상태에 영구 래치되던 결함을 수정(정지 어포던스가 있는 종류만 running 추적); `chat_response`에 `executed_ok`가 실리면 정확히 1회의 `dash_catalog_request`를 자동 dispatch(`dashResyncFrame`, 이벤트 기반 — 타이머 폴링 없음, REQ-DASHUI-021 준수).
  - **정직한 잔여**: AC-DASHUI-014 체크리스트 ⑤의 블랙리스트 매크로 승인 카드 절반은 이 쇼파일에 해당 매크로가 없어 미검증(§E.5); `panel.busy` 타일별 지속 표시는 acceptance.md 어떤 AC도 개별 검증 항목으로 열거하지 않아 이번 범위 밖(D-M5-3, 근접 확인은 승인 카드 경로로 간접 수행).
  - **테스트**: `(cd ui && npx vitest run)` 208/208 passed, `npx tsc --noEmit` exit 0; `.venv/bin/python -m pytest -q` 신규 실패 0건(M1 킥오프 기준선과 완전 동일, 잔존 실패는 모두 기존 환경적 실패); `pytest server/tests/test_architecture.py -q` 4/4 passed(신규 대시 모듈이 OSC 송신 표면을 import하지 않음을 재확인).

- **SPEC-COPILOT-EXECBODY-001** — 익스큐터 타일 1회 누름의 실제 마찰(승인 카드+`SaveShow`)을 실측으로 제거 (`status: completed`, M1~M6 전 마일스톤). EXECREF-001이 남긴 구조적 갭 — 콘솔 응답기가 익스큐터의 할당 시퀀스를 `Children()`으로 노출하지 못해(`childCount: 0`) 본문 해석이 원천 불가능했던 문제 — 를 응답기 확장으로 닫는다. 16개 AC(AC-EXECBODY-001~016) 전부 검증.
  - **M1 — 역주소 문제 조사 (GO)**: `console/lua/copilot_responder.lua`가 `"Executor <console_no>"` 형태의 주소를 `DataPool` 트리 워크 대신 네이티브 `ObjectList("Executor <no>")[1]` API로 직접 해석 가능함을 라이브 프로브로 확정(ASSUMPTION-10 GO) — 다중-페이지 오프셋 관례 하드코딩(ASSUMPTION-11, REQ-EXECBODY-007이 금지)을 회피하는 경로가 존재했다.
  - **M2 — 시퀀스 아이덴티티 노출** (`console/lua/copilot_responder.lua` `build_snapshot`): 익스큐터 노드에 한해 `node.sequenceNo` 필드를 가산적으로 추가 — 할당된 시퀀스의 풀 번호를, 표시 이름 파싱 없이(AC-EXECBODY-005) 기존 `SLOT_PROBES` 접근자로 노출한다. 기존 `{name, class, i}` 소비자는 무변경.
  - **M3 — 배포 + M4 — 안전 게이트 2단계 위임** (`server/safety/console.py` `StateBodyFetcher`): `"Executor <no>"` 참조를 만나면 1단계로 M2가 노출한 `sequenceNo`를 조회하고, 2단계로 그 번호를 `Sequence <no>` 참조로 취급해 이미 신뢰되는 시퀀스 본문 조회 경로(`DEFAULT_BODY_PATHS`)에 위임한다. 새 신뢰 경계를 만들지 않고 기존 경로로 가는 진입점만 추가.
  - **M5 — fail-closed 상속 + 빈 시퀀스 정합성**: 아이덴티티 조회 실패(미할당/타임아웃/프로퍼티 부재)는 계속 보류(`_hold`)됨을 회귀 재확인(AC-EXECBODY-007/008/011). `StateBodyFetcher`에 `allow_empty` 파라미터를 추가해, 큐 0개인 검증-확인된 빈 시퀀스는 "본문 없음"이 아니라 양성 통과로 구분(위험 커맨드가 빈 본문에 숨을 수 없으므로) — 직접 Macro/Plugin/Sequence 조회 경로의 기존 empty==unavailable 동작은 무변경.
  - **M6 — Lua `resolve_path` 갭 해소 + 배포 신뢰성 도구화**: `resolve_executor_address()` 신설로 `resolve_path()`가 `"Executor <n>"` 주소를 M1 API로 직접 분기 처리(그 외 경로는 기존 트리 워크 무변경). `server/tools/responder_roundtrip.py`에 `--expect-version` 플래그 추가 — 재-Import가 기존 동일 이름 플러그인을 실제로 갱신하지 못하는 배포 함정(실측됨)을, ping 응답 버전 대조 한 커맨드로 즉시 판정 가능하게 함. `console/lua/README.md` §2.1(배포 신뢰성) 신설.
  - **최종 라이브 인수 (AC-EXECBODY-010, 2026-07-24)**: 실제 grandMA3 onPC 2.4.2에서 채팅으로 "실행기 201번 실행해줘" → 승인 카드 0장, 감사 로그(`server/audit_logs/audit-20260723.jsonl`)가 정확히 3-이벤트 체인(익스큐터 정체 state_query → 할당 시퀀스 state_query → `Go+ Executor 201` 커맨드, 전부 `ok: true`)을 기록하고 이 구간에 승인/거부/`SaveShow` 이벤트 0건임을 확인. EXECREF-001·SHOWUI-001이 descope했던 역주소 갭이 최종적으로 닫혔다.
  - **테스트**: `pytest -q` 1739 passed/2 skipped/3 failed(기존 환경적 실패와 동일, 무관) + `npm run test`(ui) 98 passed + `test_architecture.py` 4 passed — OSC import 경계(`server/safety/**`) 기준선 대비 무변경.

- **SPEC-COPILOT-EXECREF-001** — 안전 게이트가 `Executor`를 인식 가능한 참조 타입으로 처음 인정 (`status: completed` — 본 SPEC의 범위는 처음부터 인식(S1)에 한정되어 있었고, 단일-press 복원(S2)은 이 SPEC의 일부가 아니라 별도 후속 SPEC으로 완전히 이연되었으므로, S1 완결로 이 SPEC 자체는 완료로 전환한다). plan-audit **PASS-WITH-DEBT(score 0.92)** — iteration 3/3(retry cap 도달), 유일 blocker는 MP-2(AC 표 서식 관례) 하나뿐이며 감사자가 "genuine unresolved defect가 아니라 rubric 정합 문제"로 명시 판정, 사용자가 PASS-with-debt를 명시적으로 수용(AskUserQuestion).
  - **인식 확장**(`server/safety/classify.py`): `RECOGNIZED_REFERENCE_TYPES`에 `"Executor"` 추가(REQ-EXECREF-001/002/003). `server/tests/test_safety_corpus.py`의 FN 코퍼스 참조타입 축을 `classify.RECOGNIZED_REFERENCE_TYPES`에서 동적으로 순회하도록 리팩터(REQ-EXECREF-011) — 하드코딩 리터럴 0건, 향후 타입 추가가 코퍼스를 자동 확장.
  - **동작은 변하지 않았다 (핵심 정직성 고지)**: 이 SPEC은 익스큐터 타일을 누를 때마다 뜨는 승인 카드 1장 + 쇼파일 저장 1회의 원인 — 게이트가 `Executor`를 해석 가능한 참조로 인정하지 않는다는 점 — 을 진단하고 **인식만** 교정했다. **실제 사용자 경험은 이전과 완전히 동일하다**: 타일을 눌러도 여전히 승인 카드가 1장 뜨고, 여전히 SaveShow가 발화한다. M1 단독은 무행동 변화(no-op)임이 라이브 검증으로 확인됨(design.md §2.1, AC-EXECREF-008/013) — 보류 사유 문자열이 `"unverifiable reference: no recognizable target object"`에서 `"no body path mapping for 'Executor N'"`로 바뀌었을 뿐, `hold=True`/승인 카드 발생/SaveShow 발화는 모두 그대로다.
  - **왜 여기서 멈췄나 — S2(단일-press 복원) DESCOPED**: 승인 카드 없는 단일-press 발화라는 원래 목표를 실제로 달성하려면 콘솔 실행기의 "본문 경로"를 조회할 방법이 있어야 하는데, 2026-07-23 라이브 프로브(`.moai/state/verify/showui-m6-resume/5-probe-body.log`)가 이를 구조적으로 불가능하다고 결정적으로 확인(childCount 0/4, 기존 갭 재확인). 사용자 결정(AskUserQuestion, 2026-07-23)으로 S2를 완전 이연 — 실제 실행 시 승인 카드를 없애려면 `console/lua/copilot_responder.lua`(콘솔측 응답기)를 손대야 하는데, 본 SPEC은 의도적으로 그 파일을 건드리지 않는다. 후속 SPEC `SPEC-COPILOT-EXECBODY-001`(권고, research.md §5.3, 미생성)을 추천.
  - **테스트**: pytest **1723 passed**(+132, 기준선 HEAD `0576553`의 1591 대비), 신규 실패 0건 — 유일 실패는 기존 환경적 실패(onPC UDP 9005 점유) 그대로. 라이브 재검증(AC-EXECREF-013③, 실제 onPC 대상, M1 커밋 이후 사후 수행): `cleared=False`, 승인 카드 여전히 생성, 콘솔 송신 0건 — M1 이전과 관측 동일함을 확인.
- **SPEC-COPILOT-SHOWUI-001** — 연출 컨트롤 패널(Show-Control Panel) 완료(`status: completed`). 채팅 옆 2컬럼 레이아웃에 실행기 윙의 연장을 목표로 한 타일 그리드를 구현 — M1~M6, 라이브 검증 1회(실제 grandMA3 onPC 2.4.2).
  - **⚠️ UI 표면은 DASHUI-001로 대체됨 — 현재 앱에 출하되지 않음**: 아래 "패널 UI" 항목이 기술하는 `ShowPanel`/`PanelTile` 컴포넌트(M4/M5, 커밋 857e9ed·09e2c4f)는 **다른 브랜치에서 병행 개발**된 것으로, DASHUI-001이 같은 자리(채팅 옆 콘솔 제어면)를 **신규 작성**으로 대체했다(DASHUI spec.md §88 "본 SPEC의 UI는 신규 작성이다", §128 "SHOWUI M4/M5 UI와의 병합 조정은 본 SPEC 범위 밖의 브랜치 전략 결정"). 두 세대를 한 화면에 병존시키는 병합을 시도했다가 되돌렸다 — 대체된 패널이 DASHUI가 이미 해결한 문제(익스큐터 주소 체계)를 낡은 전제로 다시 설명하는 문서 모순을 만들었기 때문. **최종본은 DASHUI-001의 대시보드 단일 세대**다. 아래 SHOWUI 기록은 그 시점의 사실로 보존하되, "패널 UI"·"v1 범위 축소" 서술은 현행 앱에 적용되지 않는다.
  - **살아남은 부분**: 아래 "프로토콜·서버" 항목(`server/web/panel.py`의 카탈로그/핀/membership + `panel_execute`/`panel_stop` 게이트 실행 경로)은 **폐기되지 않았다** — DASHUI-001의 대시보드 타일이 바로 그 경로를 재사용해 발화한다.
  - **패널 UI**(`ui/src/`): 카탈로그 자동 나열(시퀀스) + 채팅 연출 pin 타일, 그리드 순서는 핀 우선·append-only(정렬 없음). Live rail — running일 때만 amber 강조, 정지 상태는 조용함. **파괴적 발화-클래스**(All Off)는 arm→fire 정확히 2회 상호작용, **정지 클래스**(개별 Off)는 1회 press. `window.confirm` 0건 — 승인 카드/배너 패턴만 사용.
  - **프로토콜·서버**(`server/web/panel.py`, `server/web/messages.py`): 신규 WS 메시지 5종(`panel_execute`/`panel_stop`/`panel_pin`/`panel_unpin`/`panel_catalog_request`) + 이벤트 4종(`panel_catalog`/`panel_item_state`/`panel_busy`/`error(kind:"panel")`), 양측 allowlist 패리티(`PROTOCOL_VERSION` 1 유지). 실행은 전용 게이트 경유(`server/safety/` 단일 관문, OSC import 0건 — 아키텍처 테스트로 봉쇄), target은 parse-시점 정수 검증 + 카탈로그/핀 membership 검증(REQ-022) 후에만 번들 구성.
  - **핀 영속화**: `<user_data_dir>/panel_pins.json`, temp+`os.replace` 원자적 쓰기, credential-like 키 거부, 손상/부재 시 fail-open 빈 패널로 기동(다음 쓰기에서 재생성).
  - **fail-closed 재접속**: WS 단절 시 패널 running/busy 상태 소거(타일 목록은 서버 상태로 보존), 재접속 시 `panel_catalog_request`+`status_request` 재동기화 — running 자체는 의도적으로 재구축하지 않음(`PROTOCOL.md` 정정 반영).
  - **AC-SHOWUI-014 라이브 완결**: reply-port 드리프트(9005→9006) 유발 → HEALTH에서 실행 차단(감사 로그 `blocked` 기록, `kind=command` 0건) → UI 상단 오프라인 배너 + amber "⛔ 실행 차단됨" 패널 배너 + 핀 타일 비활성화(Go+/Off/ALL OFF 전부 disabled) — 9005 복귀 시 online 양성 대조까지 확인.
  - **실행기 타일 v1 범위 축소 → SPEC-COPILOT-EXECREF-001**: 드릴다운 실행기 타일(페이지 하위)을 카탈로그 소스에서 **구조적으로 제거**(fixtures 선례와 동일 패턴). 사유는 라이브 실측 — 콘솔 실번호가 `100 + i`(page-1, 8/8 실증)이고, i=101 타일이 무오류로 다른 오브젝트(Sequence 50)를 발화하는 silent wrong-object 충돌이 확인됨. 채팅 발화로 생성한 실행기 핀(정확한 콘솔 번호)은 영향 없음 — 정상 유지. 주소 수정(`console# = page*100 + i` 일반화) + 게이트 Executor 인식은 SPEC-COPILOT-EXECREF-001로 이연.
  - **테스트**: pytest 1591 passed(+onPC UDP 9005 점유로 인한 환경 실패 1건, 코드 회귀 아님) + vitest 176 passed. `panel.py` 커버리지 100%. `ruff check` 신규 0건.
  - amendment 이력: plan-audit iteration 1 FAIL 0.81 → v0.2.0 fold-in(F1~F6) → iteration 2 PASS 0.93 → v0.2.1 fix-forward(R1~R4) → sync-audit PASS 0.93(4-dim: Func 92/Sec 95/Craft 90/Consist 95).

- **SPEC-COPILOT-VWX-001** — Vectorworks 도면 대조 엔진: Instrument Data export(CSV/XLSX/tab-text) 또는 MVR 파일을 읽어 **설계상 리그(designed rig)** 모델을 만들고, 라이브 콘솔 패치와 대조해 3종 차이(누락·주소충돌·수량불일치)를 보고한다 (`status: partial-blocked`, M1~M7 완료, M0 PARTIAL, M8 BLOCKED). 신규 패키지 `server/vwx/`(reader·columns·address·rig·diff·report·mvr) + 툴 `precheck_vectorworks_diff` 4지점 등록 + `vectorworks_autopatch` 대화형 진입 툴(이번 브랜치 최종 커밋), `TOOL_NAMES` 28 → **29**. 검증: pytest **7,600 passed · 8 skipped · 0 failed**(브랜치 최종 기준). REQ 28건 · AC 29건 · ASSUMPTION 3건(68 해소·69 GO·70 PARTIAL). PRESERVE 8파일(`server/prechk/**`) 무접촉.
  - **M0 — 실물 컬럼 계약 검증 PARTIAL(실물 샘플 4종 투입)**: `vectorworks_export_sample_with_data.csv`(사용자 제공 경로A 샘플) + 외부 다중-시스템 CSV 3종으로 별칭 테이블 확장·주소 3중 표현 교차검증·조인 키 스코프 결함(포지션 단위 유효범위) 수정. **ASSUMPTION-68**(컬럼 철자 부족) NEGATIVE 해소, **ASSUMPTION-69**(절대주소 근거) v0.1.7에서 GO로 재판정. **ASSUMPTION-70**(경로B 워크시트 그리드) PARTIAL — 진짜 `path_kind=B` 실물 샘플이 저장소에 **0건**이라 코드 경로는 합성 픽스처로만 확인됨.
  - **M8 — BLOCKED**: AC-VWX-026(실물 파일 기반 종단 검증)을 닫으려면 **제목행 선행형 또는 헤더 반복형 실물 워크시트 export 최소 1건**이 필요하다. 합성물로 코드 경로 동작은 확인됐으나 "실물 워크시트의 마커·서식·인코딩 특이성을 대변하지 않는다"고 문서가 명시하므로 이걸로 GO 판정하지 않는다.
  - **결함 4건 수정(M1~M7 기간)**: P0 1건(전역 유일 조인 키가 포지션-스코프 무시) + P1 3건(다중-시스템·배제 행·헤더리스·절대주소 단독 역산). v0.1.2~v0.1.7.
  - **MVR 판독 추가(범위 확장)**: `server/vwx/mvr.py` — GeneralSceneDescription.xml을 zip 매직바이트로 자동 감지해 판독, 사용자 제출 시트 포맷 지원 추가. 원래 spec.md Out-of-Scope에 있던 MVR이 실물 투입 후 범위로 복귀.
  - **대화형 업로드 + 되묻기 통로**: 파일 업로드 위젯(`ui/src/App.tsx`, 8MB/확장자 클라이언트 검증) → `vectorworks_export_upload` WS 메시지(`server/web/messages.py`) → `ChatSession.upload_vectorworks_export()`(세션에 업로드 즉시 `analyse` 자동 실행) → `vectorworks_autopatch(action=analyse|prepare)`가 대조와 패치 준비를 하나의 세션 대화로 묶음. 모자란 값(타입·이름·FID 범위)은 질문 카드(`QuestionCard.tsx`, `ask_user` 경로)로 사용자에게 되돌림.

- **SPEC-COPILOT-AUTOPATCH-001** — 반자동 패치 실행: VWX-001의 차이 리포트에서 누락 픽스처를 콘솔에 실제로 패치한다 — 서버가 Lua `AddFixtures` 계획을 생성, **사람이 Patch 편집기에서 직접 실행**, 서버가 재조회로 검증 (`status: in-progress` — M0~M8 완료, 독립 감사 round23 FAIL/릴리스 NO-GO). REQ 26건 · AC 27건 · 독립 plan-audit round10 PASS(0.865) · 독립 코드 감사 round11~23. **테스트 +2,336**(스위트 기여분).
  - **핵심 설계 전환(v0.1.3)**: 원래 목표는 서버가 `AddFixtures`를 직접 실행하는 완전 자동이었다. M0 라이브 프로브 5차가 **10가지 실행 경로 · 8종 인자 변형 전부를 실측**한 결과 **전량 0건 생성** — 서버 단독 트리거로는 MA3 플랫폼에서 원리적으로 되지 않음이 확정됐다(필요조건: Patch 편집기 열림 + 사람 직접 실행). 이 실측 결과로 **반자동 모델**로 공식 전환(REQ-018·020·024 조정, 사전 확정 사실 1·2 반증 정정).
  - **M1~M7 전 마일스톤 완료**: M1 후보 입력 → M2 FID 배정(사용자 범위 안에서만, 충돌 사전검사) → M3 타입·모드 해석(라이브러리 열거, 퍼지 매칭은 사람 확인 필수) → M4 Lua 생성 + 주소 계획(룰북 필드 집합만, 도면 주소 그대로) → M5 실행 인계(검토용 Lua + 절차를 사람에게 넘김, 서버 쓰기 0건) → M6 멱등 + 검증 읽기(이미 있는 픽스처 제외, 재조회에서만 판정) → M7 배선 + PRESERVE. AC-002~025 전량 PASS.
  - **M8 라이브 종단(2026-08-08)**: 4관문(G1 전달 0건 쓰기 · G2 사람 실행 3건 성공 · G3 검증 3/3 관측 · G4 원복 3/3 미관측) 통과 — `AddFixtures` **최초 성공 관측**(사람+편집기 조건). AC-026.
  - **독립 코드 감사 13라운드(round11~23)**: 감사가 고침을 낳고 고침이 새 결함을 낳는 패턴이 반복(round15: "코드는 옳았고 게이트가 없었다", round16: "게이트를 지키는 게이트가 깨졌다"). round23 시점 잔여 결함 4건(high 1·medium 1·low 2), FAIL/릴리스 NO-GO. round24 미착수.

- **SPEC-COPILOT-DEPLOY-001** — Stage-1 배포 가능한 arm64 macOS MVP 완료 (M1~M6 + M10, `status: in-progress` 유지 — Stage-2 M7~M9는 별도 kickoff로 이연되었으므로 아직 `completed`로 전환하지 않음).
  - **인앱 설정 + OS 자격 증명 저장**: `server/deploy/settings.py`(비민감 설정 — OSC 포트·플러그인 임포트 디렉터리·활성 프로바이더 — OS별 표준 사용자 config 경로 저장, 자격증명 유사 키 거부) + `server/deploy/keystore.py`(macOS Keychain/Windows Credential Manager 어댑터, 저장소 미가용/잠금/거부 시 평문 디스크 폴백 0 + 세션 한정 in-memory 폴백, 크래시/진단 덤프 env-scrub) + `server/web/settings_api.py`(`/api/settings`, `/api/keys` — 키 값은 절대 응답에 미포함) + `ui/src/components/SettingsPanel.tsx`.
  - **CopilotResponder provisioning**: `server/deploy/provisioning.py` + `server/web/provision_api.py`(`/api/provision/responder`) — 번들된 Lua responder(`console/lua/`)를 임포트 디렉터리로 설치 + onPC 로드/OSC 출력 포트 안내 UI(`ResponderGuide.tsx`); 앱이 직접 발행하는 `Import Plugin` 실행은 SPEC-COPILOT-MVP-001의 단일 안전 관문을 경유하고 감사 로그에 1:1 기록됨을 회귀 테스트로 증명(AC-DEPLOY-017).
  - **health/오류 UX**: 기존 HealthMonitor 3종 상태(online/console_offline/responder_degraded)에 원인+조치 한국어 안내 레이어(`healthGuidance`) 추가; 키 부재/무효는 기존 auth 오류 스크럽 경로로 raw SDK 원문 노출 없이 설정 UI로 유도.
  - **PyInstaller onedir 패키징**: `packaging/GrandMA3-Copilot.spec` + `entitlements.plist` + `sign.sh`(ad-hoc 기본, Developer ID 서명·notarization은 인증서 확보 시 코드 변경 없이 활성화) + `build.sh` — `dist/GrandMA3 Copilot.app`(arm64) 빌드 성공; `server/resources.py`가 frozen(`sys._MEIPASS`)/dev 자산 경로를 단일 리졸버로 통합.
  - **안전 불변식 보존 + local-only 검증**: 배포 셸을 감싼 구성에서도 단일 안전 관문 아키텍처·블랙리스트 승인 회귀·OSC 송신 표면 allowlist(fail-closed) 전부 green; `127.0.0.1` bind + localhost UDP + 원격 백엔드 미의존 + 오프라인 동작 확인.
  - **패키지 `.app` end-to-end 검증**: `packaging/verify_packaged_e2e.py` 8/8 PASS(기동→설정→provisioning→health→종료); 983 pytest + 기존 vitest 전부 green.
  - **환경-게이트 N/A**(이 빌드 호스트 한정, 코드 변경 없이 적합한 환경에서 활성화 가능): universal2(arm64+x86_64) 빌드, Windows x86_64 빌드, 실제 Developer-ID 서명·공증.
  - **Stage-2 이연**: Tauri v2 데스크톱 셸, Python 백엔드 sidecar 번들, 자동 업데이트, updater 재시작 안전상태 보존(M7~M9)은 별도 kickoff로 이연 — 본 항목은 Stage-1 배포 가능 MVP 마감만 반영한다.
  - **v0.3.0 라이브 E2E 하드닝(M14~M18, `status: in-progress` 유지)**: 2026-07-20 실제 grandMA3 onPC 2.4.2 + 실제 Gemini 라이브 데모에서 983-test 단위 스위트가 놓친 통합 결함 6건 중 5건(#2~#6)을 REQ-DEPLOY-028~032/AC-DEPLOY-019~023으로 fold-in 후 TDD 구현(결함 #1은 commit `1d65375`에서 이미 수정, 이력만 기록). 전체 스위트 `1017 passed`(983→1017, +34).
    - **M14 / #2 — 기동 시 활성 프로바이더 키 주입** (`server/web/serve.py` `build_runtime` + `server/deploy/keystore.py`): `build_runtime`이 OS 자격 증명 저장소의 활성 프로바이더 키를 프로바이더 클라이언트 생성 전에 프로세스 env로 주입; 기설정 env 키는 보존(덮어쓰지 않음); 저장소 미가용 시 REQ-DEPLOY-006a 세션 한정 폴백으로 강등. 신규 인스턴스가 "No API key"로 기동 실패하는 회귀를 봉쇄.
    - **M15 / #4 — 직전 생성 연출 상태 세션 추적**: 직전에 생성된 Seq/Exec를 세션에 캡처해 다음 턴에 주입 + "맹목적 수정보다 재생성" 유도 스티어를 추가, 후속 수정 지시가 엉뚱한 대상(예: `Seq 1`)이 아닌 방금 만든 대상으로 해소되도록 함.
    - **M16 / #5 — Gemini 캐시 만료 복구 + 키 오류 분류**: `403/404 "CachedContent not found"` 주입 시 캐시 재생성 후 1회 재시도; 키 누락 `ValueError`를 `unexpected`가 아닌 인증(auth) 오류로 분류.
    - **M17 / #3 — rig-context 실제 풀 번호 노출**: `get_rig_context`가 오브젝트마다 `{no, name}`을 방출해, 모델이 위치 인덱스가 아닌 실제(비연속 가능) 풀 번호를 참조하도록 함 — 존재하지 않는 오브젝트(예: `Group 3`) 생성 방지.
    - **M18 / #6 — OSC 수신 포트 동일-포트 재바인드 복구**: `SO_REUSEADDR` + 유한 재시도(포트 드리프트 없음, REQ-026)로 재바인드; 복구 불가 시 재설정 안내가 담긴 타입드 `ReceivePortInUseError`를 발생 — 이전에는 원시 크래시.
    - **테스트 인프라**: `server/tests/conftest.py`에 스위트 전역 in-memory keyring autouse fixture 추가 — M14가 실제 OS Keychain을 건드리지 않고 무인 `pytest` 실행을 복원.
    - **이연 항목**(진행 중 감사 추적, progress.md §E.4에 기록): (a) `#6` 앱 셸 안내 표시 — 브리지는 타입드 예외를 던지지만, `server/web`이 이를 잡아 표시하는 배선은 단일 OSC 관문 보안 불변식(AC-MVP-019, `server/web`의 `server.bridge` import 금지)과 상충해 보안 allowlist 결정 대기로 이연; (b) 5개 결함 전체의 라이브 onPC 재검증 — 단위 테스트가 배선을 증명하나 실하드웨어+실 Gemini 동작은 환경-게이트; (c) `console/lua/copilot_responder.lua:322`의 더 깊은 슬롯 정합성 — 진짜 갭 있는 rig에서 Lua 소스단 수정은 라이브-콘솔-게이트.
  - **v0.4.0 Stage-2 M7 — Tauri v2 데스크톱 셸(`status: in-progress` 유지, M8/M9 별도 kickoff 이연)**: Stage-1 PyInstaller onedir 백엔드를 sidecar로 재사용하는 네이티브 데스크톱 셸을 신규 구현(`src-tauri/`) — AC-DEPLOY-024~029 대응.
    - **M7.1 / AC-DEPLOY-025 — `/ws` Origin+per-launch-token 핸드셰이크** (`server/web/app.py`, `server/web/launcher.py`, `ui/src/useCopilotSocket.ts`): WebSocket `accept()` 이전에 Origin allowlist 대조 + `secrets.token_urlsafe` per-launch 토큰 상수-시간(`hmac.compare_digest`) 비교 게이트 삽입 — cross-site WebSocket hijacking(FEAS-9) 차단. 프로토콜 v1/`messages.py` 스키마 무변경.
    - **M7.2 / AC-DEPLOY-026 ③④ — 백엔드 parent-liveness watchdog** (`server/web/launcher.py`): Tauri force-quit 시(`RunEvent::Exit` 미발화) sidecar가 자기 그룹을 self-reap — PRIMARY(pipe/heartbeat EOF, 레이스 창 없음) + FALLBACK(`getppid()==1` 폴링, bounded ≤1s) 이중 트리거로 FEAS-5 Option C 백엔드 절반 완성.
    - **M7.3 / AC-DEPLOY-027 — SAFETY-2 교차언어 이중 스캔** (`server/tests/test_deploy_safety_invariants.py`, `packaging/verify_packaged_e2e.py`): Layer① `src-tauri/**/*.rs`+`Cargo.toml`/`Cargo.lock` deny-all 정적 스캔(부재/빈 트리 fail-closed, `files_scanned>0` assert) + Layer② 실제 설정 send_port wire-level 싱크(감사 로그 1:1 대조 + 양성 관측 assert + synthetic rogue 주입 증명) — 둘 다 vacuous-pass 차단 프루프 포함.
    - **M7.4a / AC-DEPLOY-024 — Tauri v2 셸 스캐폴드 + sidecar spawn** (`src-tauri/` 신규: `Cargo.toml`, `tauri.conf.json`, `src/main.rs`, `src/sidecar.rs`, `src/tray.rs`, `capabilities/default.json`): PyInstaller onedir 백엔드를 sidecar로 spawn, 네이티브 창이 동일 `ui/dist` 로드, 트레이+연결 상태 배지(M5 재사용), capabilities에서 sidecar-spawn 외 네트워크 플러그인 deny.
    - **M7.4b / AC-DEPLOY-025·026 ①② — Rust process-group kill + Stage-2 토큰 IPC** (`src-tauri/src/sidecar.rs`, `src-tauri/src/main.rs`): Unix setsid/setpgid 프로세스-그룹 kill을 authoritative teardown으로 배선(`RunEvent::Exit`), per-launch 토큰을 Tauri IPC로 웹뷰에 주입(디스크 미기록); 부수적으로 설정 영속화 부팅 수정(sidecar가 `settings.toml` 포트를 boot 시 실제로 준수) + 중복 로드 수정 + 허위 종료 다이얼로그 수정.
    - **M7.5 / AC-DEPLOY-027 Layer②** (`server/safety/console.py`, `server/safety/gate.py`, `packaging/wire_sink.py`): OSC 송신을 `DeploySend`/`ExecOutcome.sends`로 per-send 세분화해 wire-level 감사 1:1 대조 granularity를 1건 승인 의미론 보존한 채 완성.
    - **라이브 검증**: 패키지된 `.app`을 실제 grandMA3 onPC 2.4.2에 대해 실행 — health `online`, 백엔드가 설정된 UDP 9005에 바인드, 콘솔 피드백 왕복 확인. 이 라운드에서 발견·수정된 통합 결함(전부 수정 완료): sidecar 이름 해석 ENOENT, ready-line 레이스, declared-pid 신뢰 문제, 설정-무시-부팅(9000 바인드 vs 9005 설정), 문서 중복 로드, 종료 시 허위 "시작 실패" 다이얼로그, sidecar 부재 시 패닉(→ 가독 가능한 다이얼로그로 교체).
    - **환경-게이트 N/A**: Windows Job Object(KILL_ON_JOB_CLOSE) 경로, universal2 셸 빌드, 실제 notarization — 적합 환경 확보 시 코드 변경 없이 활성화.
    - **M8/M9 Stage-2-DEFERRED**: 자동 업데이트(M8)·코드 서명/공증(M9)은 본 kickoff 스코프 밖 — 별도 kickoff에서 AC 신설·검증. `status: in-progress` 유지.
  - **v0.5.0 OSC 무접촉 부트스트랩(2026-08-12 라이브 검증)**: `install_osc_templates()`가 검증된 OSCData 임포트 템플릿(`console/osc/copilot_osc_row1_receive.xml`·`row2_send.xml`)을 콘솔 import 디렉터리에 자동 스테이징(파일시스템 전용, 기존 provisioning 안전 경계 동일). `osc_bootstrap_guide()`가 남은 GUI 전용 단계를 안내. **플랫폼 한계 실측**: onPC 2.4.2 OSC `Interface` 드롭다운은 명령줄·파일 경로가 전혀 없는 **GUI 다이얼로그 전용** 기능(`Set 'ShowData'.'OSCBase' 'Interface' "lo0"` → `Illegal value`). **우회**: 설정 완료된 쇼파일을 `CopilotOscTemplate.show`로 저장해 `New Show` 대신 `Save As`로 시작하면 Interface·OSC 행 2개·Enable Output/Input·플러그인이 쇼파일과 함께 따라옴 — 사용자 클릭 0건. `console/lua/README.md` §1.-1에 zero-touch 경로를 최우선 권장으로 문서화.

- **SPEC-COPILOT-EVAL-001** — Phase 0 boardop 실사용 평가 및 격차 분석 완료 (문서 폴백 모드, REQ-EVAL-014). onPC 구동 실행 파일 미확보(베타 신청 대기)로 인해 문서·데모 영상 관찰 기반 평가로 전환(사용자 승인, AskUserQuestion 2026-07-16)하여 run-phase를 완결했다.
  - `.moai/project/research/boardop-eval-log.md` — 환경 기록(6종 중 5종 실측 + 1종 관찰 불가 명시) + 대표 시나리오 11종(S1~S10 + 파괴적 시나리오 S-D) 3태그 분류(관찰 기반 4 / 관찰 불가 7) 및 행별 근거 출처 인용.
  - `.moai/project/research/boardop-gap-analysis.md` — 4축(①한국어 UX ②안전장치 ③도구 커버리지 ④신뢰성) 전부 커버하는 격차 14건(Phase 1(MVP) 반영 11 / 보류 3 / 미반영 0) + FSL-1.1-Apache-2.0 라이선스 검토 결론(§3 — 내부 사용·평가 허용, 경쟁 제품 코드 재사용은 릴리스별 2년 제약, 아키텍처만 벤치마킹·clean-room 구현) + AC-EVAL-005 boardop 코드 미포함 4항목 이진 체크리스트 검수(§4, 전항목 0건).
  - AC-EVAL-001~006 전부 PASS(문서 폴백 경로), AC-EVAL-007(선택적 Claude/Gemini 비교)은 라이브 세션 미실행으로 평가 제외 — acceptance.md 기준 총 7개 AC 중 6개 평가·PASS, 1개 조건부 미평가.
  - amendment 이력: v0.2.0(LLM 듀얼 프로바이더 전략) → v0.3.0(문서 폴백 재조정, plan-audit delta FAIL 0.87) → v0.3.1(수정 후 delta re-audit PASS 0.95).

### Fixed

- **감사 로그 무한 성장 (819MB~1.1GB/5일)** — 게이트 읽기 프로브(state_query/property_query/heartbeat, 실측 볼륨 99.9%)를 `probe-YYYYMMDD.jsonl` 별도 패밀리로 분리: 보존 2일 + 일별 64MiB 캡(`probe_log_capped` 마커 1개 후 드롭) + 기동 시 레거시 혼합 파일 스트리밍 압축(원자적·멱등). 명령·승인·차단 기록과 deploy 하위 송신(`deploy_of`)은 90일 보존 그대로 — 1:1 송신 회계 불변. 큐 모니터 히스토리는 `include_probes=False`로 프로브 파일을 아예 열지 않는다. 적용 실측: 1.1GB → 363MB, 이틀 내 ~1MB대 수렴.

### Changed

- `.moai/project/tech.md` §5.1 — 단일 프로바이더(Anthropic 전용) 모델 전략 서술을 SPEC-COPILOT-MVP-001 REQ-MVP-038~041(프로바이더 교체 가능 추상화 계층, Claude/Gemini 듀얼 지원, 오류율 기반 기본 프로바이더 선정, 프로바이더별 캐싱) 반영으로 갱신하여 amended SPEC 상태와의 괴리를 해소.
